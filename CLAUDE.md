---
title: "Org OS — 系统路线图与 Harness"
type: roadmap
role: "[工程师]+[AI]"
last_updated: "2026-08-28"
status: active
related_files: [content/03_doc_system/ARCHITECTURE.md, content/03_doc_system/SSOT_INDEX.md, content/01_strategy/SECRETARY_DIRECTIVES.md, content/01_strategy/, content/02_institution/, content/03_doc_system/, content/04_web_design/, content/05_ai_coding/, content/insights/, server/, .ctx/logs/]
---

# CLAUDE.md

> 本文件是 Org OS 项目的最高治理文件，分为**甲乙丙三部**：
>
> - **甲部：通用流程与指导思想（Harness）**——管总的，稳定不变，定义工作流、一改具改、检查清单、评议机制、外部索引
> - **乙部：具体执行事项（常为新的）**——AI 每次工作必须读取的上下文，完成即删，补充新项
> - **丙部：待决策事项**——AI 拿不定主意的问题，提交支书决策

***

# 甲部：通用流程与指导思想（Harness）

> Harness 是项目的核心原则——定义工作方式、核心原则和运行标准。
> 修改 Harness 须经支书确认，且必须一改具改（见 H30）。
>
> **三层架构 \[工作表达]**：按约束力强弱分三层——热层（每次必读，写全规则：H10/H20/H30/H40）→ 温层（修改时读，写全机制+指针细节：H50/H60/H70/H80）→ 冷层（按需参考，纯指针：H90 外部索引 + H100 项目产出声明）
> **分层依据**：全局约束力强（违反即出错/支书纠正）→ 写全在甲部；场景化/按需（特定操作或审核时才用）→ 指针指向对应文件
> **已迁出目录**：`.github/`（智能体治理层）已于 2026-07-21 物理迁出至 `D:\GitHub\System-Residual\.github\`——VSCode Agent 治理不再属于本仓库（D-186 终结）

***

# 热层：每次工作必读

> 🔴 **以下内容为每次工作的强制上下文，不得跳过。**

## H10. 总纲

> 🔴 **总纲 · 最高验收标准**：最小三成本（信息成本 / 操作成本 / 适应学习成本）是系统设计的最高验收标准，纳入常态化专项评议（W4 专项⑤，机制见 H60.5），不因乙部清理而中断——工作台改动与设计决策均以其验收。

> 🔴 **总纲 · 设计文档外包可读（2026-08-14 支书裁决）**：所有 `content/04_web_design/` 设计文档必须从头至尾可读、锚定一个明确目标——即使拿给外包公司重做，对方也能清晰读懂设计思路与需求（目标声明 + 阅读顺序 + 术语定义）。AI 在修改/新增 04 文档时须以此自检：新增章节须可独立理解、不依赖会话上下文；色值/组件/数据一律指向权威源（DESIGN_SYSTEM 色板等），禁止散落硬编码。

> 🔴 **总纲 · 支书原话表达纪律（2026-08-15 支书裁决，2026-08-30 加强）**：支书原话可以复述，但有几条铁律——①**极端限定词禁止 AI 自造**：支书原话中的「最重要」「关键在于」「尤其是」等表达「侧重」的极端限定词，AI 不得擅自添加或套用（这类词是支书对重要性的判断，AI 无权制造）；②**「不是…而是…」反论全面禁止**：不得使用「不是…而是…」「不是指挥权，而是知情权」等排他对立式表述——无论是否经审议，都不得写入文档与草案（2026-08-30 支书裁决加强：原「须经支书过目后才能写入」升级为「全面禁止」，此类反论"非常讨厌"）；③**禁止把「侧重」改写成「等号」或「对立」**：支书说「最重要的集中就是知情权」，AI 不得改写成「集中 = 知情权」（把侧重偷换成充要条件）或「集中不是指挥权，而是知情权」（把侧重偷换成排他对立）——「最重要」是侧重，不是唯一，改写成等号/对立会引入支书没说的内容，是「极大的 AI 懒政」，有毒且危险；④**禁止 = 与 ≠ 符号（2026-08-30 支书裁决新增，强烈要求）**：任何文档、审议草案、AI 扩充、REVIEW 相关文字中不得出现「=」「≠」符号（含「集中=决策有归属」「扁平≠没有程序」等）——符号表达是 AI 懒政，支书"真的很讨厌"，一律用自然语言写关系（且注意不替支书定性，见战略模糊自觉总纲）。

> 🔴 **总纲 · 好的 AI 扩充五条标准（2026-08-15 支书裁决，2026-08-16 GPT 范本增补「深化」）**：AI 对支书原话的扩充须同时满足五条——①**忠实**：无支书原话没有的极端限定词、无「不是…而是…」反论、无等号改写侧重（见「支书原话表达纪律」总纲）；②**关系性**：讲清本条与其它论断的 big picture 关系（呼应/对比/因果），不孤立复述原话；③**深化**：有自己的理解与深化——把分散论断抽象为统一理论结构（「价值目标→组织机制→人的成长→组织再生产」四层、「组织认识→要求→制度化→成员实践→主体性→反作用→再认识」组织学习闭环、「探索—利用」组织知识积累），让读者看到论断背后的统一逻辑；但深化须锚定支书原话、不私加、不违背支书意思、对接组织建设的真实问题（GPT 范本：有深化又不违背支书意思）；④**自然**：不机械套模板、无「AI 理解/支书原话」区隔标注——吃透后分不出支书说的还是秘书写的；⑤**克制**：不私加支书没给定的具体内容。

> 🔴 **总纲 · 条目编排顺序（2026-08-28 支书裁决）**：论断条目下的无序列表 / 子条目，必须按**逻辑顺序 + 问题意识**编排（组织运行过程中依次要回答的真问题），**时间顺序是最不重要的顺序**——不得按"支书说这句话的时间先后"机械排列。编排前先自问：这条目要回答什么真问题？前后条目之间是什么逻辑关系（递进 / 并列 / 派生）？拿不准顺序时，按"问题意识"推演并勤问支书确认。
>
> 🔴 **总纲 · 战略模糊自觉（2026-08-28 支书裁决）**：支书原话中存在**故意不说的战略模糊**（如"集中"到底意味着什么）——这是支书的战略空间与工作主动权，AI 不得替支书收口定性。AI 扩充若私加"集中是责任不是权力""扁平化会不会没人负责"等定性，等于替支书把话说死、让支书工作被动。**AI 扩充的自觉**：忠实于原话的模糊——只做「连接」（讲清本条与其他论断的关系），不做「定性」（给概念下定义 / 收口）；凡涉及"某概念本质是什么"的判断，以支书原话为准，原话未明的保持未明。宁可留白，不可越俎代庖。
>
> 🔴 **总纲 · 主客统一（2026-08-15 支书裁决，辩证法的要求）**：要在组织发展的过程中不断互动、磨合、交流、成长——①**应然**：支部作为「方兴未艾的学生组织」，首先要阐释自身所处的发展阶段及其内在矛盾，才能由此推导出相应的【要求】；②**实然**：支委班子通过制度、分工、工作流等，把对组织发展的认识转化为现实的组织实践，推动个人参与组织实践；③**主客转化**：成员作为"被要求的客体"参与这一实践，但又不是被动接受要求的客体；支部成员经由组织中介，在参与实践、理解要求并实现要求的过程中形成【自觉】，从而使原来外在于成员的组织要求转化为成员自己的行动目的，使成员由"被要求的客体"转化为支部建设的【服务对象】、参与者和主体；④**反作用与再发展**：成员主体性的形成并不是矛盾的终结，而是原有统一达到新的阶段——成员以主体身份参与组织实践，其实际经验、判断和行动又反过来检验原有要求、制度与工作方式，暴露新的矛盾和新的要求，从而提高对支委班子和组织建设的要求，推动组织在新的实践中重新认识自身、调整自身，并形成更高层次的统一。凡涉及「组织与成员关系」的视角表述，须体现这一辩证回环。

***

## H20. 标准化工作流

### H20.1 审查（每次启动时必做）

> 🔴 **以下 3 步为每次工作的强制前置，不得跳过。**

1. 读取 `.ctx/TIMESTAMPS.md` 检查周期性任务到期情况
2. 读取 `.ctx/SNAPSHOT.md` 确定当前基线
3. 阅读本文件（CLAUDE.md）确认当前待办（乙部）

### H20.2 执行（单次迭代的标准流程）

> 🟡 **以下 4 步按顺序执行。**

1. **Blueprint**：输出任务拆解计划与执行路径
2. **实施**：调用对应工具执行任务
3. **验收**：自动运行 `GetDiagnostics`；**涉及代码改动必须按 H25 测试系统运行相关测试**（前端多文件改动必跑 `server/test/module-load.test.mjs`，改动链接跑 link-integrity，改动 mock 数据跑 mock-integrity，改动活动/工作流/登录跑 agenda-flow + e2e-login），**不得仅以 GetDiagnostics 零错误代替运行验证**（语法对≠运行对，content/05_ai_coding/TEST_AND_VERIFICATION.md §17 子代理交付三查判例）
4. **记录**：追加至 `.ctx/logs/YYYY-MM-EXECUTION_LOG.md`

> 🔴 **歧义消解铁律**：人类表达可能有歧义。当任务复杂、庞大、或用户表述不清晰时，**必须**：
>
> - 在乙部中拆分为可独立完成的子任务（OPERATIONS\_GUIDE.md §10.5 分片规则）
> - 在丙部中提问确认意图（H50.2 丙部待决策机制）
> - 宁可多问一句，不可误判意图。**不知为不知，是知也。**
> - 🔴 **有疑问必须主动 /ask 支书确认，不得自行假设或基于猜测执行**——这是防止误判意图的最后一道防线。宁可多问一句，不可自行其是。
>   🔴 **【不允许随意结束 session】**：有拿不准的地方不得擅做决断，必须积极及时使用 AskUserQuestion 询问支书！！必须推进完成所有工作，不得中途放弃或跳过未决事项！！这是一条独立于歧义消解铁律的强制规则——即使任务看似简单，只要存在任何不确定，就必须先确认再执行；即使遇到困难，也必须推进到底，不得以"大致完成"为由提前结束 session。**执行顺序**：歧义消解优先（先问清再做），消解后再推进执行（不半途而废）。
>
> 🔴 **【Git 操作授权规则】**：`git commit` 可由 AI 自动执行（无需逐次确认）；`git push` **必须经支书明确确认后方可执行**，AI 绝不允许擅自 push。每次需要 push 时，AI 必须通过 AskUserQuestion 提请支书确认，获得明确同意后方可执行 push 操作。
>
> **AI 展开原则**：AI 的作用是作为一名**合格的秘书**——写出的话应是"grill 支书后的话"——吃透精神后用自己的语言讲透，而非复读或标注边界；秘书不得私加支书没说的「最重要/关键在于」等极端限定词，不得用「不是…而是…」反论替支书立论（详见 USAGE\_POLICY.md §二 + 本文件 H10 支书原话表达纪律）。

### H20.3 next\_prompt（🔴 每次工作结束的强制输出）

> 🔴 **这是每次工作的最后一步，禁止省略。若未输出 next\_prompt，视为工作未完成。**

每次工作结束前，必须输出 `next_prompt`，包含以下三要素：

- **(a) 未完成的工作项**及 T 编号（若乙部无未完成项，标注"乙部当前无未完成项"）
- **(b) 需要恢复的上下文要点**（下次接手的 AI 需要了解的关键信息）
- **(c) 建议的执行顺序**（下一步应该先做什么）
- **(d) Skill 提醒**：若未完成项涉及可调用 Skill 的场景，必须标注推荐 Skill（如"→ 推荐 Skill: brainstorming"）。可用 Skill 通过 `npx skills find <keyword>` 搜索

**⚠️ 丙部待决策事项**：若丙部有未决事项，必须在 next_prompt 中列出，交由支书决策。

***

## H20.4 支书指示分级（探索性 vs 命令性，2026-08-27 支书裁定）

> **背景**（支书原话 2026-08-27）：「我们目前的很多工作，被我的提示词框得太死，但我却没有办法每次告知 AI 哪些是我粗糙的认识（供他发散），哪些是明确准确的命令。」
>
> **定位**：支书 → AI 的沟通契约——AI 对指示的**前置识别层**（在歧义消解 H20.2 之前先判断指示类型），防止两类系统性偏差：①把支书的探索性认识当精确命令机械执行（过拟合支书散点表达）；②把支书的明确命令当可发散内容擅自增删范围。

**两级指示**：

| 类型 | 支书表达特征 | AI 处理 |
| --- | --- | --- |
| 探索性指示 | 粗线条思考/倾向/担忧/方向（「我觉得」「我想」「我担忧」「是否考虑」） | **发散**：grill 支书原话、吃透精神、提出方案与补充展开；发散须符合 H10 五条标准（忠实/关系性/深化/自然/克制）；执行前用 AskUserQuestion 收敛方向 |
| 命令性指示 | 明确可执行的要求（「做」「改」「加」「检查」+ 具体对象/范围） | **精确执行**：不得擅自增删范围、不得发散私加支书没说的内容 |

**支书未显式标注时的默认判断规则**（支书无法每次标注，AI 按以下规则默认）：
1. **可验证性**：指示有明确产出/验收对象 → 命令性；纯方向性表达 → 探索性
2. **确定性**：涉及具体对象/数字/范围/动词（做/改/加/删）→ 命令性；抽象形容（担忧/希望/倾向）→ 探索性
3. **结构提示**：「我认为/我想/我觉得/我担忧/是否考虑/能不能」→ 探索性；「执行/必须/请做/要求/逐一」→ 命令性
4. **模糊时的默认**：按探索性处理——先发散提出理解与方案，执行前 AskUserQuestion 确认收敛（宁可多问，不可机械执行错误理解）
5. **发散边界**：发散是把支书粗线条认识「吃透展开」（H20.2 AI 展开原则），**不得**私加极端限定词/「不是…而是…」反论/等号改写侧重（H10 表达纪律）——发散与命令的边界由「是否违背支书意思」锚定

**与既有机制的关系**：本制度是 H20.2 歧义消解铁律（有疑问必须 ask）、H50.2 数量过拟合补丁（散点表达不锁定数量）、H10 支书原话表达纪律（忠实/克制）的**前置识别层**——先判类型，再按对应规则处理。

**very strong 表达勤问规则**（2026-08-27 支书补充）：
> 支书原话：「支书作为人会忘记有时候把一些细节说清楚。所以包括【过拟合】的部分，我认为破解之道都是【勤问】。当模型意识到有些表达是 very strong 的，一定不能随意！」
- **触发**：支书表达含「最/尤其/关键/必须/绝对/都是/一定/重中之重」等极端限定词，或强方向性/强否定判断时，AI 判定为 very strong 表达
- **处理**：一定不能随意——不得自行解读、过度引申、或按字面机械执行；必须**勤问**（AskUserQuestion 确认真实意图与边界），宁可多问一次，不可误判一次
- **与 H10 的关系**：very strong 表达是支书对重要性的判断，AI 无权自造也无权擅自弱化/强化；AI 不确定该表达是「侧重」还是「边界」时，勤问是唯一正解（防过拟合与防曲解的双保险）

***

## H25. 测试系统（AI 必知，2026-08-30 沉淀）

> 系统有**正式测试套件**（`server/test/`，`.test.js`/`.test.mjs` 混合；数量随目录增长，不维护固定计数，以 `cd server && npm test` 实测为准）。AI 做代码改动后应主动运行验证——这是「编辑完整性」防线（content/05_ai_coding/FILE_OPERATION_RULES.md §14.1）的组成部分，不是可选步骤。

**命令**：
- 全量：`cd server && npm test`（裸 `node --test`，自动发现 `test/` 下全部 `*.test.{js,mjs}`）
- 单跑：`node --test server/test/<文件名>`
- 版本戳同步：`node docs/scripts/bump-version.mjs`（bump 后必须跑一次全量，防模块实例分裂误报，content/05_ai_coding/TEST_AND_VERIFICATION.md §17）

**套件构成**：单元/集成 9 个 `.test.js`（auth/db/resources/seed/skeleton/snapshot/uploads/report/e2e-login）+ 审计守护 8 个 `.test.mjs`（agenda-flow / b3-1 / capability-registry / click-cost / link-integrity / mock-integrity / module-load / references-official-links）。

**注意**：`click-cost`、`mock-integrity` 需外部 server 在 3000 端口（先 `npm start`），其余自包含。t235/t280-b1/m4/min3-review/t280-b5 票证专项已随 2026-08-30 脚本清理归档，覆盖由上述套件承接。

**何时必跑**：跨文件开发结束（module-load）· 改链接后（link-integrity）· 改 mock 数据后（mock-integrity）· 改活动/工作流/登录后（agenda-flow + e2e-login）。

***

## H26. 上下文文件 vs 过程产物（2026-08-30 定义）

**上下文文件** = AI 执行任务时**直接读取**、用于建立「项目当前状态 + 当前规则」认知的活跃文件。三特征：
① 描述**当前状态/规则**（非历史过程）；② 内容**可执行引用**（读即用）；③ 变更立即影响后续任务。

- **对照**：知识资产（`content/insights/`，人类阅读、非 AI 活跃上下文）；过程产物（spec/plan/一次性脚本，任务闭环即归档或删除）
- **当前上下文文件清单**：本文件（甲/乙/丙部）· `.ctx/SNAPSHOT.md`（当前基线）· `.ctx/TIMESTAMPS.md`（文件注册表）· `.ctx/REVIEW_QUEUE.md`（评议队列）· `content/03_doc_system/`（ARCHITECTURE/SSOT_INDEX/OPERATIONS_GUIDE/DOC_MAP/USAGE_POLICY/SERVICE_CATALOG）· `content/04_web_design/`（设计知识防逃逸、按需活跃：DESIGN_SYSTEM/COLOR_SYSTEM/COMPONENT_SPEC/DATA_MODEL/DATA_FLOW/COMMISSIONER 相关在动对应代码/数据时读；evolution/DESIGN_METHODOLOGY.md 设计决策前读）· `content/05_ai_coding/`（AI 协作方法论层：README + 5 分篇——FILE_OPERATION_RULES/TEST_AND_VERIFICATION/DOCUMENT_GOVERNANCE/CONTEXT_MANAGEMENT【active】/REVIEW_AND_EXPRESSION——+ DATA_CONSISTENCY_CHECKLIST.md 手册；read_strategy 按 README 标注读）· `server/README.md`（测试/部署）· 项目记忆（跨会话）
- **失忆闭环指针**：支书裁决落活层闭环规则见 `content/05_ai_coding/CONTEXT_MANAGEMENT.md`
- **清理纪律**：过程产物不留仓库——可复用部分（决策/约束/落地记录）整合进上下文文件后删除原文件；空目录同步删除；整合处标注来源与日期（判例：2026-08-30 html-slimming spec 整合入 ARCHITECTURE_EVOLUTION 后删除）
- **spec/临时设计文档位置纪律**（支书 2026-09-06 裁定）：临时 spec/设计文档入 `.trae/specs/<id>/`（gitignore 受管临时区）或同域任务目录，**用后即删**；**禁止进入 `content/`**——content 只放长期权威源（判例：2026-09-06 立项④/⑤ spec 误入 content/04_web_design/evolution，已迁删；支书裁定/验收摘要收敛于 REVIEW_QUEUE 与执行日志）

***

## H27. AI逃逸捕捉评议（2026-08-30 支书引入）

**定义**：检查提示词体系（本文件甲部 + 检查要点库 REVIEW_QUEUE 附录⑤ + skills）中「本应 rule-based（确定性执行）却写成 AI 可自行取舍的软性表述」的位置——**逃逸点**（AI 可合理化偏离规则意图）。

**五维扫描（E1-E5）**：
- **E1 规则软化**：must/禁止 写成「应/宜/建议/尽量」
- **E2 判据缺位**：要求「必须复核」但无「如何验证/验证什么」的可执行判据
- **E3 边界模糊**：方向有、边界无（「不得过度/适度」无标尺）
- **E4 例外无闸**：允许例外但无触发条件 + ask 确认闸
- **E5 验证无闭环**：无「完成后如何证明」（可执行命令/检查清单）

**流程**：扫描 → 逃逸点清单（位置+原文+E 分类+修复建议）→ 修复（软→硬）→ 检查要点库登记 → 支书复核。要点与判例见 REVIEW_QUEUE 附录⑦「AI逃逸捕捉」。

***

## H30. 一改具改原则与母本子本关系

### H30.1 一改具改

> **核心原则**：修改任何一项制度/设计/术语，必须在全仓库范围内同步更新所有相关引用，不得遗漏。
>
> **零残留验证（E2 判据，2026-08-30 AI逃逸捕捉首轮修复）**：修改完成后必须用**全仓 Grep 验证旧引用（旧表述/旧编号/旧路径）零残留**，并 Read 复核改动文件——不得以「我记得都改了」代替实测；H40 检查清单第 3 项同步执行该验证。

**执行标准**：

1. 修改前：全仓库搜索所有引用位置（Grep + SearchCodebase）
2. 修改中：逐一替换，每处替换记录到执行日志
3. 修改后：再次全仓库搜索确认零残留

**甲部自省原则**：当支书指出 Harness 存在欠缺时，必须首先反思甲部的表达与规定本身是否有缺陷——是规则没写清楚、覆盖有遗漏、还是执行标准不够具体？有则改之，无则加勉。不得仅归因于"执行不力"而回避制度层面的反思。

**吸收外部输入原则**：当支书或外部来源提供新的评议/文档/制度更新时，必须识别变更范围，归位到权威源，并触发一改具改。具体操作流程见 guides。

→ 详细操作流程见 [PROCESS\_GUIDE.md §16](content/03_doc_system/PROCESS_GUIDE.md)

### H30.2 设计母本与子本

> **本表为5条核心原则**。完整的母本子本注册表（约25条级联关系）见 [SSOT\_INDEX.md](content/03_doc_system/SSOT_INDEX.md)，文档权威层级定义见 [OPERATIONS\_GUIDE.md §1.1](content/03_doc_system/OPERATIONS_GUIDE.md)，文档导航图见 [DOC\_MAP.md](content/03_doc_system/DOC_MAP.md)。
> 三者关系：H30.2 提炼核心原则 → SSOT\_INDEX 注册全部关系 → OPERATIONS\_GUIDE §1.1 定义层级 → DOC\_MAP 标注层级。

| 关系类型   | 母本                                                                                   | 子本                             | 同步规则                                                         |
| ------ | ------------------------------------------------------------------------------------ | ------------------------------ | ------------------------------------------------------------ |
| 制度→代码  | `content/02_institution/sop/*.md`                                                    | `docs/src/*.js`                | 母本优先，代码跟随（见 [SOP\_WEBSITE_GUIDE.md](content/04_web_design/module/SOP_WEBSITE_GUIDE.md)） |
| 理论→工程  | `content/{01_strategy,02_institution,03_doc_system,04_web_design,05_ai_coding}/*.md` | `docs/src/*.js`                | guides 定义设计，代码实现设计                                           |
| 路线图→执行 | `CLAUDE.md` 乙部                                                                       | `.ctx/logs/*-EXECUTION_LOG.md` | 完成事项从乙部删除，写入执行日志                                             |
| 经验→沉淀  | `.ctx/logs/DECISION_LOG.md`                                                          | `content/insights/*.md`        | 决策日志定期沉淀为经验沉淀                                                |
| 术语→全仓  | `content/03_doc_system/USAGE_POLICY.md`                                              | 全仓库所有文件                        | 术语变更触发一改具改                                                   |

### H30.3 Guides 与 Insights 的定位

- **Guides（`content/01_strategy/`、`content/02_institution/`、`content/03_doc_system/`、`content/04_web_design/`、`content/05_ai_coding/`）**：设计理念文档。不管对应功能有没有完成，设计理念始终存在，作为知识留存。
- **Insights（`content/insights/`）**：经验沉淀文档。按一定频率根据需求和日志进行沉淀更新。
- 两者都是**稳定的知识资产**，不因执行事项的完成而删除。

### H30.4 经验沉淀规则

> 🔴 **每次向 insights/ 写入内容时必须遵守以下规则。**

1. **结构兼容性**：写入前与既有结构逐节比对，兼容则追加，不兼容则写入丙部
2. **按需正反两面论**：只有当命题存在真正的对立方案（读者可能真正会想到的替代选择）时，才需要反论。判定依据见 [OPERATIONS\_GUIDE.md §11.2/§11.3/§11.7/§11.9](content/03_doc_system/OPERATIONS_GUIDE.md)
3. **禁止冗余标记**：正文中不标注版本号/批次/时间戳，这些归YAML
4. **沉淀标签闭环**：沉淀完成后，将日志中 `[待沉淀]` 改为 `[已沉淀: 沉淀位置]`
5. **后推翻前规则**：时间靠后的决策可能推翻先前的决策，时间靠后的执行可能推翻先前的执行。经验沉淀必须标注**生效条件**——在什么前提下成立。当新经验与旧经验矛盾时，不得删除旧经验，而是在旧经验条目下追加"已被XX条件下的新经验修正"标注，在新经验条目中注明"修正了XX条件下的旧经验"。经验不打架，而是条件化。
6. **沉淀标签预审查**（2026-07-31 更新）：日志条目的沉淀标签从二档（是/否）改为条件触发——**只有产生新模式时才需标注**，否则省略标签。具体规则：
   - **按规执行**：如果工作完全按 insights/Harness/KNOWN\_PITFALLS 中已有原则执行，**省略沉淀标签**（日志条目仍需记录，只是不写沉淀判定段）
   - **`[待沉淀: 简述新发现]`**：工作中发现 insights 中没有的新模式/新教训时标注
   - **`[已沉淀: 沉淀位置]`**：完成沉淀后将`[待沉淀]`改为`[已沉淀]`
   - **不再使用`[否]`标签**：之前的`[经验沉淀: 否]`不再使用

> 🔴 **沉淀优先级联动**：沉淀按优先级写入权威源——**上下文文档（01-05）→ CLAUDE.md（重要者全部写入或更新指针）→ insights（最后，去语境化后写入）**。与 H80 反馈学习闭环的沉淀优先级一致（机制见 H80），两条路径表述统一，防止分叉。

***

## H40. 文件修改检查清单

每次修改文件时，Agent 必须按以下清单执行：

```
□ 1. 确认修改范围：哪些文件受影响？
□ 2. 检查钩稽：CLAUDE.md / guides 中是否有对该文件的引用？
□ 3. 一改具改：全仓库搜索相关引用，逐一同步更新；完成后 Grep 验证零残留（H30.1）
□ 4. 术语合规：修改内容是否符合 USAGE_POLICY.md §一？
□ 5. 母本优先：若涉及 SOP 制度变更，先改 content/02_institution/sop/ 再改代码
□ 6. YAML 更新：🔴 任何有 YAML frontmatter 的文件被修改后，必须更新 last_updated 字段。子任务修改文件时同样必须遵守。遗漏即违规。
□ 7. 记录日志：写入 .ctx/logs/YYYY-MM-EXECUTION_LOG.md
□ 8. 乙部更新：若涉及执行事项，更新 CLAUDE.md 乙部
□ 9. 表达合规：若本次修改涉及支书原话展开，必须调用 USAGE_POLICY.md §二（AI 展开原则，含 2.3 五条可复用检查规则）自查（"而非"警惕/"所以"因果链/反面假设/私加强调/元叙事标签）
□ 10. 反论合规：若本次修改涉及反论（"为什么不是..."），必须按 OPERATIONS_GUIDE.md §11.6 自检清单逐项验证，含§11.7历史范畴检查（被否定项是否仍在使用）和§11.8 AI治理技术特殊性（涉及AI工具使用时需联网补充）
□ 11. 概念命名合规：若本次修改涉及概念命名或标签创建，必须按 H40.1 概念命名守则自检
□ 12. 设计原则自检（EP-06 修复，2026-08-30 AI逃逸捕捉第三轮）：若本次修改涉及 UI/工作台/数据流/组件/颜色，必须对照 DESIGN_SYSTEM.md 相关原则的「可验证条件」与 CLICK_ROUTING.md 分层落点自检；改颜色必须过 DESIGN_SYSTEM §7.3 深色自查 9 条
□ 13. 文档目的达成度自查（2026-09-05 专项⑨）：若本次新建/大改 content 文档，按 PROCESS_GUIDE.md §18.8 五类判据自查——宣称目的真的实现了吗（不是形式主义小点）；新文档验收用「读完能做什么」判据
```

### H40.1 概念命名守则 \[工作表达]

> 支书元洞察（2026-07-19 概念复用性扫描评议）："最大的根因在于：一个大的命题可能有很多个小命题。AI专注于过拟合那些小的命题的命名。但事实上，只需要给总的大命题命名即可。"

**适用范围**：全仓库所有概念标签、速查表、insights 沉淀、governance 文档中出现的命名性标签

**五条守则**：

1. **大命题原则**：一个大的命题可能有很多个小命题，只给大命题命名，小命题作为大命题下的描述存在，不单独命名。判断标准：该命题是否有独立的理论地位或实践领域？若无，则归入大命题。
2. **"="式命名禁令**：不得使用"="连接两个概念作为标签名（如`[A=B]`）。"="暗示数学等价，但 insights 中的"="用法几乎都是单向蕴含（A导致B、A意味着B、A的判定标准是B）。替代方案：用"即"式（如`[A即B]`）或叙述式。
3. **AI层级标签禁令**：不得添加"基础层/进阶层""初级/高级"等无依据的层级标签。除非支书明确提过分层，否则并列关系不得被AI擅自标为层级递进。
4. **一致性检查**：如果只有某一类有独立标签（如只有"纪检工作内容"而没有"组织工作内容""宣传工作内容"），必须质疑组织方式的准确性——要么三类都有，要么归入统一的大命题下。
5. **术语精确性**：党支部委员简称是"支委"不是"委员"；党建 = 三会一课、主题党日、专班工作；党务 = 党员发展、考勤考察；两者都是"管理事，服务人"的工作，不用"服务同学 vs 供支委决策"区分。

***

***

# 温层：修改时必读

> 🟡 **以下内容在修改文件、更新乙部/丙部、执行支书评议时需要参考。**

***

## H50. 乙部与丙部生命周期

### H50.1 乙部任务生命周期

> 🔴 **乙部条目的写入/更新/退出为强制规则，不得违反。**

**1. 写入**：用户提出新任务 / Agent发现大任务 / 周期性任务到期 / 甲部修改衍生 / 用户明确要求

**不写入的场景**：可一次性完成的小任务（修typo/补字段），不分配T编号，但必须写入执行日志

**判断标准**：

```
用户明确要求写入？ ──是──→ 必须写入乙部
        │否
能否一次完成？ ──否──→ 必须写入乙部（按 OPERATIONS_GUIDE.md §10.5 分片）
        │是
涉及 3+ 文件或制度变更？ ──是──→ 写入乙部
        │否
→ 当次完成，仅写执行日志，不入乙部
```

**2. 更新**：开始执行→🔄进行中 / 暂停→⏸️暂停 / 描述不准→更新描述

**3. 完成退出**：已写入执行日志 → 从乙部表格直接删去（禁止仅标✅不删除）

### H50.2 丙部待决策机制

> 🔴 **以下场景必须写入丙部提交支书决策，不得自行决定。**（含甲部其他规则衍生的写入路径）
>
> 1. **拿不定主意**：AI 在执行任务时遇到选择困难，无法自行判断方向
> 2. **发现待决策项**：AI 在分析过程中识别出需要人类判断的事项（如孤立组件、设计分歧、功能去留），即使 AI 自己没有选择困难，也必须提交
> 3. **甲部衍生路径**：H30.4 结构不兼容 / H60.3 设计性问题 / 其他甲部规则明确要求入丙部的场景

**自省补丁（D-207）**：此前 H50.2 仅覆盖"拿不定主意"，未覆盖"发现待决策项"。后者是独立触发条件——AI 可能对某事项有自己的判断，但该事项涉及功能去留/设计方向，属于人类决策权范畴，AI 不得自行处置。

**理解验证补丁（D-220）**：提交丙部前，必须验证自己对问题的理解是否正确。若对问题本身（而非仅对解决方案）存在不确定，应先向支书确认理解，而非基于错误前提提交伪问题。**提交一个基于错误前提的丙部条目，比不提交更糟糕**——它浪费支书时间，且可能误导决策方向。

**数量过拟合补丁**：当支书用自然语言说出N个观察点时，AI不得自动锁定数量或自造结构性标签（如"支柱""维度"等）。支书的散点表达可能是4个、也可能调整为3个或5个——AI应先确认是否需要结构化，再决定组织方式。AI自行概括的术语属于\[工作表达]（见 USAGE\_POLICY.md §1.6），不得伪装为支书原话。

**审议交付机制（2026-08-30 支书裁决 · 重要 harness 规则）**：一旦涉及审议（向支书提交待决策/待确认事项），交付方式是 **AskUserQuestion 逐点决策**——在需要支书决策的地方，用提问机制提供需要的内容与选项让支书选择；**禁止输出一长串报告让支书通读后自行消化**（支书原话："所谓给我上下文，不是【你输出一个报告给我】，而是基于 ask user question 的机制，在需要决策的地方，提供需要的内容"）。多事项需决策时拆成多个问题逐点问；问题须自带选项与推荐，支书可一键选答；已交付待支书审的事项，后续跟进时也用 AskUserQuestion 收敛而非重述报告。

**格式规范**：

```markdown
## P.<序号> <问题标题（用人话）>

**为什么需要决策**：<一句话说清痛点>
**路线图**（每一步都需要支书选择方向）：
**第一步：<最核心的分歧点>**
| 方向 | 含义（人话） | 后果（人话） |
**当前状态**：等待支书选择第 X 步方向
```

**写作禁令**：禁止写文件路径 / 禁止用技术变量名 / 禁止3+并列选项 / 必须路线图式呈现

**生命周期**：写入丙部 → next\_prompt提交 → 支书决策 → Decision Log归档 → 执行 → 从丙部删除

**多步决策规则**：丙部条目可能包含多步决策。当所有步骤均已获得支书明确方向（包括"搁置"/"删除"等否定性决策）时，该条目视为决策完成，应退出丙部。三种决策状态的语义边界（详见 content/05_ai_coding/CONTEXT_MANAGEMENT.md「三种决策状态的语义区分」）：待决策=未决策（停留丙部）；搁置=时机判断（已决策"现在不做"，退出丙部）；删除/不需要=终局判断（已决策"明确不需要"，退出丙部）。"搁置"是已做出的决策，不是"待决策"的暂存。

***

## H60. 支书评议工作流 \[工作表达]

> "支书评议"是 T3 工作表达术语（AI 为交流便利自行概括，非严肃政治表达），用于系统性抽样评议仓库内容并触发纠偏。
>
> **元原则**（支书 2026-08-09）：**支书所有的原话都是一个 big picture 下的不同侧面，一定要去体悟这种统一性和一致性，对支书的原话融会贯通！**——评议支书原话时，须把每条原话放回整个论述体系，理解它与其他原话的统一性和关联（区分"论述的扩充"与"表达的修正"），而非孤立地逐条审视。支书原话会随时间和实践深入而更精确：①可能和原来的表达不同（要反复确认，取其精华、去其糟粕）；②大部分情况是对既有论述的扩充（支书在阅读 AI 扩充后意识到表达不清楚连 AI 都会误解，何况于人，故会强调这些问题）。
>
> **细节指针**：抽样规则、预审维度体系、评议记录格式、概念复用性扫描流程、三类评议类型速查（反论/理论复用/补丁审查）的完整规定见 [PROCESS\_GUIDE.md §18](content/03_doc_system/PROCESS_GUIDE.md)。
>
> **向支书提问/请裁的准则**（支书 2026-09-06 定稿）：**支书不是决断代码的，而是决断 strategy、product、marketing 的。** AI 负责代码与工程方案。凡需支书裁决的事项，一律先翻译成「战略/产品/运营叙事」层的选项与建议（附上下文、取舍、推荐档），禁止拿代码实现细节、字段命名、技术参数、内部机制等"代码层选择题"去问支书。做一个好秘书：**把决策做薄、把问题做厚**——能由 AI 消化执行的不上报，只把「方向/口径/产品形态/对外叙事/取舍原则」留给支书。适用场景含 H60 评议、REVIEW_QUEUE 逐条问裁、立项/设计审批、AskUserQuestion 提问。
> **向支书报告的表现准则**（支书 2026-09-06 补强）：向支书的一切文字（总结/报告/提问/方案）只陈述**它意味着什么交互、什么功能、什么产品含义**；**禁止把代码实现细节（文件路径/行号/commit/内部机制/测试内部）作为请裁或汇报内容**；技术细节只写进代码注释、子代理任务、执行日志等"给 AI 自己看"的层。写完自检：通读一遍，凡支书需"读代码才能懂"的句子都要重写成产品语言（判例：2026-09-06 支书提醒"不要给支书看纯代码，而是告知交互与功能"）。

### H60.1 触发条件

- **按需启动**：支书说"做一次评议"时启动
- **周期触发**：每周一次，作为 OPERATIONS\_GUIDE.md §17 周期任务（W3 支书内容评议）
- **常态化专项触发**：W4 专项评议循环每周轮转，承接常态化专项评议（专项清单、侧重维度与轮次进度见 H60.5 + `.ctx/REVIEW_QUEUE.md` 附录）

### H60.2 评议流程

1. AI 抽样 10-15 条命题（抽样规则见 OPERATIONS\_GUIDE.md §18.1）
2. 对每条命题按其特性灵活选择预审维度（预审维度体系见 OPERATIONS\_GUIDE.md §18.2，不锁定数量；若某命题只审部分维度，预审报告中注明"本次仅审 XX 维度，理由：……"）
3. AI 将完整预审报告写入 `.ctx/REVIEW_QUEUE.md`，通知支书可随时评议
4. 支书在 REVIEW\_QUEUE.md 中填写反馈
5. AI 读取反馈，按 H60.3 三层分流归档，按 H60.4 校验后将完整记录写入执行日志 T 编号，然后清空 REVIEW\_QUEUE.md 当前轮次（REVIEW\_QUEUE.md 是上下文文件不是日志文件，历史记录不在此留存）

> **预判差异记录**：当支书判断与AI预审结果不同时，差异本身是学习信号——须在评议记录中分析差异原因（如AI未检查历史范畴、AI错误组合概念、AI未识别重点偏移等），并评估是否需要沉淀为新的评议维度或检查规则。反论评议专用维度见 OPERATIONS\_GUIDE.md §11.9。

### H60.3 反馈分流归档

支书反馈后，AI 按反馈性质三层分流：

| 反馈类型  | 判定标准                         | 处理方式                        | 归档位置                       |
| ----- | ---------------------------- | --------------------------- | -------------------------- |
| 机械性问题 | 设问句/术语违规/主观预设/表述违规等可模式化识别的问题 | 全仓库扫描同类模式→一并修订→按 H60.4 抽样校验 | 月度执行日志（含全仓库扫描范围+修订数量+校验结果） |
| 设计性问题 | 结构性争议/设计方向分歧/规范适用范围判定        | 写入丙部 P 编号                   | CLAUDE.md 丙部               |
| 即时小修订 | typo/格式错误/链接断裂等无连锁影响的小问题     | 当场修复                        | 月度执行日志                     |

**关键区分**：机械性问题与设计性问题的边界——若反馈涉及"该规则是否适用于此场景"的判定，归设计性问题；若反馈确认违规且需扫描同类模式，归机械性问题。

**新原则整合收口（3.1 机制，2026-08-09 补）**：支书评议/决策中确立的新原则、新原话，决策后 AI 必须检查是否需要整合进长期文档——涉及 AI 行为约束/工作流的原则→CLAUDE.md 甲部（或对应 governance 文档）；涉及支书原话→content 权威源（SECRETARY\_DIRECTIVES 等）；涉及设计与经验→对应设计文档 / insights。整合动作必须写入评议记录（格式见 OPERATIONS\_GUIDE.md §18.3）。原则只有落到长期文档，后续工作才能持续遵守——"决策归档即遗忘"等同未落地。

### H60.4 修订校验原则 \[工作表达]

> "修订校验"是 T3 工作表达术语，指 AI 全仓库扫描修订后，防止过度修订的抽样校验机制。
> 修订完成与修订正确相区分——AI 修订的最大风险在于"过度修订"，而非"漏改"。

**核心约束**：当机械性问题触发全仓库扫描修订时，AI 必须防止过拟合——避免把"该改的"和"不该改的"一并修订为"标准模板"。

**校验原则**：

1. 修订完成后，AI 按修订规模抽样若干项，向支书呈现"修订前后对比"判断题
2. 抽样比例按修订规模灵活调整，不写死具体数字
3. 支书判定不通过的修订项，退回调整并重新扫描同类模式
4. 校验通过后，将抽样清单与支书判断结果写入评议记录（格式见 OPERATIONS\_GUIDE.md §18.3）

**为什么需要校验**：支书的判断是修订合理性的最终标尺。AI 自行判定的"标准表述"可能与支书认可的"工作表达"冲突——例如某处设问句是反论结构标记，不应被机械修订为陈述句。

**适用范围**：仅适用于机械性问题的全仓库扫描修订；即时小修订和设计性问题不触发校验。

**终止条件**：每轮校验后，若支书仍有反馈则继续迭代；若支书明确"暂时到此"或当前 session 上下文已接近上限，则在 next\_prompt 中记录校验进度和待续项，由下一个 session 继续。不允许因偷懒放弃，但允许因上下文限制而挂起并交接。

### H60.5 常态化专项评议机制 \[工作表达]

> **定位**：常态化专项评议的周期承接机制（支书 2026-08-10：「把一些常态化的评议检查，提高到经常性、周期性工作的位置！」）——专项评议任务**不驻留乙部**，制度存续由本机制 + `.ctx/REVIEW_QUEUE.md` 附录保证，乙部清理不灭机制。

| 专项         | 来源          | 性质                                        | 侧重维度承载            |
| ---------- | ----------- | ----------------------------------------- | ----------------- |
| ① 反论评议     | T-116       | 全仓库反论的支书评议（速查见 OPERATIONS\_GUIDE §18.5）   | REVIEW\_QUEUE 附录① |
| ② 理论复用评议   | T-117       | 跨节/跨文件引用复用评议（速查见 OPERATIONS\_GUIDE §18.5） | REVIEW\_QUEUE 附录② |
| ③ 黑话审查     | T-195       | 表述维度专项（OPERATIONS\_GUIDE §18.5 理论复用 D 表述） | REVIEW\_QUEUE 附录③ |
| ④ 支书原话逐条复核 | T-200       | SECRETARY\_DIRECTIVES 原话精确性           | REVIEW\_QUEUE 附录④ |
| ⑤ 最小三成本    | 总纲级（H10 声明） | 系统设计最高验收标准（最小信息/操作/适应学习成本）                | REVIEW\_QUEUE 附录⑤ |
| ⑥ 减负评议     | 2026-08-13 支书发起 | 全仓库减负专项（制度层混入实现层等）                  | REVIEW\_QUEUE 附录⑥ |
| ⑦ AI逃逸捕捉   | 2026-08-30 支书引入 | 提示词体系逃逸点五维扫描（E1-E5，rule-based 收敛）          | REVIEW\_QUEUE 附录⑦ |
| ⑧ 功能实现评议  | 2026-08-30 支书引入 | SOP/设计考量 ↔ 代码实现对照核查（描述的功能是否已实现，判例：思想汇报） | REVIEW\_QUEUE 附录⑧ |
| ⑨ 文件目的达成度 | 2026-09-05 支书发起 | 每篇文件真实现所宣称目的（五类判据 + v1.1 补充，判据正文见 [PROCESS\_GUIDE.md §18.8](content/03_doc_system/PROCESS_GUIDE.md)） | REVIEW\_QUEUE 附录⑨ |

**机制要点**：

- **轮转承接**：W4 每周轮转（OPERATIONS\_GUIDE.md §17.2.1），每个专项按 H60 工作流执行——抽样→预审→支书反馈→三层分流归档
- **侧重维度复用**：支书在特定评议过程中指示的侧重维度，随轮次记录于 REVIEW\_QUEUE 附录「支书特别要求」字段；后续轮次启动时先读附录继承侧重维度，避免重复摸索
- **开放机制**：专项清单可随支书指示扩展（支书 2026-08-10：「评议绝不仅仅只有这些！」）
- **与乙部关系**：一次性评议轮次写入执行日志 T 编号即归档；长期专项的「轮次进度/已执行工作/支书特别要求」以附录挂载 REVIEW_QUEUE，不占用乙部。**例外**——已排期的机制轮次（C4 等支书批准批次）驻留乙部「评议轮次排期」作待发起提醒，轮次发起后即从乙部删除（机制仍由本条款 + C 表保证）。

***

## H60.6 评议检查要点生命周期（触发式入库，2026-08-27 支书裁定）

> **定位**：评议的「方法输出」机制——让 AI 在评议/审查中**自发现**的检查维度/评判标准，在满足触发条件时入库为下轮继承基线。**触发式而非每轮强制**（支书 2026-08-27：「不是每次，而是设定一定的 trigger 来输出触发，一定防止过拟合」）。
>
> **为什么需要**：H60.3 归档只规定"本轮修复"（修订/丙部/小修订），未规定"AI 自发现方法的存续"——导致 AI 自发现的检查方法（如数据生命周期一致性、四层链接审查、点击计数）只能靠自觉写入或直接丢失，支书需反复提示。本机制让「评议一次 → 方法存续一次」。

**触发条件**（满足任一即入库；不满足则归档注明"按既有基线执行，无新检查要点"，省略入库）：
1. **新维度**：本轮发现既有检查要点库/CHECKLIST 未覆盖的评判维度（如数据生命周期一致性、链接四层法）
2. **新侧重**：支书在本轮评议中确立了新的侧重维度（H60.1 触发条件②的承接）
3. **重复判例**：同一类错误模式第二次出现（第一次记 05_ai_coding 分篇判例，第二次触发入库为检查项）

**入库分级**：
| 入库对象 | 目标位置 | 标注 |
| --- | --- | --- |
| 通用检查要点（跨场景可复用） | DATA_CONSISTENCY_CHECKLIST.md（追加小节） | 来源轮次 + 生效条件 |
| 专项检查要点 | REVIEW_QUEUE 对应专项附录「检查要点库」区 | 来源轮次 + 生效条件 |
| 错误模式判例 | content/05_ai_coding/ 对应分篇（见该层 [README.md](content/05_ai_coding/README.md) 索引） | 判例 + 纠正清单 |

**防过拟合铁律**（支书强调）：
1. 入库粒度 = **检查维度/标准（大命题）**，禁止把单个用例细节机械入库（H40.1 概念命名守则①大命题原则）
2. 每条入库须标注**生效条件**（什么场景适用）与来源轮次
3. 无新增维度时**省略入库**（与 H30.4 沉淀标签预审查同构：按规执行省略标签），不得为入库而入库

**继承**：下轮评议启动（H60.1/H60.2 抽样前）先读本专项「检查要点库」+ CHECKLIST 相关章节 + **评议方法总索引**（REVIEW_QUEUE 附录头部），作为预审维度基线——检查要点随轮次自动累积，不依赖支书每次重新提示。

**与既有机制边界**：H60.3「新原则整合收口（3.1）」管**支书确立**的原则/原话；H60.6 管 **AI 自发现**的方法性检查维度——两者互补不重复。H80 反馈学习闭环（捕获→沉淀→复用）是本机制的操作层，H60.6 是其评议场景的触发规则。

## H60.7 评议→待办归口闭环（2026-09-08 支书令）

> **定位**：评议产物的「待办去向」规则——评议产物不得散落。REVIEW_QUEUE 附录只挂长期专项承接与检查要点；一次性执行项、需决策项、已排期轮次一律按本条款归口乙部/丙部，随 H50 生命周期流转、完成即清。

**归口判据表**：

| 待办类型 | 判定标准 | 去向 | 引用流程 |
| --- | --- | --- | --- |
| 执行型 | 已裁方案或已登记缺陷、可一次完成 | 乙部「评议待办 · 执行型」，完成后归档 T 编号删行 | H50.1 + 对应检查要点 |
| 需决策型 | 需支书决策 / 逐条过目 | 丙部 P 编号，支书决策后退出 | H50.2 + 理解验证补丁（D-220） |
| 已排期机制轮次 | 支书已批准批次（C4 等） | 乙部「评议轮次排期」作待发起提醒，发起即删 | H60.5 + C 表 |
| 无完成态周期任务 | 常态化持续、无「完成」态 | 入 C 表既有行承接，不占新行 | H60.5 + C 表 |

**闭环流程**（每轮评议按 H60.2 分流归档后执行）：提取待办 → 按判据入乙/丙 → 执行或支书决策 → 归档 T 编号 → 回写 `.ctx/REVIEW_QUEUE.md`「全局评估总表」对应专项健康分（分随事走：AI 建议分、支书校正定案）→ 从乙/丙删除 → 主队列清空。

**与既有机制边界**：H60.3 分流归档管「修订落地」、H60.6 管「检查方法存续」、H60.7 管「待办去向」——互补不重复；H80 反馈学习闭环是本条款的操作层。

***

## H70. 反思触发流（🔴 支书说"反思/值得思考"时强制触发）

> **支书指令（2026-08-09）**："每次在支书提到反思的时候……这个值得好好思考！""反思的时候，所有链接'指向'的 content/docs 文件也要有选择地读！"
>
> 反思不是口头表态，是**有产出动作的学习闭环**。沉淀机制的价值在于：支书纠正过一次的问题，系统应永久记住，不再让支书反复提起。触发反思流 = 把支书的反思指令转化为实际的排查与修正。

当支书表达"反思""值得思考""为什么还在"等反思信号时，AI **必须**按以下流程执行，不得只口头反思：

1. **定位权威源最近变更**：读 [USAGE\_POLICY.md](content/03_doc_system/USAGE_POLICY.md) §1 术语表 + [SECRETARY\_DIRECTIVES.md](content/01_strategy/SECRETARY_DIRECTIVES.md) 最新 P 编号，确认最近一次定义/术语/层级变更是什么（如 2026-08-03 确立党建/党务 T1 官方定义）
2. **沿链接传播排查**：从权威源出发，**追踪所有链接"指向"的 content/docs 文件**，有选择地读取被引用文件，确认定义是否已同步（🔴 支书补充要求：不能只看权威源本身，必须顺链接读下游）
3. **全仓 Grep 旧表述**：搜索被降级/替换的旧术语、旧定义、旧层级表述，列出全部残留位置
4. **一改具改**：按 H30.1 全仓同步修正；区分「定义残留」（必须改）与「合法功能分区标题/UI 标签」（保留，如 `## 党建工作` 章节标题、`【党建工作】` 场景前缀）
5. **机制补缺**：若残留根因是审计项缺覆盖（如 C-5 未包含"已降级定义残留"），同步补强审计项，防止复发
6. **沉淀验证**：将本次"变更未传播"教训按 H30.4 沉淀进 insights，标注生效条件；反思闭环才算完成

**为什么必须顺链接读下游**：权威源更新不会自动传导到下游。支书原话、USAGE\_POLICY、insights、SOP 母本、架构文档分层散落，任何一层未同步都会造成"旧定义赫然在重要文件"（2026-08-09 党建/党务旧定义残留 5 处即此病）。只改权威源不查下游，等于没反思。

***

## H80. 反馈学习闭环

> **定位：** 把支书反馈（纠正/新原则/评议结论）固化为"捕获 → 沉淀 → 复用"的常态闭环，与 H70 反思流（触发→排查）、H60 评议（抽样评议）互补。

**三环节**：

1. **捕获**：支书反馈出现时即标记性质（元内容 / 过拟合 / 经验补充），记录于执行日志
2. **沉淀**：按优先级写入权威源——**上下文文档（01-05）→ CLAUDE.md（重要者全部写入或更新指针）→ insights（最后，去语境化后写入）**
3. **复用**：下次工作自动应用——元层索引（本条 + insights §7 元层）+ H40 自查清单 + project-memory

**沉淀优先级理由**：insights 是 01-05 的提炼，原生规则先落上下文文档，提炼后的通用表述后落 insights。

**与 H60/H70 的边界**（三机制互补，不重复）：

| 机制      | 触发               | 范围              | 产出         |
| ------- | ---------------- | --------------- | ---------- |
| H70 反思流 | 支书「反思/值得思考」信号    | 沿链接传播排查（权威源→下游） | 定义传播的排查与修正 |
| H60 评议  | 周期（W4）/支书「做一次评议」 | 抽样审查（10-15 条命题） | 评议记录       |
| H80 闭环  | 日常支书反馈出现时        | 捕获→沉淀→复用        | 经验固化       |

三者互补：反思流管「定义传播排查」，评议管「内容质量审查」，闭环管「反馈固化为规则」。

***

# 冷层：按需参考

> 🟢 **以下内容已外移至对应权威源，本节仅保留链接。AI 按需读取。**

***

## H90. 外部权威源索引

> 以下内容已外移至对应权威源，本节仅保留链接。AI 按需读取。

| 我需要...     | 去哪里                                                                                       |
| ---------- | ----------------------------------------------------------------------------------------- |
| 看一改具改      | 本文件 H30                                                                                   |
| 看检查清单      | 本文件 H40                                                                                   |
| 看乙部/丙部规则   | 本文件 H50                                                                                   |
| 看支书评议工作流   | 本文件 H60                                                                                   |
| 看甲部修改流程    | [PROCESS\_GUIDE.md §15](content/03_doc_system/PROCESS_GUIDE.md)                     |
| 看日志规范      | [OPERATIONS\_GUIDE.md §10](content/03_doc_system/OPERATIONS_GUIDE.md)                     |
| 看面向用户表述规范  | [OPERATIONS\_GUIDE.md §13](content/03_doc_system/OPERATIONS_GUIDE.md)                     |
| 看理论基石      | [SECRETARY\_DIRECTIVES.md](content/01_strategy/SECRETARY_DIRECTIVES.md)（项目顶级战略文档） |
| 看经验沉淀      | [content/insights/](content/insights/)                                                    |
| 看已知陷阱      | [05 AI 协作方法论 README.md](content/05_ai_coding/README.md)（5 分篇索引）          |
| 看运行标准      | [OPERATIONS\_GUIDE.md](content/03_doc_system/OPERATIONS_GUIDE.md)                         |
| 看术语规范      | [USAGE\_POLICY.md](content/03_doc_system/USAGE_POLICY.md)                                 |
| 查 SOP 流程   | [content/02\_institution/sop/INDEX.md](content/02_institution/sop/INDEX.md)               |
| 查决策历史      | `.ctx/logs/DECISION_LOG.md`                                                               |
| 查执行日志      | `.ctx/logs/YYYY-MM-EXECUTION_LOG.md`                                                      |
| 查母本链路      | [SSOT\_INDEX.md](content/03_doc_system/SSOT_INDEX.md)                                     |
| 查数据一致性     | [DATA_CONSISTENCY_CHECKLIST.md](content/05_ai_coding/DATA_CONSISTENCY_CHECKLIST.md)                                        |
| 取用模板       | `content/03_doc_system/工作模板/`                                                             |
| 查可用 Skills | `npx skills find <keyword>`                                                               |
| 看项目产出声明    | 本文件 H100                                                                                  |

***

***

## H100. 项目产出声明

> 系统最最重要的产出是一个完整叙事——"管理事，服务人"作为总论，囊括一整套组织架构/分工/SOP 和一整套关于 AI-driven 的组织形态的仓库工作流，而非某个功能模块。本节是元命题的治理层锚点。

### H100.1 系统最最重要的产出

支书原话（2026-07-14）："系统最最重要的产出，是【管理事，服务人】一个'从入党申请人到正式党员'的完整叙事【这个故事囊括了②和③】，其中最重要的内容（1）一方面是积极分子、发展对象……到正式党员的流程怎么走（这是大家最关心的问题）；（2）另一方面就是我们在实际工作中形成的，关于一个学生的党支部中 组织性如何判定的过程（这里面可能还有一些小小的逻辑缺漏——在这里获得的组织性和生涯发展的关系是什么，为什么这是一个很宝贵的机会，这部分我会做更加详细的判定，但是你要记得这部分是须要补充完整的）。"

支书原话（2026-07-20）："管理事，服务人 这件叙事，他不是仅仅针对支委而言的；我们需要讲好 一个人从积极分子一路成长为党员的时候，他的身份角色要发生怎样的改变。（不要把'先锋模范'看作一个 抽象符号，先锋模范在具体的工作中就能体现，任何一名党员（包括支委会在内）在管理事 和 服务人 方面都有自己的用武之地和成长空间！！）这解释了为什么我认为这是一套完整的叙事！！"

**系统产出的三个组成部分**：

| 层级                | 内容                                           | 权威源                                                                                                                                                           |
| ----------------- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ① 总论叙事            | "管理事，服务人"——从入党申请人到正式党员的完整路径，囊括②和③            | [DEVELOPMENT\_PATH.md](content/01_strategy/DEVELOPMENT_PATH.md) + [SECRETARY\_DIRECTIVES.md P-002/P-003](content/01_strategy/SECRETARY_DIRECTIVES.md) |
| ② 组织架构与 SOP       | 一整套组织架构、分工、SOP                               | `content/02_institution/sop/` + `content/02_institution/FLAT_ORGANIZATION_DESIGN.md` + `content/02_institution/COMMISSIONER_DUTY_FRAMEWORK.md`                                  |
| ③ AI-driven 仓库工作流 | 一整套关于 AI-driven 的组织形态的仓库工作流（上下文、Harness、提示词） | 本文件 CLAUDE.md（Harness）+ `content/03_doc_system/OPERATIONS_GUIDE.md` + `.ctx/`（审计底座）                                                                           |

### H100.2 逻辑缺漏与支书亲补

支书特别标注了一个"逻辑缺漏"——"在这里获得的组织性和生涯发展的关系是什么，为什么这是一个很宝贵的机会"——这部分由支书亲自补充完整，AI 不得自行假设或基于猜测补全。

支书已于 2026-07-14 亲补两个宝贵机会：

- **机会1**：民主集中制下感受真实组织的两个向度——"赋权"背景下的程序性 和 "探索"背景下的扁平化。详见 [SECRETARY\_DIRECTIVES.md P-014](content/01_strategy/SECRETARY_DIRECTIVES.md) + [DEVELOPMENT\_PATH.md 第一章收束](content/01_strategy/DEVELOPMENT_PATH.md)。
- **机会2**：AI 时代中学生党支部的探索机会——①真实地参与组织制度和组织文化的构建；②在"没有经济负担"的背景下探索 AI 时代下组织转型的萌芽和组织产品的生产。详见 [SECRETARY\_DIRECTIVES.md P-015](content/01_strategy/SECRETARY_DIRECTIVES.md) + DEVELOPMENT\_PATH.md 第一章收束。

### H100.3 引用流程

- 任何涉及"系统产出是什么"的总论性表述，引用本节 H100
- 任何涉及"发展路径"的具体叙事，引用 [DEVELOPMENT\_PATH.md](content/01_strategy/DEVELOPMENT_PATH.md)
- 任何涉及"支书论断"的具体论断，引用 [SECRETARY\_DIRECTIVES.md](content/01_strategy/SECRETARY_DIRECTIVES.md)
- 本节为治理层锚点，不承载叙事全文——叙事全文在 DEVELOPMENT\_PATH.md，论断全文在 SECRETARY\_DIRECTIVES.md

### H100.4 门面文档避讳原则

门面文档（根 README、宣传材料、开源介绍等以公开读者为对象的文本）正文不出现「支书/支书思想」等来源标签——思想融于叙述：讲透"为什么这样设计"的理由与内在逻辑，让读者读完自能体会一以贯之的理念，而非贴标签声明思想的来源。认知形成靠的是内容本身的说服力，不是署名。内部治理文档（CLAUDE.md、执行日志、SECRETARY\_DIRECTIVES.md 等以归因精确为先的文档）不受此限——内部恰恰需要明确归因，两者是不同场景，不构成矛盾。

***

***

# 乙部：具体执行事项（常为新的）

> **常为新的原则**：
>
> 1. 每个条目必须有**引用流程**——指向甲部（H10\~H100）或 guides 或 insights 中的对应原则
> 2. 条目完成后，在确认已写入【执行日志】的前提下，**直接删去**
> 3. 补充新的执行事项时，必须标注引用流程
> 4. 已完成事项的详细信息归档于 `.ctx/logs/DECISION_LOG.md` 和月度执行日志
> 5. 大任务必须按 OPERATIONS\_GUIDE.md §10.5 分片规则提前拆分

***

## 持续任务

> 持续任务没有"完成"状态，只有"进行中"或"暂停"。它们代表需要持续关注的工作方向。

| ID    | 事项                                                                                                                                                                                                                           | 引用流程                                                                                     | 修改对象                                              | 状态    |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ------------------------------------------------- | ----- |
| C-1   | **JS 组件化**：完善 `docs/src/` 下的 JS 组件（renderHeader/renderSidebar/renderFooter/renderCalendar 等），使 HTML 仅需引用/调用特定 JS 组件即可实现功能，消除 HTML 中的硬编码逻辑                                                                                    | H30.4 规则 2（方向指引）→ [SOP\_WEBSITE_GUIDE.md](content/04_web_design/module/SOP_WEBSITE_GUIDE.md)                        | `docs/src/*.js` + `docs/*.html`                   | 🔄 持续 |
| C-2   | **一改具改巡检**：定期检查仓库中是否存在信息重复散落，发现后归并至权威源                                                                                                                                                                                       | H30.1 + OPERATIONS\_GUIDE.md §1                                                          | 全仓库                                               | 🔄 持续 |
| C-3   | **经验沉淀**：从执行日志和决策日志中提炼可复用模式，写入 insights                                                                                                                                                                                      | H30.4 + OPERATIONS\_GUIDE.md §10.1 → [党支部管理与实务经验沉淀.md](content/insights/党支部管理与实务经验沉淀.md) | `content/insights/`                               | 🔄 持续 |
| C-4   | **视觉体验持续优化**：颜色方案调优 + 卡片设计（嵌套/并列/顺序排布）审校修订                                                                                                                                                                                   | DATA\_ARCHITECTURE.md §三 参与者数据流设计 + DESIGN\_SYSTEM.md                                    | `docs/src/styles.css` + 各 entry JS                | 🔄 持续 |
| C-5   | **术语与表达审计**：定期扫描全仓库过时术语（活动建设/组织建设残留、之上/之下、归档→人才库混用等）与**已降级定义残留**（术语权威层级变更后，旧定义须同步全仓为 T1 官方定义，如党建/党务定义以 USAGE\_POLICY §1.1.1 为准），并调用 USAGE\_POLICY §二.2.3 五条可复用检查规则扫描 AI 没吃透精神的表达模式（"而非"对立/"所以"因果链/反面假设/私加强调/元叙事标签），发现后归并至权威源 | H30.1 一改具改 + OPERATIONS\_GUIDE.md §1 + USAGE\_POLICY.md §二.2.3                           | 全仓库                                               | 🔄 持续 |
| C-6-1 | **常态化专项评议 · 反论评议**：全仓库反论的支书评议（速查见 OPERATIONS\_GUIDE §18.5），每周轮转，轮次进度/侧重维度见 `.ctx/REVIEW_QUEUE.md` 附录①                                                                                                                        | H60.5 + OPERATIONS\_GUIDE.md §17.2.1（W4）                                                 | 全仓库                                               | 🔄 持续 |
| C-6-2 | **常态化专项评议 · 理论复用评议**：跨节/跨文件引用复用的支书评议（速查见 OPERATIONS\_GUIDE §18.5），每周轮转，轮次进度/侧重维度见 `.ctx/REVIEW_QUEUE.md` 附录②                                                                                                                 | H60.5 + OPERATIONS\_GUIDE.md §17.2.1（W4）                                                 | 全仓库                                               | 🔄 持续 |
| C-6-3 | **常态化专项评议 · 黑话审查**：全仓库 AI 编造黑话的支书审查（OPERATIONS\_GUIDE §18.5 理论复用 D 表述），每周轮转，轮次进度/侧重维度见 `.ctx/REVIEW_QUEUE.md` 附录③                                                                                                            | H60.5 + OPERATIONS\_GUIDE.md §17.2.1（W4）                                                 | 全仓库用户文档                                           | 🔄 持续 |
| C-6-4 | **常态化专项评议 · 支书原话逐条复核**：SECRETARY\_DIRECTIVES 论断逐条复核确认原话（H60），每周轮转，轮次进度/侧重维度见 `.ctx/REVIEW_QUEUE.md` 附录④                                                                                                                  | H60.5 + OPERATIONS\_GUIDE.md §17.2.1（W4）                                                 | `content/01_strategy/SECRETARY_DIRECTIVES.md` | 🔄 持续 |
| C-6-5 | **常态化专项评议 · 最小三成本**：系统设计最高验收标准（H10 总纲声明），最小信息/操作/适应学习成本的持续评议，侧重维度基线见 `.ctx/REVIEW_QUEUE.md` 附录⑤                                                                                                                              | H10 总纲 + H60.5 + DESIGN\_SYSTEM.md §一 第2条/原则10                                           | 全系统工作台                                            | 🔄 持续 |

***

## 评议待办 · 执行型（H60.7 归口，2026-09-08 收口）

| ID | 事项 | 引用流程 | 状态 |
| --- | --- | --- | --- |
| R-23 | **① 已闭环（2026-09-13 批次 18）**：思想汇报建服务端表 `thought_reports`（随快照写穿同步 + init 拉取），`thought-report-submitted` 的 `authorize` 改为**按表复算**「提交人本人（`row.personId===actor.id`）或有权阅处角色（组织委员）」——服务端无该汇报一律 403，不再采信客户端自述 `personId`；同时该通知**受众锁定组织委员**（原无受众 → 全站广播）。**②③ 仍待办**：② 部分 kind 的展示值（活动名/姓名等）仍取自 payload（非安全字段，落地值已由服务端掌控）；③ 通知草稿类（活动/专班/党委下发）的标题正文由发布人填、服务端仅掌控受众与落点 | SYSTEM_ROLE_PERMISSION §9h + 附录⑦ 写权判据 | ① 闭环；②③ 待办 |
| R-24 | **已闭环（2026-09-13 批次 19）**：① 反馈域同域两口径——服务端 `issues` 表补种（以 `docs/data/issues.json` 为**内容单一源**，按真匿名口径脱敏：`submittedBy='匿名'`/`participants=[]`/不写 tokenHash；处置字段保留）→ API 模式支书台「反馈管理」与公开反馈页同见 4 条；② `approveDraft` 在 API 模式改为**落服务端**（`getAdapter().issues.create`，由服务端生成 id/number），并修 `issue-detail` 写回缓存键漂移（原写死 `gsm1921-issue-cache`，与 `CACHE_KEY` 不同键 → 评论写回后刷新即丢；现单一源 `ISSUE_CACHE_KEY`） | 附录⑧ 功能实现评议 + 附录⑪ 卡片/数据同源审计 | 已闭环 |
| R-25 | **卡片过拟合/欠拟合（2026-09-13 批次 19–20；支书实报「不理解为什么有『专班待议』单独卡片；既有过拟合也有欠拟合」）**：① 已闭环：撤销支书待办页顶部「专班待议（支委会）」**独立顶卡**（原无条件渲染、无数据也占位）→ 实时聚合组归入「专班」业务域折组（有报送才成组），动作移入详情；② **已闭环（批次 20 全局卡片审计）**：按同一判据清掉其余过拟合——首页「活跃专班」统计卡（无专班支部永远显示 0 个）、首页「专班进展」卡与「活动风采」卡（空态恒占位）、组织台专班看板三桶（空桶恒占位并撑 `min-h-[120px]`）→ 一律**无数据即不出**（整卡/整桶消失，不误伤有内容者）；③ **欠拟合已登记**：审计出「规范类工作在界面承载不足」清单（三会一课无按子会分列视图、民主评议党员/换届选举缺流程明细、**党费收缴零承载**、制度无版本台账）→ 落 `content/04_web_design/evolution/BRANCH_WORK_MAP.md`「过拟合/欠拟合清单」；支书裁定**党费收缴暂不纳入、仅登记缺口**（不建占位模块）；④ 判据入库（REVIEW_QUEUE）：支部自创工作（`branch-custom` / `tier=method`）**不得占首页或 tab 顶部固定槽位**、无数据不得常驻空卡；规范类工作缺的是**流程明细/台账**，一律显式登记、**不用占位卡充数** | 附录⑨ 卡片存在性判据 + BRANCH_WORK_MAP「过拟合/欠拟合清单」 | ①② 闭环；③ 登记；④ 判据入库 |
| R-26 | **日期/月份口径（2026-09-13 批次 19，支书实报「月份选择器 2026-09 与日历 2026 年七月并存」）**：① 已闭环：首页月份选择器与日历**同源**（原 `populateMonthSelector` 未传 displayMonth → 按「当日所在月」自选，而日历按 `state.displayMonth` 渲染；现以 displayMonth 为权威传参、以选择器返回值回写 state，二者恒等；组织台活动查看选择器同步回落口径）；② 已闭环：`?v=` 版本戳失同步（13 处**副作用导入**停在 `20260909e`、`about.css` 停 `20260828l`、`branch-module-catalog.test.mjs` 的 `const V` 停旧戳 → 浏览器按 URL 分裂模块实例：注册表/共享状态读空的根因）——bump 脚本补四类规则（副作用导入 / 任意 .css / CSS `url()` 资源 / test 版本字面量）并全量重戳；③ 待办：多处 `new Date().toISOString().slice(0,10)`（UTC）与 `_currentYearMonth()`（本地）混用，月初/月末存在差一日/一月的风险（判例：`secretary-overview._today()/_thisMonth()`、`todo.js`/`issues.js` 多处），需统一到 `core/utils.js` 的本地口径 | DESIGN_SYSTEM §三 日期口径 + 附录⑬ 版本串纪律 | ①② 闭环；③ 待办 |
| R-27 | **表单控件尺寸/字号统一（2026-09-13 批次 19，支书实报「同一 tab 里输入框/选择框有的高有的矮、字有的大有的小」）**：已闭环——自制 Tailwind 控件（`workforce-panel` 4 处 / `resolution-followup-manager` 3 处）归入 `input-flat` 体系；清除无效类 `py-1.5`（7 处，被 `.input-flat.text-xs` padding 覆盖）；`text-[11px]` 控件档（2 处）统一为 `text-xs`；`COMPONENT_SPEC.md §4.3` 校正（原写 40/36px 与实现 42/34px 不符）+ `REVIEW_QUEUE.md` 新增「表单控件尺寸与字号统一」判据（原「组件风格统一」只覆盖卡片容器，未覆盖控件尺寸，属规范空档） | COMPONENT_SPEC §4.3 + REVIEW_QUEUE 组件风格统一 | 已闭环 |
| R-28 | **滞留党员复核卡定位（2026-09-13 批次 20，支书裁定「删除卡、降为统计行」）**：已闭环——删除支书「全局概况」上的**纯只读**「滞留党员复核」卡（原无任何按钮，只显徽标/备注/变更留痕）；其唯一实质信息降为两处**非卡**呈现：① 指标条一行「会议应到 N 人」（title＝在册党员 − 滞留剔除）；② 内联一行「滞留 N 人已从会议应到剔除（身份保留·通知照发·表决快照剔除）——维护在组织委员『成员名册』」+ 党员结构迷你条（原并入卡内的「发展阶段分布」不丢）。写动作仍在组织委员「成员名册 → 支书成员变更确认」链；学期末待办提醒保留 | S1–S4 滞留设计 + 附录⑪ 卡片审计 | 已闭环 |
| R-29 | **支部分工（2026-09-13 批次 19–20；支书裁定「分工＝线上调整→信息与任务自动传递；不等于组织实际配置；党委与支部都要有权限；工作程序 vs 工作方法」）**：① 已闭环：模块分层 `tier`（`norm` 工作程序/规范＝必办须有人不可停用；`method` 工作方法＝支部自选可停用/可复用；单一源 `core/work-map.js`）＋停用表示 `{ownerType:'none',ownerId:''}`（`mergeWorkforceSnapshot` 与 `sanitizeConfigWorkforce` 双拦规范类）＋面板按层分组（optgroup）与停用项＋`custom-select` 补 optgroup 分组标题渲染；② 已闭环：**任务自动传递**——采纳后按本次实际改派逐条派生「履职」待办（到人→`personId`／角色→`role`，入同域折组，`sourceId=<activityId>:<moduleId>` 幂等）；③ **已闭环（批次 20 补信息侧）**：通知受众由**服务端**按活动 `extras.proposal` 复算 = `audience:'committee'` ＋ `audiencePersons`（到人负责人；**新增「按人定向」能力**，角色数组表达不了）＋ `actionRoles`（角色负责人）＋ `actionable/actionTask`；消费端受众门同步支持 `audiencePersons` 且**多受众并存命中**（原 if/return 会让 committee 吞掉其余受众）；新增服务端用例锁定「覆盖客户端伪造受众」；④ 已闭环：双端编排（支部日常＝支书台「支部分工」；党委/部署期＝党委台「支部配置→向导③角色分工」），原稿「党委台不提供分工编排」YAGNI 条已按新裁定修订；⑤ 待办：分工驱动的**信息传递**仍以「提醒＋入口」为限（未按模块自动生成周期性任务、未与活动/专班创建联动赋权） | BRANCH_WORK_MAP §一/§三 + SYSTEM_ROLE_PERMISSION §9h | ①②③④ 闭环；⑤ 待办 |
| R-30 | **思想汇报改按面板数据建模（2026-09-13 批次 21，支书原话「思想汇报是一个面板数据——一个人可以在多个季度上传他的思想汇报。不强制！……同一个人 id 下的多个思想汇报。此外思想汇报一般而言 1500 字左右。我认为还是需要用一个界面来承载！而不是展开！」）**：① **期次维度**——新增 `core/period.js`（期次纯函数单一源 `PERIOD_RE / periodOf / periodLabel / comparePeriodDesc / periodOptions / isValidPeriod`），提交时**手填期次**（`YYYY-Qn`）、缺省/非法按 `submittedAt` 推导；② **一期可多篇**——同一 personId 同一期次**无唯一性约束**（数据层允许多篇并存，支持补充稿/修改稿）；③ **篇幅口径**——`core/policy-defaults.js::thoughtReport = { wordHint: 1500, wordSoftMin: 800 }` 软提示单一源（界面显示字数与建议篇幅，**不作硬性拦截**，过短由组织初阅把关）；④ **承载方式**——**新增独立阅读页** `docs/thought-report.html` + `entries/thought-report-entry.js`（三模式：`?id=` 单篇 / `?personId=` 按人聚合 / 无参＝组织委员待初阅队列；访问门仅本人与支委层；组织委员就地通过/打回、本人就地撤回/改后重交），按人阅读口径 `listThoughtReportsByPersonGrouped`；⑤ **通知**——`core/system-notice-templates.js` 正文带期次，`server/system-notice-kinds.js` 的 `thought-report-submitted.build` **按表复算** personName/period（不信客户端 payload）。测试：`server/test/thought-report-panel.test.mjs`（面板数据模型 6 例）+ `person-consistency` 通过 | DESIGN_SYSTEM §一 最小三成本 + 附录⑧ 功能实现评议 + 附录⑦ 受众判据 | 已闭环 |
| R-31 | **第一列是人的表格统一化（2026-09-13 批次 21，支书原话「涉及第一列是人的表格，一定要安排搜索功能……只读情况下怎么实现最小查阅成本；可操作性怎么实现最小操作成本！……后台一定要做好同一个人的信息对应，切忌张冠李戴！……数据一致性的审查很重要！！」）**：① **单一检索引擎**——新增 `components/list-filter.js`（关键词 + 分面 chips + `>8` 行门槛 + 同 `stateKey` 跨重渲染保持筛选 + `table` 模式支持 `<thead>/<tbody>`），28 张按人表分批接入；② **只读查阅**——**新增独立档案页** `docs/person.html` + `entries/person-entry.js`（完整档案 / 在册留痕 / 关联概览计数），不试行内展开；③ **可操作**——**模态编辑** `components/person-edit-modal.js`（契约 `openPersonEditModal({personId, focusFields, sourceLabel, onSaved})`，每次打开按 personId 从 `PersonStore.getById()` **现取**档案、不用调用方行数据；上区只读完整档案 + `focusFields` 高亮，下区按「立即生效（姓名/学号/党小组）／报支书确认（发展阶段/在册状态/在册备注）」两组分流；名册也统一用模态）；④ **人员清单实时视图** `services/person.js::liveMembers()`（只读 Proxy，根治 13 处模块加载期人员快照张冠李戴）；⑤ **跨表一致性守卫** `server/test/person-consistency.test.mjs`（结构层 S1–S3 + 数据层 D1–D5）；⑥ **入口补全（2026-09-14 批次 24，Q-21-7）**：纪检台「考勤总表 / 待确认考勤队列」、组织台「工作量汇总」成员行、组织台考察详情面板姓名一并接 `person.html?id=`（`services/attendance.js::attendanceToLong` 透出 `personId` 作服务层唯一出口）；矩阵/转置表与非人的第一列表维持不接（Q-21-6 已裁定例外） | DESIGN_SYSTEM §一 原则9/原则10 + 附录⑪ 卡片/数据同源审计 + DATA_CONSISTENCY_CHECKLIST「两层法」 | 已闭环 |
| R-32 | **第一列是活动的表格统一化（2026-09-13 批次 21，支书原话「涉及第一列是活动的表格，也要思考！」）**：① **统一组件能力**——32 张活动表分批接入 `components/list-filter.js`（**关键词 + 月份 + 类型 + 状态**）；② **活动存储态判据单一源**——「已结束/已归档/未开始/仍在办」收敛为 `core/constants.js` 的 `isActivityEnded / isActivityArchived / isActivityNotStarted / isActivityLive`，全站禁止手写 `status==='completed' || archived`；③ **活动生命周期展示态单一源**——草稿/已发布/进行中/待归档/已执行/已归档/已取消统一走 `components/inspector.js::deriveActivityLifecycleStatus` + `ACTIVITY_LIFECYCLE` + `activityLifecycleBadgeHtml`（**本次确认唯一**）；④ **类型写入口径**——统一为权威子类中文名（`normalizeActivityType` / `ACTIVITY_SUBTYPES`） | 附录⑪ 卡片/数据同源审计 + DESIGN_SYSTEM §三 状态口径 | 已闭环 |
| R-33 | **数据一致性评议「两层法」推广为通用方法（2026-09-13 批次 21，支书原话「结构+数据 双层断言！我觉得这次做的数据一致性评议很重要！我们要推广开来！」）**：① **两层判据**——**结构层**静态扫描断言「口径只有一处实现」（禁止模块加载期人员快照、禁止凭姓名认身份、禁止手写已成单一源的判据）；**数据层**以权威源为基准逐域断言「同一实体同一字段在各域取值一致」（引用存在性 / 快照一致性 / 同域自洽 / 唯一性）；② **范本**——`server/test/person-consistency.test.mjs`（D1 引用存在性 / D2 姓名快照一致 / D3 档案字段快照一致 / D4 同域双字段自洽 / D5 唯一档案 + 账号可解析；S1–S3 结构层）；③ **推广落点**——`content/05_ai_coding/DATA_CONSISTENCY_CHECKLIST.md` 新增「数据一致性评议 · 两层法」节（可套用到活动/记录/文件等任意实体：① 定权威源 → ② 枚举引用域与字段路径 → ③ 写四类断言 → ④ 写结构层静态扫描防回潮 → ⑤ 实测暴露一律**根治**改单一源而非打补丁） | H30.1 一改具改 + DATA_CONSISTENCY_CHECKLIST §0 | 已闭环（方法入库） |
| R-34 | **检索条出现门槛统一 `SEARCH_FILTER_MIN_ROWS = 8`（2026-09-13 批次 21，grill-me 定案④⑧）**：小表不装检索条、防过拟合（过拟合反例＝在小表强加检索条，成本大于收益）——常量单一源 `core/constants.js::SEARCH_FILTER_MIN_ROWS = 8`，人/活动表共用，由 `components/list-filter.js` 在动态行数 `> 8` 时自动显隐（同 `stateKey` 跨重渲染保持筛选） | REVIEW_QUEUE 卡片过拟合/欠拟合判据 + DESIGN_SYSTEM §一 最小三成本 | 已闭环（判据入库） |
| R-35 | **实体 id 单一源 + 连字符前缀契约 + 两层守卫（2026-09-13 批次 22，Q-21-2 闭环）**：**背景**——`services/thought-report.js` 的 `'tr_' + Date.now()` 同毫秒连提两篇撞 id（重复主键 → 归集/初阅指向错乱），批次 21 单点根治后支书裁定**「A+B+C 全改」**全站排查。**裁定**：① **唯一源**——全站实体 id 一律经 `core/id.js` 生成（`generateId(prefix, sep='_')` + `randomHex()`，降级链 `crypto.randomUUID` → `crypto.getRandomValues` → `Math.random`）；② **禁止** `前缀 + Date.now()` 生成实体 id、**禁止** `Math.random()` 参与 id 生成；③ **连字符前缀契约**——`tf-`/`notice-`/`cmt-`/`mc-` 等必须显式传 `sep='-'`（`sourceId.startsWith('tf-')`/`'notice-'` 依赖，传错静默打断跳转）。**落点**：重写 `core/id.js` 为唯一源；**20 文件 / 38 处**迁移 + 3 处重复实现收敛（`issues.js::_randomSubmitterToken` → `randomHex`、`branch.js::_newBranchId` → `generateId('br','-')`、`member-confirmation.js::_nextId` → `generateId('mc','-')`）；**两层守卫** `server/test/id-uniqueness.test.mjs`（结构层禁「前缀 + `Date.now()`」/禁 `Math.random()` 参与 id/单一源在位；数据层集合内唯一 + 跨集合不冲突 + 前缀形态不冲突 + 唯一源自证） | H30.1 一改具改 + DATA_CONSISTENCY_CHECKLIST §0 | 已闭环（判据 + 守卫入库） |
| R-36 | **取数口与可见性门分离 + 受众写入值与判定同源（2026-09-13 批次 23，Q-22-1 闭环）**：**背景**——支书经 UI 发布的**任何**通知详情页均打不开（`notice.html?id=` 显示「通知不存在或已过期」），发布侧选「全体党员」的通知**实际无人可见**；根因两处互为因果——发布侧写 **sentinel**（`['all'\|'leaders'\|'activists'\|'candidates']`）而消费端受众门按**角色键**比对（`['all']` 永不命中），且 `getById` 复用 `list()` 可见性门（连发布者都取不到自己的通知）。**裁定**：① 按 id / 主键取数（`getById` 一类）**不得复用**消费端「列表可见性过滤」；② 可见性判定须有**独立单一源入口**（`canReadNotice`）并在消费点显式调用（否则出现「详情打不开」与「拆门即泄露」两难）；③ 发布侧写入的受众值与消费侧判定**必须同源**（单一注册表），sentinel 语义**含角色与阶段两类**（`activists`/`candidates`＝入党积极分子/发展对象，不是角色）。**落点**：`core/constants.js::NOTICE_AUDIENCE_SENTINELS`（+ 派生 `NOTICE_AUDIENCE_OPTIONS`）、`services/notice.js::canReadNotice`、守卫 `server/test/notice-audience.test.mjs`（N1–N8） | H30.1 一改具改 + DATA_CONSISTENCY_CHECKLIST §0 / 跨类别同源校验 | 已闭环 |
| R-37 | **枚举/常量单一源收敛（2026-09-14 批次 24，Q-21-3 闭环）**：**背景**——`RESIDENCE` 在 `services/roster.js` 与 `services/org-base-data-preview.js` 各写一份同值 `{CAMPUS:'在校',DETAINED:'滞留'}`；preview 不能 import roster（`person.js → preview` 单向依赖，反向 import 即成环）→ 长期以「为避免循环依赖」为名两处维护，只靠单测断言同值。**裁定**：① 同一口径**只允许一处定义**，且定义点应落在**依赖图的叶子模块**（无任何 import）以消解成环顾虑——落 `core/constants.js`（与 `DEVELOP_STAGES` / `NOTICE_AUDIENCE_SENTINELS` 同址）；原定义点**不再对外重复导出**（**不做 re-export 兼容层**——否则「两条 import 路径」仍是隐性两源）；② 消费点一律直取单一源（本批 10 处源码 + 5 个测试）；③ **守卫必须两层**——数据层断言**取值契约**（原「两份同值」断言在单一源下恒真＝假绿），结构层静态扫描断言**全站只此一处定义**且原定义点仍 import 该单一源 | H30.1 一改具改 + DATA_CONSISTENCY_CHECKLIST §0 | 已闭环（判例入库） |
| R-38 | **版本戳计量可信化：计数以内容比对为准 + 陈旧戳收尾自检（2026-09-14 批次 24，Q-21-4 闭环）**：**背景**——`bump-version.mjs` 报「更新 JS 文件 0 个」而文件确已改写；HTML 计数空跑也报 21 个。**裁定**：① **一切计数以「改写前后内容是否真的不同」为唯一判据**（在 `replace`/`map` 回调里置 `changed` 标志的写法会把「正则命中」算成改写＝假阳性）；② **真正要守的量是「改完有没有陈旧残留」而非「本次改了几个」**——新增收尾自检扫描全仓 `?v=` 并报告非本版本残留（陈旧戳会让浏览器按 URL **分裂出第二个模块实例**，是「注册表/共享状态读空」的根因）；③ 自检与改写**同源跳过注释行**（注释里的版本号是人工历史注记，不是缓存键）；④ **stamper 必须覆盖全部动态导入形态**——含**模板字符串** `import(\`…/${stem}-workspace.js?v=…\`)`（只认引号会长期漏戳，本批据此修掉 `settings-entry.js` 一处长年陈旧戳）。**过程留痕**：本机执行宿主对同一命令会跑两次（沙箱策略探测 + 实跑）、**回传输出可能取自空跑的那一次**——「文件已改却报 0」的真相由此澄清（取证：输出重定向落盘 + 一次性探针记录自身执行次数） | 附录⑬ 版本串纪律 + H30.1 一改具改 | 已闭环（判据 + 自检入库） |
| R-39 | **党小组升为一等实体（2026-09-14 批次 25，Q-21-8 闭环）**：党小组由「成员档案字段取值集合」升为独立清单（名称/序号/状态/成立与解散留痕），写权归支书与副支书；**允许解散非空组**——组内成员转「未分组」（不属任何党小组：党小组会应到不含、支部大会应到照计、表决名单照计），已解散组不入任何下拉与统计；**全站组清单唯一来源为活组清单**（禁再写死组名数组，原三处已收敛）；工作台未分组条与名册同口径（限本支部）。**落点**：`docs/src/services/party-group.js`、`server/routes/resources.js`、守卫 `server/test/party-group.test.mjs`（结构层 S1-S3 + 数据层 D1-D6） | H30.1 一改具改 + DATA_CONSISTENCY_CHECKLIST §0 / 跨类别同源校验 | 已闭环（判例入库） |
| R-40 | **核心组改注册表显式声明 + tab 分组主轴按行为性质四组（2026-09-14 批次 25）**：核心组判定由「显示标签反推」改为 `coreTab: true` 显式声明，判定单一源 `docs/src/core/constants.js::isCoreTab`（`services/branch.js` 与 `services/preferences.js` 共用）；理由：显示标签不得承载权限语义（改文案会悄悄改动可隐藏性），且党委台不必再靠改名「首页」加整台豁免绕开判定。**tab 分组主轴＝按行为性质四组**（工作台 / 我的职责 / 知情查看 / 制度与答复），同质薄壳合并进一个 tab 内部分段（已落地：三台「专班查看」加组织台「活动查看」→「知情查看」；纪检「补课制度」加「公邮管理」→「制度与文本」）。**落点**：`core/constants.js::isCoreTab`、`components/insight-view.js` | H30.1 一改具改 | 已闭环 |
| R-41 | **筛选行规范（2026-09-14 批次 25 定案，批次 27 落地）**：**筛选行禁止 chip**（分面一律下拉，含「全部」项；chip 只作展示不作筛选）；筛选行内所有控件统一 **34px 高 / 12px 字**，同一行不得出现第二个高度；筛选行样式与表格表头样式抽为单一源（`styles.css` / 共享片段），禁各 tab 自写。**落点**：`content/04_web_design/design-system/COMPONENT_SPEC.md` §4.3 / §4.10 / §4.12 | 附录⑬ 版本串纪律 + COMPONENT_SPEC | 已闭环（批次 27，见 R-43） |
| R-42 | **成员流入/流出登记与账号层（2026-09-14 批次 26，Q-21-9 闭环）**：① 成员流动按**复式记账**口径登记——每次流入/流出各记一笔台账（`memberFlows` 域），名册随时可对账「谁在我们名册、谁不在」；② 流入登记采集 **学号与届别**（`enrollYear` 新增字段）并**自动建号**（账号取学号、口令取支部统一默认口令，账号层可持久化），流出登记**停用账号但不删除历史**；③ **流出登记即生效**（不再要求支书二次确认；发展阶段与在册状态变更仍走确认链）；④ 台账只增不删，登错用「撤销」留痕（`revokedAt/revokedBy`）并回滚在册状态；⑤ 登记写权归组织委员与支书/副支书（前端显隐、服务层校验与 server 写门同源）；⑥ 承载位置＝组织委员工作台「成员名册」内的「成员流动」面板（不新增独立页面） | H30.1 一改具改 + DATA_CONSISTENCY_CHECKLIST「账号与成员档案同源」 | 已闭环（设计见 DATA_MODEL §2.26/§2.28） |
| R-43 | **控件选型判断规则 + 筛选行与表格样式单一源落地（2026-09-14 批次 27，Q-23-4 闭环）**：**背景**——支书实报「涉及筛选器的部分都很丑；下拉框、选择器究竟什么时候用什么，你真的有自觉吗？字体大小、组件大小都要重新审视，所有的【表】」；彻查现状：筛选控件 6 套形态并存、字号 6 档、同一行内出现 42/34/约 26/约 24 四个高度。**裁定**：① **选型先判「取值能否穷举」再判「单选/多选」**——不可穷举 → 文本输入；可穷举且单选 → 下拉；可穷举且多选 → 表单内 chip；需选具体人 → PersonPicker；互斥视图切换 → 分段钮（非筛选）；② **筛选行禁止 chip**，每维一个「全部 + 取值」下拉；③ **档位唯一**：同一筛选行控件统一 34px 高 / 12px 字——因此 **`custom-select.js` 不再给紧凑档触发器补 `h-8`（32px）**（原 32 与输入框 34 同排错位 2px 且压字）；④ **载体单一源**：筛选行落 `styles.css` 的 `.lf-bar` / `.lf-kw` / `.lf-select` / `.lf-btn`，调用点禁再内联拼装 `flex flex-wrap items-center gap-2` + `flex-1 min-w-[140px]` 散值；⑤ **表格样式单一源**：全站 `<table class="data-table">` 一处声明（表头/行线/悬停/内边距/字号），各 tab 不再各写一份 headHtml；专用矩阵 `.vs-matrix` 保留自身类族。**落点**：`docs/src/styles.css`（新增两段类族，删 `text-[10px]` 下拉死规则，**支书特批**）、`components/list-filter.js`（分面 chips → 下拉）、`components/query-view.js`（级联子类与品牌 chip → 下拉）、`components/issue-list.js`（状态分组 chip → 下拉）、8 张真表格 → `.data-table`、7 处自写筛选条 → `.lf-*`；守卫 `server/test/filter-row.test.mjs`（结构层 S1–S7 + 口径层 D1） | H30.1 一改具改 + COMPONENT_SPEC §4.3/§4.10 | 已闭环（判例 + 守卫入库） |
| R-44 | **`bump-version.mjs` 无参运行会「同日版本号回退」（2026-09-14 批次 27 发现，批次 28 修复闭环）**：脚本默认版本号为「当天日期 + `a`」，**无当日续号逻辑**；同日第二次发版若忘记显式传参，会把全站戳从 `20260914b` **回写**成 `20260914a`，而收尾自检只比对「是否等于本次 VERSION」，回退后仍报「0 处残留 ✅」静默通过（实测踩中）。**修法（Q-23-8）**：① 版本号推导抽为**纯函数单一源** `docs/scripts/version-next.mjs`——`nextVersionFor(today, existing)`（同日取最大字母 +1，跨日归零为 a，字母用尽显式报错）、`isForward(candidate, prevMax)`（只允许前进）、`isCommentLine` / `codePartOf`（注释排除规则，与 stamper、收尾自检、守卫测试三处共用，禁各写一套）；② 无参运行改按「读仓库现有戳 → 同日 max +1」推导；③ 显式传参时若小于现有最大戳 → 报错退出码 1 中止；④ 两层法守卫 `server/test/version-stamp.test.mjs`（结构层 S1–S3 + 数据层 D1–D6），其中 **S3 断言「全站活动版本戳取值集合规模为 1」**——把「陈旧戳」从发版脚本的收尾自检升级为**测试常驻守卫** | 附录⑬ 版本串纪律（承 R-38） | 已闭环 |
| R-45 | **分页控件样式单一源（2026-09-14 批次 28，Q-23-7 闭环）**：**背景**——批次 27 彻查发现表下分页控件 6 处形态不一（26 / 32px），其中四处借 `.chip-accent-on` 表「当前页」（该类的 `!important` 掩盖了「当前页按钮缺基础边框与底色」，且 chip 语义被挪用）；支书裁定**「统一为 `.page-btn` / `.page-num`」**。**裁定**：① 分页控件落 `docs/src/styles.css` 单一源——`.page-btn`（翻页钮）/ `.page-num`（页码钮）同高 **30px**（按钮规范默认档）、同边框/圆角/字号（12px）；② **当前页用自有类 `.is-current`**，禁再借 `.chip-accent-on`；③ 颜色一律取主题变量（`--neutral-*` / `--app-accent-*`），**深色主题自动跟随，禁再各写深色覆盖**（原 `.qv-page-btn` 深色补丁三条已删）。**落点**：`docs/src/styles.css`（**支书特批**）、`components/issue-list.js`、`components/query-view.js`、`entries/archive-entry.js`、`entries/tabs/secretary/feedback-tab.js`、`entries/tabs/disc/attendance-tab.js`（连体分段钮改两枚独立按钮）、`entries/tabs/visitor/activities-tab.js`；守卫 `server/test/filter-row.test.mjs` S8 | H30.1 一改具改 + COMPONENT_SPEC §4.10 | 已闭环 |
| R-46 | **撤销流出须清 server 软标记（2026-09-14 批次 29，Q-23-5 闭环）**：API 形态下档案存于 server `users` 行，而「转出」是**软标记**（原行保留、不删不匿名）——仅走档案补丁（`PATCH /members/:id/profile`）**不会清除 `transferOut`**，该行仍被 `/login`（「账号已停用」401）与用户读链排除 → **撤销流出后成员实际回不来**（mock 形态无此病：`saveMember` 语义即「save 即复活」，自动清 `removedIds`）。**修法**：① 新增语义端点 `POST /members/:id/undo-transfer-out`（清 `transferOut`/`transferredOutAt`/`removedAt`/`removedBy`/`transferOutNote`；与 `/transfer-out` **同角色集** + 同支部 + 幂等 + 不接收任何字段）；② `api-adapter.members.undoTransferOut` 暴露；③ 接线 `PersonStore.saveMember(record, { restoreFromTransferOut: true })`（与 R-10 的 `residenceMirror` 同为「api 形态分流语义端点」选项，mock 形态忽略）。**守卫**：`member-persist.test.mjs` api ⑪（含**病灶复现**断言）、`permission-gate.test.mjs` ⑤c、`member-flow.test.mjs` S4（三处接线齐备）。详见 DATA_FLOW §4.5.1 | H30.1 一改具改 + R-10 语义端点分流先例 | 已闭环 |
| R-47 | **党小组清单「全局硬编码审查评议」复查结论（2026-09-14 批次 29）**：批次 25 已禁**组名字面量数组**（守卫 S1），本批彻查发现两类**绕过该守卫的等价病灶**，一并根治并加守卫 S4：① **模块加载期派生快照**——`components/person-picker.js` 原 `const PARTY_GROUPS = [...new Set(PEOPLE.map(p => p.partyGroup))]`（展开 `liveMembers()` 的 Proxy 即物化成加载期快照，与 2026-09-13 揪出的 `const PEOPLE = getMembers()` 同病）→ PersonPicker 组筛选 Tab 不随支书台增/改名/解散联动、新增空组永远缺席；② **运行时用种子枚举代跑**——`services/branch-roster-import.js` 原用 `PARTY_GROUP_OPTIONS`（种子派生）做导入行净化与按组应到统计 → 新增组名被判非法回退、统计缺组；`entries/tabs/visitor/projects-tab.js` 原从成员档案派生筛选项 → 漏空组。**唯一正解：运行时一律 `groupOptions()`（活组按 seq 升序）**；种子枚举 `PARTY_GROUP_OPTIONS` 归位为「预览种子期口径」，守卫 S4 禁止运行时消费（白名单仅定义方 + 清档注释）。真机证据：新增「复核临时党小组」→ PersonPicker 组筛选 Tab 立刻多出该项 | H30.1 一改具改 + 目标①「硬编码审查评议·全局性进行」 | 已闭环 |
| R-48 | **「死代码清理」须先验存量兼容，勿一刀切（2026-09-14 批次 29，Q-23-6 处置原则）**：Q-23-6 原登记「`pend.out` 死显示分支 + `member-confirmation.js` 未用私有 helper」一并清理。复核后按证据**分两处置**：① **真死代码删**——`_summarize` + `DOMAIN_LABEL` 无任何调用方（产出方已随 pending 分支移除）；② **存量兼容留**——`pend.out` 行内小标与汇总计数、`_applyApproved` 的 `kind === 'transferOut'` 分支、`todo-tab` 的 refsSummary 渲染与 `_mcRefsSummaryHtml`、`submitMemberChange` 的 transferOut 拦截，**保留并加注理由**：本地存储键 `gsm1921-member-confirmations` 可跨刷新带出旧版落库 pending，若连显示与决策口一并删除，旧请求将既无法被支书处理、又持续拦截该成员阶段/在册变更（**死锁**）。**裁决**：同源一致——保留者一处不少、删除者仅限无调用方的产出方 | DOC_GOVERNANCE 单源纪律 + 存量兼容 | 已闭环 |
| R-49 | **本批新登记两项（2026-09-14 批次 29 发现，待办 / 待裁）**：① **Q-23-9**（待办）API 形态名册读链未回归——`person.js::_baseMemberRecords()` 在 api 形态返回静态种子 `[...PEOPLE]`（自陈「服务器权威读回归 C 波」），故 `getMembers()` 不含 server 新建成员 → **刚登记流入的成员立即登记流出会被判「不在本支部在册名册」**；建议随 C 波统一定口径，勿单点打补丁。② **Q-23-10**（**待支书裁定**）裁定冲突：R-10「名册新增＝组织委员专属（支书/副支书不越权，有 403 断言）」与 R-42/§9i「支书·副支书亦可登记流入」在 API 形态直接冲突（mock 形态无感）——二选一：维持 R-10 并收窄 §9i 矩阵，或维持 §9i 并为成员流动单列 `POST /members/intake` 语义端点。现状已在 `SYSTEM_ROLE_PERMISSION.md §9i 注④` 如实写明 | R-10 / R-42 冲突仲裁 | Q-23-9 登记待办、**Q-23-10 已由 R-50 结案**（批次 30） |
| R-50 | **裁定冲突仲裁：维持 §9i，为成员流动单列流入端点（2026-09-14 批次 30，Q-23-10 闭环）**：**冲突**——R-10「名册新增＝组织委员专属（支书/副支书不越权，`permission-gate` 有 403 断言）」与 §9i/R-42「成员流动登记写权＝组织委员 + 支书/副支书」在 **API 形态直接冲突**：流入建档原走 `POST /members` → 支书/副支书登记流入被 **403 阻断**（mock 形态无感，因 mock 分支不区分角色门；本批写 api 全链测试时首跑命中）。**支书裁定：维持 §9i**。**修法**——`server/routes/member.js` 抽**同一实现体、两个写门**：`POST /members`（`ORG_COMMISSIONER_ROLES`，R-10 专属不动）与 `POST /members/intake`（`MEMBER_FLOW_ROLES`，§9i）；`api-adapter.members.intake` 暴露；接线 `PersonStore.saveMember(record, { memberFlowIntake: true })`（仅 `registerIntake` 流入建档路径；名册 tab「新增成员」仍走 `/members`）。**纪律**：**不放宽名册越权面**；两门共享实现体，禁复制出第二份。**守卫**：`member-flow.test.mjs::S4`（断言 `/members/intake` 存在且用 `MEMBER_FLOW_ROLES`、`/members` 仍为 `ORG_COMMISSIONER_ROLES`、适配器有 `intake`、服务层带 `memberFlowIntake`）、`permission-gate.test.mjs` ⑤d（三角色 201 + 支书直调 `/members` 仍 403 + 强制归支部 + 默认 participant）与 ⑥/⑦ 扩容（普通成员/党委组织员 403、注入 role/branchId 400）、`member-persist.test.mjs` api ⑫（三角色皆可流入登记）。**同批另定**：Q-23-9（api 形态 `getMembers()` 不含 server 新建成员）**保持登记、随「服务器权威读回归 C 波」一并处理**（不与 C 波分开单点打补丁） | R-10 / R-42 冲突仲裁 + H30.1 一改具改 | 已闭环（R-49 的 Q-23-10 项由此结案） |
| R-51 | **控件档位高度须由算式显式决定 + 尺规复核须生产同构（2026-09-14 批次 31，Q-23-11/Q-23-12 闭环）**：**来源**——支书指令「字体大小、组件大小都要重新审视！所有的【表】」，本批以「逐 tab 真机量尺」收口（8 台工作台 / 74 个 tab 访问）。**① 实测结论（生产同构）**：筛选行控件 **79 处全部 34px / 12px**、全站表格 **10 张全部 `.data-table` / 12px**、筛选行内 chip **0 处**、`PAGEERRORS` **0**、零 `.chip-accent-on` 借壳——批次 27/28 的收敛在生产环境成立。**② 同批揪出的真缺陷**：标准档控件实测出 **42 / 43 / 47 三值并存**——增强下拉触发器 43px（`.cs-trigger.input-flat` 的 `padding: 10px` 使算式得 43）、无 text 工具类的裸 `input-flat` 47px（行高取继承 21px）；根因是**档位高度里的「行高」一项没显式声明，靠 UA 默认或 Tailwind CDN 的 `text-*` 工具类提供**。**③ 修法（支书特批改 `docs/src/styles.css`）**：五处载体（标准/紧凑 × 输入框 / 原生下拉 / 增强下拉触发器）补 `line-height: 1rem` 并把标准档触发器 padding 校为 12px，档位统一由算式决定——**紧凑档 `8+8+16+2 = 34px`、标准档 `12+12+16+2 = 42px`**；复测 43 / 47 消失，11 处标准档全为 42。**④ 纪律（新增）**：**尺规类真机复核必须在生产同构环境**——Tailwind / 字体 CDN **一律不得 abort**；本批首跑照抄既有 e2e 的 abort 写法，缺 preflight 致行高退化，量出「27 处 32px」的**伪证**，并据此误报缺陷（已如实更正，见 Q-23-11）。**⑤ 选人载体口径裁定（同批）**——§4.13 原写「禁止用 select 下拉罗列人名」，全站实测有 **4 处例外**（党委台任命支书、换组织向导内任命、支书台分工到人、决议落实责任人；后两处是「支委角色 或 具体成员」的混合指派，硬换 PersonPicker 会丢掉「按角色指派」一档）；支书裁定**写清例外、保留下拉**——规范改为**语义两分**（「选名单里的成员」→ 必走 PersonPicker；「把职责任命/指派给谁」→ 允许下拉）并据实登记例外表。**守卫**：`filter-row.test.mjs::D2`（按「内边距 + 显式行高 + 边框」核算五处载体的高度，禁无行高声明；含对 HEAD 版样式的病灶复现）与 **S9**（逐行判 `<option>` 是否直接插值人名变量，非白名单即红；并防「白名单僵尸」） | COMPONENT_SPEC §4.3⑥ / §4.13 + DATA_CONSISTENCY_CHECKLIST §0 结构层 | 已闭环 |
| R-52 | **字号/组件全站单档化（13px / 38px）+ 「状态与载体失配」型非闭环（2026-09-14 批次 32–33，Q-23-14/Q-23-15 闭环）**：**来源**——支书两处实报：㈠「考察上传都没有填写考察的框，点提交却说必须填写考察内容！这种明显的非闭环，难道代码中检查不出来吗？」；㈡「考勤、考察的人作第一列表格字体都很小、搜索框筛选器很小，同一页表单却很大……**我一直让你统一，你居然一直没有问清楚我的所指！！这是你 grill me 不到位！**」。**① 字号并档（先问后做）**——**不擅自定档**：AskUserQuestion 先定「统一所指」（同页同层级同档／表格筛选器变大／表单变小），支书答「全部统一，但表格与筛选器太小、表单太大，可折中」；再定数值，支书选 **13px / 38px（对称中点）**。落地：`.input-flat` / `.input-flat.text-xs`（**降为历史类名**）/ 下拉触发器 / `.lf-btn` / `.data-table` 正文全部同档（`10+10+16+2 = 38px`；表格行高 1.25rem），清 8 处调用点叠加的 `text-sm`/`p-2`/`rounded-lg` 与 `type=date`、`#wf-form-zone` 的 34px 特例。真机复核：表格 10 张全 13px、控件 90 处全 38px/13px、零页面错误。**② 非闭环根因与修法**——`PersonPicker` 多选**点选即改选中集并即时刷新触发器徽标**，但 `onSelect` 只在点「确认选择」时触发 → 依赖回调渲染的从属输入行（考察「逐人考察内容」）**从不出现**，提交又按 `getSelected()` 取人 → 「请填写 X 的考察内容」而框不存在。修法：多选**点选即回调 + 关面板再回调**，调用方**重建前保态**（改选不清空已填内容）。**③ 衍生病灶**——`destroy()` 内调 `_closePanel()`，与「回调 → 调用方重渲染 → 再 destroy」**成环**（`party-committee-review::P3` 由 11 秒劣化到 17.6 分钟超时）；修法：`destroy()` 先断 `_onSelect` 再关面板。**纪律**：**「销毁」路径必须先摘回调再触发副作用**，凡「回调里重渲染、重渲染里销毁」的组合都要防环。**④ 守卫（回应「难道检查不出来吗」）**——真机闭环守卫 `server/test/inspection-loop-e2e.test.mjs`（组长台/组织台两条：「关面板即有填写框」＋「改选不丢内容」＋「提交落库」）；静态守卫 `ux-guard.test.mjs::⑦`（`onSelect` 渲染逐人输入行者必须保态）；同类排查（全站扫「提交读取的输入 id 是否被渲染过」）未见第二处。 | COMPONENT_SPEC §4.3 输入组件规范表与尺寸/字号单一源①~⑦；DATA_CONSISTENCY_CHECKLIST §0 | 已闭环（全量 560/560） |
| R-53 | **分页须在引擎层实现（2026-09-14 批次 34，Q-23-16 闭环）**：**来源**——支书实报「涉及到人、活动以及其他可能无限增长的表格，仍有部分没有分页」。**核查**——全站分页此前只散落在 5 处（`issue-list` / `query-view` / `archive-entry` / 反馈管理 / 活动动态），而**统一检索引擎 `components/list-filter.js` 自身没有分页** → 经它渲染的 **28 处按人/按活动表**（名册 50 人、考勤明细 66 人次、考察明细 40 人…）一律无分页，行数随档案增长无上限。**修法（一处实现、28 处受益）**——`renderFilteredList` 内置分页：`pageSize` 缺省 **10 条/页**（与既有分页口径同）、状态并入同一 `stateKey`（`{ q, facets, page }`，**跨重渲染保留页码**）、筛选/关键词/清除变化自动回第 1 页、**页数 ≤1 不渲染翻页控件**（小表零负担）、翻页控件走 `.page-btn` / `.page-num` **单一源**（R-45 口径）且页码窗口化、事件一次委托。**纪律（新增）**：**凡「可能无限增长」的列表能力（分页 / 虚拟滚动 / 排序）一律做在引擎层并由守卫锁住**，禁各页各写一版；调用点不得私自关掉（守卫扫全站 `pageSize: 0` 为零）。**守卫**：`filter-row.test.mjs::S10`。**真机复核**：名册 `共 50 人 · 第 1 / 5 页`（翻页钮 30px、翻页生效、筛选后控件自动消失）、纪检考勤矩阵 `第 1 / 7 页`、组长台考察明细 `第 1 / 4 页` 且当页恰 10 行、`PAGEERRORS` 0 | COMPONENT_SPEC §4.3（筛选行规范·分页内置统一引擎） | 已闭环（全量 561/561） |

## 评议轮次排期（H60.7 · C4 已批准，发起即删）

| 轮次 | 来源 | 状态 |
| --- | --- | --- |
| （当前无待发起轮次） | 功能实现评议 / 最小三成本均已收官（2026-09-10） | — |

> **注**：AI 逃逸 / 理论复用下轮按支书指令随时启动（入本表或直接发起）；发起轮次后即从本表删行，轮次进度照 REVIEW_QUEUE 附录。

***

## P1 — 紧急/阻塞项

当前无紧急/阻塞项。

***

## P2 — 数据一致性

| ID    | 事项 | 引用流程 | 修改对象 | 状态 |
| ----- | -- | ---- | ---- | -- |
| 当前无待办 | —  | —    | —    | —  |

***

## P3 — 治理/测试/文档

| ID    | 事项                                                                                                                                                                                                    | 引用流程                                                        | 修改对象                                                                                           | 状态       |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | -------- |
| T-118 | **治理文档完善+用户文档体系梳理**：基于Harness框架同步推进治理文档完善，梳理现有用户文档体系，识别缺失/冗余/过时（2026-08-03 根目录文档更新 + 2026-08-10 Agent 残留清理/主题色色盘表述/常态化评议制度化已推进）                                                                       | H30.2 母本子本 + H100 项目产出声明 + 支书 2026-08-10「开发的不同阶段会有不同的方法和观念」 | content/ + docs/                                                                               | 🔄 进行中   |
| T-206 | **支书论述做法改革·支书继续调整**：SECRETARY\_DIRECTIVES.md 做法改革已落地（2026-08-04：支书原话作正文/多条原话无序列表/AI 扩充入引用块/去"支书原话："标签/后缀式日期，文件头部 L16 已写入"正文形式"条款，16 条带原话条目全部改造完成）。AI 自查（2026-08-14）已产出 14 项疑似待调整点清单（计数/日期矛盾、引号标点规则、AI 标注残留等）——**待支书逐条确认后 AI 修订** | H60 支书评议 + SECRETARY\_DIRECTIVES.md 头部"正文形式"条款          | content/01\_strategy/SECRETARY\_DIRECTIVES.md                                              | 🔄 待支书确认  |

> **注**：常态化专项评议（反论/理论复用/黑话审查/原话复核/最小三成本）已制度化，由 W4 专项评议循环承接，不再驻留乙部——专项清单、侧重维度与轮次进度见 CLAUDE.md H60.5 + `.ctx/REVIEW_QUEUE.md` 附录（乙部清理不灭机制）。

> **滞留清理（2026-09-11 支书裁定：完成项不滞留乙部）**：乙部只保留**未闭环的可执行项**；已完成工作一律不在此留注（其权威登记=EXECUTION_LOG 对应当条 + REVIEW_QUEUE 附录），如需再启动必须**重新注册**新条目/新轮次并标注轮次，不得以「已完成注记」形式滞留。

***

# 丙部：待决策事项

> **丙部是"待决策"的问题池**——AI 拿不定主意的事项提交支书决策。
> 生命周期：写入 → next\_prompt 提交 → 支书决策 → Decision Log 归档 → 执行 → 从丙部删除
> 详细规则见甲部 H50.2

> **P.11/P.12/P.13（2026-09-08 归口）已于 2026-09-09 支书决策完毕**：P.11 全局评估总表 v1 AI 定案生效（REVIEW_QUEUE 总表已标定案）；P.12 概况侧只读复查特批执行（报告 .ctx/logs/archive/2026-09-09-P12-overview-audit.md，修复已于 2026-09-09 特批落地 ebf55d16）；P.13 Tailwind CDN 治理=保持现状关闭（E-5 页内优化已保留）。历史 P.8/P.9/P.10 已决策退出（D-270/271/272）。决策归档见执行日志 T-2026-09-073。

- **P.14（2026-09-09 归口）push 放行**：本会话早期 6 笔已随远端推进（`origin/main` = `665613c5` 起，推送非本地执行）；**本地现存领先 15 笔**——`ddaa320c`（黑话第 5 轮）、`22120e64`（活动写入 S-1~S-3）、`60dfa271`（S-4 写入 UI 统一）、`5138e638`（文档同步）、`dac678e4`（收口批）、`9e7a088f`（支书五项批次）、`4245c31a`（对比度 C-1 + 功能实现评议 8 项）、`f25cd9cb`（A~E 第二批：R-6/R-7/C①~C⑥/门面口径）、`349e0679`（A~E 第三批：R-8/R-9/拖拽机测/成员变更确认链端点）、`b30bcd85`（A~D 第四批：R-10 三链语义端点+副书同权 / R-11 深色第三批 / 滞留清理）、`cce378da`（第五批：R-12 深色第四轮 + 黑话第 6 轮 + 数字不硬编码清扫）、`620edbcf`（第六批：R-13 浅色对比存量核销）、`ff48f31f`（第七批：R-14 样式表专批）、`aa3fc0b1`（第八批：R-15 深色收官 + 黑话第 7 轮）、本批（第九批：列表可点扫修 + 意见反馈真匿名 + 正式表决无记名）；工作区 clean；**待支书明确批准后 push，推送前先 `git status` 核对**。
