# Handoff · 会话交接（2026-09-09，供新对话接手）

> 用途：书记将用新对话继续操作。本文件汇总结当前仓库状态、待办归属与基线纪律。全量执行记录见 `.ctx/logs/2026-09-EXECUTION_LOG.md`（T-072~T-076 与当日各批）。

## 1. 近期交付快照（2026-09-08 ~ 09-09）

- **设置中心**（完整上线，版本串 20260909e）：`settings.html`（12 根页+7 工作台=19 页）。侧边栏右下「设置」→ 左分组栏+右内容；**外观**（字号/主题三态/强调色，**按人隔离 R1-A：首登出厂默认、登录人只读写 person 键、全局键仅服务访客**）；**我的工作台**（个人 tab 顺序拖拽/上移下移/恢复默认，preferences.js personId 键，核心组锁定）；**支部治理**（书记/副书记·副书同权：支部信息+换组织向导内嵌+工作台默认顺序写 config+支部制度参数只读卡+**配置变更记录**列表/单键回滚/why 留痕/上限100）；**域参数 L2**（纪检考察超期/组织滞留复核窗/组长学期提醒=各域负责人本域可调）；书记台「支部配置」tab 已移除（迁移完成）。
- **支部归属显式化**：`getBoundBranch`（无归属/查无=null 不再被示例支部 br-b1 冒充）；登录无归属 header 显「未绑定支部」、settings 统一「未找到您所属支部」提示、向导入口不唤起。示例支部维持为可整体替换的模板默认。
- **config 审计内核**：configChangeHistory 支持 why；`rollbackBranchConfig` 单键回滚（聚合拒绝、回滚再留痕不改写历史）；api/server `PATCH /config/rollback` 同源；历史保留最近 100。
- **主题色一致性**：bootstrap/header/书记 calendar·assign·todo/纪检补课 6 处改 person-aware 渲染时解析（theme.js `getAppliedAccentColors`）。
- **代码减负批**：死导出清理 −62 行、4 个内部函数去 export、8 处过时注释校正；**删除 form-renderer.js**（0 生产引用）+ 专属单测 + 契约段落撤回（034d1653）。
- **文档全量同步**：SNAPSHOT 覆盖式 **v20**；help 新增 0.4 设置中心速览卡；README×2/ARCHITECTURE/SERVICE_CATALOG/DEPLOYMENT/USAGE_POLICY/DATA_MODEL 全部 12 根+7 台（19 页）与最新语义（d7a484ad）。
- **config 颗粒度 content 声明（书记审定定稿）**：PARTY_COMMITTEE_DESIGN **§2.6 配置颗粒度与变更流分层**（六层+党委旁路矩阵/词条收口/放行程序/变更流/内容接缝）、SYSTEM_ROLE_PERMISSION **§9h config 写权矩阵**、USAGE_POLICY 词条（L2-L3 配置层/域参数/制度默认/制度刚性/branch-default/institutional/POLICY_OVERRIDABLE/换壳工作单）。
- **工程化评估上调 v5**：超参 78→84（域参数 config 驱动落地）、组合 78→80、综合 ≈76→**≈78**；REVIEW_QUEUE 全局评估总表同步（044df9df）。
- 更早（09-08）：?reset=init 回填修复、党小组进展页签、全前端冒泡 0 error、H60.7 评议→待办归口闭环、乙部 E-1~E-5 执行清项、REVIEW_QUEUE 主队列清空/评估总表建表并定案（v1：11 专项+工程化）。

## 2. 当前基线事实

- 版本串 `?v=20260909e`（全站统一）；`CODE_VERSION=90`（core/cross-page-state.js 尾注）。
- 分支 `main` ahead origin 约 80 commits，**未 push（须书记批准）**。
- 关键新文件/新模块：`docs/settings.html`、`docs/src/entries/settings-entry.js`、`components/appearance-controls.js`、`services/preferences.js`（个人偏好/`resolveTabOrder`）、`services/branch.js`（`getBoundBranch`/`rollbackBranchConfig`/审计 why）、`core/policy-defaults.js`（`_FACTORY`→副本、`POLICY_OVERRIDABLE`）、`core/config-clean.js`（policyOverrides/rollback 常量）。
- 测试关键文件：`server/test/{preferences,theme-pref,policy-config,branch-affiliation,branch-config-audit}.test.mjs` 等；全量基线 422 例=410 绿+12 环境受限（浏览器/独立 3000 服务，口径同 T-074）。

## 3. 待办与归属（重要：新对话接手点）

**A. 待书记裁决（当前最相关）**
- push 放行：`git push`（约 80 commits，ahead main）；建议先 `git status` 核对。
- 乙部「评议轮次排期」4 项待发起（黑话审查第4轮 / 原话复核余批 / 功能实现下轮 / 最小三成本下轮——发起走 H60/H60.7，即从乙部删行）。

**B. 工作区遗留（勿用 git add -A）**
- `docs/src/components/tab-bar.js`：**U3a 大重构 WIP，未提交、未触碰**——归属任务待厘清（若非本仓近期批次产物，可能是早期未收口改动；提交前先 git diff 审阅）。
- `.ctx/` 与 `content/` 其余均已按纪律提交/审定，无其它待提交内容。

**C. 常规终端补验清单（沙箱/浏览器受限项，书记抽查或新对话跑）**
- 浏览器目视：设置中心（拖拽真机手测 R2、变更记录回滚 UI、无归属卡、主题色同金抽验）；各台 smoke。
- 环境受限 12 例：b3-1-makeup-writeback、click-cost C1-C3、mock-integrity M1/M2、e2e-login、module-config-e2e、block-config-ui-e2e S3、online-committee、party-committee-review P3（跑法见各测试头注；静态形态先行、server 用例需 `DISABLE_PASSWORD_CHECK=1`）。
- help DCL 首屏优化保留（P.13 Tailwind 治理=保持现状关闭）。

**D. 已登记未结（纪律台账）**
- work-overview 概况侧保态缺口：只读复查报告 `.ctx/logs/2026-09-09-P12-overview-audit.md`，修复待书记特批（禁改文件）。
- settings「配置变更记录」对 party-staff 的党委台 UI 未接线（内核已含门）。
- R1-A 首帧外观小闪烁=已接受局限。
- function-catalog/help 等计数已同步（若后续再加根页须同步 19→N）。

## 4. 常用命令与纪律速查

- 静态前端测试：`node --test server/test/<file>.mjs`（纯逻辑）；全量 `cd server && npm test`（注入 DISABLE_PASSWORD_CHECK）。
- 改前端源码后：GetDiagnostics 0 + node --check（非 CJS 文件用 GetDiagnostics）+ 相关纯逻辑测试；版本串更新走 `node docs/scripts/bump-version.mjs <新串>`（整库统一+CODE_VERSION+盲区手工），**收口批再 bump，小批沿用当串**。
- 禁改零触碰：content/（书记批改层，改动须书记/审定流程）、styles.css 本体、core/mock-adapter.js、core/domain.js、components/inspector.js、services/roster.js、entries/tabs/secretary/overview-tab.js、components/work-overview.js、components/org-setup-wizard.js（逻辑）、docs 只读 import 面。
- config 写权分层与放行程序见 PARTY_COMMITTEE_DESIGN §2.6（书记审定定稿）；评估总表维护=AI 建议分+书记校正即定案，REVIEW_QUEUE「全局评估总表」。
- 少日志纪律（书记 2026-09-09 批评登记式逃逸）：日志只记决策/里程碑，例行完成并入当条，不逐项新开编号；能闭环当场闭环，不用"登记/待抽查"占位。
