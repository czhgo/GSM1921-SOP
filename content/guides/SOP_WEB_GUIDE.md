---
role: "[人机]"
title: "SOP 网页优化与同步指南"
type: guide
---

# SOP 网页优化与同步指南

> 本指南整合了 SOP 文档优化方法论、网页展示问题诊断、数据映射规则与优化提案机制，为 SOP-网页联动维护提供一站式参考。

---

## A. 问题诊断

### A.1 内容两极分化

当前 SOP 文档和模板库存在两极分化问题：

| 问题类型 | 症状 | 影响 |
|---------|------|------|
| **内容冗余** | 部分 SOP 过多重复的上下文铺垫、过长的引言段落 | 核心流程被淹没，可读性差 |
| **内容粗糙** | 部分模板仅占位、缺少可操作的步骤指引 | 模板无法直接使用，实用性低 |
| **结构不统一** | 文档间信息层级不一致（有的3层，有的5层） | 导航混乱，查找成本高 |

### A.2 跨文档系统性缺陷

| 编号 | 问题 | 影响范围 | 优先级 | 建议 |
|------|------|---------|--------|------|
| G-01 | **三份 SOP 均缺少「一句话摘要」** | 全部 | **P0** | 立即补充，这是网页角色卡片 hover 面板的必需要素 |
| G-02 | **无统一的 Front Matter `summary` 字段** | 全部 | **P0** | 在 YAML front matter 中增加 `summary: "≤30字摘要"`，网页和 AI 均可直接读取 |
| G-03 | **内嵌模板与 SOP 正文混排** | 纪检/组织/宣传 | **P1** | 所有代码块格式的内嵌模板一律提取至 `content/references/模板库/`，SOP 中仅保留链接引用 |
| G-04 | **目录锚点不稳定** | 全部 | P2 | 锚点含中文、括号、特殊符号时，GitHub 锚点转换行为不可预测；建议锚点目标标题使用纯中文或纯英文，避免混搭符号 |
| G-05 | **文档更新记录全部内嵌于 SOP 末尾** | 全部 | P3 | 版本记录在网页展示中无价值却占据空间；建议仅保留最近 3 条，其余由 Git 历史覆盖 |
| G-06 | **无统一的「关联文档」尾节** | 组织 | P3 | 组织委员 SOP 缺少独立的「关联文档」节（目前散落在 §九 附录），与纪检/宣传委员不一致 |

### A.3 网页渲染影响汇总

| 渲染位置 | 当前可用数据 | 缺少数 | 影响 |
|---------|------------|--------|------|
| 侧边栏角色卡片 hover 面板 | 无（三份 SOP 均缺 summary） | 3 | 面板空白，用户无法快速了解委员职责 |
| 内容区 SOP 全文渲染 | 排版混乱（内嵌模板+长表格） | — | 移动端体验极差 |
| 目录侧边导航 | 层级过深（3-4 层） | — | 导航信息过载，定位缓慢 |
| 关联文档交叉引用 | 不一致（部分缺少独立关联节） | 1 | 文档间跳转断裂 |

---

## B. 优化方法论

### B.1 核心策略：网页可视化驱动的倒推优化

> **以网页可视化展示为下游应用目标，推动 SOP 内容的结构化与优化。**

网页不是 SOP 的"复制粘贴展示区"，而是 SOP 的**可视化检验工具**：

- 当 SOP 在网页上展示效果差（冗余/混乱/不可操作）→ SOP 本身需要先优化
- 当模板在网页上无法直接填充使用 → 模板结构需要先调整

### B.2 联动优化闭环（4步循环）

```
┌─────────────────────────────────────────────────────────┐
│                  STEP 1: 网页展示效果审查                   │
│                                                         │
│  将当前 SOP / 模板渲染到网页界面上                           │
│  目视检查：[  ] 信息层级是否清晰？                          │
│           [  ] 核心流程是否能一眼定位？                      │
│           [  ] 模板是否可逐字段填充？                        │
│           [  ] 是否有大段无用铺垫文字？                      │
└────────────────────────┬────────────────────────────────┘
                         │ 发现不合理的展示效果
                         ▼
┌─────────────────────────────────────────────────────────┐
│                  STEP 2: 倒推 SOP 修改点                    │
│                                                         │
│  根据展示问题反向推导 SOP 缺陷：                             │
│  展示冗余   → SOP 结构松散、缺少"一句话摘要"                  │
│  层级混乱   → SOP 的小标题层级不规范                         │
│  难以操作   → SOP 缺少步骤化的 Checklist                    │
│  模板难用   → 模板字段定义不明确                             │
│                                                         │
│  产出: SOP 修改建议清单                                     │
└────────────────────────┬────────────────────────────────┘
                         │ 逐项修改 SOP
                         ▼
┌─────────────────────────────────────────────────────────┐
│                  STEP 3: 执行 SOP 更新                      │
│                                                         │
│  按修改建议清单 → 逐项更新 SOP 源文件：                       │
│  - 精简冗余段落                                            │
│  - 规范化小标题层级（H2→H3→H4）                              │
│  - 增加步骤 Checklist / 决策流程图                          │
│  - 补全模板的字段定义 + 填写示例                              │
│                                                         │
│  更新 SOP Front Matter 中的 last_updated                   │
└────────────────────────┬────────────────────────────────┘
                         │ SOP 已更新
                         ▼
┌─────────────────────────────────────────────────────────┐
│                  STEP 4: 同步更新网页渲染                     │
│                                                         │
│  将更新后的 SOP 重新渲染到网页 → 确保网页（作为 SOP 的可视化   │
│  呈现载体）始终与最新版 SOP 保持一致                           │
│                                                         │
│  记录: SOP vX.Y → 网页更新 → 验证展示效果一致                 │
└─────────────────────────────────────────────────────────┘
```

### B.3 SOP 结构化标准

#### 通用结构模板

每份 SOP 文档理想结构：

```markdown
# [文档标题]

> **一句话**: [30字以内的核心摘要]

## 适用角色
- [角色A]: [职责简述]
- [角色B]: [职责简述]

## 核心流程

### [阶段1]: [名称]（时间窗口）
1. [动作1]
2. [动作2]

### [阶段2]: [名称]（时间窗口）
1. [动作1]
2. [动作2]

## Checklist

- [ ] [检查项1] — [负责人] / [时限]
- [ ] [检查项2] — [负责人] / [时限]

## 常见问题（FAQ）

**Q: [问题]**
A: [解答]

## 关联文档
- [文档A](链接) — [关系说明]
```

#### 字数精简原则

| 当前问题 | 优化原则 |
|---------|---------|
| 大段政治背景铺垫 | 移至独立的"背景说明.md"，正文只保留 1 行引用链接 |
| 重复的"为什么要做这件事" | 每份 SOP 只在开头有一段 ≤80 字的 Why |
| 历史过程叙述 | 不放在操作指南中，移至 Insights 的经验沉淀 |

#### 模板可用性标准

模板必须满足：

1. **字段定义明确** — 每个填空项有 label + 说明文字 + 示例值
2. **可逐字段填充** — 不需要读完整篇才能开始填第一个字段
3. **有至少一个完整的填写示例** — 模板末尾附一段「填写示例」
4. **打印友好** — Markdown 渲染后适合 A4 打印

### B.4 SOP-网页 联动索引表

#### 当前映射

| SOP 文档 | 网页渲染位置 | 展示方式 | 优化状态 |
|---------|-------------|---------|---------|
| 常见工作场景快速指南.md | Module 2 参考指南 → 时间线卡片 | 展开/折叠 (WHAT/HOW) | 待评估 |
| 支委与党小组定人定责定岗说明.md | 侧边栏角色卡片 → 角色信息 | 角色标识 + 标签 | 待评估 |
| 组织/宣传/纪检委员指南 | 角色卡片 hover/focus 面板 | 职责摘要 | 待评估 |

#### 优化优先级

| 优先级 | SOP 文件 | 优化要点 |
|--------|---------|---------|
| P0 | 常见工作场景快速指南.md | 场景标签多、时间线长→需要更好的折叠分组 |
| P1 | 各功能委员工作流程指南 | 职责表→结构化攻略 Checklist 模式 |
| P2 | 模板库中各模板 | 多模板需要统一的卡片预览+下载入口 |

### B.5 执行计划与责任分配

#### Phase 1: 诊断扫描（本月）

| 动作 | 目标 |
|------|------|
| 将 3 份核心 SOP 渲染到网页，截图标记展示问题 | 产出展示问题清单 |
| 每个模板填充一遍，记录填充中的困惑点 | 产出模板可用性问题清单 |

#### Phase 2: SOP 修改（下月）

| 动作 | 目标 |
|------|------|
| 按问题清单优化 SOP 文本 | 每份 SOP 一次迭代 |
| 同步更新网页渲染逻辑 | 确保展示正确 |

#### Phase 3: 机制固化

| 动作 | 目标 |
|------|------|
| 将"SOP 修改→网页同步"流程纳入 QUARTERLY_TASKS 周期性任务 | 季度执行 |
| 在 PR 模板中增加"SOP-网页一致性检查" checklist | 防止回退 |

#### 责任分配

| 角色 | 职责 |
|------|------|
| 外交部 (UI Agent) | 网页渲染效果审查 → 产出展示问题清单 |
| 发改委 (Doc Agent) | 根据展示问题倒推 SOP 修改建议 → 执行 SOP 修改 |
| 检察院 (Review Agent) | 验证 SOP 更新后网页展示效果一致性 |
| 档案馆 (Log Agent) | 记录每次 SOP-网页联动更新的版本号和时间戳 |

---

## C. 展示问题清单

> 以下为 3 份核心 SOP 执行 Step 1「网页展示效果审查」的产出，方法论依据见本指南 §B.2。

### C.0 审查标准

| 检查维度 | 标准（来源） | 合格条件 |
|---------|------------|---------|
| **一句话摘要** | B.3 通用结构模板 | H1 下方 ≤30 字的 `> **一句话**:` 引用块 |
| **信息层级** | B.3 字数精简原则 | 小标题层级 ≤ 4 层（H2→H3→H4），无跳级 |
| **核心流程可定位** | B.2 Step 1 检查项 | 从目录/首屏能 3 秒内定位到核心工作流 |
| **冗余铺垫** | B.3 字数精简 | 无大段政治背景铺垫（>100 字），Why 段 ≤ 80 字 |
| **Checklist 存在** | B.3 通用结构 | 每份 SOP 至少一个 `## Checklist` 节 |
| **内嵌模板归属** | B.3 模板可用性标准 | 内嵌的模板/表格有字段定义 + 示例值，或归入模板库 |
| **文档长度** | B.3 精简原则 | 单一 SOP ≤ 500 行（含附录），超长则考虑拆分 |
| **目录锚点可用** | copilot-instructions 锚点追踪 | 目录中所有锚点可跳转 |

### C.1 纪检委员工作流程指南.md（v3.10, 405 行）

#### 展示问题清单

| 编号 | 问题 | 严重度 | 位置 | 网页展示后果 |
|------|------|--------|------|------------|
| J-01 | **缺少"一句话摘要"** | P0 | H1 (#) 之后直接进入「文档说明」 | 网页角色卡片 hover 面板无法提取摘要文字，展示空白 |
| J-02 | **「文档说明」段过长** | P1 | L23-L35 | 网页展开面板首屏被说明文字占满，核心职责表被挤到折叠区下方 |
| J-03 | **核心职责表信息密度过高** | P2 | L66-L81（7 行 + 2 段调整说明） | 网页卡片式渲染时表格被截断，需横向滚动；移动端不可用 |
| J-04 | **内嵌考勤表模板混入 SOP 正文** | P1 | L165-L176 | 网页渲染时 ASCII 表格破坏排版流，且该模板不应在纪检 SOP 中作为内嵌代码块 |
| J-05 | **内嵌记录表模板混入 SOP 正文** | P1 | L247-L259 | 同上，破坏网页排版 |
| J-06 | **§附录 文档更新记录过长** | P2 | L392-L406（10 条版本记录） | 版本记录在网页展开面板中占据大量空间，可折叠或移至页脚 |
| J-07 | **§(一)/§(二) 两域职责与 §1.1 职责表存在语义重复** | P2 | L83-L98 vs L66-L81 | 网页展示时用户会看到两遍"考勤管理""参与记录"描述，造成阅读疲劳 |

#### 倒推 SOP 修改建议

| 编号 | 建议 | 对应展示问题 |
|------|------|------------|
| J-FIX-01 | H1 后增加：`> **一句话**: 纪检委员负责三会一课刚性考勤、支部成员三层参与记录、补课督办与复盘监督，月底汇总提交组织委员。` | J-01 |
| J-FIX-02 | 将「文档说明」精简为 3 行：核心定位 + 特别说明（不展开历史） | J-02 |
| J-FIX-03 | §1.1 核心职责表保留，调整说明移至 §附录；§(一)/§(二) 改为简表（每个域 ≤ 3 行） | J-03, J-07 |
| J-FIX-04 | 考勤表模板 / 记录表模板从正文中提取，移至 `content/references/模板库/` 并以链接方式引用 | J-04, J-05 |
| J-FIX-05 | 文档更新记录仅保留最近 3 条，其余精简为"详见 Git 历史" | J-06 |

#### 结构健康度评分

| 维度 | 得分 | 说明 |
|------|------|------|
| 信息层级规范性 | 7/10 | 层级清晰但有跳级现象（目录中去「视频/海报预览」等旧节残留） |
| 核心流程可达性 | 6/10 | §1.3 Global 核心职责是亮点（DRY），但首屏被"文档说明"阻断 |
| 冗余度 | 7/10 | Global 职责提炼有效减少了重复，但两域职责与核心表仍有语义重叠 |
| Checklist 完备度 | 9/10 | §5.2 有完整的每周/每月/学期检查清单，结构良好 |
| **综合** | **7.3/10** | |

### C.2 组织委员工作流程指南.md（v1.8, 688 行）

#### 展示问题清单

| 编号 | 问题 | 严重度 | 位置 | 网页展示后果 |
|------|------|--------|------|------------|
| Z-01 | **缺少"一句话摘要"** | P0 | H1 后直接进入「文档说明」 | 同 J-01 |
| Z-02 | **文档总长 688 行，严重超标** | P0 | 全文 | 网页渲染加载时间长；手机端滚动距离过长；AI 读取消耗大量上下文窗口 |
| Z-03 | **目录过于冗长** | P2 | L34-L106（73 行目录） | 网页侧边栏目录导航无法承载三层深度嵌套；移动端目录全展开后首屏被占满 |
| Z-04 | **§3.2 思想汇报统计表模板 内嵌** | P1 | L274-L292 | 同 J-04 |
| Z-05 | **§1.2 四大工作场景 为重复概括** | P2 | L138-L159 | 与 §1.1 核心职责表存在明显语义重叠，读者会在不同位置读到相同信息 |
| Z-06 | **§五 协助党员发展工作 过于详细** | P2 | L391-L479（89 行） | 此节可独立为 `党员发展流程详细指南.md`，当前混入组织委员 SOP 稀释了核心流程 |
| Z-07 | **FAQ 节过长** | P2 | L564-L612（49 行，7 个 Q&A） | 网页 FAQ 折叠面板展开后内容过多 |
| Z-08 | **§2.2 锚点 `公邮来件归档（纪检委员查收后转交）` 包含括号和中文，GitHub 锚点转换后不可预测** | P3 | 目录 L50 | 网页内链可能断链 |

#### 倒推 SOP 修改建议

| 编号 | 建议 | 对应展示问题 |
|------|------|------------|
| Z-FIX-01 | H1 后增加：`> **一句话**: 组织委员负责考察档案维护与材料全案复核、思想汇报归档统计、预备党员转正流程协调及发展对象培训班领队。` | Z-01 |
| Z-FIX-02 | 拆分：将 §五「协助党员发展工作」独立为 `content/SOP/党员发展流程详细指南.md`，组织委员 SOP 中仅保留 1 段概述 + 链接引用 | Z-02, Z-06 |
| Z-FIX-03 | 目录精简：三级标题不列入主目录，仅保留 H2 标题 | Z-03 |
| Z-FIX-04 | §1.2 四大工作场景 与 §1.1 核心职责合并为一个表格，删除重复 | Z-05 |
| Z-FIX-05 | 思想汇报统计表模板移至模板库，仅保留链接引用 | Z-04 |
| Z-FIX-06 | §2.2 锚点标题中的括号替换为简洁中文（如 `公邮来件归档`） | Z-08 |

#### 结构健康度评分

| 维度 | 得分 | 说明 |
|------|------|------|
| 信息层级规范性 | 6/10 | 三级目录嵌套过深，部分标题含括号和特殊字符 |
| 核心流程可达性 | 5/10 | 688 行中核心流程被大量展开描述淹没 |
| 冗余度 | 4/10 | 四大工作场景与核心职责表重复明显；党员发展流程应独立 |
| Checklist 完备度 | 8/10 | §八 有每日/每周/每月/学期检查清单，结构完整 |
| **综合** | **5.8/10** | |

### C.3 宣传委员工作流程指南.md（v1.9, 672 行）

#### 展示问题清单

| 编号 | 问题 | 严重度 | 位置 | 网页展示后果 |
|------|------|--------|------|------------|
| X-01 | **缺少"一句话摘要"** | P0 | H1 后直接进入「文档说明」 | 同 J-01 |
| X-02 | **文档总长 672 行，严重超标** | P0 | 全文 | 同 Z-02 |
| X-03 | **目录过于冗长** | P2 | L31-L63（33 行目录） | 同 Z-03 |
| X-04 | **§4.2 内嵌了 3 个完整模板示例**（支部基本信息卡/支部荣誉清单/活动总结标准格式） | P1 | L338-L416 | 这些模板应属于 `content/references/模板库/`，混入 SOP 正文使网页展示时出现大段模板代码 |
| X-05 | **§3.4 材料要求标准 为静态规范表，不属于操作流程** | P2 | L284-L313 | 网页渲染时可以保留，但更适合作为参考指南独立节或模板库附录 |
| X-06 | **§五 宣传档案队伍建设 为长期规划内容** | P2 | L443-L478 | 队伍建设是学期级工作，混入日常操作指南加重了文档重量；可移至独立 `宣传队伍建设方案.md` |
| X-07 | **§3.0 按活动类型的宣传规范 对照表** 是本文档最大亮点，但被淹没在 §三 中的第三个子节 | P2 | L189-L211 | 此表应提升至 §一 核心职责的显眼位置，而非藏在 §三 内部 |
| X-08 | **§2.1 工作流程图内含"Mermaid"代码块标记但实际为 ASCII 图** | P3 | L227-L233 | 网页渲染 Mermaid 语法时报错，应改为纯 ASCII 或移除 `mermaid` 语言标记 |

#### 倒推 SOP 修改建议

| 编号 | 建议 | 对应展示问题 |
|------|------|------------|
| X-FIX-01 | H1 后增加：`> **一句话**: 宣传委员负责宣传与档案制度建设、宣传骨干队伍管理、支部大会档案统筹及党小组活动的宣传指导监督，每周向学工报送支部活动。` | X-01 |
| X-FIX-02 | 拆分：§五「宣传档案队伍建设」独立为 `content/guides/宣传队伍建设方案.md`；§四内嵌模板并入 `content/references/模板库/` | X-02, X-04, X-06 |
| X-FIX-03 | §3.0 对照表 提升至 §一 末尾作为"角色定位速查表" | X-07 |
| X-FIX-04 | §3.4 材料要求标准 保留但标注为"规范参考（来自模板库）" | X-05 |
| X-FIX-05 | 修复 Mermaid 代码块标记 | X-08 |
| X-FIX-06 | 目录精简为仅含 H2 标题 | X-03 |

#### 结构健康度评分

| 维度 | 得分 | 说明 |
|------|------|------|
| 信息层级规范性 | 6/10 | 层级合理但目录过长，Mermaid 标记错误 |
| 核心流程可达性 | 5/10 | §3.0 对照表是亮点但位置靠后，672 行中核心流程不易定位 |
| 冗余度 | 5/10 | 模板内嵌和队伍建设描述占比过大 |
| Checklist 完备度 | 8/10 | §八 有完整的每周/每月/学期检查清单 |
| **综合** | **6.0/10** | |

### C.4 优先级行动计划

#### Phase 1: 立即修复（P0，本次会话可达）

| 动作 | 目标文件 | 产出 |
|------|---------|------|
| 为 3 份 SOP 补充 `summary` front matter 字段 | 纪检/组织/宣传委员工作流程指南.md | 网页 hover 面板可用 |
| 为 3 份 SOP 补充 H1 下的「一句话摘要」引用块 | 同上 | 首屏快速定位 |

#### Phase 2: 结构优化（P1，下次会话）

| 动作 | 目标文件 | 产出 |
|------|---------|------|
| 拆分组织委员 SOP 的 §五 → 独立文件 | 新建 `content/SOP/党员发展流程详细指南.md` | 组织委员 SOP 瘦身至 ≤500 行 |
| 拆分宣传委员 SOP 的 §五 → 独立文件 | 新建 `content/guides/宣传队伍建设方案.md` | 宣传委员 SOP 瘦身至 ≤500 行 |
| 提取所有内嵌模板至模板库 | `content/references/模板库/` | SOP 正文干净 |

#### Phase 3: 标准化（P2-P3，下月）

| 动作 | 目标 |
|------|------|
| 统一目录锚点命名规范 | 去除括号和特殊字符 |
| 统一关联文档尾节结构 | 三份 SOP 对齐 |
| 将 SOP 更新→网页同步纳入周期性任务 | Q3: SOP 文本审查 |

---

## D. 数据映射规则

> 帮助书记在日常维护 Markdown 母本时，清晰地了解"改什么不会出事，改什么会崩系统"，从而安全、自信地推进 SOP 迭代。

### D.1 核心钩稽逻辑

#### 数据源识别

`index.html` 是一个 **AI 静态编译产物（单文件架构）**。它的所有业务数据（步骤文本、角色徽章、时间偏移、注意事项）并非运行时从 Markdown 动态加载，而是由 AI 编译代理在人工调用时一次性将 Markdown 母本"翻译"并硬编码进 HTML 中。

**数据流向（单向、人工触发）：**

```
流程指南/常见工作场景快速指南.md   ──编译代理──►   index.html (timeline-node 节点组)
流程指南/组织委员工作流程指南.md   ──编译代理──►   index.html (timeline-node 节点组)
流程指南/宣传委员工作流程指南.md   ──编译代理──►   index.html (timeline-node 节点组)
流程指南/纪检委员工作流程指南.md   ──编译代理──►   index.html (timeline-node 节点组)
```

编译代理依赖以下 Markdown 结构特征提取数据：

| 提取依据 | 在 Markdown 中的形式 | 示例 |
|---------|---------------------|------|
| **场景名称** | `## 【领域】场景名称` 格式的 `##` 级别标题 | `## 【活动建设】组织生活会` |
| **步骤序号** | 表格首列的 emoji 序号（`1️⃣`…`🔟`…） | `\| 会前准备 \| 1️⃣ 确定主题 \|` |
| **时间偏移（T-N/T+N）** | 表格"时间节点"列中的反引号代码块 | `` `[时间: T-7天]` `` |
| **角色分配** | 表格"负责人"列中的方括号标签 | `\[角色: 党小组组长\]` |
| **步骤描述文本** | 表格"具体操作"列 | `块块组长根据支委会部署确定会议主题` |
| **联系人** | 表格"联系人"列 | `储子禾/王峥旭/辛长乐` |
| **注意事项** | `#### ⚠️ 注意事项` 下方的 `- ✅` / `- ❌` 列表项 | `- ✅ 考勤为刚性考勤` |

#### 字段映射

每个 HTML `timeline-node` 节点对应 Markdown 表格中的**一行数据**，字段对应关系如下：

| HTML 属性 / 元素 | 对应 Markdown 列或文本 | 说明 |
|----------------|----------------------|------|
| `data-scenario="org-life"` | `## 【活动建设】组织生活会` 标题 | 场景 ID，snake-case 短字符串（如 `org-life`、`theme-party`），编译代理负责映射 |
| `data-domain="activity"` | 场景标题中的领域前缀（`【活动建设】` / `【组织建设】`） | 活动建设类→`activity`，组织建设类→`organization` |
| `data-role="leader"` | 表格"负责人"列中的 `[角色: 党小组组长]` | 见下方角色映射表 |
| `.tl-dot` 内序号数字 | 表格首列 emoji 序号（`1️⃣` → `1`） | 仅取数字 |
| `.badge-time` 内容 | `` `[时间: T-7天]` `` → 提取为 `T-7` | 编译代理截取时间标签中的相对时间 |
| `.js-expand-trigger` 标题文本 | 表格"步骤"列（`1️⃣ 确定主题`） | 仅取步骤名称 |
| `.js-expand-target` What 区 | 表格"具体操作"列 | 详细描述文本 |
| `.js-expand-target` Who 区 | 表格"负责人"列 + 表格"联系人"列 | 拼合为"角色 + 具体姓名" |
| 注意事项区 | `#### ⚠️ 注意事项` 下 `- ✅`/`- ❌` 列表 | 每个场景展示最多 5 条 |

**角色（`data-role`）映射表：**

| Markdown `[角色: ...]` 标签 | HTML `data-role` 值 | 对应 role-btn 按钮 |
|---------------------------|--------------------|--------------------|
| `[角色: 党小组组长]` | `leader` | 党小组组长 |
| `[角色: 组织委员]` / `[角色: 宣传委员]` / `[角色: 纪检委员]` | `commissioner` | 条条委员 |
| `[角色: 活动组织者]` | `organizer` | 活动组织者 |
| `[角色: 深度参与者]` | `deep` | 深度参与者 |
| 全体与会 / 无特定角色 | `all` | 全部场景（不过滤隐藏） |

### D.2 同步安全区（低成本变更）

以下操作编译代理可以**极低成本、完美同步**到 HTML，书记可以大胆修改：

#### 可安全修改的内容

| 操作类型 | 示例 | 编译成本 |
|---------|------|---------|
| **修改步骤的描述文本** | 将"通知到人"的具体说明改为更准确的措辞 | 极低：更新对应节点的 `.js-expand-target` What 区文字 |
| **修改联系人姓名** | 将"侯嘉嵘"更新为新任组织委员姓名 | 极低：更新对应节点 Who 区姓名 |
| **修改注意事项条目的文字** | 优化某条 `✅` 提示的措辞 | 极低：更新对应场景的注意事项区文本 |
| **调整步骤的时间偏移** | 将 `T-7天` 改为 `T-5天` | 极低：更新对应节点的 `.badge-time` 文本 |
| **在现有场景末尾追加一个新步骤** | 为【活动建设】组织生活会增加第13步 | 低：新增一个 `timeline-node` 节点，复制现有节点结构即可 |
| **修改「注意事项」条目的数量（±1～2条）** | 新增一条合规提示 | 低：直接在注意事项区追加 `<li>` 即可 |
| **修改合规模板文件名** | `党支部工作记录.docx` → 新版文件名 | 低：更新 What 区内的文件链接文字和 href |
| **修改场景的副标题描述** | 更新 `scenario-header` 内的灰色说明文字 | 极低：更新 `<span class="text-gray-400">` 内的文本 |

### D.3 架构雷区（高危变更）

以下操作如果由书记**直接修改 Markdown 母本**而不同步通知编译代理更新 HTML，将导致 HTML 状态机逻辑错误或数据不一致：

#### 危险操作 — 会导致 HTML 状态机崩溃或渲染错误

| 危险操作 | 为什么会崩 | 正确做法 |
|---------|-----------|---------|
| **擅自改变时间锚点书写格式** | 如把 `` `[时间: T-7天]` `` 改写成"提前一周"或"活动前7天"，编译代理无法识别 `badge-time` 的相对时间标记，时间徽章将显示为空或原始文本 | 必须保持 `` `[时间: T-N天]` `` 或 `` `[时间: T+N天]` `` 或 `` `[时间: 活动中]` `` 三种固定格式之一 |
| **擅自增加 `[角色: ...]` 标签中的新角色类别** | 如新增 `[角色: 团支书]`，HTML `data-role` 不存在对应枚举值，该节点将被所有 role-btn 过滤器完全隐藏，用户永远看不到 | 必须先由书记告知书记/AI 扩展 role-btn 路由（新增按钮 + 过滤逻辑）再添加新角色标签 |
| **修改场景标题的编号格式** | 如把语义化名称标题改为纯数字编号标题，`data-scenario` 的 ID 映射将断裂，domain 过滤逻辑失效 | 场景标题必须保持语义化名称格式（如`## 【活动建设】组织生活会`），scenarioId 沿用 snake-case 短字符串 |
| **更改 `data-domain` 分类边界** | 将【组织建设】意见建议反馈从 `organization` 挪到 `activity`，domain-btn 过滤会把该场景路由到错误的分组 | 必须同步修改 HTML 中该场景所有节点和 `scenario-header` 的 `data-domain` 值 |
| **删除现有场景的中间步骤而不重新编号** | 删除【活动建设】组织生活会的步骤5（签到），但 `.tl-dot` 序号仍为 1-8，视觉上序号跳空，数据血缘断裂 | 删除步骤后必须同步重新编号所有后续节点的 `.tl-dot` |
| **在 Markdown 表格中合并多步骤行** | 将步骤6+7合并为一行，编译代理会生成一个节点覆盖两步，丢失原有两步的单独 `data-role` 归属 | 每个需要独立过滤的步骤必须单独一行 |
| **修改场景标题中的领域前缀** | 如将 `## 【组织建设】意见建议反馈` 误改为 `## 【活动建设】意见建议反馈`，编译代理据此前缀判断 `data-domain`，会导致场景路由到错误分组 | 场景标题必须保持 `## 【活动建设】...` 或 `## 【组织建设】...` 语义前缀，不得擅自改变 |

### D.4 快速决策树

```
我想修改 Markdown 母本中的某些内容
│
├─ 是否只修改"具体操作/描述文字"列？
│   └─ YES → 安全，直接改，然后告知 AI 代理同步 HTML 文本
│
├─ 是否修改"时间节点"列的格式（如从 T-7 天改为"提前一周"）？
│   └─ YES → 危险，必须保持 `[时间: T-N天]` 格式
│
├─ 是否要增加一个全新的步骤？
│   ├─ 追加到场景末尾 → 低风险，复制最后一个节点结构
│   └─ 插入到中间 → 中等风险，后续步骤全部需要重新编号
│
├─ 是否要增加一个全新的角色类别？
│   └─ YES → 危险，必须先扩展 HTML role-btn 路由再写 Markdown 标签
│
├─ 是否要增加一个全新的场景（如【组织建设】新场景名）？
│   └─ YES → 中等风险，需在 HTML 中新增 scenario-header + N 个 timeline-node
│
└─ 是否只修改联系人姓名或注意事项文字？
    └─ YES → 安全，直接改，告知 AI 代理同步即可
```

### D.5 版本钩稽关系表

下表记录了当前 HTML 与各 Markdown 母本的版本对应关系，书记可据此判断 HTML 是否需要重新编译：

| Markdown 母本 | 当前版本 | HTML v6.0 基于此版本编译 | 备注 |
|--------------|---------|------------------------|------|
| `流程指南/常见工作场景快速指南.md` | v1.7 | v1.7 | 9 个场景全部收录 |
| `流程指南/支委与党小组定人定责定岗说明.md` | — | — | 未单独展示，内容已融入各场景 Who 区 |
| `流程指南/组织委员工作流程指南.md` | — | 部分 | 核心职责融入【组织建设】积极分子考察/信息平台支持/考勤记录查询 节点 Who 区 |
| `流程指南/宣传委员工作流程指南.md` | — | 部分 | 核心职责融入【活动建设】组织生活会/党小组主题党日/品牌活动建设 节点 Who 区 |
| `流程指南/纪检委员工作流程指南.md` | — | 部分 | 核心职责融入【活动建设】党小组主题党日/【组织建设】考勤记录查询/意见建议反馈 节点 Who 区 |

> **触发重新编译的信号**：当某个 Markdown 母本的业务逻辑（角色、步骤、时间节点）发生变化时，书记应明确告知 AI 代理"需要重新编译 index.html 的场景 X"，并提供具体变化描述。AI 代理将按照 WORKFLOW_MASTER.md 的涟漪更新协议执行同步。

---

## E. 优化提案机制

> 本表单是支委向系统提交规则修改的唯一接口。拒绝形式主义，只需写明"哪里卡壳"以及"期望怎么改"。填写后提交给书记，由 AI 引擎执行全站重构。

### E.1 影响范围 (Scope) 请打勾 [x]

- [ ] **底层架构与原则建议**（Permanent：如建议进一步精简某类多余的填表流程）
- [ ] **全局通用业务规则**（Global：影响所有活动的通用标准，如报销/场地规则）
- [ ] **支委分工与权责调整**（Role：涉及某项任务负责人的变更）
- [ ] **特定业务场景专用**（Scenario-Specific：仅针对某一类具体活动）：
  - 场景名称/编号：_______________

### E.2 现场痛点与卡壳环节 (Pain Point)

- **卡壳的 SOP 文件与步骤**：（如：常见场景指南，党小组主题党日活动第3步）
- **实际执行困难**：（一句话说明原流程为何反直觉或不可行）

### E.3 期望的规则更改 (Proposed Fix)

- **业务建议**：（直接给出新的规则逻辑，无需在意公文措辞。如：非品牌主题党日活动允许微信群口头报备，免去填表。）

---

*(以下为 AI 处理区，人类无需填写)*

**AI 提取指令**：读取上述内容，转化为 REVIEW_STATE.md 中的结构化待办任务，并分配正确的 Scope 标签。

---

*本指南由以下文档合并而成：SOP_WEB_OPTIMIZATION.md、SOP_DISPLAY_ISSUES.md、SOP优化提案反馈卡.md、SOP数据映射与同步指南.md（2026-05-03 合并）。*
