// role: [工程师]+[AI]
// party-cross-nav.js — 党务管理跨支委导航栏（已废弃）
// party/ 页面已删除，此组件不再使用，保留空导出以避免 import 报错

/**
 * 渲染跨支委导航栏（已废弃，空操作）
 * @param {string} _currentPageRole - 忽略
 * @param {HTMLElement} _mountEl - 忽略
 */
export function renderPartyCrossNav(_currentPageRole, _mountEl) {
  // party/ 页面已删除，不再渲染跨支委导航
}
