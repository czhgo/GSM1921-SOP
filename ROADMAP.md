---
title: "Org OS — 项目路线图"
type: roadmap
owner: "Org OS Agent 集群"
role: "[人机]"
last_updated: "2026-05-06"
status: active
---

# ROADMAP

> **"常为新"原则**: 已完成事项保留简写摘要（✅ ~~strikethrough~~）供上下文理解，但简化为一行不展开细节。决策历史统一归集至 `.ctx/logs/DECISION_LOG.md`。仅活跃待办保留完整描述。

---

## 一、标准化三步工作流

### 1.1 审查（每次启动时必做）
1. 读入 `.github/copilot-instructions.md`
2. 读取 `.ctx/TIMESTAMPS.md` 检查周期性任务到期情况
3. 读取 `.ctx/SNAPSHOT.md` 确定当前基线
4. 阅读本文件（ROADMAP.md）确认当前待办

### 1.2 执行（单次迭代的标准流程）
1. **Blueprint**：输出任务拆解计划与执行路径
2. **实施**：调用对应工具/Agent集群
3. **验收**：自动运行 `GetDiagnostics` 或执行 lint/type-check
4. **记录**：追加至 `.ctx/logs/YYYY-MM-EXECUTION_LOG.md`
5. **next_prompt**：给出下一次迭代的明确入口

> **Snapshot 更新规则**：`.ctx/SNAPSHOT.md` **不再每次执行更新**。仅在以下明确触发条件满足时更新：
> - (a) 大版本升级（v3→v4），如架构重大变更、角色体系迭代
> - (b) 用户显式指令触发（如"/snapshot"或"生成快照"）
> - (c) 项目阶段性收官（Phase 1/2/3 完成）
>
> 日常变更仅由 `EXECUTION_LOG` + `DECISION_LOG` + `TIMESTAMPS` 承载，Snapshot 保持精准、低频、高信息密度。

### 1.3 钩稽联动
修改任何文件前，必须检查本文件中是否存在对该文件的钩稽声明并按要求联动更新。

---

## 二、角色三分类体系（已完成，不再维护）

角色定义已完成，相关文档见：
- `content/guides/ROLE_CLASSIFICATION.md`（v2.0）
- `content/guides/ROLE_CLASSIFICATION.md` v3.0（含可扩展性评估 8.6/10）

---

## 三、工程路线图

> **结构说明**：以下按工程工作流组织为六层（§A~§F），替代旧有的 "短期/长期规划" 二分法。
> 已完成事项在各层开头做简明汇总，理论基石（§0）保留原文。

---

### §0. 理论基石

> 以下四节为本项目的核心理论贡献，确立制度层面的设计原则。其内容不受工程重组影响，保持原文不动。

---

#### §0.1 双域管理理论 — 党建 x 党务 [2026-05-04 确立]

> **这是本项目最重要的理论贡献。** 我们放弃了旧有的"活动建设 / 组织建设"二分法，
> 升维为 **「党建工作」x「党务工作」** 的双域管理模型。

##### 一、概念定名

| 新称 | 旧称（废弃） | 核心含义 | 对应模块 |
|------|------------|---------|---------|
| **党建工作** | 活动建设 | 办好活动、探索创新边界（可以是活动创新，也可以是建设知识共享社区等创造性工作） | **党建工作台**（原推演工作台） |
| **党务工作** | 组织建设 | 保证组织稳定、合理、准确运行（符合规定、纪律严明、格式规范） | **党务管理** |

> **区分要诀**：党建是"创新探索"——可以发挥想象，积极探索党建边界；党务是"合规运行"——符合规定，纪律严明，格式规范。

##### 二、两个模块在管理视图中的分工

```
                    ┌─────────────────────────────────┐
                    │          Org OS 管理视图         │
                    └─────────────┬───────────────────┘
            ┌─────────────────────┴─────────────────────┐
            │                                           │
   ┌────────┴────────┐                        ┌────────┴────────┐
   │  党建工作台      │                        │   党务管理       │
   │  (创新探索域)    │                        │   (合规运行域)    │
   ├─────────────────┤                        ├─────────────────┤
   │ · 活动创建写入   │                        │ · 发展党员追踪   │
   │ · 创新工作探索   │                        │ · 材料催缴归档   │
   │ · 专班运行管理   │                        │ · 思想汇报统计   │
   │ · 知识共享建设   │                        │ · 合规文件只读   │
   │ · 考勤考察记录   │                        │ · 考勤考察存档   │
   └────────┬────────┘                        └────────┬────────┘
            │                                           │
            └─────────────┬─────────────────────────────┘
                          │
              ┌───────────┴───────────┐
              │   三委员 双域协同       │
              │   详见 ↓ 下表           │
              └───────────────────────┘
```

##### 三、三委员在双域中的职责重定义

| 委员 | 党建工作（党建工作台） | 党务工作（党务管理） | 角色标识 |
|------|---------------------|-------------------|---------|
| **组织委员** | 🔴 **专班协调视图**（所有专班的招募、赋权、运行跟踪均由组织委员统筹。各条支委均可发起专班需求，但招募执行归口组织委员。含发布招募表单 + 专班详情展开。） | 党员发展与考察（考察档案、材料催缴、思想汇报归档、预备转正流程——符合规定，纪律严明） | 👥 群体管理 |
| **宣传委员** | **活动与专班视图**（宣传材料产出、活动档案归档、每周一报送——按活动+专班混合卡片管理，选中展开子任务详情） | 宣传与档案制度合规建设（档案制度、模板体系、档案队伍建设——格式规范，合规存档） | 🖊 笔杆子 |
| **纪检委员** | **活动与专班视图**（考勤管理、活动复盘督办、参与三层记录——按活动+专班混合卡片管理，选中展开子任务详情） | 补课制度执行 + 公邮查收分发 + 意见建议反馈平台维护（纪律严明，格式规范） | 🛡 盾牌保护 |

> **看板设计原则（2026-05-06）**：
> - 看板仅两列：**待启动** + **进行中**。无"已完成"列——活动/专班全部完成即归档。
> - 子任务完成后不消失：灰显文字 + 删除线 + opacity:0.5 + 绿色对勾标记，沉到子任务列表底部。
> - 此设计体现"完成不放飞，但不下沉视野"的管理精神。

> **数据共享规则**：
> - 纪检委员记录的考勤考察数据，在后台与组织委员**通用同源**。
> - 组织委员在此基础上叠加专班维度的考勤考察（专班成员的贡献统计）。
> - 宣传委员的归档数据来自前述两者的产出，不可自行编造。

##### 四、信息流：宣传委员 -> 组织委员 -> 宣传专班

```
宣传委员 想招募宣传专班
    │
    ▼
向组织委员提出需求（说明所需人数、所需技能、存续周期）
    │
    ▼
组织委员审核 -> 在赋权面板中招募 -> 将同志信息赋权为宣传专班成员
    │
    ▼
宣传委员接收已赋权的专班成员 -> 分配具体任务
    │
    ▼
组织委员跟踪专班工作量（考勤考察记录与纪检数据共通）
```

> **判例**：宣传委员不可自行招募宣传专班。所有专班的赋权操作统一归口于组织委员。

##### 五、旧术语迁移对照

| 旧术语 | 新术语 | 迁移状态 |
|--------|--------|---------|
| 活动建设域 | **党建工作域** | 🔴 全量替换 |
| 组织建设域 | **党务工作域** | 🔴 全量替换 |
| 活动建设 / 组织建设（二元并称） | **党建 x 党务** | 🔴 全量替换 |
| ORGANIZATION_BUILDING_MODULE | 保留文件名但内容描述更新为"党务管理模块" | 🟡 渐进迁移 |

---

#### §0.2 差异化视图设计规范（角色维度 x 活动类型维度）

> **核心原则**：同一数据源（`ActivityRecordStore`），不同切面展示。角色决定**可见范围和操作权限**，活动类型决定**字段侧重和交互模式**。

##### A. 角色维度 — 数据访问权限与视图（v6.0 — 2026-05-05 双域分化）

> **2026-05-04 重大修正**：书记明确——**条条支委（组织/宣传/纪检）无权直接写入活动**。活动的创建写入必须依托**党小组组长**（即活动所属党小组的组长承办写入）。条条支委在自己的职能域（宣传→宣传档案、组织→党员发展/专班、纪检→考勤）有 write 权限，但对活动的写入不能越俎代庖。
>
> 写入链路：条条支委提出需求 → 组织委员协调 → 党小组组长承办并写入活动 → 条条支委在各自职能域操作。
>
> **2026-05-05 双域分化**：书记进一步明确——同一个角色在「党建工作台」和「党务管理」中看到的是**完全不同的视图和功能集**。此前一维表囫囵吞枣，现升维为二维矩阵。

**党建工作台（创新探索域）— 角色视图矩阵：**

| 角色 | 主视图 | 可见范围 | 活动操作权限 | 专属功能 |
|------|--------|---------|------------|---------|
| **普通参与者** *(默认)* | 全量活动日历（只读） | 全局活动 + 通知 + 招募 | read | 信息浏览基线；无写入口 |
| **党小组组长** | 日历视图 + 管辖活动筛选 | 管辖党小组活动 + 全局日历 | **read/write/confirm** | 组内活动写入承办 / 组员出勤确认 / **发起专班** |
| **活动组织者** | 日历视图（何时何地参会） | 自己组织的活动 | **read/write/assign** | 任务分配 / 进度追踪 |
| **深度参与者** | 日历视图（何时何地参会） | 被分配的任务 | **read/write**（仅任务状态） | 任务状态更新 |
| **组织委员** | **任务看板**（待启动/进行中/已完成） | 组织域任务 + 全局活动只读 | **read/recruit** | 专班招募统筹 / 赋权操作 / **发起专班** |
| **宣传委员** | **任务看板**（待启动/进行中/已完成） | 宣传域任务 + 全局活动只读 | **read** | 活动宣传材料 / 周报档案 / **发起专班** |
| **纪检委员** | **任务看板**（待启动/进行中/已完成） | 纪检域任务 + 全局活动只读 | **read** | 考勤跟踪 / 补课督办 / 复盘检查 / **发起专班** |
| **党支书** | 全局聚合视图 + 日历 | 全部数据（跨角色全集） | **全部权限** | 党课 / 主持大会 / 赋权管理 / 批量操作 / **发起专班** |

> **2026-05-06 关键修正 — 日历视图范围**：
> - 日历视图仅在 **只读模式**（全员）和 **管理模式中的 organizer/deep**（需要知道何时何地参会）中展示
> - 条条支委（组织/宣传/纪检）在管理模式中的视图为 **任务看板** 而非日历
> - 原因：宣传/纪检在党建维度服务于活动和专班，不需要日历提醒（这是给参与者用的），需要的是按任务划分的看板管理
  
> **2026-05-06 关键修正 — 专班发起权**：
> - 党小组组长、条条支委（组织/宣传/纪检）、党支书均可发起专班
> - 组织委员负责**招募统筹**（不是唯一发起者）
> - 发起≠招募：发起是提出需求，招募是统筹执行

**党务管理（合规运行域）— 角色视图矩阵：**

| 角色 | 主视图 | 可见范围 | 操作权限 | 专属功能 |
|------|--------|---------|---------|---------|
| **默认概览** *(无指定委员)* | 三委员统计摘要卡片 | 全量统计数据（只读） | read | 聚合仪表盘；无编辑入口 |
| **组织委员** | 看板视图（党员发展泳道） | 党员发展全流程 + 材料催缴 | **write**（发展党员域） | 考察档案 / 材料催缴 / 思想汇报归档 / 预备转正 |
| **宣传委员** | 多维表格（宣传档案12字段） | 宣传档案全量 | **write**（宣传档案域） | 档案归档 / 材料标准 / 模板体系 |
| **纪检委员** | 查询视图（筛选表单 + 督办） | 纪检域全量 | **write**（纪检域） | 补课制度执行 / 公邮查收分发 / 意见建议反馈 |
| **党支书** | 全局聚合 + 可切换子角色 | 全部数据（跨角色全集） | **全部职能域 write** | 制度维护 / 批量催缴 / 批量归档 / 批量导出 |

> **双域分离原则**：
> - 党建工作台聚焦「创新探索」——活动生命周期管理、专班运行、知识共享
> - 党务管理聚焦「合规运行」——发展党员、材料归档、纪律监督
> - **同一角色跨域时视图完全不同**。组织委员在党建台是专班统筹者，在党务管理是党员发展管理者。
>
> **默认信息面板原则**（2026-05-05 新增）：
> - 进入党建工作台或党务管理时，若未选定具体角色，展示**全量信息只读面板**
> - 目的：信息透明基线——"所有信息都能看到但不能编辑"
> - 选择具体角色后才激活对应的读写/管理面板

> **宣传专班备注**（2026-05-04）：宣传档案专班是专班制的**一种特例**。专班制本身是通用制度框架——组织委员作为唯一管理节点，任何委员均可提出专班需求（见 §0.3 六）。多维表格视图不应被理解为"宣传委员专属"，而应理解为"宣传委员的职能域展示形态"。

##### B. 活动类型维度 — 专用数据视图

> 基于用户重大理论创新：**活动管理四维度**（时长x品牌x发起方向x子类型）。子类型直接映射到 `ACTIVITY_CAT_COLOR` 的7色系。

| 活动子类型 | 颜色标识 | 推荐视图 | 核心字段 | 特殊交互 |
|-----------|---------|---------|---------|---------|
| **共建** (品红) | `#E11D48` | 表格视图 + 日历 | name, theme, date, type, leaders, filledBy | 跨支部协作标记 |
| **学习** (蓝) | `#3B82F6` | 表格视图 | name, theme, desc, date, type, link, filledBy | 学习材料附件 |
| **参访** (绿) | `#10B981` | 甘特视图 | name, theme, desc, date, leaders, leaderPhotos, filledBy | 参访照片墙 |
| **座谈** (橙) | `#F97316` | 表格视图 | name, theme, desc, date, type, leaders, filledBy | 发言记录子记录 |
| **会议** (紫) | `#8B5CF6` | 甘特视图 | name, theme, desc, date, type, filledBy | 会议纪要子记录 |
| **主题教育** (红) | `#DC2626` | 表格视图 | name, theme, desc, date, type, link, filledBy | 教育材料链接 |
| **长期** (青) | `#06B6D4` | 看板视图 | name, theme, desc, startDate, endDate, status | 阶段流转 |

##### C. 视图组件规范

| 维度 | 展示字段 | 交互方式 | 权限控制策略 |
|------|---------|---------|-------------|
| **多维表格（宣传委员）** | 12字段全显：☐ # 名称 主题 描述 🔗链接 📅时间 ✨类型 ✨领导 📷照片 填写人 + | 行点击展开子记录；行悬停显示编辑/删除；全选批量操作 | 宣传委员: write/delete; 其他角色: readonly |
| **日历（参与者）** | 月网格 + 彩色活动标签 + 图例 | 点击日期展开当日活动列表；图例筛选颜色 | 参与者: readonly; 管理者: 可创建 |
| **甘特图（组织者）** | 时间轴 + 任务条 + 里程碑 | 拖拽调整时间；缩放切换周/月 | 组织者: write; 其他: readonly |
| **看板（组织委员）** | 泳道列（待办->进行中->完成） | 卡片拖拽跨列 | 组织委员: write; 其他: readonly |
| **查询视图（纪检委员）** | 筛选表单 + 结果列表 | 多条件组合筛选；排序/导出 | 纪检委员: full access |

##### D. 子记录关联结构

```
ActivityRecord (主记录)
├── id, name, theme, desc, link, date, type
├── leaders[], leaderPhotos[]
├── filledBy, createdAt
└── subRecords[] (子记录数组)
    ├── { type: "attendance", items: [...] }    // 考勤子记录
    ├── { type: "materials", items: [...] }      // 材料子记录
    └── { type: "publicity", items: [...] }      // 宣传子记录
```

- **自增表格**：每种子记录类型支持动态添加行（"+ 添加考勤"/"+ 添加材料"/"+ 添加宣传"）
- **统一绑定**：所有子记录通过 `parentId` 绑定到主记录 `id`
- **权限继承**：子记录的操作权限继承自主记录的当前管理者角色

##### E. 决策树模型 — 活动四维度层级（2026-05-04 提炼精修）

```
                    ┌─────────────────────────┐
                    │    Q1: 组织场景（必选）    │
                    │  determines role + flow  │
                    └───────────┬─────────────┘
                ┌───────────────┴───────────────┐
       ┌────────┴────────┐            ┌─────────┴──────────┐
       │    主题党日       │            │     三会一课        │
       │  (非强制 · 学习型) │            │ (明确定义 · 制度型)  │
       │ 承办: 按需指定党小组│            │ 党小组会 / 支部大会  │
       └────────┬────────┘            │ 支委会 / 党课       │
                │                      └─────────┬──────────┘
                └──────────────┬────────────────┘
                               │
                    ┌──────────┴───────────┐
                    │ Q2: 活动形式（必选）    │
                    │ determines color+data │
                    └──────────┬───────────┘
          ┌────────┬────────┬──┴──┬────────┬────────┐
       学习活动   参访活动  座谈交流 共建活动  会议活动
       (蓝 #3B8) (绿 #10B) (橙 #F97) (品红)  (紫 #8B5)
       ┌材料链接  ┌照片墙   ┌发言记录 ┌跨支部  ┌纪要+考勤
       │          │        │        │        │
       │  ┌───────┴────────┴────────┴────────┘
       │  │  组织生活会 = 会议活动的一种特殊内容形态
       │  │  发展党员   = 跨多个会议/活动的流程
       │  │  党员培训   = 学习活动的子类
       │
       └── 所有这些内容形态均由 Q1+Q2 的组合唯一确定 ──┘

                    ┌──────────┴───────────┐
                    │   Q3: 时长（必选）     │
                    │   短期 / 长期          │
                    └──────────┬───────────┘
                    ┌──────────┴───────────┐
                    │ Q4: 发起方向（必选）   │
                    │ 自上而下 / 自下而上     │
                    │ -> 决定写入权限门禁     │
                    └──────────────────────┘
```

> **注**：
> - 品牌维度（brand/non-brand）为该树的叠加层，待双轨工作流稳定后再上线。
> - **赋权范围**：党支书具备最高赋权能力。组织委员可招募/赋权专班（含宣传专班等跨职能专班）。党小组组长可赋权活动组织者和深度参与者。宣传委员和纪检委员因职能性质不可转授权。
> - **「长期活动」已从 Q2 移除**：时长是独立维度(Q3)，不可在活动形式中二次选择。
> - **「组织生活会/发展党员/党员培训」已从 Q1 移除**：它们不是组织场景框架，而是 Q1+Q2 组合后衍生出的具体内容形态。
> - 表单视觉已按决策树三行分组：Q1 行 -> Q2-Q4 行 -> 选填行，每行有层级徽标 + ★必填标记。

---

#### §0.3 专班制（Task Force System）— 制度创新探索 [2026-05-04]

> **🔴 双域管理升级（2026-05-04）**：根据 §0.1 双域管理理论，**组织委员在党建工作域的最高优先级职能是专班管理**。
> 所有专班的创建、招募、运行、解散统一归口于组织委员。其他委员（宣传委员等）如需专班，须向组织委员提出需求，由组织委员在赋权面板中执行。
> 这意味着：宣传专班虽然服务于宣传委员，但其赋权操作、工作量跟踪、解散评议均通过组织委员完成。
> 下文中"由该条委员发起并管理"的旧表述需按此规则重新理解：**发起=提出需求，管理=组织委员执行**。

##### 一、问题起点

当前组织格局为**条块二元结构**：
- **条**（vertical）：纪检委员 / 组织委员 / 宣传委员 — 按职能分工
- **块**（horizontal）：第一党小组 / 第二党小组 / 第三党小组 — 按成员分布

专班的引入打破了这一格局：它既不完全是「条」（跨职能），也不完全是「块」（跨小组），而是一个**临时性、目标导向的横向切口**。

##### 二、核心定义

```
专班 = 赋权（Authorization） x 工作量考察（Workload Accounting）

赋权做了什么：
  将特定角色权限（如宣传委员的素材归档权）临时授予非委员同志
  -> 解决"宣传委员一个人忙不过来"的瓶颈

工作量考察做了什么：
  记录专班成员的参与时长和产出
  -> 解决"做了事没记录、年底评议没依据"的痛点

两者统一：
  赋权即记账。赋权动作本身触发工作量记录线程，
  专班解散时自动生成工作量汇总报告。
```

##### 三、专班类型矩阵

| 专班名称 | 挂靠角色 | 赋权内容 | 典型场景 | 存续周期 |
|---------|---------|---------|---------|---------|
| **宣传专班** | 宣传委员 | 素材归档 + 新闻稿起草 + 推送排版 | 大型活动/年度总结前期 | 按活动周期 |
| **SOP 维护专班** | 党支书 | SOP 文档编辑 + 流程测试 + bug 反馈 | 系统升级/制度修订期 | 按版本周期 |
| **活动筹备专班** | 组织委员/活动组织者 | 场地协调 + 物资采购 + 外部联络 | 参访/共建活动 | 按活动周期 |
| **档案整理专班** | 宣传委员 | 历史活动档案补录 + 照片标注 | 年底/评优季 | 2-4 周 |
| **技术保障专班** | 党支书 | 部署/域名/服务器 + 功能开发 | 系统新建/大版本 | 按项目周期 |

##### 四、与条块体制的衔接规则

```
                  ┌──────────────┐
                  │   党支部      │
                  └──────┬───────┘
          ┌──────────────┼──────────────┐
     ┌────┴────┐    ┌────┴────┐    ┌────┴────┐
     │宣传委员  │    │组织委员★ │    │纪检委员  │  <- 条（职能）
     │ 提出需求 │    │专班管理  │    │         │
     │    ↓     │    │  ↕      │    │         │
     │宣传专班<─┼────┤宣传专班  │    │ ✗无专班 │  <- 班（组织统筹）
     │(服务宣传)│    │活动筹备  │    │(内控隔离)│
     │          │    │SOP维护   │    │         │
     │          │    │档案整理  │    │         │
     └─────────┘    └────┬─────┘    └─────────┘
         │               │              │
         └───────────────┼──────────────┘
                  ┌──────┴───────┐
                  │  党小组 1~3   │  <- 块（分布）
                  └──────────────┘

规则：
  1. ★ 组织委员是唯一专班管理节点（党建工作域最高优先级职能）
  2. 宣传委员提出宣传专班需求 -> 组织委员审核后在赋权面板中执行招募
  3. 纪检委员的条因内控要求不生长专班（审查权力不可转授）
  4. 党支书的条可跨所有职能提出专班需求，仍由组织委员执行管理
  5. 专班成员来自任意党小组，不改变其原有党小组归属
  6. 同一同志可同时加入多个专班（工作量分别记账，考勤数据与纪检共享）
```

##### 五、生命周期模型

```
┌─ 发起 ────────────────────────────────────────────────────┐
│  由挂靠角色的委员向组织委员提出专班需求                     │
│  输入：专班名称 + 目标人数 + 存续周期 + 工作量考核标准       │
│  ★ 组织委员审核后在赋权面板中发起专班                        │
├─ 招募 ────────────────────────────────────────────────────┤
│  = 赋权：组织委员在赋权面板中勾选同志 -> 授予对应子角色权限   │
│  = 记账：赋权动作触发工作量记录线程（自动开始计时）          │
├─ 运行 ────────────────────────────────────────────────────┤
│  专班成员在 SOP 页面看到专属工作台（任务清单 + 材料入口）    │
│  成员完成提交 -> 记账线程记录产出类型和数量                   │
│  组织委员可随时查看实时工作量面板（考勤数据与纪检委员共享）  │
├─ 解散 ────────────────────────────────────────────────────┤
│  组织委员点击「解散专班」-> 自动生成工作量汇总报告            │
│  赋权回收 -> 成员权限恢复至原有水平                           │
│  工作量报告写入该同志的个人档案（年底评议引用）              │
└──────────────────────────────────────────────────────────┘
```

##### 六、工作量考察的数据模型（草案）

```
TaskForceRecord {
  id: string                    // tf-{timestamp}
  name: string                  // "宣传专班"
  sponsorRole: string           // "prop-commissioner"
  sponsorName: string           // "张三"
  activityId: string?           // 关联活动（可选）
  startDate: string
  endDate: string?
  status: 'recruiting' | 'active' | 'dissolved'
  members: [{
    memberName: string
    memberGroup: string         // "第一党小组"
    assignedRole: string        // "deep" / "organizer" / custom
    joinedAt: string
    contributions: [{
      date: string
      type: 'photo' | 'copy' | 'layout' | 'archive' | ...
      count: number
      hours: number
    }]
  }]
  reportUrl: string?            // 解散时生成的工作量报告链接
}
```

##### 七、网页端实现路线图

| 阶段 | 内容 | 依赖 | 状态 |
|------|------|------|------|
| α.1 | 宣传专班作为首发试点（当前已完成 recruit UI + 赋权白名单） | C2.D2 | ✅ 已完成 |
| α.2 | 宣传专班工作量面板（宣传委员视图中查看成员贡献统计） | α.1 | ⬜ 下一步 |
| β.1 | 通用专班创建/解散流程（任意委员发起） | α.2 | ⬜ 待启动 |
| β.2 | 工作量记录线程（赋权自动开始记账） | β.1 | ⬜ 待启动 |
| γ.1 | 多专班并行 + 成员跨班工作量聚合 | β.2 | ⬜ 待启动 |
| γ.2 | 年底工作量总报告自动生成 + 评议引用 | γ.1 | ⬜ 待启动 |

---

#### §0.4 SOP<->网页双向修改规则 [2026-05-04 确立]

##### 一、问题起点

我们把文本 SOP 做成网页后，用户在网页端试用的过程中会形成反馈（对视图、对工作流、对交互逻辑的改进意见）。这些反馈需要同时修改两处：
- **网页代码**（`index.html` + `src/*.js` + `src/styles.css`）—— 让功能落地
- **文本 SOP**（`content/SOP/*.md`）—— 让制度母本保持权威

但目前缺乏明确规则来约束这个双向修改的流程。

##### 二、核心规则

```
规则 0（最高优先级）：文本 SOP 是母本，网页是实施层。

规则 1（同步原则）：
  用户对网页/工作流的任何反馈，视为同时对 SOP 制度母本和网页代码
  的修改指令。两者必须同步修改，不得只改一处。

规则 2（方向指引）：
  ┌─ 用户反馈 ──> 先改 SOP 母本（content/SOP/），确定制度层面的变更 ──> 再改网页代码 ──> 验证
  └─ 理由：母本精确后代码有锚点可依；代码改了母本没改 = 母本"腐化"

规则 3（冲突裁决）：
  若网页实现与 SOP 母本描述不一致，以 SOP 母本为准。
  网页实现视为"过时实现"，需按母本更新。

规则 4（实操化审核）：
  每一次 SOP 文本修改都应审视：
  - 是否为实操中真实遇到的问题？（避免空谈）
  - 描述是否简洁可执行？（避免冗长）
  - 是否有对应的网页交互支撑？（避免脱节）
```

##### 三、文档追踪

| 涉及文件 | 修改时需同步检查 |
|---------|---------------|
| `content/SOP/*.md` | 制度母本 —— 先改 |
| `index.html` | UI 入口 —— 随后改 |
| `src/*.js` | 逻辑实现 —— 随后改 |
| `ROADMAP.md` | 记录变更摘要 —— 最后同步 |
| `.ctx/TIMESTAMPS.md` | 更新时间戳 —— 最后同步 |
| `.ctx/CONTEXT.md` | §8.3 记录会话变更 —— 最后同步 |

##### 四、SOP 实操化精简方向 [2026-05-04 诊断]

> 用户已明确指出当前 SOP 文本的三大问题：
> - **空谈多**：讨论了很多概念和框架，但不是实践管理中的实操
> - **实操少**：真正落在支委/组长层面可执行的操作指引不够
> - **实操部分过分冗长**：偶有实操内容却又写得太繁复，不够精炼
>
> **方向**：下一轮 SOP 文本修改应以"可执行、可核查、精炼"三原则为指导：
> - 可执行：每一条应该是"做什么"而非"讨论什么"
> - 可核查：每一条应能用网页交互验证是否完成
> - 精炼：能用半页说清楚的不写一页

---

### §A. 基建层（已完成，仅汇总）

> 以下所有事项已于 2026-05-03 ~ 2026-05-04 完成，此处仅做归档摘要。详细实现见对应源文件。

#### A1. CSS 基础设施与设计令牌

| 编号 | 事项 | 涉及文件 | 完成标志 |
|------|------|---------|---------|
| A1.1 | 四层色盘系统 `:root` CSS变量（Layer 0 Party -> 1 Primary -> 2 Accent -> 3 Neutral -> 4 Surface） | `src/styles.css` | ✅ |
| A1.2 | Tailwind colors 扩展对齐（party.DEFAULT/red/gold + primary.950） | `index.html` | ✅ |
| A1.3 | body/html 基础样式重写（font-size:16px基线 + text-size-adjust + optimizeLegibility） | `src/styles.css` | ✅ |
| A1.4 | 排版工具类 8 级（.text-h1~h3 / .text-body-lg~body / .text-caption+overline） | `src/styles.css` | ✅ |
| A1.5 | 字体设计令牌（--text-xs~3xl + --leading-* + --tracking-*）+ 衬线字体标题系统 | `src/styles.css` | ✅ |
| A1.6 | FOUT 消除机制（预加载 + 系统回退 + 超时兜底） | `index.html` | ✅ |
| A1.7 | 额外修复：8个缺失 CSS 类（.btn-secondary / .party-stat-* / .commissioner-*） | `src/styles.css` | ✅ |

#### A2. 组件库五态全覆盖

| 编号 | 事项 | 涉及文件 | 完成标志 |
|------|------|---------|---------|
| A2.1 | Header 导航栏（primary-900深红 · 党徽wrapper · scrolled阴影 · hamburger三态） | `index.html` + `src/styles.css` | ✅ |
| A2.2 | Sidebar 侧边栏（surface-sidebar · module-tab default/hover/active · divider+section-title） | `index.html` + `src/styles.css` | ✅ |
| A2.3 | 按钮系统 btn-primary + btn-secondary（default/hover/active/disabled 四态） | `src/styles.css` | ✅ |
| A2.4 | 卡片系统 card + card-flat（surface-card · neutral-200 border · hover->primary-300） | `src/styles.css` | ✅ |
| A2.5 | 输入框 input-flat + select（focus->primary-500 · 自绘SVG箭头 · placeholder neutral-400） | `src/styles.css` | ✅ |
| A2.6 | module-tab + commissioner-tab（active->surface-active bg + primary-700 text · focus-visible） | `index.html` + `src/styles.css` | ✅ |

#### A3. 工作流引擎核心

| 编号 | 事项 | 涉及文件 | 完成标志 |
|------|------|---------|---------|
| A3.1 | 工作流引擎核心设计（状态机+流转规则+事件触发） — `WorkflowEngine` 类 + `createEngine` 工厂 + `canTransition`/`calcTimeoutStatus` 纯函数 | `src/workflow/engine.js` | ✅ |
| A3.2 | 活动类型流程定义 v1.0（长期/短期/品牌/非品牌 5套定义） | `src/workflow/definitions.js` | ✅ |
| A3.3 | 工作流可视化界面（SVG节点图 + 进度条 + 状态标签 + 流转历史 + 阻塞横幅） | `index.html` + `src/styles.css` + `src/workflow/renderer.js` | ✅ |
| A3.4 | definitions.js 重写 v2.0（短期7状态 / 长期9状态含 PREPARING/GROUP_FORMING/SYNCING） | `src/workflow/definitions.js` | ✅ |
| A3.5 | 短期活动引擎 v2.0（PREPARING阶段纪检/宣传对接节点校验 + 考勤子状态 + 宣传统筹子状态 + 角色流转守卫 + ROLE_PERMISSIONS矩阵） | `src/workflow/engine.js` + `definitions.js` | ✅ |
| A3.6 | 长期活动引擎（SYNCING子状态：内容同步+考勤考察+协调层运作 各3步骤） | `src/workflow/definitions.js` | ✅ |

#### A4. 已实现但未在旧 C2 中编号的功能

| 功能 | 涉及文件 | 说明 |
|------|---------|------|
| 扁平化卡片系统 | `styles.css` -> `.card-flat` | .glass -> .card-flat 迁移已完成 |
| 扁平化输入框 | `styles.css` -> `.input-flat` | .glass-input -> .input-flat 迁移已完成 |
| Select 组件统一样式 | `styles.css` select.input-flat | 移除原生箭头，自定义 SVG 箭头，聚焦变色 |
| 日历图例系统 | `index.html` #calendar-legend + calendar.js _renderLegend() | 按实际颜色动态渲染，可收起/展开 |
| 条条支委统一入口+弹窗选择 | `index.html` + `styles.css` + `events.js` | 1 按钮 -> 弹窗选择 3 角色，toggle 收回 |

---

### §B. 数据层

> 数据模型定义、持久化方案与状态管理。

---

#### B1. 数据模型定义

##### B1.1 ActivityRecord — 活动主记录（已完成）

```
ActivityRecord {
  id, name, theme, desc, link, date, startDate?, endDate?
  type: string                  // 子类型（7色系之一）
  duration: 'short' | 'long'   // 时长维度
  direction: 'top-down' | 'bottom-up'  // 发起方向
  leaders[], leaderPhotos[]
  filledBy, createdAt
  subRecords[]                  // 考勤/材料/宣传子记录
}
```

- **状态**：模型已在 `definitions.js` 和 `events.js` 中完整落地，四维度（时长x品牌x发起方向x子类型）字段均已写入。
- **涉及文件**：`src/workflow/definitions.js`、`src/events.js`、`src/constants.js`

##### B1.2 TaskForceRecord — 专班记录（草案已有，代码待建）

```
TaskForceRecord {
  id, name, sponsorRole, sponsorName
  activityId?, startDate, endDate?
  status: 'recruiting' | 'active' | 'dissolved'
  members: [{ memberName, memberGroup, assignedRole, joinedAt, contributions[] }]
  reportUrl?
}
```

- **状态**：✅ 已完成。`src/service.taskforce.js` 中建立了完整 TaskForceRecordStore（CRUD + localStorage + 成员贡献追踪 + ActivityRecordStore 关联）。包含 MOCK_TASKFORCES 预置数据（2 条）。
- **下一步**：B1.2.2 宣传专班白名单实际数据迁移入结构化模型 — ✅ 已完成（2026-05-04）。`migrateFromLegacy()` 函数实现全 localStorage 模式匹配扫描 + 自动迁移 + 旧键清理 + 迁移报告。

##### B1.3 Notice — 通知数据模型（待新建）

```
Notice {
  id: string                    // notice-{timestamp}
  title: string
  content: string
  priority: 'urgent' | 'normal' | 'low'
  publisher: string             // 发布者角色
  publishDate: string
  expireDate: string?
  targetModule: string?         // 点击后跳转的目标模块
}
```

- **状态**：✅ 已完成。`src/service.notice.js` 中建立了 NoticeStore（CRUD + localStorage + Mock 数据 3 条），renderNoticeList 函数实现排序/过滤/渲染。
- **涉及文件**：`src/service.notice.js`（新建）
- **下一步**：B1.3.2 Notice 写入权限对接 §D4 — ✅ 已完成（2026-05-04）。新增 `NoticePermission` 模块（canPublish/canManage/check），add/update/remove 三重 role 校验。

##### B1.4 SeriesRecord — 系列活动/长期活动（2026-05-06 新增设计）

> **背景**：用户提问"如果是长期活动，应该怎么写入？是算作专班还是别的？"
> 答案：**长期活动 ≠ 专班**。三者模型独立但关联。

```
SeriesRecord {
  id: string                    // series-{timestamp}
  title: string                 // 系列活动名称，如"2026春季主题党日系列"
  scenario: string              // 组织场景（theme-party / branch-meeting / ...）
  activityType: string          // 活动形式（learning+visit / meeting / ...）
  direction: 'top-down' | 'bottom-up'
  hostGroup?: string            // 承办党小组
  recurrenceRule: {
    frequency: 'weekly' | 'monthly' | 'quarterly'
    interval: number            // 间隔（如每 1 月、每 2 周）
    startDate: string
    endDate?: string            // 不填 = 无截止
  }
  subActivityIds: string[]      // 展开后的子活动 ID 列表
  organizerName?: string
  deepParticipantName?: string
  archived: boolean             // 全部子活动完成后归档
  createdAt: string
}
```

**三者关系（独立但关联）**：

| 概念 | 维度 | 举例 | 管理角色 |
|------|------|------|---------|
| **短期活动** | 事件（一次性） | "4月15日学习二十大精神主题参访" | 党小组组长写入 |
| **系列活动** (= 长期活动) | 时间模式（重复性） | "2026春季每月主题党日" — 展开为 3 个子活动 | 党小组组长创建、书记审核 |
| **专班** | 人员组织（功能性） | "宣传联络专班" — 为系列活动中的各子活动提供宣传支持 | 组织委员招募统筹 |

**数据流**：
```
系列活动 (SeriesRecord)
  │ 创建时选择 recurrenceRule
  │ 系统自动展开为多个子 ActivityRecord
  ▼
├── 子活动 1 (2026-03-15) ← 宣传专班服务于此
├── 子活动 2 (2026-04-15) ← 宣传专班服务于此
├── 子活动 3 (2026-05-15) ← 宣传专班服务于此
  │
  │ 全部子活动完成 → series.archived = true, 不留在看板中
  ▼
归档库
```

**当前实现状态**：`duration: 'short' | 'long'` 字段存在于 ActivityRecord 中。选择 'long' 时应弹出 recurrenceRule 配置面板（待 Q3 实现）。当前 Mock 状态下选择 'long' 仅标记字段，不展开子活动。

> **设计精神**：系列活动是"时间的重复"（同一个模式反复发生），专班是"人的组织"（同一群人围绕目标协作）。两者不可混淆——系列活动由党小组组长/书记操作，专班由组织委员统筹。宣传/纪检委员的"活动与专班视图"同时展示关系和活动，因为它们的工作同时服务于两者。

---

#### B2. localStorage 持久化

| 编号 | 事项 | 状态 | 涉及文件 |
|------|------|------|---------|
| B2.1 | ActivityRecord 持久化（活动写入 -> localStorage 存储 -> 刷新不丢失） | ✅ 已完成 | `src/events.js` |
| B2.2 | 赋权面板数据持久化（localStorage 写入赋权白名单，刷新后不丢失） | ✅ 已完成 | `src/events.js`（2026-05-03 三步走第一步 G1.3） |
| B2.3 | 自下而上活动强制"先赋权再写入"校验链路 | ✅ 已完成 | `src/events.js`（2026-05-03 三步走第一步 G1.4） |
| B2.4 | TaskForceRecord 持久化（专班创建/解散/成员变动写入 localStorage） | ⬜ 待启动 | 依赖 B1.2 |
| B2.5 | Notice 持久化（通知发布/过期/删除写入 localStorage） | ⬜ 待启动 | 依赖 B1.3 |

---

#### B3. 状态管理完善

| 编号 | 事项 | 状态 | 说明 |
|------|------|------|------|
| B3.1 | `activeRole` 状态管理（角色切换 -> 视图刷新 -> 权限门禁） | ✅ 已完成 | `src/events.js` + `src/state.js` |
| B3.2 | 书记角色识别 — `activeRole='secretary'` 时触发专属面板 | ✅ 已完成 | `src/events.js` `_updateSecretaryDashboard()` |
| B3.3 | 全局视图切换状态 — 书记代理任一子角色视角 | ✅ 已完成 | `index.html` + `src/events.js`（8角色代理 + renderCalendar 包装过滤） |
| B3.4 | 赋权统计状态实时刷新 — 4张统计卡片 `_refreshSecretaryStats` | ✅ 已完成 | `index.html` + `src/events.js` |
| B3.5 | 非委员角色状态联动（党小组组长/组织者/深度参与者/默认参与者面板激活） | 🟡 结构已就绪，JS 数据流待填充 | `index.html` HTML 面板已建好，角色激活联动已通 |

> **下一步 B3.5.1**：补全非委员角色的 JS 数据绑定，使 HTML 面板从骨架变为功能面板。

---

### §C. 视图层

> 各角色差异化视图的实现状态与待办。

---

#### C1. 主页（Dashboard）[2026-05-04 提出]

> 默认打开网页时应展示**主页**，而非直接进入某个管理视图。

##### 构成（三要素）

```
┌─────────────────────────────────────────────────────┐
│                      主页                            │
├─────────────────────┬───────────────────────────────┤
│                     │                               │
│   (1) 活动日历        │   (2) 人员招募信息              │
│   综合展示所有已写入 │   基于特定专班或特定活动       │
│   活动的日期        │   进行招募的通知               │
│   （后台数据集驱动） │   （赋权面板驱动）             │
│                     │                               │
├─────────────────────┴───────────────────────────────┤
│                                                     │
│   (3) 重要通知模块                                    │
│   支委/支书发布的全员通知 + 材料截止提醒              │
│   + 活动倒计时                                       │
│                                                     │
└─────────────────────────────────────────────────────┘
```

##### 数据来源

| 组件 | 数据源 | 说明 |
|------|--------|------|
| 活动日历 | `ActivityRecord[]`（后台数据集） | 所有已写入的活动按日期排列，点击跳转至该活动详情 |
| 人员招募 | `TaskForceRecord[]`（专班数据）+ `ActivityRecord[]`（活动角色缺口） | 展示待招募的专班/活动，含所需人数和技能要求 |
| 重要通知 | 新数据模型 `Notice[]` | 支委/支书发布的通知，支持优先级和过期时间 |

##### 导航逻辑

```
用户打开网页
    │
    ▼
主页（默认首页）
    │
    ├─ 点击活动日历中的某个活动 -> 跳转党建工作台 -> 该活动详情视图
    ├─ 点击招募信息中的某个条目 -> 跳转党务管理 -> 赋权面板
    ├─ 点击重要通知中的某个条目 -> 跳转对应模块
    └─ 点击侧边栏 -> 直接进入党建工作台 / 党务管理 / 资料查询
```

##### 实施路线

| 阶段 | 编号 | 内容 | 依赖 | 状态 |
|------|------|------|------|------|
| α | C1.1 | HTML 结构：新增 `view-section#view-dashboard` + 三组件骨架 | 无 | ✅ 已完成（2026-05-04） |
| α | C1.2 | 活动日历组件：读取 ActivityRecord 渲染日历摘要 | ActivityRecord 数据集 | ✅ 已完成（2026-05-04） |
| α | C1.3 | 人员招募组件：读取 TaskForceRecord + ActivityRecord 渲染招募卡片 | 专班数据模型 | ✅ 已完成（2026-05-04） |
| β | C1.4 | 重要通知：Notice 数据模型 + 发布/展示功能 | Notice CRUD（§D4） | ✅ 已完成（2026-05-04） |
| γ | C1.5 | 导航联动：日历->活动详情、招募->赋权面板、通知->对应模块 | C1.1~C1.4 | ✅ 已完成（2026-05-04） |

> **C1.5 实施记录**（2026-05-04）：在 `src/main.js` renderDashboard 中增加了委托事件处理——通知点击→targetModule / 招募点击→party / 日历点击→calendar+selectedActivityId+viewMode='detail'

---

#### C2. 三委员视图完善

| 编号 | 事项 | 状态 | 涉及文件 |
|------|------|------|---------|
| C2.1 | 组织委员视图 — 发展党员追踪看板（四阶段概览：申请人->积极分子->发展对象->预备党员 + 泳道追踪 + 材料催缴 + 思想汇报 + 合规文件只读） | ✅ 已完成 | `index.html` + `src/styles.css` |
| C2.2 | 纪检委员视图 — 补课跟踪·复盘督办（内控隔离卡片 + 补课/未读邮件/出勤率三指标 + 复盘督办待办） | ✅ 已完成 | `index.html` + `src/styles.css` |
| C2.3 | 宣传委员视图 — 宣传专班·档案归档（专班招募卡片 + 档案/材料/模板/周报四Tab + 格式规范内联） | ✅ 已完成 | `index.html` + `src/styles.css` |
| C2.4 | 宣传委员多维表格 12 字段实现 + 行展开/悬停/批量操作交互 | 🟡 视图骨架完成，交互 JS 待补全 | `index.html` + `src/events.js` |
| C2.5 | 纪检委员查询视图（多条件组合筛选 + 排序/导出） | ⬜ 待启动 | `index.html` + `src/events.js` |

> **下一步 C2.4.1**：补全宣传委员多维表格的行编辑、行删除、批量操作 JS 绑定。
> **下一步 C2.5.1**：实现纪检委员查询视图的筛选表单 + 结果列表。

---

#### C3. 其他管理者视图

| 编号 | 事项 | 状态 | 说明 |
|------|------|------|------|
| C3.1 | 党小组组长视图（表格视图任务总览 + 日历 + 组员出勤确认） | 🟡 面板骨架已就绪 | HTML 面板已建，数据绑定待补全 |
| C3.2 | 活动组织者视图（甘特视图任务时间线 + 表格 + 任务分配/进度追踪） | 🟡 面板骨架已就绪 | 同上 |
| C3.3 | 深度参与者视图（表格视图我的任务 + 日历 + 任务状态更新） | 🟡 面板骨架已就绪 | 同上 |
| C3.4 | 默认参与者视图（日历视图彩色排布 + 图例只读） | 🟡 面板骨架已就绪 | 同上 |

> **下一步**：配合 §B3.5.1（状态管理完善），逐角色补全 JS 数据绑定。

---

#### C4. 党支书全视图

| 编号 | 事项 | 状态 | 涉及文件 |
|------|------|------|---------|
| C4.1 | 书记角色识别 — `activeRole='secretary'` 触发专属面板 | ✅ 已完成 | `src/events.js` `_updateSecretaryDashboard()` |
| C4.2 | 独占操作入口 —「布置党课」「主持大会」按钮仅书记可见并预填表单 | ✅ 已完成 | `index.html` + `src/events.js` |
| C4.3 | 全局视图切换 — 书记可切换至任意子角色视角查看日历（8角色代理 + renderCalendar 包装过滤） | ✅ 已完成 | `index.html` + `src/events.js` |
| C4.4 | 赋权增强 — 统计卡片实时刷新 + 撤销赋权即时更新（4张统计卡片 `_refreshSecretaryStats`） | ✅ 已完成 | `index.html` + `src/events.js` |
| C4.5 | 批量操作入口（批量催缴/批量归档/批量导出） | ✅ 已完成（2026-05-04） | `panel-secretary` HTML + `party.js` `_initSecretaryBatchOps()` — 催缴/归档/CSV导出 |
| C4.6 | **BUG 修复**：`getActiveCommissioner()` — 原函数只识别 `secretary`/`commissioner`，`org/prop/disc-commissioner` 全部坠入 `default`，导致三委员面板从未渲染 | ✅ 已完成 | `src/party.js` L92 — 修正路由：`disc→inspector/org→organizer/prop→publicity` |
| C4.7 | 党小组组长 `data-modules` 修正：`"calendar,party"` → `"calendar"` — 党务管理不再显示党小组组长 | ✅ 已完成 | `index.html` L203 |
| C4.8 | `_updateManagerVisibility()` 权限修正：活动写入仅对组长/组织者/党支书可见；条条支委只显示职能域面板 | ✅ 已完成 | `src/events.js` L466 |
| C4.9 | **C5 党支书全局聚合视图**：新增 `panel-secretary` + 全量统计卡片 + 批量操作 + 八角色视图代理切换 + 快捷入口 | ✅ 已完成（2026-05-04） | `index.html`(+57行) + `party.js`(+145行) — `refreshSecretaryAggregateView` |
| C4.10 | `_handleViewProxyChange` 暴露到 `window` 供跨模块调用 | ✅ 已完成（2026-05-04） | `src/events.js` `_initSecretaryFeatures()` |

> **下一步 C4.5.1**：为书记面板增加批量操作按钮和确认对话框。

---

---

### §D. 交互层

> 模块间跳转、数据写入工作流、赋权操作、通知系统。

---

#### D1. 模块切换与导航联动

| 编号 | 事项 | 状态 | 说明 |
|------|------|------|------|
| D1.1 | 侧边栏模块切换（党建工作台 / 党务管理 / 资料查询 三模块） | ✅ 已完成 | `index.html` module-tab 体系 |
| D1.2 | 条条支委统一入口 + 弹窗选择（1 按钮 -> 弹窗选择 3 角色，toggle 收回） | ✅ 已完成 | `index.html` + `styles.css` + `events.js` |
| D1.3 | 模块切换时视图自动刷新 | ✅ 已完成 | `src/events.js` |
| D1.4 | 主页 ↔ 子模块导航联动（日历->活动详情、招募->赋权面板、通知->对应模块，返回主页） | ✅ 已完成（2026-05-04） | C1.5 导航联动 + 三子模块「返回主页」链接 + 全局 data-nav |
| D1.5 | 默认首页设置（当前打开网页直接进入管理视图，需改为先展示主页） | ✅ 已完成（2026-05-04） | `src/state.js` activeModule = 'dashboard'，`src/main.js` initApp 默认渲染 #view-dashboard |

> **D1.5 实施记录**（2026-05-04）：`state.js:135` `activeModule:'dashboard'`，`main.js:52` views 首位 dashboard，`main.js:152` initApp setState 默认 dashboard。已验证：网页启动即显示主页。

---

#### D2. 数据写入工作流

| 编号 | 事项 | 状态 | 涉及文件 |
|------|------|------|---------|
| D2.1 | 活动写入时读取 `#activity-type-input` 选择器的值 | ✅ 已完成 | `src/events.js` |
| D2.2 | 写入流程附加 duration(短期/长期) + direction(自上而下/自下而上) 维度 | ✅ 已完成 | `src/events.js` + `index.html` |
| D2.3 | 四维度完整写入活动数据（时长x品牌x发起方向x子类型） | ✅ 已完成 | `src/events.js` + `src/constants.js` |
| D2.4 | 自下而上活动：强制"先赋权再写入"校验链路 | ✅ 已完成 | `src/events.js` |
| D2.5 | 写入后自动刷新主页活动日历 | ⬜ 待启动 | 依赖 §C1.2 |

---

#### D3. 赋权面板（宣传专班招募）

| 编号 | 事项 | 状态 | 涉及文件 |
|------|------|------|---------|
| D3.1 | 宣传委员赋权白名单（HTML recruit UI + 勾选同志 -> 授予子角色权限） | ✅ 已完成 | `index.html` + `src/events.js` |
| D3.2 | 赋权面板数据持久化（localStorage 写入，刷新后不丢失） | ✅ 已完成 | `src/events.js` |
| D3.3 | 赋权撤销即时更新（撤销 -> 回收权限 -> 更新 localStorage） | ✅ 已完成 | `src/events.js` |
| D3.4 | 宣传专班工作量面板（宣传委员视图中查看成员贡献统计） | ⬜ 待启动 | 对应 §0.3 七 α.2 |
| D3.5 | 通用专班创建/解散流程（组织委员在赋权面板统一管理所有专班） | ⬜ 待启动 | 对应 §0.3 七 β.1 |

> **当前瓶颈**：赋权操作仍为宣传委员直连模式，尚未按 §0.1 四 信息流归口到组织委员。
> **下一步 D3.5.1**：重构赋权面板，将"发起专班"权限从宣传委员迁移至组织委员，宣传委员仅保留"提出需求"入口。

---

#### D4. 通知系统（Notice CRUD）

| 编号 | 事项 | 状态 | 说明 |
|------|------|------|------|
| D4.1 | Notice 数据模型建立（id / title / content / priority / publisher / dates / targetModule） | ✅ 已完成（2026-05-04） | `src/service.notice.js` |
| D4.2 | Notice 发布界面（支委/支书专属发布表单，含优先级和过期时间设置） | ✅ 已完成（2026-05-04） | D4.1 → `NoticePermission` + party.js `refreshNoticeSection/publishNotice` |
| D4.3 | Notice 列表展示（主页"重要通知"组件按优先级排序 + 过期自动隐藏） | ✅ 已完成（2026-05-04） | `renderNoticeList` in `service.notice.js` |
| D4.4 | Notice 点击跳转（点击通知 -> 跳转 targetModule） | ✅ 已完成（2026-05-04） | C1.5 导航联动 |

---

#### D5. SOP 反馈卡系统（搁置中，暂不激活）

> 原 C1.3 五项子任务（反馈卡下载/提交/四类别输入/整合迭代/架构问题升级）在当前优先级中排后。
> 待主页（§C1）与通知系统（§D4）上线后再激活此模块。

| 编号 | 原编号 | 内容 | 优先级 |
|------|--------|------|--------|
| D5.1 | C1.3.1 | 反馈卡下载功能（一键获取反馈卡模板） | P1 |
| D5.2 | C1.3.2 | 反馈卡提交入口 + 结构化输入系统 | P1 |
| D5.3 | C1.3.3 | 四类别内容输入：Permanent/Global/Role/Scenario-Specific | P1 |
| D5.4 | C1.3.4 | 反馈内容整合与长期迭代机制 | P2 |
| D5.5 | C1.3.5 | 架构问题自动识别 -> ROADMAP 反馈通道 | P2 |

---

### §E. 制度层

> 制度设计 -> 工程落地的映射与实施状态。

---

#### E1. 专班制实施（α -> γ）

> 对应 §0.3 七 的完整路线图。当前进度：

| 阶段 | 内容 | 状态 | 工程映射 |
|------|------|------|---------|
| α.1 | 宣传专班首发试点（recruit UI + 赋权白名单） | ✅ 已完成 | §D3.1~D3.3 |
| α.2 | 宣传专班工作量面板 | ⬜ 下一步 | §D3.4 |
| β.1 | 通用专班创建/解散流程 | ⬜ 待启动 | §D3.5 |
| β.2 | 工作量记录线程（赋权自动开始记账） | ⬜ 待启动 | 需新建 `src/workforce-tracker.js` |
| γ.1 | 多专班并行 + 成员跨班工作量聚合 | ⬜ 待启动 | — |
| γ.2 | 年底工作量总报告自动生成 + 评议引用 | ⬜ 待启动 | — |

> **当前阻塞**：β.1 需要先完成赋权面板重构（§D3.5.1），将专班管理权归口组织委员。

---

#### E2. SOP 实操化精简

> 对应 §0.4 四 的诊断与方向。

| 编号 | 事项 | 状态 | 说明 |
|------|------|------|------|
| E2.1 | 按"可执行、可核查、精炼"三原则逐篇审查 `content/SOP/*.md` | ⬜ 待启动 | 需要人工逐篇审读 |
| E2.2 | 消除空谈段落：将讨论性内容迁移至 `content/guides/` | ⬜ 待启动 | — |
| E2.3 | 精炼实操段落：每项操作指引控制在半页以内 | ⬜ 待启动 | — |
| E2.4 | 每次 SOP 修改后同步更新网页代码（按 §0.4 规则 1） | ⬜ 待启动 | — |

---

#### E3. 双域管理落实

> 对应 §0.1 的理论 -> 全量代码替换。

| 编号 | 事项 | 状态 | 说明 |
|------|------|------|------|
| E3.1 | 党建工作台模块命名（原"推演工作台" -> "党建工作台"） | ✅ 完成 | 文件重命名 calendar→workspace，全量替换 |
| E3.2 | 党务管理模块命名（原"组织建设模块" -> "党务管理"） | ✅ 完成 | 全量替换，仅 ROADMAP+TERMINOLOGY 保留迁移对照 |
| E3.3 | 三委员双域职责在网页端可视化呈现（§0.1 三 的表格 -> 网页角色卡片） | ⬜ 待启动 | 可在 C2 三委员视图中增加职责说明卡片 |
| E3.4 | 信息流"宣传委员 -> 组织委员 -> 宣传专班"在赋权面板中落实 | ⬜ 待启动 | 同 §D3.5.1 |

---

### §F. 维护层

> 文档同步、经验沉淀、测试验证。

---

#### F1. 文档持续同步

> 对应 §0.4 规则 0~4 的执行保障。

| 编号 | 事项 | 状态 | 说明 |
|------|------|------|------|
| F1.1 | 每次工程变更后检查 SOP 母本是否需要同步更新 | 🔴 缺乏自动化 | 当前依赖人工记忆 |
| F1.2 | ROADMAP 变更摘要记录（每次修改后同步更新本文件） | 🟡 部分执行 | — |
| F1.3 | `.ctx/TIMESTAMPS.md` 更新时间戳 | 🟡 部分执行 | — |
| F1.4 | `.ctx/CONTEXT.md` §8.3 记录会话变更 | 🟡 部分执行 | — |

---

#### F2. 经验蒸馏

| 编号 | 事项 | 状态 | 说明 |
|------|------|------|------|
| F2.1 | 从 EXECUTION_LOG 和 DECISION_LOG 中提炼可复用模式 | ⬜ 待启动 | 可由社科院 Agent 执行 |
| F2.2 | 将 SOP 实操化精简中发现的通用模式沉淀为模板 | ⬜ 待启动 | 依赖于 §E2 |
| F2.3 | 架构决策追溯链维护（每次重大决策 -> DECISION_LOG -> 本文件摘要） | 🟡 进行中 | — |

---

#### F3. 测试与验证

##### F3.1 已完成验证

| 测试项 | 方法 | 结果 |
|--------|------|------|
| CSS变量一致性 | `GetDiagnostics` all files | ✅ 零诊断错误 |
| 字体加载无FOUT | 预加载+系统回退+超时兜底 | ✅ 三保险 |
| 模块视图切换 | 5个 module-tab 点击测试 | ✅ 无异常 |
| 日历着色准确性 | ACTIVITY_CAT_COLOR 7色系 | ✅ 颜色正确 |
| 图例动态渲染 | _renderLegend() 按实际颜色 | ✅ 按需显示 |

##### F3.2 待实施

| 编号 | 事项 | 状态 | 对应旧编号 |
|------|------|------|-----------|
| F3.3 | Phase D4 动效/emoji/打印样式 | 🔴 未启动 | C2.D4.1~3 |
| F3.4 | Phase D5 审查/文档/记账 | 🔴 未启动 | C2.D5.1~3 |
| F3.5 | 赋权持久化回归测试（刷新后白名单不丢失 + 撤销即时生效） | ⬜ 待补 | — |
| F3.6 | 书记全局视图切换回归测试（8角色代理全部通路） | ⬜ 待补 | — |
| F3.7 | 自下而上活动写入门禁测试（未赋权拒绝写入 + 已赋权放行） | ⬜ 待补 | — |

---

### §G. 品牌活动网页架构设计（全量搁置）

> **搁置原因**：当前优先主页（§C1）+ 通知系统（§D4）+ 专班制（§E1）。品牌维度为叠加层，待双轨工作流稳定后再上。
> 原5个子任务（C1.2.1~C1.2.5）详见 `content/guides/BRAND_ACTIVITY.md`。

---

### §H. 旧版 C1.4 E1.3-E1.8 子功能（搁置，待重评估）

> 原 C1.4（补课跟踪/发展党员/意见反馈/公邮提醒/合规引用/场景域）的大部分功能已被 C2 三委员视图覆盖。
> 剩余未覆盖项需在下一轮规划中重新评估，暂不独立追踪。

---

## 四、待讨论议题

| # | 作用域 | 问题 | 涉及文件 | 备注 |
|---|--------|------|---------|------|
| D.1 | Global | `content/SOP/Org_OS_极客操作手册.md` 命名是否合理？ | - | - |
| D.2 | Global | `content/references/` 作为只读引用是否需要更严格的目录保护？ | - | - |
| D.3 | Global | SNAPSHOT 归档策略（已纳入 C1.6） | - | - |

---

## 五、待办修改项

| ID | 标题 | 类型 | 优先级 | 状态 | 负责人 | 备注 |
|----|------|------|--------|------|--------|------|
| E.1 | 品牌活动与非品牌活动发起方式 — 主题党日活动场景增加两种路径 | process_change | 低 | Pending | 书记/组长 | ⏸️ 悬置：需书记明确解锁 |

---

## 六、暂缓议题

> **STRICT CONSTRAINT: The Agent MUST NOT modify or attempt to resolve these issues until explicitly unlocked by the 书记.**

| # | 作用域 | 问题 | 涉及文件 | 悬置原因 |
|---|--------|------|---------|---------|
| F.1 | `[Global]` | 党小组工作手册（块块组长专用）尚未建设 | 待新建文件 | 本轮先通过SOP规定块块职责 |

---

## 七、已完成三步走执行计划（2026-05-03 制定，2026-05-04 全部收官）

> 以下三步已于 2026-05-03 ~ 2026-05-04 全部完成，此处仅做归档。

### 第一步：C1.1.7 补全 — 活动管理维度完整落地 ✅

| 子任务 | 内容 | 目标文件 |
|--------|------|---------|
| ✅ G1.1 | events.js 在写入活动时读取 `#activity-type-input` 选择器值 | `src/events.js` |
| ✅ G1.2 | 写入流程附加 duration + direction 维度 | `src/events.js` + `index.html` |
| ✅ G1.3 | 赋权面板数据持久化（localStorage 写入） | `src/events.js` |
| ✅ G1.4 | 自下而上活动强制"先赋权再写入"校验链路 | `src/events.js` |

### 第二步：C1.1.8 党支书全视图增强 ✅

| 子任务 | 内容 | 目标文件 |
|--------|------|---------|
| ✅ G2.1 | 书记角色识别 `_updateSecretaryDashboard()` 显隐切换 | `src/events.js` |
| ✅ G2.2 | 独占操作入口「布置党课」「主持大会」预填表单 | `index.html` + `src/events.js` |
| ✅ G2.3 | 全局视图切换 — 8角色代理 + renderCalendar 包装过滤 | `index.html` + `src/events.js` |
| ✅ G2.4 | 赋权增强 — 4张统计卡片 `_refreshSecretaryStats` | `index.html` + `src/events.js` |

### 第三步：C1.5 DESIGN-OPT Phase D1 — CSS 基础设施重写 ✅

| 子任务 | 内容 | 目标文件 |
|--------|------|---------|
| ✅ G3.1 | 重写 `:root` CSS变量，四层色盘系统 | `src/styles.css` |
| ✅ G3.2 | 更新 Tailwind colors 扩展与 `:root` 对齐 | `index.html` |
| ✅ G3.3 | 重写 body/html 基础样式 | `src/styles.css` |
| ✅ G3.4 | 建立排版工具类 8 级 | `src/styles.css` |

### 时间线与产出（已全部完成）

| 步骤 | 工作项 | 状态 |
|------|--------|------|
| 第一步 | 4 子任务 | ✅ |
| 第二步 | 4 子任务 | ✅ |
| 第三步 | 4 子任务 + 8类修复 | ✅ |

---

## 八、SNAPSHOT 管理机制

> **已确立**：2026-05-03 实施按需生成机制。Snapshot 不再每次执行更新，仅在里程碑/用户触发/阶段收官时生成。

| 触发条件 | 示例 | 归档位置 |
|---------|------|---------|
| 大版本升级 | v3->v4（架构重大变更） | `.ctx/snapshots/SNAPSHOT_v{N}_{date}.md` |
| 用户显式指令 | "/snapshot" 或 "生成快照" | `.ctx/snapshots/` |
| 阶段性收官 | Phase 1/2/3 完成 | `.ctx/snapshots/` |

| 要素 | 说明 |
|------|------|
| 归档路径 | `.ctx/snapshots/` — 历史版本只读存放 |
| 活跃文件 | `.ctx/SNAPSHOT.md` — 始终为当前最新基线 |
| 版本命名 | `SNAPSHOT_v{N}_{YYYYMMDD}.md` |
| 索引清单 | `.ctx/snapshots/INDEX.md` — 全量版本目录+生成原因+日期 |
| 更新日志 | 每次 Snapshot 生成需同步记录至 `DECISION_LOG.md` |
| 可追溯性 | 所有 Snapshot 版本通过 git 历史 + DECISION_LOG 双重可追溯 |

---

## 九、快速导航

| 我需要... | 去哪里 |
|----------|--------|
| 了解项目全貌 | [ARCHITECTURE.md](ARCHITECTURE.md) |
| 看待办任务 | 本文件 §三（工程路线图 §B~§F） |
| 看理论基石 | 本文件 §三 §0.1~§0.4 |
| 看待办修改项 | 本文件 §五 |
| 看暂缓议题 | 本文件 §六 |
| 查决策历史 | `.ctx/logs/DECISION_LOG.md` |
| 查全局规则 | [.github/copilot-instructions.md](.github/copilot-instructions.md) |
| 查母本链路 | [.github/SSOT_INDEX.md](.github/SSOT_INDEX.md) |
| 查代码架构 | [ARCHITECTURE.md](ARCHITECTURE.md) |
| 查 SOP 流程 | [content/SOP/INDEX.md](content/SOP/INDEX.md) |
| 取用模板 | `content/references/模板库/` |
| 提改进反馈 | [content/guides/SOP_WEB_GUIDE.md §E](content/guides/SOP_WEB_GUIDE.md) |
| 查执行日志 | `.ctx/logs/YYYY-MM-EXECUTION_LOG.md` |
| 查文件时间戳 | `.ctx/TIMESTAMPS.md` |
| 查看品牌活动方案 | [content/guides/BRAND_ACTIVITY.md](content/guides/BRAND_ACTIVITY.md) |

---

## 十、下一轮建议启动清单

> 按依赖关系排序，无阻塞项可并行启动。T24~T25 轮完成四大修复+意见反馈系统。

| 优先级 | 编号 | 事项 | 所在章节 | 状态 |
|--------|------|------|---------|------|
| **P0** | C1.1 | 主页 HTML 结构（纯骨架，零依赖） | §C1 | ✅ 完成 |
| **P0** | B1.3 | Notice 数据模型建立 | §B1.3 | ✅ 完成 |
| **P0** | B1.2 | TaskForceRecordStore 建立 | §B1.2 | ✅ 完成 |
| **P0** | E3 | Emoji 全量清除（HTML + JS ~100处） | §E | ✅ T18 完成 |
| **P0** | MPA | 多页面网页群架构 | 架构 | ✅ T19 骨架完成 |
| **P1** | C4.X | 党建工作台按角色差异化视图 | §C4 | ✅ T18 完成 |
| **P1** | D1.5 | 默认首页设置（dashboard） | §D1 | ✅ 完成 |
| **P1** | §0.2A | 双域分化二维矩阵 v6.0 | §0.2A | ✅ T19 完成 |
| **P1** | R1 | 消除 events.js/party.js 重复统计逻辑 | 重构 | ✅ T18 完成 |
| **P1** | TERM | 术语清理（组织建设→党务管理 等） | 维护 | ✅ T24 全量完成 |
| **P1** | T25-1 | **党务管理 commission tab 内容差异化** — getActiveCommissioner() 改为 DOM 优先检测 | party.js | ✅ T25 完成 |
| **P1** | T25-2 | **h2 标题从 card 内部移至 card 上方** — 全 4 视图统一 | 全部 HTML | ✅ T25 完成 |
| **P1** | T25-3 | **意见反馈独立子页面** — feedback.html + service.feedback.js + feedback-entry.js | 新建 | ✅ T25 完成 |
| **P1** | T25-4 | **党支书反馈数据后台** — 党务管理 panel-secretary 中展示反馈统计+待处理列表 | party.html + party.js | ✅ T25 完成 |
| **P1** | T25-5 | **header 意见反馈链接** — search.html → feedback.html | header.js | ✅ T25 完成 |
| **P1** | T25-6 | **sidebar 新增 feedback 导航项** | sidebar.js | ✅ T25 完成 |
| **P2** | MPA-2 | 多页面完整内容迁移（从 index.html 拆分到独立页面） | 架构 | ✅ T24 完成 |
| **P2** | MPA-3 | index.html 精简为纯主页 | 架构 | ✅ T24 完成 |
| **P2** | MPA-4 | 跨页面导航验证 + 侧边栏高亮 | 架构 | ✅ T24 完成 |
| **P2** | C2.4.1 | 宣传委员多维表格交互补全 | §C2 | ⬜ 下一步 |
| **P2** | C3.1 | 党小组组长视图 JS 数据绑定 | §C3 | ⬜ 下一步 |
| **P3** | E2.1 | SOP 实操化精简审读 | §E2 | 需人工 |
| **P3** | F3.5~F3.7 | 回归测试补全 | §F3 | 待确认 |

> **T19 成果摘要**：...（已归档至 LOG）
>
> **T24 成果摘要**：...（已归档至 LOG）
>
> **T25 成果摘要**：
> - **Commission tab 修复**：getActiveCommissioner() 从 state 驱动改为 DOM 优先检测——直接点击 tab 即可切换对应面板内容，不再依赖 sidebar 角色选择
> - **h2 标题重新定位**：全 4 视图（workspace / party / search / archive）的 h2 标题从 card 内部移至 card 上方，层次分明
> - **意见反馈系统上线**：新建 feedback.html 独立子页面，集成 SOP 反馈卡三要素（Scope / Pain Point / Proposed Fix），localStorage 持久化，匿名/实名可选。侧边栏新增"意见反馈"导航项，header 链接同步更新
> - **党支书数据后台**：党务管理 党支书板块新增「意见反馈数据集」卡片——待处理/处理中/已处理三统计 + 待处理列表预览 + 跳转链接
>
> **下一步中心工作**：C2.4.1 宣传委员多维表格交互补全 + C3.1~C3.3 党小组组长/组织者/深度参与者 JS 数据绑定 + F3.5~F3.7 回归测试
>
> **T26 成果摘要**：
> - **权限系统建立**：新建 service.auth.js（ViewModeStore + AuthStore + canManage/canWriteActivity/canAuthorize/canRecruitTaskForce + Mock 赋权记录）
> - **Header 重构**：移除 subtitle + action-btn，换为 view-mode-switcher（管理模式/只读模式切换，mode 唯一主控入口）
> - **Sidebar 改造**：角色卡片点击 → 弹出 view-mode-picker modal → 管理/观摩二选一
> - **index.html 硬编码→共享组件**：header/sidebar 改为 renderHeader()/renderSidebar() 动态渲染
> - **About 页面重写**：分章节系统说明书（5章 + 致谢 + 责任自负声明）
>
> **T27 成果摘要**：
> - **赋权链简化**：条条支委天然权限（制度赋予，无需 secretary 赋权）；leader → organizer/deep 活动域赋权
> - **术语重命名**：观摩→只读模式, 管理→管理模式
> - **删除 root src/ 冗余目录**：docs/src/ 是唯一源码目录
> - **view-mode-switcher 样式重构**：纯色矩形方框（管理模式红 #CE1126 / 只读模式灰 #4B5563），移除角色名，仅 workspace+party 渲染
> - **删除视图代理冗余**：party.html 移除「视图代理」卡片 + workspace.html 移除 proxy select + party.js 删除 _initSecretaryProxyView()
> - **secretary-dashboard 角色色对齐**：统一 #CE1126 / #7A0010
> - **guides 新增 §F Mock vs 真实鉴权辨析**
>
> **T28 成果摘要**：
> - **★必填标记视觉柔和化**：颜色 neutral-400，字号 0.55rem
> - **赋权链全量重写**（对齐 ROADMAP §0.2A+§0.3）：secretary→leader/commissioners（最高赋权）；leader→organizer/deep（活动域）；org-commissioner→organizer/deep/专班成员（专班域）；prop/disc→无赋权能力
> - **工作台面板路由重写**（_routeRolePanel）：secretary/leader+管理→活动写入面板；org-commissioner+管理→专班招募面板；commissioner-group+管理→三委员重定向卡片；organizer/deep+管理→日历视图
> - **Bug修复**：commissioner-redirect div 重复创建（DOM 插入位置错误）+ canInitiateTaskForce 新增
> - **关于图标**：info circle → question mark circle
>
> **T29 成果摘要**：
> - **三委员看板替代日历**：组织委员→专班协调看板；宣传/纪检→活动与专班视图（全部 2 列：待启动+进行中）
> - **日历范围限定**：仅只读模式全员 + 管理模式中的 secretary/leader/organizer/deep 显示日历
> - **party.html 移除专班管理**：专班功能完全归位于党建工作台
> - **About 排版重写**：仿宋GB2312→华文中宋，max-w-4xl，leading-loose，致谢段落化
> - **侧边栏·header mode 去重叠（D-074）**：Header 成为 mode 唯一主控入口，Sidebar 为纯角色选择器
> - **组织委员专班发布渠道（D-075）**：新增发布招募表单（名称/人数/技能/周期/说明）
> - **宣传/纪检活动与专班混合视图（D-076）**：卡片区分活动/专班类型，点击展开子任务详情
> - **看板去已完成列（D-077）**：2 列设计，完成后归档，子任务灰显不消失
> - **活动写入表单补地点字段（D-079）** + **系列活动模型设计（D-080）**
>
> **当前状态**：T29 完成标志着全面架构收束——三委员在双域中的全部网页视图形态已确定并落地。后续工作：多维表格交互补全 + 非委员角色 JS 数据绑定 + SOP 文档全面更新。
