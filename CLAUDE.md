---
title: "Org OS — 系统路线图与 Harness"
type: roadmap
owner: "Org OS Agent 集群"
role: "[人机]"
last_updated: "2026-05-18"
status: active
---

# CLAUDE.md

> 本文件是 Org OS 项目的最高治理文件，分为**甲乙丙三部**：
> - **甲部：通用流程与指导思想（Harness）**——管总的，稳定不变，定义工作流、理论基石、运行标准
> - **乙部：具体执行事项（常为新的）**——AI 每次工作必须读取的上下文，完成即删，补充新项
> - **丙部：待决策事项**——AI 拿不定主意的问题，提交书记决策

---

# 甲部：通用流程与指导思想（Harness）

> Harness 是项目的"宪法"——定义工作方式、核心原则和运行标准。
> 修改 Harness 须经书记确认，且必须一改具改（见 H3）。

---

## H1. 标准化工作流

### H1.1 审查（每次启动时必做）

> 🔴 **以下 4 步为每次工作的强制前置，不得跳过。**

1. 读入 `.github/copilot-instructions.md`
2. 读取 `.ctx/TIMESTAMPS.md` 检查周期性任务到期情况
3. 读取 `.ctx/SNAPSHOT.md` 确定当前基线
4. 阅读本文件（CLAUDE.md）确认当前待办（乙部）

### H1.2 执行（单次迭代的标准流程）

> 🟡 **以下 4 步按顺序执行。**

1. **Blueprint**：输出任务拆解计划与执行路径
2. **实施**：调用对应工具/Agent 集群
3. **验收**：自动运行 `GetDiagnostics` 或执行 lint/type-check
4. **记录**：追加至 `.ctx/logs/YYYY-MM-EXECUTION_LOG.md`

### H1.3 next_prompt（🔴 每次工作结束的强制输出）

> 🔴 **这是每次工作的最后一步，禁止省略。若未输出 next_prompt，视为工作未完成。**

每次工作结束前，必须输出 `next_prompt`，包含以下三要素：

- **(a) 未完成的工作项**及 T 编号（若乙部无未完成项，标注"乙部当前无未完成项"）
- **(b) 需要恢复的上下文要点**（下次接手的 AI 需要了解的关键信息）
- **(c) 建议的执行顺序**（下一步应该先做什么）

**⚠️ 丙部待决策事项**：若丙部有未决事项，必须在 next_prompt 中列出，交由书记决策。

### H1.4 甲部修改工作流

> 🔴 **当用户对甲部提出修改意见时，必须按以下 5 步执行，不得跳过任何步骤。**

**核心认知**：CLAUDE.md 甲部不是一成不变的，也不是不可以修改的——**但改就要一改具改！**

**Step 1 — 修改甲部**：将用户的新看法准确写入甲部对应章节

**Step 2 — 影响评估**：识别修改波及的范围：
- (a) 甲部内部其他引用该原则的章节
- (b) guides/ 中与该原则对应的文件
- (c) insights/ 中引用该原则的经验条目
- (d) content/SOP/ 中依赖该原则的制度条文
- (e) docs/src/ 中实现该原则的代码

**Step 3 — 写入乙部**：将 Step 2 识别出的所有同步更新任务写入乙部，作为甲部修改的**衍生任务链**：
```
| T<轮次>-<序号> | 甲部 H2.x 修改同步：更新 <guide文件名> | 治理 | H3.1 一改具改 + H4.6 | <文件路径> | ⬜ 待启动 |
| T<轮次>-<序号>+1 | 甲部 H2.x 修改同步：更新 <SOP文件名> | 治理 | H3.1 一改具改 + H4.6 | <文件路径> | ⬜ 待启动 |
```

**Step 4 — 逐项执行**：按乙部中的任务链顺序执行，每项完成后更新状态

**Step 5 — 全局验证**：所有衍生任务完成后，Grep 确认全仓库无旧版表述残留

**禁止**：仅修改甲部而不写入乙部衍生任务——甲部修改是最高影响的变更，必须确保全仓库同步。

### H1.5 钩稽联动

> 🟡 **修改任何文件前触发。**

修改任何文件前，必须检查本文件及 guides 中是否存在对该文件的钩稽声明并按要求联动更新。

### H1.6 Snapshot 更新规则

> 🟢 **仅在特定触发条件下执行。**

`.ctx/SNAPSHOT.md` **不再每次执行更新**。仅在以下触发条件满足时更新：
- (a) 大版本升级（v3→v4），如架构重大变更、角色体系迭代
- (b) 用户显式指令触发（如"/snapshot"或"生成快照"）
- (c) 项目阶段性收官（Phase 1/2/3 完成）

日常变更仅由 `EXECUTION_LOG` + `DECISION_LOG` + `TIMESTAMPS` 承载。

**Snapshot 格式规范**（必须遵循，不得自创结构）：
```
## I. 全局物理拓扑    ← 🔴 强制：仓库目录树（逐文件标注职责，含 .github/ 和 .ctx/ 子项）
## II. 分层架构       ← 🔴 强制：Layer 表（编号/名称/位置/角色）
## III. 核心文件清单  ← 治理文件表 + 前端代码表（文件/状态/说明）
## IV. 核心理论       ← 理论摘要表（理论/核心公式/详细文档链接）
## V. 权限矩阵摘要   ← 角色×双域权限表
## VI. 版本里程碑    ← 版本历史表（版本/日期/里程碑）
```
**I 和 II 为强制最低要求**，任何 Snapshot 不得缺少这两节。III~VI 按需补充。

### H1.7 乙部任务生命周期机制

> 🔴 **乙部条目的写入/更新/退出为强制规则，不得违反。**

乙部是"常为新的"执行上下文，其条目遵循严格的**写入→更新→完成退出**生命周期：

**1. 写入（何时写入乙部）**：
- (a) 用户在对话中提出新任务 → 按 H5.4 流程写入
- (b) Agent 在 Blueprint 阶段发现大任务 → 按 H6.4 分片规则拆分后写入
- (c) 周期性任务到期（TIMESTAMPS 触发）→ 写入并标注来源
- (d) 执行过程中发现衍生任务 → 写入并标注 `衍生自: T<轮次>-<序号>`
- (e) 甲部修改触发的同步更新 → 按 H1.4 Step 3 写入衍生任务链
- (f) 用户明确要求写入乙部 → 必须写入，无论任务大小

**不写入乙部的场景**：用户未明确要求且可一次性完成的小任务（如修 typo、补字段）。此类任务**不分配 T 编号**，但**必须写入执行日志**，在日志中标注 `当次完成（未入乙部）`。

**判断标准**：
```
用户明确要求写入？ ──是──→ 必须写入乙部
        │否
能否一次完成？ ──否──→ 必须写入乙部（按 H6.4 分片）
        │是
涉及 3+ 文件或制度变更？ ──是──→ 写入乙部（影响范围大）
        │否
→ 当次完成，仅写执行日志，不入乙部
```

**写入格式**：必须包含 ID、事项、类型、引用流程、修改对象、状态六列。引用流程必须指向甲部 H 编号或具体 guide 文件。

**2. 更新（何时更新乙部条目）**：
- (a) 任务开始执行 → 状态从 `⬜ 待启动` → `🔄 进行中`
- (b) 分片任务完成子片 → 更新对应子片状态
- (c) 任务暂停（因阻塞/需人工确认）→ 状态 → `⏸️ 暂停`，注明原因
- (d) 发现任务描述不准确 → 更新描述，不得删除原 ID

**3. 完成退出（何时从乙部删除）**：
- **前置条件**：已写入执行日志（H6.2 模板），且日志中包含该任务的 T 编号
- **操作**：将条目从乙部表格中**直接删去**
- **禁止**：不得仅标记 ✅ 而不删除——✅ 仅用于当次工作中完成但尚未更新乙部的临时标注
- **归档**：完成事项的详细信息由执行日志和决策日志承载，乙部不保留历史

**4. 异常处理**：
- 任务被取消（非完成）→ 删除条目，在执行日志中记录取消原因
- 任务被合并到其他任务 → 删除被合并条目，在保留条目中注明合并来源
- 任务优先级变更 → 移动到对应优先级分组，保留原 ID

### H1.8 丙部待决策机制

> 🔴 **AI 遇到拿不定主意的问题时，必须写入丙部提交书记决策，不得自行决定。**

**核心认知**：乙部是"待执行"的任务，丙部是"待决策"的问题。AI 不得将需要书记判断的事项自行消化。

**1. 写入（何时写入丙部）**：
- (a) 执行过程中遇到方案选择（A vs B vs C）→ 写入丙部，提供选项+分析
- (b) 制度条文存在歧义或冲突 → 写入丙部，列出歧义点
- (c) 涉及人员/职责/权限的变更 → 写入丙部，说明影响范围
- (d) 用户提出的需求存在多种实现路径 → 写入丙部，列出各路径优劣

**2. 格式**：

> **写作原则**：丙部的读者是**书记**，不是 AI 或开发者。必须让书记在 30 秒内理解"为什么需要决策"和"选什么有什么后果"。禁止堆砌技术细节和文件路径。

**格式规范**：

```markdown
## P.<序号> <问题标题（用人话）>

**为什么需要决策**：<一句话说清痛点，不用技术术语>

**路线图**（每一步都需要书记选择方向）：

**第一步：<最核心的分歧点>**

| 方向 | 含义（人话） | 后果（人话） |
|------|-------------|-------------|
| A <方向名> | <用日常语言描述> | <选了会怎样> |
| B <方向名> | <用日常语言描述> | <选了会怎样> |

→ 如果选 A，进入下一步；如果选 B，本议题结束（或说明后续）

**第二步：<次级分歧点>**（仅在上一步选了需要细化的方向时出现）
...

**当前状态**：等待书记选择第 X 步方向
```

**写作禁令**：
- ❌ 禁止在正文中写文件路径（如 `docs/src/entries/main-entry.js`），影响范围写在末尾小字
- ❌ 禁止用技术变量名（如 `mockDB.activities`、`_fmtDate()`）代替人话
- ❌ 禁止一次性给出 3 个以上并列选项而不说明选择逻辑
- ✅ 必须用路线图式呈现：每一步一个分歧点，选了再往下走
- ✅ 必须用日常语言描述选项含义和后果
- ✅ 必须标注"当前状态"和"等待书记选择第几步"

**3. 生命周期**：

```
写入丙部 → next_prompt 提交 → 书记决策 → 决策结果写入 Decision Log → 执行 → 写入 Execution Log → 从丙部删除
```

- **写入**：AI 在执行过程中发现问题 → 立即写入丙部
- **提交**：每次 next_prompt 中必须列出丙部未决事项，交由书记决策
- **决策**：书记选择选项或填写"其他" → 决策结果触发后续流程
- **归档**：决策结果写入 Decision Log（D-编号），关联丙部条目编号
- **执行**：根据决策结果执行 → 产生的任务写入乙部（T-编号）
- **退出**：决策完成且执行启动后，从丙部删除

**4. 与日志的关系**：

| 维度 | 丙部 | Decision Log | Execution Log |
|------|------|-------------|---------------|
| 定位 | 待决策的问题 | 已做出的决策 | 已执行的操作 |
| 时序 | 决策前 | 决策时 | 决策后 |
| 写入者 | AI | AI（记录书记的决策） | AI |
| 读者 | 书记 | 全团队 | AI（恢复上下文） |
| 生命周期 | 决策后删除 | 永久保留 | 永久保留 |

**5. 与乙部的关系**：
- 丙部的决策结果可能产生新的乙部任务
- 乙部任务执行中发现的决策问题写入丙部
- 两者互为上下游：丙部 → 决策 → 乙部 → 执行 → 可能发现新丙部条目

**6. next_prompt 强制包含**：
每次 next_prompt 必须包含 `⚠️ 丙部待决策事项` 部分，列出所有未决事项。若无未决事项，标注"当前无待决策事项"。

---

## H2. 核心理论基石

> 以下四节为本项目的核心理论贡献，确立制度层面的设计原则。
> 此处仅保留**核心原则与判例**，详细设计文档见对应 guides（AI 按需读取，减少上下文占用）。

### H2.1 双域管理理论 — 党建 x 党务

> **这是本项目最重要的理论贡献。**

- **党建工作** = 创新探索 → **党建工作台**
- **党务工作** = 合规运行 → **党务管理**
- **区分要诀**：党建是"创新探索"，党务是"合规运行"
- **默认面板原则**：未选定角色时展示全量信息只读面板（信息透明基线）
- **党建工作内部分类**：活动建设（活动组织实施）与专班建设（定人定责定岗）为并列概念，归不同人管理；专班制是专班建设配套的工作要点，主要涉及招募、考察等对专班人员的支持保障工作

**三委员双域职责**：

| 委员 | 党建工作台 | 党务管理 |
|------|-----------|---------|
| **组织委员** | 专班建设（招募统筹·定人定责定岗） | 党员发展全流程（考察/催缴/归档） |
| **宣传委员** | 活动与专班视图（宣传材料/周报） | 宣传档案合规建设（模板/制度） |
| **纪检委员** | 考勤管理·考察管理·活动监督复盘 | 补课制度/公邮管理 |

**纪检委员三支柱**（覆盖活动+专班两个平行维度）：
- **考勤**：党小组活动考勤由组长/组织者上传，会议考勤由纪检委员上传，纪检委员确认后录入考勤总表
- **考察**：党小组活动考察由组长/组织者上传，专班考察由专班负责人/组织委员上传，纪检委员确认后录入考察总表
- **活动监督复盘**：纪检委员对活动/专班工作时间流有阅览权限，超时未完成可邮件提醒；对复盘总结可批注、打回、确认

**党建板块与党务板块的本质区分**：党建板块服务同学们（供支部成员查询自己的出勤情况），党务板块供支委会决策（考察情况留给支委会进行下一步发展决策）。党建和党务在后台指向同一份数据，不同切面。

**关键判例**：
- 条条支委无权直接写入活动，写入链路：支委提出需求 → 组织委员协调 → 党小组组长承办写入
- 纪检委员考勤数据与组织委员通用同源——组织委员的「活动出勤」「思想汇报提交」数据直接使用纪检委员维护的考勤系统
- 宣传委员不可自行招募宣传专班，赋权操作统一归口组织委员
- 多维表格视图是"职能域展示形态"，非"宣传委员专属"

→ 术语对照见 [TERMINOLOGY.md](content/guides/governance/TERMINOLOGY.md)
→ 委员系统+专班制见 [COMMISSIONER_SYSTEM.md](content/guides/design/COMMISSIONER_SYSTEM.md)

### H2.2 差异化视图设计规范

- **核心原则**：同一数据源（`ActivityRecordStore`），不同切面展示
- **双域分化**：同一角色跨域时视图完全不同
- **四维度决策树**：Q1 组织场景 → Q2 活动形式(7色系) → Q3 时长 → Q4 发起方向
- **赋权范围链**：党支书→组织委员→党小组组长→宣传/纪检不可转授权
- **品牌活动定位**：品牌活动是活动的子类型（标签），不改变活动本身的数据结构。品牌认定在网页上由书记操作。品牌活动如需独立展示网页，将来可探索

→ **详细内容**（双域视图矩阵/7色系/视图组件规范/子记录结构/决策树模型）见 [MANAGEMENT_MODE.md](content/guides/architecture/MANAGEMENT_MODE.md) §八
→ 日历视图见 [CALENDAR.md](content/guides/design/CALENDAR.md)
→ 子记录数据结构见 [DATA.md](content/guides/architecture/DATA.md) §2.1B

### H2.3 专班制（Task Force System）

- **核心定义**：专班 = 赋权（Authorization） x 工作量考察（Workload Accounting）
- **赋权即记账**：赋权动作触发工作量记录线程，解散时自动生成汇总报告
- **问题起点**：条块二元结构 → 专班是临时性、目标导向的横向切口
- **核心规则**：★ 组织委员是唯一专班管理节点；纪检委员不生长专班（内控隔离）
- **生命周期**：发起 → 招募(=赋权+记账) → 运行 → 解散(赋权回收+工作量报告)

→ **详细内容**（专班类型矩阵/条块衔接规则/生命周期详细流程/双域管理升级注）见 [COMMISSIONER_SYSTEM.md](content/guides/design/COMMISSIONER_SYSTEM.md) §A.3~A.8

### H2.5 赋权关系链（Authorization Chain）

> **这是角色视图切换的权限基础。**赋权者有权切换所有被赋权者的视图（只读查看，不可修改）。

**赋权关系树**：

```
党支书
├── 条条支委（组织委员 / 宣传委员 / 纪检委员）
└── 党小组组长
    └── 组织者 / 深度参与者

组织委员
└── 组织者 / 深度参与者（专班域赋权）
```

**组织者的两种赋权路径**（D-15 ③）：
- Ⅰ 自上而下布置的活动或专班：从支委或党小组组长处承包任务，赋权随承包自动生效
- Ⅱ 自下而上创建的活动或专班：必须联系本组党小组组长或组织委员，经赋权后方可进行任务分派

**组织者退出机制**（D-15 ⑤）：
- 组织者退出需经支委会确定好接手的组织者并负责地完成工作交接后方可退出
- 长期活动/专班中组织者最好保持留在岗位，帮助深度参与者负责地交接工作

**核心规则**：
- 赋权者可查看被赋权者的工作视图（只读），但不可修改
- 党支书可查看所有角色的视图
- 组织委员可查看组织者和深度参与者的视图（专班域）
- 党小组组长可查看组织者和深度参与者的视图
- 三委员之间可相互查看（只读），但不可修改彼此的工作
- 组织者与深度参与者之间是平等协商关系，不是上下级验收关系（D-15 ②扁平化原则）

**视图模式三分类**：

| 模式 | 谁使用 | 能看到什么 | 能修改什么 | 继承关系 |
|------|--------|-----------|-----------|---------|
| 管理模式 | 角色对应的管理者 | 自己角色的工作台 | 自己角色的数据 | —（基类） |
| 管理者只读 | 赋权者/支委查看他人 | 被查看者的工作台 | 无 | 继承管理模式，删去写入/修改功能 |
| 参与者只读 | 普通参与者/支部成员 | 各支委的工作成果 + 自己的参与记录 | 无 | 独立视图 |

**三种模式的本质与继承关系**：

- **管理模式** = 管理者管理自己的事务（支委管理自己的职能域，组长管理自己的党小组）
- **管理者只读** = 管理模式减去写入/修改功能。看到的内容与管理模式完全一致，只是所有编辑操作被禁用。用于支委之间互查、赋权者查看被赋权者——了解他人进度，不插手
- **参与者只读** = 独立视图，不是管理模式的子集。参与者可以看到各支委的工作成果（了解党支部运行逻辑、了解支委为支部做了什么），也可以看到自己的参与记录。目的是让参与者提前了解党支部的运行逻辑，认识到支委为党支部做了什么事情

**身份切换器的核心目的**：明确"只读"是哪种只读——
- 支委之间切换 → 管理者只读（管理模式-写入，了解同僚工作进度）
- 赋权者查看被赋权者 → 管理者只读（管理模式-写入，了解下属工作进度）
- 支部成员查看 → 参与者只读（独立视图，了解支部运行+自己的参与情况）

**关键规则**：管理者只读和管理模式看到的内容完全一致（同一工作台），区别仅在于写入权限的有无。参与者只读看到的是完全不同的视图——不是管理视角的工作台，而是面向参与者的信息面板。

→ 详细实现见 [COMMISSIONER_SYSTEM.md](content/guides/design/COMMISSIONER_SYSTEM.md) §C 权限矩阵

### H2.6 组织者与深度参与者的扁平化设计

> **这是党支部扁平化参与途径的理论基础。**（D-15 决策确立）

- **核心定义**：组织者和深度参与者是为了建设【活动】或【专班】、推动组织建设更加扁平化的设计。活动与专班是并列概念，都需要人手
- **扁平化含义**：组织者和深度参与者之间没有上下级关系，只是分工内容不同。身份不是最重要的，年级和政治面貌不决定是否胜任
- **核心比喻**：组织者是项目的脑子，深度参与者是项目的手
- **组织者两种情况**：
  - Ⅰ 自上而下布置的活动或专班：从支委或党小组组长处承包任务，发挥桥梁作用，及时同步任务进展。对深度参与者进行分工（分派任务），分工需经平等协商
  - Ⅱ 自下而上创建的活动或专班：必须联系本组党小组组长或组织委员，由党小组组长或组织委员赋权后方可进行任务分派
- **考勤要求**：活动——深度参与者必须出席，组织者最好出席（允许因特定原因无法出席，但必须与深度参与者和支委/党小组组长同步工作）；专班——没有严格的活动地点、时间限制，对高年级同志更加友好
- **组织者核心职能**：做好分工记录（支委会根据实际工作完成情况进行考察）。组织者可能不参与具体工作（也可以参与），但分工记录是最重要的产出
- **组织者退出机制**：组织者退出需经支委会确定好接手的组织者并负责地完成工作交接后方可退出。长期活动/专班中组织者最好保持留在岗位，帮助深度参与者负责地交接工作
- **产出物**：
  - 活动：组织者必须打包好深度参与者的工作成果（考勤数据+宣传档案材料），形成对深度参与者的工作考察记录及自己的工作记录（如参与了具体分工），鼓励提交活动复盘和创新提案
  - 专班：组织者必须提交对深度参与者的工作考察记录及自己的工作记录，鼓励提交创新提案
- **术语规范**：统一使用"组织者"，不使用"活动组织者"（活动与专班并列，组织者不绑定活动）

→ 详细设计见 [MANAGEMENT_MODE.md](content/guides/architecture/MANAGEMENT_MODE.md) §三

### H2.4 SOP<->网页双向修改规则

```
规则 0：文本 SOP 是母本，网页是实施层。
规则 1：用户反馈视为同时对 SOP 母本和网页代码的修改指令，两者必须同步。
规则 2：先改 SOP 母本 → 再改网页代码 → 验证。
规则 3：网页实现与 SOP 母本不一致时，以 SOP 母本为准。
规则 4：每次修改审视——是否实操？是否简洁可执行？是否有网页交互支撑？
```

→ 完整指南见 [SOP_WEB.md](content/guides/design/SOP_WEB.md)

---

## H3. 一改具改原则与母本子本关系

### H3.1 一改具改

> **核心原则**：修改任何一项制度/设计/术语，必须在全仓库范围内同步更新所有相关引用，不得遗漏。

**执行标准**：
1. 修改前：全仓库搜索所有引用位置（Grep + SearchCodebase）
2. 修改中：逐一替换，每处替换记录到执行日志
3. 修改后：再次全仓库搜索确认零残留

### H3.2 设计母本与子本

| 关系类型 | 母本 | 子本 | 同步规则 |
|---------|------|------|---------|
| 制度→代码 | `content/SOP/*.md` | `docs/src/*.js` | 母本优先，代码跟随（H2.4 规则 2） |
| 理论→工程 | `content/guides/*.md` | `docs/src/*.js` | guides 定义设计，代码实现设计 |
| 路线图→执行 | `CLAUDE.md` 乙部 | `.ctx/logs/*-EXECUTION_LOG.md` | 完成事项从乙部删除，写入执行日志 |
| 经验→沉淀 | `.ctx/logs/DECISION_LOG.md` | `content/insights/*.md` | 决策日志定期蒸馏为经验沉淀 |
| 术语→全仓 | `content/guides/governance/TERMINOLOGY.md` | 全仓库所有文件 | 术语变更触发一改具改 |

### H3.3 Guides 与 Insights 的定位

- **Guides（`content/guides/`）**：设计理念文档。不管对应功能有没有完成，设计理念始终存在，作为知识留存。
- **Insights（`content/insights/`）**：经验沉淀文档。按一定频率根据需求和日志进行蒸馏更新。
- 两者都是**稳定的知识资产**，不因执行事项的完成而删除。

### H3.4 经验沉淀规则

> 🔴 **每次向 insights/ 写入内容时必须遵守以下规则。**

**1. 结构兼容性判断**：
- 写入前，必须将新内容与既有行文结构逐节比对
- 若既有结构可兼容 → 追加到对应章节，不另起炉灶
- 若既有结构逻辑不能兼容 → 写入丙部，由书记决断如何组织

**2. 正反两面论**：
- 经验沉淀必须同时回答"为什么是这样"和"为什么不是那样"
- 只说结论不说反面论证 = 教条，不是经验
- 反面论证包括：被否决的方案、被纠正的认知、被淘汰的设计

**3. 禁止冗余标记**：
- 禁止在正文中标注版本号（v7.0/v8.0）、批次标记（T24/T25）、时间戳
- 这些元信息属于 YAML frontmatter 的 `milestone` 字段，不属于正文
- 正文只留干货——原则、教训、正反论证

---

## H4. Harness 运行标准

### H4.1 文件修改检查清单

每次修改文件时，Agent 必须按以下清单执行：

```
□ 1. 确认修改范围：哪些文件受影响？
□ 2. 检查钩稽：CLAUDE.md / guides 中是否有对该文件的引用？
□ 3. 一改具改：全仓库搜索相关引用，逐一同步更新
□ 4. 术语合规：修改内容是否符合 TERMINOLOGY.md？
□ 5. 母本优先：若涉及 SOP 制度变更，先改 content/SOP/ 再改代码
□ 6. YAML 更新：修改后更新 last_updated 字段
□ 7. 记录日志：写入 .ctx/logs/YYYY-MM-EXECUTION_LOG.md
□ 8. 乙部更新：若涉及执行事项，更新 CLAUDE.md 乙部
```

### H4.2 YAML Frontmatter 规范

所有 `content/` 目录下的 `.md` 文件必须包含以下字段（按顺序）：

```yaml
---
title: "中文标题"
type: <枚举值>
role: "[人]"/"[人机]"/"[AI]"
owner: "负责人"
last_updated: "YYYY-MM-DD"
version: "X.Y"
status: active/archived/suspended
audience: [...]
related_files: [...]
---
```

**type 枚举值**（仅限以下值，不得自创）：

| type | 含义 | 典型位置 |
|------|------|---------|
| `roadmap` | 项目路线图与 Harness | 根目录 CLAUDE.md |
| `architecture` | 架构设计文档 | 根目录 ARCHITECTURE.md, guides/ |
| `SOP` | 制度母本 | content/SOP/ |
| `guide` | 设计理念与操作指南 | content/guides/ |
| `governance` | 治理规范 | content/guides/ |
| `design` | 功能设计方案 | content/guides/ |
| `experience_doc` | 经验沉淀 | content/insights/ |
| `reference` | 参考模板 | content/references/ |
| `template` | 可复用模板 | content/references/模板库/ |
| `index` | 目录索引 | 各目录 README.md |
| `log` | 执行日志 | .ctx/logs/ |
| `decision_log` | 决策日志 | .ctx/logs/ |
| `audit_report` | 审计报告 | .ctx/logs/ |
| `snapshot` | 系统快照 | .ctx/snapshots/ |

**已废弃的 type 值**：`CLAUDE.md`（改为 `roadmap`）、`模板`（改为 `template`）、`导航`（改为 `index`）

### H4.3 术语规范

所有文档必须遵守 [TERMINOLOGY.md](content/guides/governance/TERMINOLOGY.md) 中的标准术语。

**当前最高优先级术语对照**：

| 废弃术语 | 标准术语 |
|---------|---------|
| 活动建设 | 党建工作 |
| 组织建设 | 党务工作 |
| 推演工作台 | 党建工作台 |
| 观摩模式 | 只读模式 |
| ROADMAP.md | CLAUDE.md |

### H4.4 角色三分类

文件按 `[人]`/`[人机]`/`[AI]` 三类标记，定义见 [ROLE_CLASSIFICATION.md](content/guides/governance/ROLE_CLASSIFICATION.md)。

### H4.5 编码规范

全仓库必须遵守以下编码标准，确保中文内容在所有工具链中正确显示：

1. **文件编码**：所有文本文件（`.md`、`.js`、`.html`、`.css`、`.json`、`.yaml`）必须使用 **UTF-8 无 BOM** 编码
2. **换行符**：统一使用 **LF**（`\n`），禁止 CRLF（`\r\n`）。`.gitattributes` 中应设置 `* text=auto eol=lf`
3. **中文标点**：Markdown 正文中使用**中文标点**（，。：；！？""''），代码和表格内使用英文标点
4. **git 操作编码**：执行 `git show`/`git diff` 等命令时，必须设置 `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8`，避免中文乱码
5. **PowerShell 输出**：使用 `Out-File -Encoding utf8` 保存文件，禁止使用默认的 Windows 编码

### H4.6 文档关系定义

**CLAUDE.md、guides、insights、logs 之间的关系**：

```
                    CLAUDE.md（甲部 Harness）
                    ┌──────────────────────┐
                    │ 核心原则 + 判例       │  ← 最高权威，定义"是什么"和"为什么"
                    │ 乙部执行事项          │  ← 当前工作上下文
                    └──────────┬───────────┘
                               │ 引用
              ┌────────────────┼────────────────┐
              ▼                ▼                ▼
     ┌────────────┐   ┌────────────┐   ┌────────────┐
     │  guides/   │   │ insights/  │   │   logs/    │
     │ 设计理念   │   │ 经验沉淀   │   │ 运行记录   │
     └────────────┘   └────────────┘   └────────────┘
     定义"怎么设计"   提炼"什么经验"   记录"做了什么"
```

**四者职责**：

| 维度 | CLAUDE.md 甲部 | guides/ | insights/ | logs/ |
|------|---------------|---------|-----------|-------|
| **定位** | 宪法（原则+判例） | 法律（详细设计） | 判例法（经验模式） | 庭审记录（事实） |
| **内容** | 核心原则、关键判例、运行标准 | 完整设计文档、架构方案 | 可复用经验、管理哲学 | 操作步骤、决策过程 |
| **稳定性** | 最稳定（修改须书记确认） | 稳定（设计理念始终存在） | 渐进增长（定期蒸馏） | 只增不改（归档后冻结） |
| **信息流向** → | ← guides 补充细节 | ← CLAUDE.md 授权原则 | ← logs 蒸馏提炼 | ← 执行过程产生 |
| **信息流向** ↓ | ↓ 乙部规定当前工作 | ↓ 代码实现设计 | ↓ 指导未来决策 | ↓ 归档为历史 |

**关键规则**：
- CLAUDE.md 甲部是 guides 的**摘要和索引**，不是 guides 的副本。甲部保留核心原则+判例，详细设计归 guides
- 当甲部与 guides 内容冲突时，以**甲部为准**（H7.1 上层优先原则）
- guides 的更新如果涉及甲部引用的原则变更，必须**同步更新甲部**（H3.1 一改具改）
- insights 从 logs 中蒸馏提炼，不直接从 guides 中产生
- logs 是原始记录，insights 是提炼后的知识，两者不可互相替代

---

## H5. 编号命名规则

### H5.1 甲部 Harness 编号体系

采用**字母+数字**的层级编号，格式为 `H<层>.<节>`：

```
H1   ← 一级章节（大领域）
H1.1 ← 二级章节（具体事项）
H1.1.1 ← 三级章节（细则，仅在必要时使用）
```

**层级约束**：
- 最多**三级**（H1.1.1），不得出现四级（H1.1.1.1）
- 若三级不够用，说明该节应拆分为独立的二级章节
- 新增章节时，在当前同层级末尾追加编号，**不得插队**

**编号预留策略（防数字爆炸）**：
- 一级章节编号按 10 递增预留：H10, H20, H30...（当前 H1~H6 实际使用 H10~H60）
- 但为可读性，当前阶段使用连续编号（H1~H9），当 H9 用满后切换为 H10 递增制
- 二级编号按 1 递增，不预留
- 三级编号按 1 递增，不预留

**编号语义对照**：

| 编号 | 语义 | 当前内容 |
|------|------|---------|
| H1 | 工作流 | 审查/执行/next_prompt/甲部修改/钩稽/Snapshot/乙部生命周期/丙部待决策 |
| H2 | 理论基石 | 双域/视图/专班/双向修改/赋权关系链 |
| H3 | 一改具改 | 母本子本关系 |
| H4 | 运行标准 | 检查清单/YAML/术语/角色/编码/文档关系 |
| H5 | 编号规则 | 本节 |
| H6 | 日志规范 | 执行日志/决策日志权责与模板 |
| H7 | 文档层级 | 权威层级与索引规范 |
| H8 | 快速导航 | 导航表 |
| H9 | 保留 | — |

### H5.2 乙部任务编号体系

格式：`T<轮次>-<序号>`

- **轮次**：按工作日递增（T30, T31, T32...），每轮可包含多个任务
- **序号**：同一轮次内的任务序号（1, 2, 3...）
- **跨轮延续**：若某任务未完成，在新轮次中保留原编号，不重新编号

**防数字爆炸策略**：
- 轮次编号与日期绑定，不与任务数量绑定。一个工作日 = 一个轮次，无论当天有多少任务
- 轮次编号不要求连续——T30 之后可以是 T35（中间跳过的轮次编号不回收）
- 序号上限：单轮次内序号原则上不超过 20。若超过，说明该轮次应拆分为多个轮次
- **持续任务**使用 `C-<序号>` 前缀，不占用 T 编号空间
- **待讨论议题**使用 `D.<序号>` 前缀，**暂缓议题**使用 `F.<序号>` 前缀

### H5.3 编号对应关系

乙部任务编号与日志条目之间**必须建立双向可追溯关系**：

```
CLAUDE.md 乙部任务 T30-7  ←→  EXECUTION_LOG 中包含 "T30-7" 的条目
```

**执行日志中的编号规则**：
- 每条日志条目标题格式：`## YYYY-MM-DD | T<轮次> — <一句话摘要>`
- 条目内用 `T<轮次>-<序号>` 标注具体完成的任务
- 若一次工作完成多个任务，在条目内逐项标注

**决策日志中的编号规则**：
- 格式：`D-<序号>`，全局递增，不按月重置
- 若某决策与乙部任务相关，标注对应任务 ID（如 `关联任务: T30-7`）

### H5.4 用户补充工作的流程

用户在对话中直接提出的任务（非乙部已有条目），按以下流程处理：

1. **判断是否涉及决策**：
   - 若涉及方案选择（A vs B）、制度变更、架构调整 → **必须写入决策日志**
   - 若仅为执行性操作（修 bug、补字段）→ 仅写入执行日志
2. **写入乙部**：将用户补充的任务追加到乙部对应优先级分组，分配新编号
3. **执行**：按 H1.2 标准流程执行
4. **完成后**：从乙部删除，写入执行日志

**特殊场景**：用户直接要求"立即做 X"且 X 可在当次工作中完成——先追加到乙部（标注 `🔄 当次`），完成后立即标记 ✅ 并在下次乙部更新时删除。

### H5.5 Guides 文件编号

Guides 文件不使用数字编号，使用**语义化文件名**（大写+下划线），按 MECE 分类归入子目录（见 guides/README.md）。

---

## H6. 日志规范

### H6.1 执行日志与决策日志的权责划分

| 维度 | 执行日志（EXECUTION_LOG） | 决策日志（DECISION_LOG） |
|------|--------------------------|------------------------|
| **定位** | 做了什么 | 为什么这样做 |
| **记录内容** | 操作步骤、修改文件、产出物 | 决策背景、选项分析、最终决定、影响范围 |
| **写入时机** | 每次工作结束后**必须写入** | 仅在做出**非显而易见的决策**时写入 |
| **写入者** | 执行 Agent | AI（记录书记的决策） |
| **读者** | 下次接手的 Agent（恢复上下文） | 书记/支委（理解为什么）、经验蒸馏（提炼模式） |
| **归档** | 月度归档 | 持续追加，不归档 |
| **蒸馏去向** | → insights/（经验沉淀） | → insights/（管理哲学） |

**关键区分**：
- "修改了 3 个文件的术语"→ 写执行日志
- "选择方案 A 而非方案 B，因为..."→ 写决策日志
- 一次工作可能同时产生两种日志条目

### H6.2 执行日志模板

> **版式规则**：老人老办法，新人新办法。旧日志条目保持原版式不变，新条目使用以下模板。

```markdown
## YYYY-MM-DD | T<轮次> — <一句话摘要>

- **来源**: <用户指示 / Agent 执行 / 周期性唤醒>
- **时间**: YYYY-MM-DD
- **变更文件**: <文件1>, <文件2>, ...
- **关键动作**: T<轮次>-<序号> <摘要> / T<轮次>-<序号> <摘要>
- **变更详情**:
  - **T<轮次>-<序号>: <任务标题>**：
    - `<文件路径>`: <修改说明>
- **设计决策**:
  - <决策内容>（关联决策: D-<序号>，如涉及）
- **结果**: <完成情况>
- **蒸馏标签**: [经验蒸馏: 是/否 — <说明>]
```

**与旧版式的差异**（仅新增项）：
1. 标题格式统一为 `YYYY-MM-DD | T<轮次> — <摘要>`（旧条目无 T 编号）
2. 关键动作行使用 `T<轮次>-<序号>` 标注（旧条目使用 T1/T2 等非标准编号）
3. 设计决策行新增 `关联决策: D-<序号>` 可选项
4. 蒸馏标签保留原有格式不变

### H6.3 决策日志模板

```markdown
## D-<序号> <日期> — <决策标题>

### 背景
<为什么需要做决策>

### 选项
| 选项 | 描述 | 优势 | 劣势 |
|------|------|------|------|
| A | <描述> | <优势> | <劣势> |
| B | <描述> | <优势> | <劣势> |

### 决定
<选择哪个，一句话>

### 理由
<为什么这样选>

### 影响范围
- <受影响的文件/模块/流程>

### 一改具改检查
- [ ] 全仓库引用已同步
```

### H6.4 Token 限制分片规则

> **核心原则**：当任务预估超过单次工作的 token 限制时，必须提前分片。

**执行标准**：
1. **预估**：开始 Blueprint 阶段时，评估任务规模。若涉及 5 个以上文件或 200 行以上修改，视为"大任务"
2. **分片**：将大任务拆分为多个子任务，每个子任务可在单次工作中完成
3. **写入乙部**：将拆分后的子任务写入 CLAUDE.md 乙部，标注 `🔄 分片中` 状态
4. **顺序执行**：按乙部中的顺序逐个完成，每完成一个子任务更新乙部状态
5. **上下文传递**：每个子任务完成后，在执行日志中写入 `next_prompt`，确保下一个子任务可恢复上下文

**分片模板**：
```
| T31-1 | <总任务名>（1/3：<子任务1>） | <引用流程> | P1 | 🔄 分片中 |
| T31-2 | <总任务名>（2/3：<子任务2>） | <引用流程> | P1 | ⬜ 待启动 |
| T31-3 | <总任务名>（3/3：<子任务3>） | <引用流程> | P1 | ⬜ 待启动 |
```

---

## H7. 文档权威层级与索引规范

### H7.1 文档权威层级

```
┌─────────────────────────────────────────────┐
│ 宪章层    CLAUDE.md（甲部 Harness）           │  ← 最高权威
├─────────────────────────────────────────────┤
│ 治理层    guides/（设计理念 + 操作规范）       │  ← 制度与设计
├─────────────────────────────────────────────┤
│ 架构层    ARCHITECTURE.md                    │  ← 技术架构
├─────────────────────────────────────────────┤
│ 操作层    content/SOP/（制度母本）            │  ← 工作流程
├─────────────────────────────────────────────┤
│ 参考层    content/references/（模板库）       │  ← 可复用资源
├─────────────────────────────────────────────┤
│ 审计层    .ctx/（日志/快照/时间戳）           │  ← 运行记录
└─────────────────────────────────────────────┘
```

冲突裁决规则：上层优先。CLAUDE.md > guides > SOP > 代码实现。

### H7.2 索引规范（README 的职责）

每个目录的 README.md 是该目录的**唯一入口索引**，职责为：
1. **列出所有文件**及其一句话说明
2. **按 MECE 分类**组织（不得有文件不属于任何分类，不得有文件属于多个分类）
3. **标注权威源**：当本目录文件与其他目录存在内容重叠时，标注哪个是权威源
4. **减少 AI token 消耗**：AI 读取 README 即可决定需要深入读取哪些文件，无需逐个扫描

**禁止**：README 中不得重复文件内容，仅做索引和导航。

---

## H8. 快速导航

| 我需要... | 去哪里 |
|----------|--------|
| 了解项目全貌 | [ARCHITECTURE.md](ARCHITECTURE.md) |
| 看当前待办 | 本文件乙部 |
| 看理论基石 | 本文件 H2.1~H2.4 |
| 看运行标准 | 本文件 H4 |
| 看编号规则 | 本文件 H5 |
| 看日志规范 | 本文件 H6 |
| 看文档层级 | 本文件 H7 |
| 查术语规范 | [TERMINOLOGY.md](content/guides/governance/TERMINOLOGY.md) |
| 查角色分类 | [ROLE_CLASSIFICATION.md](content/guides/governance/ROLE_CLASSIFICATION.md) |
| 查设计理念 | [content/guides/](content/guides/) |
| 查经验沉淀 | [content/insights/](content/insights/) |
| 查 SOP 流程 | [content/SOP/INDEX.md](content/SOP/INDEX.md) |
| 查决策历史 | `.ctx/logs/DECISION_LOG.md` |
| 查执行日志 | `.ctx/logs/YYYY-MM-EXECUTION_LOG.md` |
| 查全局规则 | [.github/copilot-instructions.md](.github/copilot-instructions.md) |
| 查母本链路 | [SSOT_INDEX.md](SSOT_INDEX.md) |
| 取用模板 | `content/references/模板库/` |

---

---

# 乙部：具体执行事项（常为新的）

> **常为新的原则**：
> 1. 每个条目必须有**引用流程**——指向甲部（H1~H8）或 guides 或 insights 中的对应原则
> 2. 条目完成后，在确认已写入【执行日志】的前提下，**直接删去**
> 3. 补充新的执行事项时，必须标注引用流程
> 4. 已完成事项的详细信息归档于 `.ctx/logs/DECISION_LOG.md` 和月度执行日志
> 5. 大任务必须按 H6.4 分片规则提前拆分

---

## 持续任务

> 持续任务没有"完成"状态，只有"进行中"或"暂停"。它们代表需要持续关注的工作方向。

| ID | 事项 | 引用流程 | 修改对象 | 状态 |
|----|------|---------|---------|------|
| C-1 | **JS 组件化**：完善 `docs/src/` 下的 JS 组件（renderHeader/renderSidebar/renderFooter/renderCalendar 等），使 HTML 仅需引用/调用特定 JS 组件即可实现功能，消除 HTML 中的硬编码逻辑 | H2.4 规则 2（方向指引）→ [SOP_WEB.md](content/guides/design/SOP_WEB.md) | `docs/src/*.js` + `docs/*.html` | 🔄 持续 |
| C-2 | **一改具改巡检**：定期检查仓库中是否存在信息重复散落，发现后归并至权威源 | H3.1 + H7.2 | 全仓库 | 🔄 持续 |
| C-3 | **经验蒸馏**：从执行日志和决策日志中提炼可复用模式，写入 insights | H3.3 + H6.1 → [党支部管理与实务经验沉淀.md](content/insights/党支部管理与实务经验沉淀.md) | `content/insights/` | 🔄 持续 |

---

## P1 — 紧急/阻塞项

| ID | 事项 | 类型 | 引用流程 | 修改对象 | 状态 |
|----|------|------|---------|---------|------|
| （当前无P1事项） | | | | | |

---

## P2 — 功能开发

| ID | 事项 | 类型 | 引用流程 | 修改对象 | 状态 |
|----|------|------|---------|---------|------|
| T55-1 | F.2 品牌活动网页架构设计（1/3）：确认双轨工作流是否已稳定，可否启动品牌维度 | 治理 | H2.2 差异化视图 + [BRAND_ACTIVITY.md](content/guides/design/BRAND_ACTIVITY.md) | BRAND_ACTIVITY.md | ✅ 已完成 |
| T55-2 | F.2 品牌活动网页架构设计（2/3）：更新 BRAND_ACTIVITY.md 状态为 active，补充与当前架构的对接方案 | 功能 | H2.4 规则 2 → [SOP_WEB.md](content/guides/design/SOP_WEB.md) | BRAND_ACTIVITY.md, docs/src/ | ⬜ 待启动 |
| T55-3 | F.2 品牌活动网页架构设计（3/3）：实现品牌活动网页模块 | 功能 | H2.4 规则 2 | docs/src/ | ⬜ 待启动 |
| T55-4 | F.3 SOP 反馈卡系统（1/3）：确认当前反馈卡模板与网页意见反馈功能的覆盖范围 | 治理 | H2.4 规则 2 → [SOP_WEB.md](content/guides/design/SOP_WEB.md) | FEEDBACK_FORM.md, docs/src/ | ✅ 已完成 |
| T55-5 | F.3 SOP 反馈卡系统（2/3）：补齐四类别输入+整合迭代功能 | 功能 | H2.4 规则 2 | docs/src/ | ⬜ 待启动 |
| T55-6 | F.3 SOP 反馈卡系统（3/3）：反馈卡下载/提交+架构升级 | 功能 | H2.4 规则 2 | docs/src/ | ⬜ 待启动 |
| T55-7 | 代码术语对齐：visitor-observe → participant-observe（5处引用，auth.js/ws-disc-commissioner-entry.js/ws-visitor-entry.js/header.js） | 治理 | H3.1 一改具改 + H4.3 术语规范 | docs/src/ | ⬜ 待启动 |

---

## P3 — 治理/测试/文档

| ID | 事项 | 类型 | 引用流程 | 修改对象 | 状态 |
|----|------|------|---------|---------|------|
| （当前无P3事项） | | | | | |

---

# 丙部：待决策事项

> **丙部是"待决策"的问题池**——AI 拿不定主意的事项提交书记决策。
> 生命周期：写入 → next_prompt 提交 → 书记决策 → Decision Log 归档 → 执行 → 从丙部删除
> 详细规则见甲部 H1.8

当前无待决策事项

---

## 待讨论议题

| # | 作用域 | 问题 | 引用流程 |
|---|--------|------|---------|
| D.1 | Global | ~~`content/guides/governance/AGENT_HANDBOOK.md` 命名是否合理？~~ 已迁移至 `content/guides/governance/AGENT_HANDBOOK.md` | H4.3 术语规范 — 已解决 |
| D.2 | Global | `content/references/` 作为只读引用是否需要更严格的目录保护？ | H7.1 文档权威层级 |

---

## 暂缓议题

> **STRICT CONSTRAINT: The Agent MUST NOT modify or attempt to resolve these issues until explicitly unlocked by the 书记.**

| # | 作用域 | 问题 | 悬置原因 |
|---|--------|------|---------|
| ~~F.1~~ | ~~Global~~ | ~~党小组工作手册（块块组长专用）尚未建设~~ | ~~本轮先通过 SOP 规定块块职责~~ → 已激活为丙部 P.2 |
| ~~F.2~~ | ~~Global~~ | ~~品牌活动网页架构设计~~ | ~~待双轨工作流稳定后再上线~~ → 已激活为乙部 T55-1~T55-3 |
| ~~F.3~~ | ~~Global~~ | ~~SOP 反馈卡系统~~ | ~~待主页+通知系统稳定后激活~~ → 已激活为乙部 T55-4~T55-6 |


