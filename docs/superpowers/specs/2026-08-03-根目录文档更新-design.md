# 根目录文档更新 · 设计文档

> 日期：2026-08-03
> 类型：spec（brainstorming 产物）
> 关联任务：CLAUDE.md 乙部 T-118（治理文档完善+用户文档体系梳理，本任务部分推进）
> 触发原因：P1 后端基建落地（Node 一体化后端）导致根目录文档大面积过时

## 一、背景与目标

2026-08-02~03 完成的 P1 后端基建（`server/` Node 一体化服务：Express + better-sqlite3，同源托管 `docs/` 静态 + `/api/v1` REST，16 项测试全绿）使仓库从"纯前端静态"变为"静态前端 + 本地后端"双形态。当前根目录文档仍停留在 2026-07-31 状态，存在大量与磁盘事实不符的计数、已删除文件引用、过时的架构表述与路线图描述。

**目标**：将根目录四类文档（`README.md`、`CLAUDE.md`、`content/README.md`、`.ctx/` 审计底座）系统更新至 2026-08-03 的最新状态，做到"文档与代码事实一一对应"。

**语言表达方向**（书记要求探讨的部分）：对 `README.md` 采用**读者旅程**逻辑组织（方案 B，已批准），用通俗友好、术语精确的语言重写，技术细节集中收尾。

## 二、范围

| 文件 | 更新程度 | 说明 |
|------|---------|------|
| `README.md` | 全面重构 | 按方案 B 重排为九章，修复全部事实错误 |
| `CLAUDE.md` | 轻量更新 | YAML（last_updated + related_files）+ 乙部 T-142 状态 |
| `content/README.md` | 仅时间戳 | 内容已最新，仅 last_updated 2026-07-21 → 2026-08-03 |
| `.ctx/SNAPSHOT.md` | 升级 v15 | 拓扑/计数/理论/里程碑四项更新 |
| `.ctx/TIMESTAMPS.md` | 批量刷新 | 时间戳 + server/ 段 + 已删文件清理 + 新增文件补登 |

范围外：content/ 各子目录文档（本轮不涉及）、docs/src/ 代码、.ctx/logs/ 执行日志。

## 三、README.md 重构（方案 B：读者旅程）

### 3.1 新章节结构（九章，编号连续）

按"想用→能用→会用→懂它"组织：

| 章 | 标题 | 内容来源 |
|----|------|---------|
| 一 | 这是什么？ | 现有"一"，更新 Phase 表述 |
| 二 | 怎么开始用？ | 现有"三"提前，改写为快速上手 |
| 三 | 我能用它做什么？ | 现有"二" |
| 四 | 我该看什么？ | 现有"四"角色路径 + "七"内容地图合并 |
| 五 | 出问题怎么办？ | 现有"五" |
| 六 | 理解这个系统 | 现有"六"理论 |
| 七 | 系统架构与部署 | 现有"技术架构速览"重构（纳入 server/） |
| 八 | 迭代路线图 | 现有"九" |
| 九 | License | 现有 License |

### 3.2 各章要点

**一、这是什么？**
- 保留"制度即代码 / 角色即视图 / 经验可传承"三件事
- 修正末段："当前处于 Phase 1（纯静态部署）阶段，后续规划接入后端与工作流引擎" → "系统已接入一体化后端（2026-08），支持账号登录与数据持久化"

**二、怎么开始用？**（快速上手，提前到第二章）
- 路径一（最快体验）：公网静态演示 `https://czhgo.github.io/GSM1921-SOP/`，无需安装（开发演示账号模式）
- 路径二（完整运行）：克隆仓库 → `cd server && npm install && npm start` → 访问 `http://127.0.0.1:3000/login.html`（账号密码登录 → API 数据源）
- 说明"双击 docs/index.html"的纯静态说法已过时，不再作为推荐启动方式；演示账号信息指向 `server/README.md`，README 不重复凭证细节
- 保留"第二步：找到你的角色入口""第三步：按角色阅读对应文档"的精简版（指向第四章）

**三、我能用它做什么？**
- 成员功能列表保留（活动日历/个人考勤/支委工作透明化/标准流程查阅/意见反馈），补充"个人待办"（最小三成本落地，通知/活动/专班自动派生）
- 角色工作台表格重构：双列"党建工作台 | 党务管理" → 单列"工作台（党建 + 党务 + 待办合一）"，六角色：
  - 党支书：全局概况 + 常设赋权 + 事项管理 + 通知发布 + 待办
  - 组织委员：专班建设 + 考察上传 + 人才库 + 发展党员 + 待办
  - 宣传委员：宣传任务 + 项目看板 + 档案归档 + 周报报送 + 待办
  - 纪检委员：考勤管理 + 考察管理 + 活动监督复盘 + 补课制度 + 公邮管理 + 待办
  - 党小组组长：本组活动组织 + 考勤上传 + 考察上传 + 复盘提交 + 待办
  - 普通成员：成员只读面板（活动动态 + 个人考勤 + 个人待办）
- 移除"组织者/深度参与者"独立行（v13 已并入首页"我的角色"，实现时用 Grep 核对角色术语）
- 保留"管理事、服务人"收尾句

**四、我该看什么？**
- 角色阅读路径三行保留（普通成员/支委与党小组组长/系统维护者），核对链接有效性（重点：SECRETARY_PRONOUNCEMENTS.md、sop/INDEX.md、ARCHITECTURE.md、OPERATIONS_GUIDE.md、DOC_MAP.md、SSOT_INDEX.md、CLAUDE.md）
- 合并原"七、内容地图：5 个核心问题对应文档"表格，核对"工作流是什么 → CLAUDE.md"表述（现后端已落地，补充"系统怎么跑起来 → server/README.md"一行）
- 系统维护者路径补充 `server/README.md`

**五、出问题怎么办？**
- 保留原文（GitHub Issues 报告 + 执行/决策日志 + 经验沉淀 + 术语不明查 USAGE_POLICY）
- 核对引用路径：`.ctx/logs/YYYY-MM-EXECUTION_LOG.md` 示例、DECISION_LOG.md、USAGE_POLICY.md

**六、理解这个系统**
- 理论列表保留（管理事服务人 / 党建与党务 / 专班 / 条块二元 / 扁平化设计 / 差异化视图）
- 核对"28 条路线级论断"数字与 P-045/P-047 表述（如已变化则修正为最新事实）
- 末尾链接保留（SECRETARY_PRONOUNCEMENTS.md + DEVELOPMENT_PATH.md）

**七、系统架构与部署**（技术集中章）
- 架构表格更新（精确计数，见 §3.3）：
  - 前台（页面）：`docs/` 14 个页面（8 根 + 6 个 workspace 工作台），无 `party/` 目录
  - 中台（逻辑）：`docs/src/entries/` 15 个 entry + components 18 个（17 js + 1 css）+ core 12 个 + modules 1 个 + workflow 6 个
  - 后台（服务）：`docs/src/services/` 20 个 service JS + `docs/src/mock/` 10 个
  - 后端（server/）：Express + better-sqlite3 单进程，同源静态 + `/api/v1` REST（auth/resources/uploads + snapshot 全量写穿）
  - 母本层：`content/02_institution/sop/`
- 数据变更铁律保留（SOP 母本 → 服务层 → 入口 → 页面）
- 部署三种形态：公网 GitHub Pages 静态演示（dev 模式）/ 本地完整运行（node server.js）/ 北大计算中心对接（链接 server/README.md + SCHOOL_IT_DEPLOYMENT.md）

**八、迭代路线图**
- Phase 1 — Static OS：✅ 已完成（GitHub Pages 静态部署）
- Phase 2 — 后端基建：✅ 已完成（2026-08，Node 一体化后端 + 认证 + 数据持久化；删除"Supabase 规划中"表述）
- Phase 3 — Workflow Engine：规划中（保留，措辞更新为"后续规划"）

**九、License**：保留 MIT。

### 3.3 事实更新清单（README 现有错误 → 修正值）

| # | 现有表述 | 问题 | 修正 |
|---|---------|------|------|
| R1 | "docs/（19 个页面，含 workspace/ 和 party/ 子目录）" | 计数错 + party/ 已删 | docs/（14 个页面：8 根 + 6 个 workspace 工作台） |
| R2 | "docs/src/entries/（18 个 entry JS）" | 计数错 | 15 个 |
| R3 | "docs/src/services/（7 个 service JS）" | 计数错 | 20 个 |
| R4 | "纯前端静态架构，无需后端部署，GitHub Pages 直接托管" | 过时（P1 落地） | 快速上手改为双路径（公网演示 + 本地 server） |
| R5 | "Phase 2 — Supabase Backend 规划中" | 不实 | Phase 2 后端基建 ✅（2026-08，Node 一体化） |
| R6 | 章节编号断裂（"技术架构速览"无编号，直接从七跳到九） | 结构缺陷 | 重排为九章连续编号 |
| R7 | 角色表格"组织者/深度参与者"行 | v13 已移除独立视图 | 移除/并入"我的角色"表述（Grep 核对） |
| R8 | "当前处于 Phase 1（纯静态部署）阶段" | 过时 | 更新为后端已接入 |

## 四、CLAUDE.md 轻量更新

| # | 位置 | 现状 | 更新 |
|---|------|------|------|
| C1 | YAML last_updated | "2026-08-01T12:00:00" | "2026-08-03" |
| C2 | YAML related_files | 无 server/ | 追加 `server/` |
| C3 | 乙部 P3 T-142 末尾 | "**待执行**：阶段3（后台数据库+文件存储，需后端支持）" | 改为"**阶段3已完成**：后台数据库 + 文件存储 + 认证 API（P1 后端基建，2026-08-02~03，见 docs/superpowers/plans/2026-08-02-后端基建-P1.md）"；状态 🔄 → ✅ |
| C4 | 乙部 P3 T-118 | "⏳ 待启动" | 备注"2026-08-03 根目录文档更新已部分推进"（保持待启动，剩余部分另议） |

> 注：CLAUDE.md 乙部已有 T-188/T-189（2026-08-01~02 完成项）记录，本次仅做上述四项轻量更新，不做结构性改动（甲部 Harness 稳定不变）。

## 五、content/README.md（仅时间戳）

- YAML last_updated："2026-07-21" → "2026-08-03"
- 正文内容已与最新结构一致（01~05 + insights 六类），本轮不修改正文

## 六、.ctx/SNAPSHOT.md 升级 v15

### 6.1 YAML

- version: "v14" → "v15"
- date/last_updated → 2026-08-03
- milestone → "根目录文档系统更新（README 读者旅程重构）+ P1 后端基建落地"

### 6.2 I. 全局物理拓扑

- `docs/` 描述："9 个根 HTML + workspace/ 子目录" → "8 个根 HTML + workspace/ 子目录（6 个工作台），无 party/ 目录"
- 新增 `server/` 段：`server.js`（入口）+ `app.js`（createApp 工厂）+ `db.js`（11 JSON 资源表）+ `seed.js`（复用前端 mock）+ `routes/`（auth/resources/uploads）+ `test/`（16 用例）+ `data.db`
- 新增 `docs/superpowers/` 段（specs/ + plans/）

### 6.3 III. 计数修正（与磁盘实际核对）

| 项 | v14 现值 | 实际 | 修正为 |
|----|---------|------|--------|
| 根 HTML | 9 个 | 8 个 | 8 个 |
| entries | 16 个 | 15 个 | 15 个 |
| components | 18 个（含 party-cross-nav） | 18 个（17 js + 1 css，含 custom-select，无 party-cross-nav） | 18 个（17 js + 1 css） |
| core | 9 个 | 12 个（+ data-adapter/mock-adapter/api-adapter） | 12 个 |
| mock | 11 个（含 party） | 10 个 | 10 个 |
| modules | 2 个（含 party） | 1 个（references） | 1 个 |
| workflow | 7 个（含 activityRecord） | 6 个 | 6 个 |
| services | 20 个 | 20 个（assignment/permission-manager 已删，secretary-overview/person/todo 已加） | 20 个（清单更新） |
| 代码实现层页面 | 15 页面 | 14 页面 | 14 页面 |
| "### 前端页面（15 个 HTML）" | 表头 15，实际列 14 行 | 14 | 表头改 14 |

- II 分层架构 Layer 4 描述追加 `server/`
- III 页面清单：现有 14 行保留（无 members.html），仅表头计数修正；若清单中含已删文件则移除

### 6.4 IV. 核心理论增补

| 理论 | 核心公式 | 详细文档 |
|------|---------|---------|
| 打卡化判定 | 完成必须对应真实产物——`complete(task)` 副作用集合仅含状态翻转即为打卡化设计缺陷 | insights 工程演进与设计方法论.md §6.23 |

### 6.5 VI. 里程碑增补

| 版本 | 日期 | 里程碑 |
|------|------|--------|
| **v15** | **2026-08-03** | **根目录文档系统更新（README 读者旅程九章重构 + CLAUDE.md/content README/.ctx SNAPSHOT+TIMESTAMPS 同步）+ P1 后端基建落地（Node 一体化服务：Express + better-sqlite3 + auth/resources/uploads API + 16 测试全绿）** |

## 七、.ctx/TIMESTAMPS.md 批量刷新

| # | 位置 | 更新 |
|---|------|------|
| T1 | YAML last_updated | "2026-07-31" → "2026-08-03" |
| T2 | 根目录 README.md | 2026-07-31 → 2026-08-03 |
| T3 | 新增 `server/` 段 | server.js / app.js / db.js / seed.js / routes/auth.js / routes/resources.js / routes/uploads.js / test/*.test.js（8 文件）/ README.md / package.json / package-lock.json / .gitignore / data.db（运行时产物，可标"-"） |
| T4 | 新增 docs/superpowers/ 段 | specs/ 四个（2026-08-01-visitor-visual-redesign / 2026-08-01-homepage-2026-08 / 2026-08-02-真实工作集成 / 2026-08-02-赋权整合闭环）+ plans/ 两个（2026-08-02-后端基建-P1 / 2026-08-02-赋权整合闭环） |
| T5 | 清理已删条目 | docs/members.html、docs/src/entries/members-entry.js、docs/src/mock/party.js、docs/src/modules/party.js、docs/src/components/party-cross-nav.js、docs/src/workflow/activityRecord.js、docs/src/services/assignment.js、docs/src/services/permission-manager.js（如仍存在则标记 🗑️ 已删除或移除行） |
| T6 | 补登新增文件 | components/custom-select.js、core/data-adapter.js、core/mock-adapter.js、core/api-adapter.js、services/secretary-overview.js、services/person.js、services/todo.js（如缺失） |
| T7 | 周期性任务 M6（README 审查） | "待初始化" → 标记本轮已执行（2026-08-03） |
| T8 | 核对各段与磁盘实际一致 | 以 Glob 实测为准，差异项顺带修正 |

## 八、语言表达规范（适用于全部改写内容）

1. **通俗友好**：面向支部成员的"人话"优先，不使用生硬排比、不用"成长空间在于/用武之地还包括"类机械表达
2. **术语精确**：T1 术语必须准确——"党小组组长"（不用"块块组长"）；支部全称"光华管理学院本科生党支部"；"党建工作（三会一课、主题党日、专班）"与"党务工作（民主评议党员、换届选举、发展党员）"区分正确
3. **用户视角无黑话**：L3 技术表达（如"域"）不上页面；技术细节集中在第七章
4. **"管理事、服务人"叙事贯穿**：前六章以"你进来能做什么、怎么看、遇到问题怎么办"为线索，理解章讲理论依据
5. **读者旅程逻辑**：先体验（二）、再功能（三）、再阅读（四）、再排障（五）、最后理解（六、七）
6. **计数精确**：所有页面/文件计数以磁盘实际为准（14/15/20/10/12/18/6/1 + server/）

## 九、一改具改与验收（H2.1）

### 9.1 执行前检查

- 用 Glob 实测下列计数：`docs/*.html`（8）、`docs/workspace/*.html`（6）、`entries/*.js`（15）、`services/*.js`（20）、`mock/*.js`（10）、`core/*.js`（12）、`components/*`（18）、`workflow/*.js`（6）、`modules/*.js`（1）、`server/**`（按 §六 清单）
- 用 Grep 确认待删除文件已不在磁盘（members.html、party*.js、assignment.js、permission-manager.js、activityRecord.js、party-cross-nav.js）

### 9.2 执行后零残留验证（Grep 全仓库）

以下关键词不得再出现于根目录四文档（若出现于历史日志属正常，不处理）：

- `party/` 目录引用、`members.html`、`members-entry.js`
- `纯前端静态`、`无需后端部署`（README 中）
- `19 个页面`、`18 个 entry`、`7 个 service`（README 中）
- `Supabase`（路线图中）
- SNAPSHOT 中 `9 个根 HTML`、`16 个 entry`、`11 个`、`2 个`、`7 个`（workflow/modules 场景）、`15 页面`

### 9.3 结构验证

- README 九章标题编号连续（一~九），无重复、无跳号
- README 所有站内链接用 Read/WebFetch 方式核对存在性（重点：四章角色路径、五章日志路径、七章 server/README.md + SCHOOL_IT_DEPLOYMENT.md）
- SNAPSHOT/TIMESTAMPS/CLAUDE.md YAML last_updated 全部为 2026-08-03

## 十、交付物清单

1. `README.md` — 九章读者旅程重构完成
2. `CLAUDE.md` — YAML + T-142 + T-118 四项更新
3. `content/README.md` — 时间戳更新
4. `.ctx/SNAPSHOT.md` — v15 升级（拓扑/计数/理论/里程碑）
5. `.ctx/TIMESTAMPS.md` — 批量刷新（含 server/ 段、M6 触发标记）
6. 本 spec + 实施计划提交 git（commit 自动，push 须书记确认）
