---
title: "2026-08-02 后端基建（P1）实施计划"
type: plan
role: "[工程师]+[AI]"
status: active
created_at: "2026-08-02"
related_files:
  - docs/superpowers/specs/2026-08-02-真实工作集成-design.md
  - docs/src/core/data-adapter.js
  - docs/src/core/api-adapter.js
  - docs/src/services/runtime.js
---

# 后端基建（P1）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立 Node.js 一体化后端（Express + SQLite + 附件上传），使现有 14 个前端页面通过 DataAdapter 从 localStorage 平滑切换到服务器数据源。

**Architecture:** 单 Node 进程同时服务静态页面与 REST API。前端复用已就绪的 DataAdapter 抽象层：`init()` 从 API 拉取全量数据填充 mockDB 缓存（读路径），`persist()` 全量快照写回后端（写穿透路径，service 层零改动）。认证用简单 token（sessions 表），前端 `AuthStore` 登录改为调 API。附件经 multer 落盘 `uploads/`，静态访问受登录校验。

**Tech Stack:** Node.js 18+、Express 4、better-sqlite3、multer、node:test（内置测试框架）、Playwright（端到端验证）。

**前置知识：**
- 前端 `docs/src/core/data-adapter.js` 已实现 mock/api 双数据源与 `persist()` 写穿透缓存；api 模式下 `init()` 会调用 adapter 的 10 个资源分组 `list()` 方法填充 mockDB。
- 前端 `docs/src/core/api-adapter.js` 已实现全部 REST 调用（Bearer token 认证），路由设计为 `/api/v1/...`。
- 前端 `docs/src/services/runtime.js` 已 `registerApiAdapter(ApiAdapter)` 并 `setDataSource('mock')`，P1 目标即切换为 `'api'`。
- mock 纯数据模块（`docs/src/mock/people.js`、`activities.js`、`notices.js`、`taskforces.js`、`seed.js`）无 window 依赖，Node 可直接 `import()`。
- Node 18+ 内置 `fetch` 与 `node:test`，无需安装测试框架。

**目录约定：** 后端全部位于 `server/`，与前端 `docs/` 平级。

---

## 文件结构总览

```
server/
├── package.json          # 依赖与脚本
├── server.js             # Express 入口：静态托管 + 路由挂载
├── app.js                # 可测试的 Express 应用工厂（listen 由 server.js 执行）
├── db.js                 # better-sqlite3 初始化、建表、查询辅助
├── seed.js               # 从 docs/src/mock/*.js 导入种子数据写入 SQLite
├── auth.js               # token 签发/校验中间件（sessions 表）
├── routes/
│   ├── auth.js           # POST /api/v1/auth/login|logout, GET /me
│   ├── resources.js      # 10 个资源 GET list + GET /bootstrap + POST /snapshot
│   └── uploads.js        # POST /api/v1/uploads + 受保护 GET /uploads/*
└── test/
    ├── db.test.js
    ├── auth.test.js
    ├── resources.test.js
    └── uploads.test.js
```

修改的前端文件：
- Modify: `docs/src/services/runtime.js` — 根据会话 token 切换数据源
- Modify: `docs/src/services/auth.js` — `AuthStore.login` 调后端 API 获取 token
- Modify: `docs/src/core/bootstrap.js` — 启动时若已有 token 则 `setDataSource('api')`

---

## Task 1: Node 项目骨架与可测试应用工厂

**Files:**
- Create: `server/package.json`
- Create: `server/app.js`
- Create: `server/server.js`
- Test: `server/test/skeleton.test.js`

- [ ] **Step 1: 写失败测试**

```js
// server/test/skeleton.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { createApp } from '../app.js';

test('健康检查返回 ok', async () => {
  const app = createApp({ dbPath: ':memory:' });
  const server = app.listen(0);
  const { port } = server.address();
  const res = await fetch(`http://127.0.0.1:${port}/api/v1/health`);
  assert.equal(res.status, 200);
  const body = await res.json();
  assert.equal(body.ok, true);
  server.close();
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test server/test/skeleton.test.js`
Expected: FAIL — `Cannot find module '../app.js'`

- [ ] **Step 3: 创建 package.json 与 app 工厂**

```json
// server/package.json
{
  "name": "gsm1921-sop-server",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "better-sqlite3": "^12.0.0",
    "express": "^4.19.0",
    "multer": "^1.4.5-lts.1"
  }
}
```

```js
// server/app.js — Express 应用工厂（可注入 dbPath 供测试隔离）
import express from 'express';
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { initDb } from './db.js';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const DOCS_DIR = path.resolve(__dirname, '../docs');

export function createApp({ dbPath = ':memory:' } = {}) {
  const app = express();
  app.use(express.json({ limit: '2mb' }));

  // 健康检查
  app.get('/api/v1/health', (req, res) => res.json({ ok: true }));

  // 静态托管前端（需先建表再注册后续路由）
  app.locals.db = initDb(dbPath);

  // 静态文件（最后注册，避免吞掉 API 404 判断）
  app.use(express.static(DOCS_DIR));

  return app;
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd server && npm test` 或 `node --test server/test/skeleton.test.js`
Expected: PASS — 健康检查 200

- [ ] **Step 5: 创建 server.js 入口**

```js
// server/server.js
import { createApp } from './app.js';
import { fileURLToPath } from 'node:url';

const PORT = process.env.PORT || 3000;
const DB_PATH = process.env.DB_PATH || fileURLToPath(new URL('./data.db', import.meta.url));

const app = createApp({ dbPath: DB_PATH });
app.listen(PORT, () => {
  console.log(`[server] 光华党支部管理引擎后端已启动: http://localhost:${PORT}`);
});
```

- [ ] **Step 6: Commit**

```bash
git add server/package.json server/app.js server/server.js server/test/skeleton.test.js
git commit -m "feat(server): Express 骨架 + 健康检查 + 静态托管"
```

> **Task 1 已实施偏差记录（2026-08-02，Windows/Node 24 实证）：**
> 1. `better-sqlite3` 版本 `^11.0.0` → `^12.0.0`：Node 24（ABI 137）无 v11 预编译二进制，装 v11 会退回 node-gyp 源码编译（Windows 无 VS C++ 工具则失败）；v12.11.1 有 `node-v137-win32-x64` 预编译。
> 2. `npm test` 脚本由 `node --test test/` → `node --test`：Node 24 在 Windows 下将目录参数当作模块路径解析报 `MODULE_NOT_FOUND`；裸 `node --test` 自动发现 `test/*.test.js`（跨平台安全）。
> 3. `server.js` 的 `DB_PATH` 由 `.pathname` → `fileURLToPath(new URL(...))`：Windows 下 `.pathname` 产生 `/D:/...` 非法前缀导致 SQLite 打不开库。**后续所有 server.js 片段均用此写法。**

---

## Task 2: SQLite 数据库层

**Files:**
- Create: `server/db.js`
- Test: `server/test/db.test.js`

- [ ] **Step 1: 写失败测试**

```js
// server/test/db.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { initDb } from '../db.js';

test('initDb 建表成功，核心表存在', () => {
  const db = initDb(':memory:');
  const tables = db.prepare("SELECT name FROM sqlite_master WHERE type='table'").all()
    .map(r => r.name);
  for (const t of ['users', 'activities', 'notices', 'todos', 'sessions']) {
    assert.ok(tables.includes(t), `缺少表 ${t}`);
  }
});

test('upsertJson 支持嵌套 JSON 字段读写', () => {
  const db = initDb(':memory:');
  db.prepare('INSERT INTO activities (id, data) VALUES (?, ?)')
    .run('act-test', JSON.stringify({ id: 'act-test', assignments: [{ personId: 'p1', role: 'organizer' }] }));
  const row = db.prepare('SELECT data FROM activities WHERE id = ?').get('act-test');
  const parsed = JSON.parse(row.data);
  assert.equal(parsed.assignments[0].personId, 'p1');
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test server/test/db.test.js`
Expected: FAIL — `Cannot find module '../db.js'`

- [ ] **Step 3: 实现 db.js**

```js
// server/db.js — SQLite 初始化与建表
import Database from 'better-sqlite3';

// 资源表：每张表 id 主键 + data JSON（保持与前端数据结构完全一致，嵌套字段零损失）
const RESOURCE_TABLES = [
  'users', 'activities', 'tasks', 'attendances', 'inspections',
  'taskforces', 'notices', 'todos', 'assignments', 'handovers', 'makeup_tasks',
];

const SCHEMA = `
CREATE TABLE IF NOT EXISTS sessions (
  token TEXT PRIMARY KEY,
  person_id TEXT NOT NULL,
  created_at TEXT NOT NULL
);
CREATE TABLE IF NOT EXISTS attachments (
  id TEXT PRIMARY KEY,
  filename TEXT NOT NULL,
  path TEXT NOT NULL,
  size INTEGER NOT NULL,
  uploaded_by TEXT NOT NULL,
  uploaded_at TEXT NOT NULL
);
`;

export function initDb(dbPath) {
  const db = new Database(dbPath);
  db.pragma('journal_mode = WAL');
  db.exec(SCHEMA);
  for (const t of RESOURCE_TABLES) {
    db.exec(`CREATE TABLE IF NOT EXISTS ${t} (id TEXT PRIMARY KEY, data TEXT NOT NULL)`);
  }
  return db;
}

// 辅助：整表替换写入（写穿透快照用）
export function replaceCollection(db, table, rows) {
  db.exec(`DELETE FROM ${table}`);
  const stmt = db.prepare(`INSERT OR REPLACE INTO ${table} (id, data) VALUES (?, ?)`);
  for (const row of rows) {
    stmt.run(row.id, JSON.stringify(row));
  }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `node --test server/test/db.test.js`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add server/db.js server/test/db.test.js
git commit -m "feat(server): SQLite 建表层 + JSON 资源表"
```

---

## Task 3: 种子数据导入

**Files:**
- Create: `server/seed.js`
- Test: `server/test/seed.test.js`

- [ ] **Step 1: 写失败测试**

```js
// server/test/seed.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { initDb } from '../db.js';
import { seedDatabase } from '../seed.js';

test('seedDatabase 写入 users 与 activities', async () => {
  const db = initDb(':memory:');
  await seedDatabase(db);
  const users = db.prepare('SELECT data FROM users').all().map(r => JSON.parse(r.data));
  const acts = db.prepare('SELECT data FROM activities').all().map(r => JSON.parse(r.data));
  assert.ok(users.length >= 50, `人员应 >= 50，实际 ${users.length}`);
  assert.ok(acts.length >= 25, `活动应 >= 25，实际 ${acts.length}`);
  assert.ok(users.some(u => u.role === 'secretary'), '应包含书记角色');
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test server/test/seed.test.js`
Expected: FAIL — `Cannot find module '../seed.js'`

- [ ] **Step 3: 实现 seed.js**

```js
// server/seed.js — 从前端 mock 纯数据模块导入种子
import { initDb, replaceCollection } from './db.js';

export async function seedDatabase(db) {
  const [peopleMod, activitiesMod, noticesMod, taskforcesMod, seedMod] = await Promise.all([
    import('../docs/src/mock/people.js'),
    import('../docs/src/mock/activities.js'),
    import('../docs/src/mock/notices.js'),
    import('../docs/src/mock/taskforces.js'),
    import('../docs/src/mock/seed.js'),
  ]);

  replaceCollection(db, 'users', peopleMod.PEOPLE);
  replaceCollection(db, 'activities', activitiesMod.ACTIVITIES);
  replaceCollection(db, 'notices', noticesMod.MOCK_NOTICES);
  replaceCollection(db, 'taskforces', taskforcesMod.MOCK_TASKFORCES || []);
  replaceCollection(db, 'tasks', seedMod.SEED_TASKS || []);
  replaceCollection(db, 'assignments', seedMod.SEED_ASSIGNMENTS || []);
  replaceCollection(db, 'handovers', seedMod.SEED_HANDOVERS || []);
}
```

> 注意：`docs/src/mock/taskforces.js` 的导出名需在实现时确认（可能是 `MOCK_TASKFORCES` 或 `TASKFORCES`），若不符改为实际导出名。

- [ ] **Step 4: 运行测试确认通过**

Run: `node --test server/test/seed.test.js`
Expected: PASS

- [ ] **Step 5: server.js 启动时自动 seed（仅空库）**

```js
// server/server.js（修改）
import { createApp } from './app.js';
import { initDb } from './db.js';
import { seedDatabase } from './seed.js';
import { fileURLToPath } from 'node:url';

const PORT = process.env.PORT || 3000;
const DB_PATH = process.env.DB_PATH || fileURLToPath(new URL('./data.db', import.meta.url));

const db = initDb(DB_PATH);
if (db.prepare('SELECT COUNT(*) AS c FROM users').get().c === 0) {
  await seedDatabase(db);
  console.log('[server] 已导入种子数据');
}
const app = createApp({ dbPath: DB_PATH });
app.listen(PORT, () => console.log(`[server] http://localhost:${PORT}`));
```

- [ ] **Step 6: Commit**

```bash
git add server/seed.js server/test/seed.test.js server/server.js
git commit -m "feat(server): 种子数据导入（复用前端 mock 纯数据模块）"
```

---

## Task 4: 认证 API（token 会话）

**Files:**
- Create: `server/routes/auth.js`
- Modify: `server/app.js`
- Test: `server/test/auth.test.js`

- [ ] **Step 1: 写失败测试**

```js
// server/test/auth.test.js
import { test, before, after } from 'node:test';
import assert from 'node:assert/strict';
import { createApp } from '../app.js';
import { seedDatabase } from '../seed.js';

let server, base, app;

before(async () => {
  app = createApp({ dbPath: ':memory:' });
  await seedDatabase(app.locals.db);
  server = app.listen(0);
  base = `http://127.0.0.1:${server.address().port}`;
});
after(() => server.close());

test('登录成功返回 token 与用户信息', async () => {
  const res = await fetch(`${base}/api/v1/auth/login`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ personId: 'p13' }),
  });
  assert.equal(res.status, 200);
  const body = await res.json();
  assert.ok(body.token, '应返回 token');
  assert.equal(body.user.id, 'p13');
  assert.equal(body.user.role, 'secretary');
});

test('登录失败（未知人员）返回 401', async () => {
  const res = await fetch(`${base}/api/v1/auth/login`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ personId: 'nobody' }),
  });
  assert.equal(res.status, 401);
});

test('me 接口携带 token 返回当前用户', async () => {
  const loginRes = await fetch(`${base}/api/v1/auth/login`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ personId: 'p11' }),
  });
  const { token } = await loginRes.json();
  const res = await fetch(`${base}/api/v1/auth/me`, { headers: { Authorization: `Bearer ${token}` } });
  assert.equal(res.status, 200);
  const body = await res.json();
  assert.equal(body.id, 'p11');
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test server/test/auth.test.js`
Expected: FAIL — 404 路由不存在

- [ ] **Step 3: 实现 auth 路由与 token 中间件**

```js
// server/routes/auth.js
import { Router } from 'express';
import { randomUUID } from 'node:crypto';

export function createAuthRouter(db) {
  const router = Router();

  router.post('/login', (req, res) => {
    const { personId } = req.body || {};
    const userRow = db.prepare('SELECT data FROM users WHERE id = ?').get(personId);
    if (!userRow) return res.status(401).json({ error: '未知人员' });
    const user = JSON.parse(userRow.data);
    const token = randomUUID();
    db.prepare('INSERT INTO sessions (token, person_id, created_at) VALUES (?, ?, ?)')
      .run(token, personId, new Date().toISOString());
    res.json({ token, user });
  });

  router.post('/logout', (req, res) => {
    const token = (req.headers.authorization || '').replace('Bearer ', '');
    if (token) db.prepare('DELETE FROM sessions WHERE token = ?').run(token);
    res.status(204).end();
  });

  router.get('/me', (req, res) => {
    const token = (req.headers.authorization || '').replace('Bearer ', '');
    const session = token ? db.prepare('SELECT * FROM sessions WHERE token = ?').get(token) : null;
    if (!session) return res.status(401).json({ error: '未登录' });
    const user = JSON.parse(db.prepare('SELECT data FROM users WHERE id = ?').get(session.person_id).data);
    res.json(user);
  });

  return router;
}

// token 校验中间件（保护资源写接口）
export function requireAuth(db) {
  return (req, res, next) => {
    const token = (req.headers.authorization || '').replace('Bearer ', '');
    const session = token ? db.prepare('SELECT * FROM sessions WHERE token = ?').get(token) : null;
    if (!session) return res.status(401).json({ error: '未登录' });
    req.session = session;
    next();
  };
}
```

- [ ] **Step 4: 挂载路由**

```js
// server/app.js（修改）
import { createAuthRouter } from './routes/auth.js';
import { createResourcesRouter } from './routes/resources.js';
import { createUploadsRouter } from './routes/uploads.js';

// app.locals.db = initDb(dbPath) 之后追加：
app.use('/api/v1/auth', createAuthRouter(app.locals.db));
app.use('/api/v1', createResourcesRouter(app.locals.db));
app.use('/api/v1', createUploadsRouter(app.locals.db));
```

> Task 5/7 会创建 `routes/resources.js` 与 `routes/uploads.js`，此处在 Task 5/7 完成后再导入。为避免中间提交挂掉，Task 4 先仅挂 auth 路由，其余在各自 Task 挂载。

- [ ] **Step 5: 运行测试确认通过**

Run: `node --test server/test/auth.test.js`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add server/routes/auth.js server/app.js server/test/auth.test.js
git commit -m "feat(server): token 会话认证 API"
```

---

## Task 5: 资源读取 API（10 个 list + bootstrap）

**Files:**
- Create: `server/routes/resources.js`
- Modify: `server/app.js`
- Test: `server/test/resources.test.js`

- [ ] **Step 1: 写失败测试**

```js
// server/test/resources.test.js
import { test, before, after } from 'node:test';
import assert from 'node:assert/strict';
import { createApp } from '../app.js';
import { seedDatabase } from '../seed.js';

let server, base;

before(async () => {
  const app = createApp({ dbPath: ':memory:' });
  await seedDatabase(app.locals.db);
  server = app.listen(0);
  base = `http://127.0.0.1:${server.address().port}`;
});
after(() => server.close());

test('activities list 返回活动数组', async () => {
  const res = await fetch(`${base}/api/v1/activities`);
  assert.equal(res.status, 200);
  const list = await res.json();
  assert.ok(Array.isArray(list));
  assert.ok(list.length >= 25);
  assert.ok(list[0].assignments, '保留嵌套 assignments 字段');
});

test('bootstrap 返回全部资源分组', async () => {
  const res = await fetch(`${base}/api/v1/bootstrap`);
  const body = await res.json();
  for (const k of ['activities', 'todos', 'notices', 'users']) {
    assert.ok(Array.isArray(body[k]), `bootstrap 缺少 ${k}`);
  }
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test server/test/resources.test.js`
Expected: FAIL — 404

- [ ] **Step 3: 实现 resources 路由**

```js
// server/routes/resources.js
import { Router } from 'express';

// 资源名 → 表名映射（与 data-adapter 的分组名对齐）
const RESOURCE_TABLES = {
  activities: 'activities',
  tasks: 'tasks',
  attendances: 'attendances',
  inspections: 'inspections',
  taskforces: 'taskforces',
  notices: 'notices',
  todos: 'todos',
  assignments: 'assignments',
  handovers: 'handovers',
  makeupTasks: 'makeup_tasks',
  users: 'users',
};

function listTable(db, table) {
  return db.prepare(`SELECT data FROM ${table}`).all().map(r => JSON.parse(r.data));
}

export function createResourcesRouter(db) {
  const router = Router();

  // 每个资源 GET list
  for (const [name, table] of Object.entries(RESOURCE_TABLES)) {
    router.get(`/${name}`, (req, res) => res.json(listTable(db, table)));
  }

  // 全量引导：一次拉取全部资源（data-adapter init() 在 api 模式的填充来源）
  router.get('/bootstrap', (req, res) => {
    const out = {};
    for (const [name, table] of Object.entries(RESOURCE_TABLES)) {
      out[name] = listTable(db, table);
    }
    res.json(out);
  });

  return router;
}
```

- [ ] **Step 4: 挂载路由到 app.js**

```js
// server/app.js（修改，在 auth 之后）
import { createResourcesRouter } from './routes/resources.js';
app.use('/api/v1', createResourcesRouter(app.locals.db));
```

- [ ] **Step 5: 运行测试确认通过**

Run: `node --test server/test/resources.test.js`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add server/routes/resources.js server/app.js server/test/resources.test.js
git commit -m "feat(server): 资源读取 API + bootstrap 全量引导"
```

---

## Task 6: 写穿透快照 API

**Files:**
- Modify: `server/routes/resources.js`
- Test: `server/test/snapshot.test.js`

- [ ] **Step 1: 写失败测试**

```js
// server/test/snapshot.test.js
import { test, before, after } from 'node:test';
import assert from 'node:assert/strict';
import { createApp } from '../app.js';
import { seedDatabase } from '../seed.js';

let server, base;

before(async () => {
  const app = createApp({ dbPath: ':memory:' });
  await seedDatabase(app.locals.db);
  server = app.listen(0);
  base = `http://127.0.0.1:${server.address().port}`;
});
after(() => server.close());

test('snapshot 全量覆盖保存，重启后可读回', async () => {
  const tokenRes = await fetch(`${base}/api/v1/auth/login`, {
    method: 'POST', headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ personId: 'p13' }),
  });
  const { token } = await tokenRes.json();

  const activities = await (await fetch(`${base}/api/v1/activities`)).json();
  activities.push({ id: 'act-new', title: '新增测试活动', date: '2026-08-30' });

  const res = await fetch(`${base}/api/v1/snapshot`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` },
    body: JSON.stringify({ activities }),
  });
  assert.equal(res.status, 204);

  const after = await (await fetch(`${base}/api/v1/activities`)).json();
  assert.ok(after.some(a => a.id === 'act-new'), '快照保存后应能读回新增活动');
});

test('snapshot 未登录返回 401', async () => {
  const res = await fetch(`${base}/api/v1/snapshot`, {
    method: 'POST', headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ activities: [] }),
  });
  assert.equal(res.status, 401);
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test server/test/snapshot.test.js`
Expected: FAIL — snapshot 404

- [ ] **Step 3: 实现 snapshot 接口**

```js
// server/routes/resources.js（在文件顶部导入 requireAuth 与 replaceCollection）
import { requireAuth } from './auth.js';
import { replaceCollection } from '../db.js';

// 在 createResourcesRouter 内、bootstrap 之后追加：
router.post('/snapshot', requireAuth(db), (req, res) => {
  const payload = req.body || {};
  for (const [name, table] of Object.entries(RESOURCE_TABLES)) {
    if (Array.isArray(payload[name])) {
      replaceCollection(db, table, payload[name]);
    }
  }
  res.status(204).end();
});
```

- [ ] **Step 4: 运行测试确认通过**

Run: `node --test server/test/snapshot.test.js`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add server/routes/resources.js server/test/snapshot.test.js
git commit -m "feat(server): 全量快照写穿透 API（认证保护）"
```

---

## Task 7: 附件上传与受保护静态访问

**Files:**
- Create: `server/routes/uploads.js`
- Modify: `server/app.js`
- Test: `server/test/uploads.test.js`

- [ ] **Step 1: 写失败测试**

```js
// server/test/uploads.test.js
import { test, before, after } from 'node:test';
import assert from 'node:assert/strict';
import { randomUUID } from 'node:crypto';
import { createApp } from '../app.js';
import { seedDatabase } from '../seed.js';

let server, base;

before(async () => {
  const app = createApp({ dbPath: ':memory:' });
  await seedDatabase(app.locals.db);
  server = app.listen(0);
  base = `http://127.0.0.1:${server.address().port}`;
});
after(() => server.close());

test('登录后上传附件返回元数据', async () => {
  const login = await (await fetch(`${base}/api/v1/auth/login`, {
    method: 'POST', headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ personId: 'p13' }),
  })).json();

  const form = new FormData();
  form.append('file', new File([Buffer.from('fake-png')], 'test.png', { type: 'image/png' }));
  const res = await fetch(`${base}/api/v1/uploads`, {
    method: 'POST',
    headers: { Authorization: `Bearer ${login.token}` },
    body: form,
  });
  assert.equal(res.status, 201);
  const meta = await res.json();
  assert.ok(meta.id);
  assert.equal(meta.filename, 'test.png');
});

test('未登录上传返回 401', async () => {
  const form = new FormData();
  form.append('file', new File([Buffer.from('x')], 'a.png', { type: 'image/png' }));
  const res = await fetch(`${base}/api/v1/uploads`, { method: 'POST', body: form });
  assert.equal(res.status, 401);
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `node --test server/test/uploads.test.js`
Expected: FAIL — uploads 404

- [ ] **Step 3: 实现 uploads 路由**

```js
// server/routes/uploads.js
import { Router } from 'express';
import multer from 'multer';
import path from 'node:path';
import fs from 'node:fs';
import { randomUUID } from 'node:crypto';
import { fileURLToPath } from 'node:url';
import { requireAuth } from './auth.js';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const UPLOAD_DIR = path.resolve(__dirname, '../uploads');
fs.mkdirSync(UPLOAD_DIR, { recursive: true });

const ALLOWED = { 'image/jpeg': '.jpg', 'image/png': '.png', 'application/pdf': '.pdf',
  'application/vnd.openxmlformats-officedocument.wordprocessingml.document': '.docx',
  'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet': '.xlsx' };
const MAX_SIZE = 10 * 1024 * 1024; // 10MB（spec §12 决策）

const storage = multer.diskStorage({
  destination: UPLOAD_DIR,
  filename: (req, file, cb) => cb(null, `${randomUUID()}${ALLOWED[file.mimetype] || ''}`),
});
const upload = multer({ storage, limits: { fileSize: MAX_SIZE }, fileFilter: (req, file, cb) => {
  cb(null, Boolean(ALLOWED[file.mimetype]));
} });

export function createUploadsRouter(db) {
  const router = Router();

  router.post('/uploads', requireAuth(db), upload.single('file'), (req, res) => {
    if (!req.file) return res.status(400).json({ error: '文件类型不支持或为空' });
    const meta = {
      id: randomUUID(),
      filename: req.file.originalname,
      path: `/uploads/${req.file.filename}`,
      size: req.file.size,
      uploadedBy: req.session.person_id,
      uploadedAt: new Date().toISOString(),
    };
    db.prepare('INSERT INTO attachments (id, filename, path, size, uploaded_by, uploaded_at) VALUES (?, ?, ?, ?, ?, ?)')
      .run(meta.id, meta.filename, meta.path, meta.size, meta.uploadedBy, meta.uploadedAt);
    res.status(201).json(meta);
  });

  // 受保护静态访问：仅登录用户可下载附件
  router.get('/uploads/:name', requireAuth(db), (req, res) => {
    const safe = path.basename(req.params.name);
    res.sendFile(path.join(UPLOAD_DIR, safe));
  });

  return router;
}
```

- [ ] **Step 4: 挂载路由**

```js
// server/app.js（修改）
import { createUploadsRouter } from './routes/uploads.js';
app.use('/api/v1', createUploadsRouter(app.locals.db));
```

- [ ] **Step 5: 运行测试确认通过**

Run: `node --test server/test/uploads.test.js`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add server/routes/uploads.js server/app.js server/test/uploads.test.js
git commit -m "feat(server): 附件上传（multer）+ 受保护静态下载"
```

---

## Task 8: 前端切换至 API 数据源

**Files:**
- Modify: `docs/src/services/auth.js` — `AuthStore.login` 调后端
- Modify: `docs/src/core/bootstrap.js` — 启动时按 token 切换数据源
- Modify: `docs/src/services/runtime.js` — 数据源切换入口
- Test: `server/test/skeleton.test.js`（复用现有测试确保后端仍可启动）

- [ ] **Step 1: runtime.js 增加 API 数据源初始化**

```js
// docs/src/services/runtime.js（修改：新增导出）
import { registerMockAdapter, registerApiAdapter, setDataSource, getDataSource } from '../core/data-adapter.js';
import { MockAdapter } from '../core/mock-adapter.js';
import { ApiAdapter } from '../core/api-adapter.js';

registerMockAdapter(MockAdapter);
registerApiAdapter(ApiAdapter);
setDataSource('mock');

// P1 后端接入：登录后由 auth.js 调用此函数切换到 API 数据源
export function enableApiMode(token) {
  setDataSource('api', { apiBaseUrl: '', authToken: token });
  sessionStorage.setItem('gsm1921-api-token', token);
}

export function isApiMode() {
  return getDataSource() === 'api';
}
```

- [ ] **Step 2: auth.js 登录调后端 API**

```js
// docs/src/services/auth.js（修改 AuthStore.login，保留原有签名与 A-11 防串扰）
import { enableApiMode } from './runtime.js';

login(personId) {
  // 1. 现有本地角色判定（开发模式/离线兜底）
  const role = _getUserRoleFromMemory(personId);
  _writeLogin({ personId, role });

  // 2. 后端登录获取 token（失败静默降级到本地，不阻断使用）
  fetch('/api/v1/auth/login', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ personId }),
  })
    .then(r => r.ok ? r.json() : null)
    .then(data => {
      if (data && data.token) {
        enableApiMode(data.token);
        // 以后端返回的角色为准刷新本地会话
        if (data.user && data.user.role) {
          _writeLogin({ personId: data.user.id, role: data.user.role });
        }
        console.info('[AuthStore] 已切换至 API 数据源');
      }
    })
    .catch(e => console.warn('[AuthStore] 后端登录失败，保持本地模式', e));
},
```

- [ ] **Step 3: bootstrap.js 启动恢复 API 模式**

```js
// docs/src/core/bootstrap.js（在现有初始化逻辑前追加）
import { registerApiAdapter, setDataSource } from './data-adapter.js';
import { ApiAdapter } from './api-adapter.js';

registerApiAdapter(ApiAdapter);
const savedToken = sessionStorage.getItem('gsm1921-api-token');
if (savedToken) {
  setDataSource('api', { apiBaseUrl: '', authToken: savedToken });
}
```

> 说明：`runtime.js` 已注册 ApiAdapter，但 workspace 入口可能绕过 runtime 直接走 bootstrap；两处注册幂等，安全。

- [ ] **Step 4: 验证后端测试仍全绿**

Run: `cd server && npm test`
Expected: PASS（全部测试）

- [ ] **Step 5: Commit**

```bash
git add docs/src/services/auth.js docs/src/core/bootstrap.js docs/src/services/runtime.js
git commit -m "feat(frontend): 登录切换 API 数据源 + 启动恢复会话 token"
```

---

## Task 9: 端到端验证

**Files:**
- Create: `server/test/e2e-login.test.js`（Playwright 脚本）
- Modify: `server/README.md`（若存在则更新启动说明，否则创建简短说明）

- [ ] **Step 1: 启动后端并验证登录 → 工作台数据来自后端**

```bash
cd server
npm install
node server.js   # 默认 http://localhost:3000
```

Run: `node --test server/test/e2e-login.test.js`（需后端已启动）

```js
// server/test/e2e-login.test.js
import { test } from 'node:test';
import { chromium } from 'playwright';

test('登录书记 → 工作台渲染且数据来自 API', async () => {
  const browser = await chromium.launch({ headless: true });
  const page = await browser.newPage();
  await page.goto('http://localhost:3000/login.html', { waitUntil: 'domcontentloaded' });
  // 选择"书记"身份登录（登录页 dev 模式选择器以实际为准）
  await page.click('text=书记');
  await page.waitForTimeout(1500);
  const apiMode = await page.evaluate(() => sessionStorage.getItem('gsm1921-api-token') !== null);
  assert.equal(apiMode, true, '登录后应获得 API token');
  const title = await page.title();
  assert.match(title, /党建工作台|工作台/);
  await browser.close();
});
```

- [ ] **Step 2: 手动冒烟检查**（浏览器打开 http://localhost:3000）
  1. login.html 以"书记"登录 → 进入 secretary 工作台，概览四面板显示真实数值
  2. 首页 index.html 近期活动/专班进展/活动风采正常渲染（读自后端）
  3. 修改一条数据（如发通知）→ 刷新页面数据仍在（写穿透生效）
  4. 纪检工作台考勤 Tab 正常

- [ ] **Step 3: 验证部署说明写入 README**

```markdown
<!-- server/README.md -->
# 后端服务

```bash
cd server
npm install
npm start        # http://localhost:3000
```

- 数据文件：`server/data.db`（SQLite 单文件，备份即复制该文件）
- 附件目录：`server/uploads/`
- 测试：`npm test`
- 首次启动自动从前端 mock 数据导入种子；对接北大计算中心时将 `docs/` 与 `server/` 一并部署
```

- [ ] **Step 4: Commit**

```bash
git add server/test/e2e-login.test.js server/README.md
git commit -m "test(e2e): 登录切换 API 数据源端到端验证"
```

---

## 自审记录

**1. Spec 覆盖：**
- spec §3.1 架构（Express+SQLite+附件）→ Task 1/2/7
- spec §3.2 数据模型迁移 → Task 2/3
- spec §3.2 附件存储 → Task 7
- spec §3.1 前端 fetch 层、localStorage 仅会话 → Task 8
- spec §9 P1 里程碑"14 页可登录访问" → Task 9

**2. 占位符扫描：** 无 TBD/TODO；Task 3 对 `taskforces.js` 导出名的确认说明为明确的实现指引而非占位。

**3. 类型一致性：** `replaceCollection(db, table, rows)`、`requireAuth(db)`、`createApp({dbPath})`、`enableApiMode(token)` 在各 Task 中签名一致。资源名 `makeupTasks`→表 `makeup_tasks` 映射唯一且集中定义在 resources.js。

**P2 前置说明：** P1 交付后，P2（共享表格引擎 + 考勤/宣传归档表）在 `table_defs`/`table_rows` 新表与对应 API 上构建；P1 的 `bootstrap`/`snapshot` 全量机制在数据规模较小时继续可用，P2 起新增资源级增删改 API。

---

## 最终审查修复记录（2026-08-03）

P1 后端基建最终审查发现 2 Critical + 2 Important，本次全部修复并以强化版 Playwright E2E 作为验收（TDD：先改 E2E 使其失败 → 实现 → 全绿）。

### C1 读路径未接线（Critical）

- 根因：`bootstrap.js` 恢复 API 模式时只 `setDataSource('api')` 从不调用 `init()`；随后 `loadWorkspaceData` 的 `BranchService.loadDB()` 从 localStorage 覆写 mockDB，后端数据被本地旧备份冲掉。
- 修复：
  - `docs/src/core/bootstrap.js`：恢复 API 分支改为 `await init()`（从后端拉全量数据填充 mockDB），失败 `catch` 后 `setDataSource('mock')` 完整回退本地模式，后续流程照常走 loadDB。
  - `docs/src/services/mock.js`：`loadDB()` 函数体最前加守卫 `if (getDataSource() === 'api') return;`，API 模式下跳过 localStorage 恢复（防覆盖）。新增 `import { getDataSource }`（data-adapter.js 无 import，无循环依赖）。
  - 顺带修复实证发现的路径不匹配：`api-adapter.js` 的 `makeupTasks.list()` 原调 `/api/v1/makeup-tasks`（404），服务端按资源名注册 `/api/v1/makeupTasks` —— 已对齐（否则 init() 的 Promise.all 会因 404 整体失败并误回退 mock）。

### C2 写路径未接线（Critical）

- 根因：`data-adapter.js` 的 `persist()` 在 API 分支是 no-op（只打日志），服务层 15+ 处写入（notice/todo/attendance/handover/inspection/makeup/review/taskforce/activity 等）全部落空。
- 修复（`docs/src/core/data-adapter.js`）：
  - `persist()` API 分支改为「防抖全量快照写穿 + 本地备份」：`_mockAdapter?.saveDB()` 写 localStorage 双保险（防服务器瞬时不可达丢数据；mockDB 在 API 模式由 init() 填充，本地备份不参与读），再 `_scheduleSnapshot()`。
  - 新增 `_snapshotTimer` / `SNAPSHOT_DEBOUNCE_MS = 800` / `_scheduleSnapshot()`（已有排程则合并）/ `_flushSnapshot()`（async：读 mockDB 10 个服务端集合构造 payload → `getAdapter().snapshot(payload)`；flush 时数据源已切回 mock 则跳过；失败仅 `console.warn` 不阻断 UI）。
  - `docs/src/core/api-adapter.js`：新增 `snapshot(payload)`（POST /api/v1/snapshot，认证保护），并修正文件头"占位实现"过时措辞。
  - `init()` API 分支追加 `restoreNicheCollections()`（`docs/src/core/mock-adapter.js` 新增导出）：从 localStorage 仅恢复非服务端集合（actSubRecords/tfSubRecords/complianceReferences/fileSpaceRecords/experienceDeposits/imageRecords），避免 API 模式下文件空间/经验沉淀/合规引用等功能空态（P2 才入后端）；10 个服务端集合绝不动。

### I1 附件 path 前缀（Important）

- `server/routes/uploads.js`：上传元数据 `meta.path` 由 `/uploads/xx` 改为 `/api/v1/uploads/xx`，与真实下载路由 `/api/v1/uploads/:name` 一致。`uploads.test.js` 只断言 filename/id，不受影响（清理用的 `path.basename(meta.path)` 依旧正确）。

### I2 express.json 413（Important）

- `server/app.js`：统一错误中间件最前加 `if (err.status) return res.status(err.status).json({ error: err.message });` —— express.json 超 2mb 抛出的 PayloadTooLargeError 自带 `err.status=413`，先前被误判为 500。

### E2E 强化（`server/test/e2e-login.test.js`，验收测试）

1. 读路径断言：`page.on('request')` 捕获登录跳转后页面加载期间（含 CODE_VERSION 自检 reload）的 GET `/api/v1/*`，断言至少命中 1 次 GET `/api/v1/activities` —— 证明 bootstrap init() 确实从服务器拉数。TDD 红阶段实证：实现前 0 次捕获，实现后命中。
2. 写穿闭环断言：`page.evaluate` fetch POST `/api/v1/snapshot`（Bearer token 取 sessionStorage）写入唯一标题活动（date=当天）→ `page.reload()` → 断言 `document.body.textContent` 包含该标题（首页「近期活动」列表容器默认隐藏但已渲染进 DOM，textContent 计入）+ 服务端 GET `/api/v1/activities` 读回该标题（双保险；选择说明见测试内注释：日历视图只渲染类型短标签，故 UI 断言用 body.textContent）。
3. 既有断言（token / header「党支部书记」/ title 匹配 /管理引擎/ / bootstrap users 非空）全部保留。

### Mock 模式回归冒烟（一次性脚本，已删不提交）

- chromium dev 卡片登录组织委员 + 书记 → 首页正常渲染（body 1943/2034 字符、统计卡渲染），0 pageerror、0 console error，无白屏、无 ReferenceError。API 分支改动均以 `getDataSource()==='api'` 为条件，mock 模式不受影响。

### 验证结果

- `node --test test/e2e-login.test.js`：通过（读路径 + 写穿闭环）。
- `npm test`：16/16 全绿（6.6s）。
- 临时文件已清理；改动文件：data-adapter.js / mock-adapter.js / mock.js / bootstrap.js / api-adapter.js / server/app.js / server/routes/uploads.js / server/test/e2e-login.test.js / 本计划文件。
