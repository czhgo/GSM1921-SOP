---
title: "工程化评估与改造行动线"
type: design
role: "[工程师]+[AI]"
created: 2026-09-03
last_updated: "2026-09-09"
status: active
related_files: [content/04_web_design/evolution/ARCHITECTURE_EVOLUTION.md, content/04_web_design/evolution/WORKFLOW_BLOCK_CONTRACT.md, content/04_web_design/evolution/PARTY_COMMITTEE_DESIGN.md, content/04_web_design/module/SOP_WEBSITE_GUIDE.md, content/04_web_design/deploy/DEPLOYMENT_GUIDE.md, .ctx/REVIEW_QUEUE.md]
---

# 工程化评估与改造行动线

> **定位**：支书 2026-09-03 问询「如果做一次 模块化、插件化、开源化 的 100 分评估，你会怎么输出？我以此来指导你」（原「模块化 / 插件化 / 开源化评估」据此更名，2026-09-09）。本文档为该工程化评估与改造方向的**现行唯一口径**：评估结论、工程做法纪律与行动优先级一体维护，作为后续开发顺序的方向选择依据。**覆盖式维护、不逐轮留历史行**，口径沿革见附。
> **受众**：[工程师]+[AI]（架构维护者）+ 支书（方向裁决人）
> **关联**：`content/04_web_design/evolution/ARCHITECTURE_EVOLUTION.md`（组件化/插件化演进母文档，评估正文已压为其 §二 历史结论段）、`content/04_web_design/evolution/WORKFLOW_BLOCK_CONTRACT.md`（L3 工作流块契约，P2 行动承接）、`content/04_web_design/evolution/PARTY_COMMITTEE_DESIGN.md`（两级治理设计）、`.ctx/REVIEW_QUEUE.md`（全局评估总表，本文件一行）。

---

## 一、评估总览

> **支书 2026-09-06 再评定调**：① 综合分反映的是**二开组合能力**（模板型交付下「别人能否快速换壳用起来」），不是纯工程内聚分；② **重改进不唯分**——分数只作方向指引，重点看残项清单与行动是否推进（本轮 74→76 主要是**口径回归二开组合能力 + 新增可换壳证据**，不是"凭空改出 2 分"）；③ **开源长期交付形态 = 模板型**——仓库是「制度即代码」模板，随附示例组织（本科生党支部）只是可整体替换的默认值，文档与演示以「给新组织换壳」为叙事主线（落地：四 P4a 根 README 30 分钟换壳指南 + 演示数据一键重置 `?reset=1`）。

| 维度 | 得分 | 一句话结论 |
|------|------|-----------|
| 模块化 | **87 / 100**（后续演进链见 §5.2；本格正文停在批次 42，批 25–47 的逐段加分理由全在 §5.2） | 五层分层 + 组件积木 + tab 懒加载为真；esc/fmtDt 微工具、类型/阶段元数据、写场景清单等重复/三写均已收敛（近三轮新代码引入重复趋零）；**2026-09-14 起两轮连续上台阶**——第一轮（批次 25–35）三项「能力/口径散落在调用点」收成单一源（版本号推导 `version-next.mjs`、**分页收进统一检索引擎**、**人×项目矩阵 `relation-matrix.js`**）；第二轮（批次 37–40）把上一轮成果做成**全程收口**（**反向枚举**补掉未接引擎的 13 处以上手写渲染 → 21 文件接入；翻页标记下沉**叶子件 `pager.js`** 且全站 6 处手写翻页并轨；矩阵补**人维分页**与 `cellClass`；**宽表推广三域**并清空矩阵收敛白名单；同步清掉 `.vs-matrix*` 死样式与失真注释）；**2026-09-15（批次 41）矩阵推广的「待迁清单」清空**（末域＝思想汇报台账 → 人 × 期次宽表；顺带补 `listAllThoughtReports()` 全量归一出口，读侧归一收成唯一出口 → §3.4 R25）；**2026-09-15 再一批（批次 42）模块化 81→82**——说明文件口径**守卫化**（`doc-consistency.test.mjs` S1–S8：把「文档里的数字/名称」变成常驻断言，含单一源登记处防僵尸与已废止功能域防回潮）＋工作台 tab **分组单一轴**（五台四组、党委台院级例外登记）＋**功能减法**（公邮废止：成套摘除 tab／两薄壳文件／两数据域全链路／深链／测试夹具／文档，域数与漂移面同步减少，见 §3.4 R26 与 REVIEW_QUEUE Q-23-26），测试 571→586——**全量 586/586 通过**，同一环境前置；残项=徽章/选择器等组件出口仍散、服务层个别 UI 依赖 |
| 插件化 | 75 / 100 | registry 自注册 + config.modules/blocks/workforce「配置即组合」+ module-compose v0 契约（depends/conflictsWith 纯校验 + 测试 + 文档）落地；残项=requiredRoles 门禁未被 workspace-shell 消费（仅元数据）、manifest 防失同步测试未全落地 |
| 开源化 | 75 / 100 | 根 README 已一般化、以「复用与二次开发（给其他组织）」为核心章节且增 30 分钟换壳指南（四 P4a）；数据真人化可整体替换（people/accounts 2026-09-06 基线）；MIT + CONTRIBUTING 齐；**2026-09-15 批次 42（74→75）说明文件与实测口径全量对齐**——34 处漂移收口（tab 数/名称/顺序、页面数、数据五数分口径）、旧界面名统一（考勤总表→考勤明细）、并新增**文档口径守卫** `doc-consistency.test.mjs`（换壳者可信任文档：模板型交付下文档可信度即交付质量）；残项=无 English 版、release 发布工作流未做 |
| 超参数可调性 | 84 / 100 | policy-defaults 集中默认单一源逐项标注 branch-default 可调 / institutional 固定；2026-09-09 批4 已接 config 驱动：POLICY_OVERRIDABLE 白名单三键经设置「域参数」卡 UI 覆盖 +「支部制度参数」卡制度默认只读展示（放行程序唯一通道）；残项=全量 config 引擎未达（未登记项仍源码，放开须走放行程序） |
| 组合能力（二开视角） | 80 / 100 | 组合面=模块/块/分工启停排序（2026-09-09 批3 起操作位=设置→支部治理「工作台默认顺序 / 支部信息与向导」（支书/副，副书同权）+ 党委台「支部配置」party-config-tab（party-staff））+ config.modules/blocks/workforce + L3 block manifest + module-compose v0 契约与测试 6/6 绿；设置中心页签个人化 + 支部默认编排直存 + config 审计回滚（2026-09-09）；残项=requiredRoles 未消费、拖拽编排仅主题党日一处（L1→L5 未达） |
| **综合（当前）** | **≈ 79 / 100** | 五维均值（82/75/75/84/80 = 79.2）——支书 2026-09-06 再评定调「综合分反映二开组合能力 + 重改进不唯分 + 开源长期交付形态=模板型」；2026-09-09 收尾批 AI 建议分上调拍定（超参 78→84、组合 78→80）；**2026-09-14（批次 25–35）模块化 75→78**（三项引擎化/单一源证据见三·3.4，测试 541→566）；**2026-09-14 再一轮（批次 37–39）模块化 78→80**（全量收口 / 翻页单一源下沉 / 矩阵人维分页 / 宽表推广三域 / 死样式清理，测试 566→569——**全量 569/569 通过**，须先 `npm start` 起外部 3000 端口服务，否则 `click-cost` / `mock-integrity` / `b3-1-makeup-writeback` 三文件 `ERR_CONNECTION_REFUSED`，属环境前置）；**2026-09-15（批次 41）模块化 80→81**（Q-23-18 末域收口：矩阵待迁清单清空 + 读侧归一收成唯一出口 `listAllThoughtReports()`，测试 569→571——**全量 571/571 通过**，同一环境前置）——**2026-09-15（批次 42）模块化 81→82 与开源化 74→75**（说明文件口径守卫化 + 分组单一轴 + 公邮废止减法；测试 571→586，**全量 586/586 通过**，同一环境前置）——**综合分均值 78.8→79.2，仍未过半分不进位，故综合仍记 ≈79**（历轮同方向，按「重改进不唯分」不重复上调综合分，只如实记录残项减少）；失分仍=requiredRoles 未消费、拖拽编排 L1→L5 未达、server 登录缺省口令弱（行动线 P1b 已加口令校验，缺省 123456 + dev/demo 免密路径在，生产须显式换密）、无 English 版、制度项放行需裁决 |

> **主要矛盾 / 残项 TOP（一句）**：当前未闭合项集中在——组件出口仍散（徽章/选择器等）与服务层个别 UI 依赖（模块化）、requiredRoles 门禁未消费与 manifest 防失同步测试未全（插件化/组合能力）、全量 config 引擎未达（制度项放行需裁决，超参数）、拖拽编排仅主题党日一处 L1→L5 未达（组合能力）、无 English 版与 release 发布工作流（开源化）、server 登录缺省口令（运行安全残余，P1b 已加校验、生产须显式换密）——均挂四行动线后续批次或残项跟踪（逐维依据见二）。

> 注（支书 2026-09-03 口径修正）：本仓库是支部自己的内部系统，**.ctx 日志与 references/历史会议材料均为内部资产、保留上传**，不存在"出仓脱敏"需求；三·3.3 R12 与行动线 P1b 中的出仓子项（脱敏/移出/账号外置/.ctx ignore）**全部撤销**。真正保留的工程项仅是「运行安全」：server 登录不校验密码在多人/计算中心部署时任何人可凭 personId 冒名登录（该运行安全项已由 P1b 于 2026-09-03 修复：默认口令校验可换 `LOGIN_PASSWORD` + `DISABLE_PASSWORD_CHECK=1` 逃逸门；本条保留撤销口径原貌）。

---

## 二、评分依据（逐维：得分项 / 失分项 / 残项；口径唯一=一速览，沿革见附）

### 2.1 模块化 —— 83/100

得分项：
- 五层分层清晰：core / services / components / modules(capabilities) / entries(tabs)；tab 动态 import 懒加载、capability 自注册。
- 组件积木化已落地：徽章全站统一、表单字段/外壳（forms.js）、收件箱、汇报域（reporting.js）等；表单 B1~B4 后输入件已全局收编（input-flat 13px 单档、label for 关联、aria）。
- 组件平铺层同域多文件已收敛：forms.js / badges.js / reporting.js 三库出口建成（聚合重导出、实现不搬运、零行为变化）。
- esc/fmtDt 微工具、类型/阶段元数据、写场景清单等重复/三写均已收敛，近三轮新代码引入重复趋零（去重证据与去向见三·3.3）。
- **2026-09-14（批次 25–35）三项「能力/口径散落在调用点」的病灶收成单一源**（详表见三·3.4）：
  - **版本号推导**：`docs/scripts/version-next.mjs` 纯函数单一源（同日续号 / 只允许前进 / 注释排除两级规则），发版脚本与守卫共用；
  - **分页**：收进统一检索引擎 `components/list-filter.js`（每页 10、页码并入 `stateKey`、页数 ≤1 不出控件），**一处实现、28 处按人/按活动表一次受益**（此前全站仅 5 处各自实现，引擎自身无分页）；
  - **人×项目矩阵**：新建 `components/relation-matrix.js`（`byPerson`／`byItem` 互为转置、项目维列上限 6 + 一键展开、横向滚动 + 首列吸附），考勤矩阵与考察宽表由「各自实现」改为接入，`.vs-matrix` 等未迁实现入守卫白名单台账。
  - 另：控件档位/表格/筛选行/分页样式落 `styles.css` 类族单一源（档位算式显式，同表单 42/43/47 三值并存 → 单档 38px×13px）；成员流动「同一实现体挂两个写门」取代复制实现。
- **2026-09-14 再一轮（批次 37–39）：把上一轮「收进引擎」的成果做成全程收口**（详表见三·3.4 R21–R24）：
  - **全量收口**：批次 34 只覆盖「已在引擎内的 26 处」，本批以**反向枚举**补掉**未接引擎的手写渲染 13 处以上**（表格 4 处 + 卡片列表 9 处以上），21 个文件接入统一检索引擎；引擎补「无关键词无分面则不渲染检索条」以支持「只需分页」的桶 / 分组子列表；
  - **翻页标记单一源下沉**：抽为**叶子件** `components/pager.js`（避免与矩阵相互成环），并把全站**另有 6 处手写翻页控件**并轨到此（`.archive-page-btn`/`.issue-page-btn`/`.qv-page-btn`/`.feedback-page-btn`/`.visitor-act-page-btn`/`att-table-prev·next` 全部撤除）；
  - **矩阵能力补全**：人维分页（每页 10 人，行/列两个方向同一切片）、`cellClass` 单元格附加类钩子；
  - **宽表推广三域**：支部分工（人×工作项）、专班报名总表（人×专班）、表态矩阵并入单一源 → **全站矩阵实现收敛为 1 处**（矩阵收敛白名单清空）；随之清掉 `styles.css` 中被取代的 5 条 `.vs-matrix*` 死样式与失真注释（第 5 次特批）。
  - **矩阵推广收口（批次 41，2026-09-15）**：末域「思想汇报台账」迁入**人 × 期次**宽表（行＝在册成员、列＝期次、cell＝最需处理状态 + 篇数）→ **矩阵「待迁清单」清空**（COMPONENT_SPEC §4.10）；顺带补 `listAllThoughtReports()` **全量归一出口**，把读侧归一收成唯一出口（§3.4 R25）。

失分项 / 残项（后续跟踪）：
- 徽章/选择器等组件出口仍散：除三库外，query-view/picker/modal 等仍被各层逐文件直连。
- 服务层个别 UI 依赖（如 services/notice.js import badgeHtml 生成 HTML）。
- **手写表格收敛台账**仍留 2 处人工登记位（考勤明细 / 党小组清单，行数天然有界），未进一步收敛。

### 2.2 插件化 —— 75/100

得分项：
- registry 自注册 + 按 scope 组装（workspace:secretary / workspace:party-committee）；支部实例化 + 支部级 fileSpaceIsolated。
- config.modules/blocks/workforce「配置即组合」：支部 tab/块 启停·排序 + 分工归属，净化单一源 config-clean.js（server 严格口径单向权威），tab-nav 守卫防隐藏冲突。
- L3 block manifest：块的 inputs 声明/事件/校验契约 v1.1（WORKFLOW_BLOCK_CONTRACT）+ manifests.js 双块 + 校验器 + config.blocks.workflowBlocks 配置区 + 主题党日 manifest 驱动（S4 试点；独立表单渲染桥 2026-09-09 代码减负撤回）。
- module-compose v0 契约：depends/conflictsWith 纯校验（引用存在/互斥同含/depends 禁环）+ 测试 6/6 绿 + 文档闭环（契约「组合声明」节 + registry 头注释）。

失分项 / 残项（后续跟踪）：
- requiredRoles 门禁未被 workspace-shell 消费（仅元数据，组合后授权弱）。
- manifest 防失同步测试未全落地。
- module-compose 仅前端纯校验：服务端 config 校验留 v1（插件安装/卸载概念未立）。

### 2.3 开源化 —— 75/100

得分项：
- 全程中文可读设计/规范文档；design（content）与 spec 职责已纠偏；代码带 role 标注与设计源链接；测试覆盖厚（模块加载 + 多组 E2E）。
- 根 README 已一般化、以「复用与二次开发（给其他组织）」为核心章节，且增 30 分钟换壳指南（四 P4a）；浏览器演示数据一键重置 `?reset=1`。
- 数据真人化可整体替换：people/accounts 2026-09-06 基线 + server/.env.example（示例账号外置 env），新机器按 README 可独立跑通并自建数据。
- LICENSE（MIT）+ 根 CONTRIBUTING.md 已补仓（2026-09-04）。

失分项 / 残项（后续跟踪）：
- 无 English 版。
- 语义化发布（release）工作流未做：`?v=` 软版本利于日常整体失效，但非语义化版本发布。

### 2.4 超参数可调性 —— 84/100

得分项（已参数化或已声明，判据：代码近旁可答「可调/不可调 + 默认出处」）：
- **policy-defaults.js 集中默认单一源**（行动线 P3c，2026-09-05）：票决门槛（应到 >2/3 且无反对，2026-09-06 支书裁，附录⑩ S2 R2-3）/ 会议类型与上传位例外 / 考察超期天数 / 应到名单 roster（partyStages+excludeDetained，**已含滞留口径**，2026-09-06）逐项标注 branch-default 可调 / institutional 固定 + 出处 + 消费点；消费点一律引用派生，不在业务层新写字面量。
- **域参数（L2）config 驱动落地（2026-09-09 批4 支书批）**：`POLICY_OVERRIDABLE` 覆盖白名单（只定义于 policy-defaults，每项 = path/type/钳制范围/domain 域节）三键——纪检考察超期 `inspection.overdueDays` / 组织滞留集中复核窗 `memberConfirmation.semesterDetainedWindows` / 组长学期提醒开关 `leader.semesterReportReminder.enabled`——经**设置 → 支部治理「域参数」卡 UI 覆盖**（域负责人仅本域、支书/副支书/party-staff 全量；值=覆盖、null=恢复该域默认、整体 null=全量恢复）；读侧 `applyPolicyOverrides` 注入 `POLICY_DEFAULTS`，全站判定随参数生效；净化/钳制单一源 = config-clean `sanitizeConfigPolicyOverrides`（server 与前端 branch.js 同源）。
- **制度参数 UI（同批）**：设置 → 支部治理「支部制度参数」卡把票决门槛 / 应到口径 / 会议考勤类型 / 记录人 / 标因作为**制度默认只读展示区**（支书/副视角，数据单一源 = policy-defaults，含域参数默认行）——「可调/不可调 + 默认出处」在 UI 可见；放开为支部可调 = 放行程序（支书裁决 → 登记白名单 → kind 注释同步 → 审计 why 回填，反向收权同理，见 PARTY_COMMITTEE_DESIGN §2.6）。
- 表决配置 optionSet/voterScope/quorumCheck：默认=场景函数 vote-config.defaultVoteConfig(scenarioId)，每活动 voteConfig 可覆盖。
- 分工归属 config.workforce（SOP 缺省责任人，支委会议题改派）。
- 色值：styles.css 令牌与 COLOR_SYSTEM 固定/可调四层表对齐——党建红 `--party-red`/党徽金 `--party-gold` 固定不可调（已声明令牌化）、`--accent-*` 主题自选；styles.css ↔ COLOR_SYSTEM ↔ constants 三源一致、零游离字面量（行动线 P3e / 8.6 色值收敛）。

失分项 / 残项（后续跟踪）：
- 全量 config 引擎未达：仅 `POLICY_OVERRIDABLE` 白名单三键可经 UI 覆盖；未登记项（branch-default 未登记 / institutional 制度项）仍源码层改——放开须走放行程序（支书裁决，见 PARTY_COMMITTEE_DESIGN §2.6，行动线 P4d）。

### 2.5 组合能力（二开视角）—— 80/100

得分项：
- 组合面=模块/块/分工启停排序（2026-09-09 批3 起入口=设置→支部治理「工作台默认顺序 / 支部信息与向导」卡（支书/副，副书同权）与党委台「支部配置」party-config-tab（party-staff），原支书台「工作台配置」tab 已废止）+ config.modules/blocks/workforce「配置即组合」+ config-clean 单源净化。
- 设置中心页签个人化（2026-09-09 设置中心收口）：本人「我的工作台」tab 顺序个人化（偏好层，本机生效），与支部默认编排分层共存、互不覆盖——个人排序 ≠ 支部默认顺序（config.modules）。
- 支部默认编排 + 审计回滚（2026-09-09）：设置 → 支部治理「工作台默认顺序」卡直存 config.modules（tab 启停/排序 chips；核心组固定不可关由 config-clean + tab-nav 守卫）；configChangeHistory 逐键留痕（why 透传）+「配置变更记录」只读列表单键「回滚此更改」（rollbackBranchConfig：回滚再留一痕、历史不改写、上限 100 条）——组合配置可安全回退。
- 能力注册表：capabilities/* 自注册，scope→工作台 tab 清单（台内固定组合的声明面）。
- L3 block manifest（inputs 声明/事件/校验 = 块级组合契约，落地于主题党日 manifest 驱动 S4；独立表单渲染桥 2026-09-09 代码减负撤回）+ module-compose v0 契约与测试 6/6 绿。
- 场景联动：scenarioId → defaultVoteConfig / 决策树模板（默认组合，可换）。
- 二开可换壳证据（2026-09-06）：根 README 30 分钟换壳指南 + `?reset=1` 演示重置（行动线 P4a）。

失分项 / 残项（后续跟踪）：
- requiredRoles 门禁未消费（组合后授权弱）。
- 拖拽编排（块组合的图形面）仅主题党日 manifest 驱动一处，L1→L5 愿景未达（行动线 P4d）。

---

## 三、工程做法与纪律

### 3.1 统一扎口做法（本评估确立的第一个工程做法）

支书定调：**同一域可以有若干个函数（甚至若干个实现文件），但对外只暴露一个库文件；调用方一律 import 该库，不直接触碰内部实现。**

落地样板（P0 试点一，已完成）：
- 新建 `docs/src/components/forms.js` —— 表单域唯一出口，聚合重导出字段积木（form-field：textField/textareaField/selectField/dateField/labelHtml/errorHtml）与外壳（form-shell：recordFormShell）。
- 三个直连调用方（dispatch-tab / write-tab / taskforce-tab）全部收口到库；仓库中已无直接 import form-field.js / form-shell.js 的调用方。
- 同批按同纪律完成：徽章域 `docs/src/components/badges.js`（P0 试点二）、汇报域 `docs/src/components/reporting.js`（P0 试点三）——三库后组件平铺层同域多文件收敛（见四 P0·统一扎口）。
- 回归：模块加载 116/116、party 相关 E2E 5/5 全绿，零行为变化。

**改造纪律（每次扎口必守）**：
1. 新增库聚合重导出，内部实现文件不搬运、不改名。
2. 调用方只改 import 来源一行；运行期零行为变化。
3. 每批扎口后跑 module-load + 受影响 E2E 回归。
4. 禁止双轨：一旦建库，同域内新代码一律走库，存量直连同步收口。

### 3.2 数据域接线契约 v1（2026-09-03 person 域试点确立）

**背景**：盘点显示全站 68 处直连 mock；mock/index.js 实为"旧兼容中转"（person 函数早已落到 services/person.js 又被 re-export 回 mock）。债根 = **UI/服务层 import 面挂在 mock，而非真正实现所在的 service**。收官态 = **UI 层（entries/components/modules）对 mock 的 import 直连清零**——全仓 mock 引用仅存于 services/core 数据层（种子接入点，契约允许）。

**试点与批次（全部已完成，2026-09-03）**：
- **试点（person 域）**：全站人名函数（getPersonById/getPersonName/PersonStore）import 面从 `mock/index`、`mock/people` 统一迁至 **`services/person.js`**（唯一服务出口）；`_personName` 兼容别名调用点全部改 `getPersonName`；`mock/index.js` 清除 person 中转段，退化为**纯种子/展示格式化数据仓**；涉及约 40 个文件，module-load + party/multi-user/write-hover/function-catalog 7/7 回归绿。
- **批次二（展示格式化提升）**：5 个纯展示格式化函数自 mock/* 原样提升至对应业务 service（attendanceToLong → services/attendance.js；inspectionToDisplay/inspectionToLong/inspectionToWide → services/inspection.js；reviewToDisplay → services/review.js）；mock/attendance|inspection|review.js 仅剩种子数组；7 个 UI 调用方 import 面改挂 services；module-load + mock-integrity + party + multi-user 7/7 回归绿。
- **批次三（成员名单 PEOPLE 收口）**：`services/person.js` 增 `PersonStore.getMembers()`（成员名单语义 = 静态党员种子，不含登录系统账号 mockDB.users——区别于 getAll）；全站 UI/组件/entry/tab 层 16 个文件直连 mock PEOPLE 改为经 `PersonStore.getMembers()` 模块级捕获；UI 层 PEOPLE 直连 mock **清零**（mock/index 的 PEOPLE re-export 仅剩 services 层 7 处种子引用，契约允许）；回归 module-load + party/multi-user/block-entry-guard/capability-registry 18/18 全绿。
- **批次四（余种子与机构/登录收口，收官）**：党委机构名 `branch.js` 增 `getCommitteeName()`，monitor/dispatch/branches 三个党委 tab 直连 mock/branches `PARTY_COMMITTEE` 改走服务（header 品牌软编码同域收敛）；账号登录 `auth.js` 增 `AuthStore.verifyCredentials()`，login-entry 直连 mock/accounts `mockLogin` 改走认证服务（真实后端接入时仅替换该实现）；达成 P0 收官态（UI 层 mock 直连清零）；回归 module-load + party-committee/dispatch + write-hover 5/5 全绿。

**契约条款**：
1. **人名与人员对象获取**（getPersonById/getPersonName/PersonStore）唯一出口 = `services/person.js`；任何层禁止从 `mock/*` 获取人名。
2. **成员名单**（PEOPLE 语义）唯一出口 = `services/person.js` 的 `PersonStore.getMembers()`；UI/tab/组件层禁止直连 mock 种子数组；种子数据仅许 service 层引用。
3. **展示格式化函数**（attendanceToLong/inspectionToLong/reviewToDisplay 等）统一栖身各业务 domain service（attendance/inspection/review），随记录读写同域演进；mock 数据模块不承载格式化逻辑。
4. 新代码一律遵守 1~3；存量收口按批次推进，每批回归。

### 3.3 冗余/重复去重审计与复核约定

> **定位**：支书 2026-09-03 要求「对多余、冗余、重复实现的代码高度批判性地思考并执行」。本审计只收录可证实（文件+行号）的重复，供支书裁决去重顺序。**审计原则：优先统一「同一业务事实的唯一出口」，再谈删代码——先扎口后去重，避免删完又长出第二份。**

#### 重复分组证据（收敛表；原证据文件:行，去向=行动编号见四，明细见执行日志 T-2026-09-033/034/035/036 与 2026-09-03 段）

| 组 | 重复事实 | 原证据（文件:行） | 收敛去向 |
|----|---------|----------------|---------|
| R1 微工具逐文件复制 | HTML 转义 `esc` 至少 10 个文件各写一份；`fmtDt` 双份 | inspector.js:482 / help-catalog.js:20 / activity-entry.js:43 / vote-widget.js:14 / vote-summary-panel.js:16 / review-tab.js:15-28 / report-up-tab.js:15-28 / party-config-tab.js:17 / branches-tab.js:14 / dispatch-tab.js:17（utils.js:127 的 esc 是 CSV 专用，非 HTML 转义） | ✅ 已收敛（P0a：core/utils.js 新增 `escHtml/fmtDt` 唯一出口，各文件 import 别名 `esc`，调用面零改动） |
| R2 类型/阶段元数据多写 | 三会四子类清单三份；阶段枚举另见 CANDIDATE_STAGES 与各 seed | constants.js:481-492 / inspector.js:356,374 / makeup.js:13,44 / dashboard/gallery.js:23（色名） | ✅ 收口 + 2 项复核保留（P0b：MEETING_TYPES→`ACTIVITY_CLASSIFICATION['three-meetings'].subtypes` 派生；TYPE_META/STATUS_META、MANDATORY_ACTIVITY_TYPES 与 gallery 色名复核保留，见下） |
| R3 产出块 id 消费端手写 | write-tab 重列 4 id 与 constants OUTPUT_BLOCK_DEFS 重复；calendar-tab 手写 THEME_DAY_BLOCK_ID 与 manifests.js blockId 重复 | write-tab.js:111,114-117,125-130 / constants.js:557-564 / calendar-tab.js:36 / manifests.js:26 | ✅ 已收敛（P0b：write-tab 产出块 4-id 字面量 → OUTPUT_BLOCK_DEFS 派生；calendar-tab `THEME_DAY_BLOCK_ID` → `THEME_PARTY_DAY_MANIFEST.blockId` 派生） |
| R4 场景目录子集三处手写 | 6 个活动场景的 id+label+颜色在 constants.js、decision-tree.js、calendar-tab WRITE_TEMPLATES 各维护一份（全集唯一源=sopData.js 12 场景） | sopData.js:10-165 / constants.js:121-129 / decision-tree.js:36-96 / calendar-tab.js:394-418 | ✅ 已收敛（P2b：constants 新增 `SCENARIO_WRITE_IDS/SCENARIO_LABELS` 派生；decision-tree 与 calendar-tab 改派生引用，scene-write-sync 测试兜底） |
| R5 业务链路三写 | flow 语义在 function-catalog desc、mermaid-sources FLOW_LINKS、以及声称的 sopData 各一份；无键集比对测试（注释声称的「防失同步测试」不存在） | function-catalog.js:63-76 / mermaid-sources.js:29-106 / catalog-sync.test.mjs::T2 | ✅ 已收敛（P0c：新增 `server/test/catalog-sync.test.mjs::T2` 键集**双向**断言，任一侧增删即红） |
| R6 tab id 散落导航侧 | entry 的 defaultTab/onNavTarget 硬编码 tab id，与能力 tab 清单及 config.modules.hiddenTabIds 无同一性守卫；隐藏后导航静默 no-op | 8 个 ws-*-entry.js / tab-bar.js:198-200 / branch.js:47-53 | ✅ 已收敛（P2a：`core/tab-nav.js` 纯决策 + tab-bar 守卫——隐藏 tab 回退首个可见 tab、杜绝静默白屏，tab-nav 4 态测试绿） |
| R7 跨层双净化 | 支部 config（modules/blocks）净化规则 server 与前端各一份，无互引注释、独立演化 | server/routes/resources.js:259-316 / docs/src/services/branch.js:132-176 | ✅ 已收敛（P1a：共享纯模块 `core/config-clean.js`，server 严格口径为单向权威，两端同源引用、本地净化实现删除） |
| R8 表决规则跨层重复 | optionSet 枚举+「异议须附言」server 与前端各一份，仅注释声明「对齐」 | server/routes/committee.js:30-36,90-92 / docs/src/services/vote-config.js:7-18 | ✅ 已收敛（P1a：`OPTION_ENUMS` 从 committee.js 导出 + `test/catalog-sync.test.mjs::T4` 键集双向断言） |
| R9 角色集合 ≥5 份 | 支委/支书/党委组织员角色 `Set` 分散多文件 | server/routes/auth.js:69-71 / member.js:13 / committee.js:15,17 / resources.js:56-59 / docs/src/core/constants.js:185 | ✅ 已收敛（P2c：constants 新增 BRANCH_COMMISSION_ROLES/SECRETARY_ROLES/PARTY_STAFF_ROLE/COMMITTEE_IDS 单一源，五处本地手写清零，roles-sync 4 断言绿） |
| R10 删除/归档级联同构 | DELETE 活动联动清理、归档级联 tasks，server 与前端同构实现（注释自认） | server/routes/resources.js:165-204 / docs/src/services/mock.js:142-204 | — 未列入去重队列（保持观察） |
| R11 资源名/ID 注册表两端各一 | RESOURCE_TABLES + ID_PREFIX 与前端快照键名两端手维护 | server/routes/resources.js:13-46,87-100 | — 未列入去重队列（保持观察） |
| R12 运行安全 | server 登录不验密码（凭 personId 发 token）；18 个账号明文 123456；demo 态/信任模型注释自认。**支书 2026-09-03 口径：.ctx 与历史材料为内部资产保留上传，非风险项；本条仅保留运行安全部分** | server/routes/auth.js:8-17 / docs/src/mock/accounts.js:4-26 | ✅ 出仓子项撤销 + 运行安全项已修复（行动线 P1b：默认口令校验可换 `LOGIN_PASSWORD` + `DISABLE_PASSWORD_CHECK=1` 逃逸门） |

> 反例（无需拆分，防过度去重）：block manifest 校验唯一源在前端 manifests.js（server 测试经浏览器复用同文件），职责与 R7 的「config 形状净化」不同——勿误并。

### 3.4 单一源 / 引擎化收敛台账（2026-09-14 起集中三轮：批次 25–35 收敛、批次 37–41 收口、批次 42 口径守卫化）

> **定位**：接 3.3 去重队列的**方法论升级**。3.3 处理的是「同一事实被抄了几份」；支书连续下达的整改（见 `.ctx/logs/2026-09-EXECUTION_LOG.md` 批次 25–42）暴露了**更高一层的同类病灶**：**能力与口径散落在调用点**——20 余处各写一份分页、三处各写一套矩阵、档位高度靠外部环境供给、组清单在模块加载期被物化成快照、说明文件里的数字与名称无人守。故本台账记的是「**把能力/口径收到单一源，并守住它**」的条目，判据与守卫一并登记。**三轮之别**：R13–R20 是「**收进引擎**」；R21–R25 是「**收口到底**」（反向枚举未接引擎的实现点、把单一源搬到不会成环的叶子上、语义挂载点与领域语义分离、连被取代的注释一起清、把「归一」也收成单一出口）；**R26 起是「把口径变成断言」**——不再依赖人记得同步。

| 组 | 病灶形态（可证实的现状） | 收敛去向（单一源） | 守卫 |
|----|----------------------|-----------------|------|
| R13 版本号推导三处各写 | `bump-version.mjs` 内联「当天日期 + a」且无参运行会把同日戳往回写；注释排除规则只在一处置信 | `docs/scripts/version-next.mjs` 纯函数单一源（`nextVersionFor` / `isForward` / `isCommentLine` + `codePartOf`），发版脚本与守卫共用 | `version-stamp.test.mjs` S1–S3 + D1–D6（含「全站活动戳取值集合规模为 1」） |
| R14 分页散落 5 处、引擎无分页 | 全站分页只在 issue-list / query-view / archive-entry / 反馈 / 活动动态 各写一版；**统一检索引擎自身没有分页** → 其承载的 28 处按人/按活动表无分页 | `components/list-filter.js` **引擎内置分页**（每页 10、页码并入 `stateKey`、筛选回第 1 页、页数 ≤1 不出控件），28 处一次受益 | `filter-row.test.mjs::S10`（含「全站调用点零 `pageSize: 0`」） |
| R15 矩阵三处各自为政 + 列无上限 | 考勤矩阵 / 考察人视图 / 表决 `.vs-matrix` 各写一套；宽表列随项目累积无限增长 | `components/relation-matrix.js`（`byPerson`／`byItem` 互为转置、项目维列上限 6 + 一键展开、横向滚动 + 首列吸附） | `relation-matrix.test.mjs` S1–S6 + 真机①（S1–S4 为本条落地时语区，S5/S6 见 R23/R25）；S4 把未迁矩阵设为白名单红线 |
| R16 组清单「派生化快照 / 种子枚举代跑」 | 批次 25 已禁组名字面量；批次 29 又揪出**等价绕过**：模块顶层 `[...new Set(PEOPLE.map(p => p.partyGroup))]`（展开 Proxy 即冻结）、运行时用 `PARTY_GROUP_OPTIONS` 净化导入与统计 | `services/party-group.js::groupOptions()`（活组实体唯一条出口）；种子枚举归位「预览种子期口径」 | `party-group.test.mjs` S1–S4 |
| R17 控件档位/表格/分页样式各写 | 表格表头 7 处各写一份、档位高度靠 UA 与 CDN 工具类供给（同表单实测 42/43/47 三值并存）、分页钮借 chip 表当前页 | `docs/src/styles.css` 类族单一源：`.data-table` / `.lf-*`（筛选行）/ `.page-btn`+`.page-num`+`.is-current`；档位算式显式（单档 38px×13px） | `filter-row.test.mjs` S1–S12 + D1–D2（S1–S8 为本条落地时语区，S9–S12 见 R18/R21/R22） |
| R18 选人载体口径过宽 | §4.13「禁止 select 列人名」与实现有 4 处口径差（任命支书 / 向导内任命 / 分工到人 / 落实责任人） | 规范改**语义两分**（选名单成员 → PersonPicker；任命/指派到人 → 允许下拉）+ 例外台账 | `filter-row.test.mjs::S9`（逐行判 `option` 插值人名变量 + 白名单防僵尸） |
| R19 同一业务动作两个写门 | 成员流动「流入登记」与「名册新增」在 API 形态角色门冲突（支书登记流入被 403），修法易复制出第二份实现 | `server/routes/member.js` **同一实现体 `createMemberRow` 挂两个写门**（`/members` 组织委员专属、`/members/intake` 流动登记角色集） | `member-flow.test.mjs::S4`、`permission-gate.test.mjs` ⑤d/⑥/⑦、`member-persist.test.mjs` api ⑫ |
| R20 状态与载体失配（非闭环） | 多选人员后关面板即「已选」，但依赖回调渲染的逐人填写框从未出现 → 提交报必填而框不存在；`destroy()` 内关面板与「回调→重渲染→再销毁」成环（用例由 11 秒劣化为 17.6 分钟超时） | `person-picker.js` 多选**点选即回调 + 关面板再回调**、调用方**重建前保态**、**销毁前先摘回调** | `inspection-loop-e2e.test.mjs`（真机闭环）、`ux-guard.test.mjs::⑦` |
| R21 分页「收进引擎」但未收口 | R14 只覆盖「已在引擎内的 26 处」；**绕过引擎、自己手写渲染的仍有 13 处以上**（表格 4 + 卡片列表 9 以上）。判据：引擎级单一源只覆盖「已在引擎内的调用点」，覆盖不了「绕过引擎各写一版」的地方 | 21 个文件接入 `renderFilteredList`（筛选 + 分页一站式）；引擎补「**无关键词无分面则不渲染检索条**」以支持「只需分页」的桶 / 分组子列表；**分组结构（看板桶 / 待办域折组 / 按期次分组）一律每组一实例、只给分页、不推平** | `filter-row.test.mjs::S12`（`<table` 收敛台账，4 处登记位 + 防僵尸） |
| R22 翻页标记仍散在调用点（且单一源位置会成环） | R14/R17 之后**仍有 6 处手写翻页控件**（`archive-page-btn` / `issue-page-btn` / `qv-page-btn` / `feedback-page-btn` / `visitor-act-page-btn` / `att-table-prev·next`）；而翻页标记若留在 `list-filter.js`，与「表态矩阵并入矩阵」会形成 `relation-matrix → list-filter → inspector → vote-summary-panel → relation-matrix` 的**模块环** | 翻页标记抽为**叶子件** `components/pager.js::pagerHtml`（不依赖任何业务模块），引擎与矩阵共用；6 处并轨后统一读 `data-lf-page` | `filter-row.test.mjs::S10`（改断言 `pager.js` 内 `if (pages <= 1)`）+ **S11**（`class="page-btn"`/`page-num` 只允许由 `pager.js` 产出） |
| R23 矩阵人维无上限 + 单一源吞语义 | R15 只封顶「项目维（列）」，**人维全量**；且表态矩阵的「异议红底 / 未投灰字」若并入通用矩阵就会被吞掉 | `relation-matrix.js` 补 `MATRIX_ROW_LIMIT = 10`（**人维在行 / 列两个方向同一切片**）、`cfg.rowLimit`、`.rm-pager`；新增 `cfg.cellClass(pid,iid)` **挂载点**（单一源只给挂载点，不吞领域语义） | `relation-matrix.test.mjs::S1` + **S5**（`rowLimit: 0` 须登记备案 + 白名单防僵尸） |
| R24 宽表只覆盖 2 域 + 被取代实现留痕 | Q-23-18 登记的其余域（支部分工 / 专班报名 / 表态矩阵 / 思想汇报台账）未迁；表态矩阵并入后 `styles.css` 留下 5 条 `.vs-matrix*` **死样式**与一条「例外：`.vs-matrix`」**失真注释**（注释与代码不同源即误导，比死样式更危险） | 推广三域（支部分工 人×工作项、专班报名总表 人×专班、表态矩阵并入）→ **全站矩阵实现收敛为 1 处**；清死样式 + 修正注释（`styles.css` 第 5 次特批）；**保留语义类** `.vs-none`/`.vs-object`/`.vs-note`/`.vs-tally`/`.vs-quorum`/`.vs-locked` | `relation-matrix.test.mjs::S4`（矩阵白名单**清空**）+ `filter-row.test.mjs::S3`（表格类族只允许 `.data-table`） |
| R25 「归一」出口不全 → 调用点各写兜底 | 思想汇报的读取侧归一（`_effective`：状态缺省/非法 → 已归档、无期次按 `submittedAt` 推导）此前只有「按人」「待初阅」两个出口；组织台要建**人 × 期次**台账时必须**跨人**取全量归一集合，无处可取（只能调用点自己补状态与期次的兜底 → 回到「同一口径几处」的老病） | `services/thought-report.js` 补**全量归一出口** `listAllThoughtReports()`（`listPendingReviews` 同批改走它 → **归一只此一处**）；台账改用 `renderRelationMatrix`（人维每页 10 人、期次列封顶 6 可展开、期次排序走 `core/period.js::comparePeriodDesc` 单一源），cell 语义（状态优先 + 篇数）留在域内 | `relation-matrix.test.mjs::S6`（接入单一源 + 列维＝期次 + 期次排序走单一源 + 行维取 `liveMembers()` 实时视图 + **禁回潮按人分组自建列表**）+ **真机②**（首列「姓名」/ 人维 ≤10 / 列头 `YYYY-Qn` 新→旧 / 有可下钻 cell 与「—」/ 待初阅队列仍在） |
| R26 「说明文件里的数字与名称」无人守 → 结构一改就漂 | 说明文件与代码之间实测积了 **34 处口径漂移**（各台 tab 数与名称、已删 tab 仍列在帮助页、tab 顺序相反、数据域表数在三份文档各有版本、页面数 19 vs 实 21、旧界面名长期未收敛）——**根因是没有守卫**，只能靠人记得同步，每换一次结构就漂一次 | ① 全部按**代码实测**修正（文档服从代码，不服从记忆）；② 数字分**五口径**并写明定义（mockDB 顶层业务域／持久化域／server 表／资源名／快照键，**严禁互相代入**）；③ 新增 `server/test/doc-consistency.test.mjs`，把口径变成**常驻断言** | `doc-consistency.test.mjs`（S1 各台 tab 数与代码注册数组一致（help §0.1 与 §2.x 两处都核）／S2 tab 名齐备且**已删 tab 名禁复现**／S3 分组名只有一套轴 + 旧组名禁回潮／S4 旧界面名黑名单零命中／S5 数据五数文档 vs 实测／S6 页面数 vs `docs/` 实况／S7 单一源登记处路径存在且非僵尸／S8 已废止功能域零残留／**S9 §0.2 总索引的守卫引用必须真实存在**（防文档写「S1–S5」而实际已到 S6） |
| R27 结构层守卫锁「形态」，锁不住「体验」 | 分页守卫（`filter-row` S3/S10/S11/S12、`relation-matrix` S1–S6）全是**按文件静态断言**：只能证明「引擎／矩阵在位」，**证明不了「七个工作台每个 tab 跑起来都真的分了页」**——而支书第 1 项提的恰是**体验**（「我注意到还是有部分表格没有能够做到分页」），病灶可能藏在「某个 tab 的某段数据」上 | 新增**真机全站普查** `server/test/page-sweep.test.mjs`：七个工作台 × **全部 tab** 逐一进页面断言（P1 引擎列表渲染行数 > 10 必有翻页控件／P2 矩阵人维 ≤ 10 行且计数口径「共 N 人 · 第 x / y 页」／P3 手写表格 > 10 行须有翻页／P4 首列是人的引擎表须有搜索框（按引擎门槛 `SEARCH_FILTER_MIN_ROWS`）／P5 每 tab 零 pageerror）；普查门槛值由 S0 锁单一源。**首次普查 0 违规**；唯一初始告警经复核为**守卫口径过严** → 修守卫、不改产品。同批补齐思想汇报台账「按人 / 按期次」互转视图（六域转置全覆盖） | `page-sweep.test.mjs`（S0 + 七个工作台真机普查 + S1 非空转，9 项；覆盖基线 tab 65 ／ 列表 53（>10 的 12）／ 矩阵 4 ／ 手写表 2） |
| R28 「同一病灶只修一处」当成了「修完」 | 支书批次 32 实报的「提交报必填、框却不在位」修好后，**同一形态全站还有 92 处**（提交动作的字段级必填校验，20+ 文件）——已修且经真机验证的仅 **1 处**；**没有「同类规模」这一步**，于是「修完一处」被当成「修完一类」 | ① 立**台账** `server/test/form-loop-registry.mjs`（**93** 条：file / 字段 / 流程 / 可否自动化，**2** 条非自动化逐条写明 reason；**分列口径随处置方式演进**：47-K「独立页+前置链 / 需前置业务数据 / 服务层重复守卫」→ 47-M 起改为「**已实测不可达**（写明纳入条件）」与「**还没去做**」分开写，47-Z 后**余 2 条均有裁定归属**——① `service/议题说明`：**两形态口径不一致的产品缺口**（`Q-23-48` 待裁）；② `service/活动复盘`：**服务层防御性重复守卫**（UI 层同名额已登记，**标不修**））；② 建**真机闭环普查** `server/test/form-loop-sweep.test.mjs`（**54** 条流程 + **17** 条真机成功路径：空必填点提交必须出现可见提示，且**提示点名的字段须有可见载体**；载体不在位却报字段级必填＝违规；既无提示也无提交成功＝静默失败＝违规。⚠ `machine:true` 只保证**必填校验分支**，不保证成功路径）；③ 台账守卫 S0–**S5**（规模不得静默缩水 / 白名单必带 reason / machine:true 全部纳入覆盖 / 无僵尸条目 / 出处文案存在 / **成功路径清单字段齐备**）；④ 纪律入 `DATA_CONSISTENCY_CHECKLIST` **第十三范本 + §0.1 第十三问「同类规模」** 与 `CLAUDE.md R-67`（后续强化见 `R-73`（三缺一）/ `R-78`（不可达的两条出口）/ 范本第二十五–二十六） | `form-loop-sweep.test.mjs`（S0–S5 + **54** 条真机闭环 + **17** 条真机成功路径，**77 项**；计数为批次 47-Z 后值） |
| R28-附 工具脚本会把「**数据**」当「**代码**」改写 | `bump-version.mjs` 的 server-test 补戳正则按「字符串里含 `/src/….js`」判缓存键 → **命中了台账数据**：`form-loop-registry.mjs` 里 **113 条 `file` 字段**被补上 `?v=`，**数据被改坏**；而守卫 S4 当时只报「文件不存在」，看不出被谁改坏（**真事故**，2026-09-15 批次 44） | 缓存键**语境判据**抽为单一源 `docs/scripts/version-next.mjs::isCacheKeyLine`（补戳与收尾陈旧戳自检同判据）；台账数据改写为 `SRC + '相对路径'` 拼接，使字面量里**不再出现 `/src/….js` 连写**，补戳脚本无法命中 | `version-stamp.test.mjs::S4–S6 + D7–D9`（含「非 import 语境逐字不变」）· `form-loop-sweep.test.mjs::S4`（防污染断言：数据里不得出现 `?v=`） |
| R29 授权声明必须可核验（注释不得伪造「支书批」） | 一条「开学第 1 周·请逐人归集组员进展」提醒**随批4「域参数」一起进仓**，注释写着「支书 2026-09-09 批」，而全仓**找不到**支书就该**具体功能**的问答记录——把「**批次整体通过**」当成「**逐项通过**」，并在注释里**伪造授权凭证**（支书原话：「我为什么会批准这些信息…这完全是滑稽」） | 立 `CLAUDE.md R-70`：新增**面向用户的提醒 / 流程 / 人工动作**须在报告中**单列逐项请支书确认**，不得随批次「一起进」；注释里的「支书批」**必须能指到具体问答记录**，指不到不许写。守卫判据**收窄**到「支书作为批准者的断言」（业务语汇「报支书确认」「支书确认才生效」不计——收窄前 370 条命中、收窄后 229 条） | `doc-consistency.test.mjs::S12`（**同一行须带可核验日期 `YYYY-MM-DD`**；12 文件 13 条迁移台账已**清空**＝此后零容忍）。⚠ 如实标注：**日期能否真指到问答记录，机器查不了**，该半截靠支书复核——不假装守卫覆盖了整句纪律 |
| R30 「人工归集」类要求须由服务端代劳 | 同一条提醒要求支书「**请逐人归集**」组员进展——而四项（思想汇报 / 考察 / 复盘 / 在办事项）**都已建服务端表**、**均有资源出口**，缺的只是「按 `personId` 聚合」的读接口。**算得出来的东西派给人做**＝虚假工作量 | 纯聚合**单一源** `docs/src/services/member-progress.js::aggregateMemberProgress`（服务端**同源 import**，不自写判定）＋ 读接口 `GET /api/v1/leader/member-progress`（`requireAuth`；`today` 由参数给定防两端时区漂移）＋ 前端双态载入器 `loadMemberProgress`；界面由「催人干活」改为「**呈报实况**」 | `member-progress.test.mjs::S1–S4`（**S1＝结构单一源**：接口源码不得出现 `isTodoExpired` / `AttendanceStatus` / `reportCategory` 等自写判定；S2 四项真算到 · S3 双态同源逐字相等 · S4 参数守卫 400/401） |

> **本轮方法论要点（可迁移）**：①「**同一事实几份**」→ 3.3 去重；「**同一能力几处**」→ 本台账**引擎化**（R14/R15）；②「**口径寄生环境**」→ 算式显式（R17）与「量尺规须生产同构」；③「**绕过守卫的等价写法**」→ 守卫须按**语义**判而非按字面判（R16 快照 / R18 人名变量 / R13 注释两级规则）；④「**为修一个 bug 复制一份实现**」→ 同一实现体挂多门（R19）；⑤「**能力收进引擎 ≠ 已收口**」→ 必须**反向枚举**（先查使用点、再查实现点），因为单一源只覆盖「已在它内的调用点」（R21/R22，两轮才补完）；⑥「**单一源搬迁 = 一次全仓改签**」→ 换文件 / 换名字后须 grep 旧路径（含 `?v=`）并断言 0 命中，静态守卫只锁形态、锁不住引用完整性（R22 的真实事故）；⑦「**单一源不吞语义**」→ 通用能力只提供**挂载点**（如 `cellClass`），领域语义留在域内（R23）；⑧「**被取代的实现痕迹要连注释一起清**」——注释与代码不同源即误导，会被后续 AI 当规范读（R24）；⑨「**归一也是一份口径**」——字段补全 / 状态兜底这类「读侧归一」同样只能有一个出口，出口不全，调用点就会各写一份兜底（R25）；⑩「**文档里的数字与名称也是口径**」——文档与代码不一致不是文风问题，而是**缺守卫**：凡说明文件里出现的「数量／名称」都必须能指到代码出处，并写成**常驻断言**（R26）；⑪「**静态断言锁形态、真机普查锁体验**」——支书以「我体验到的」提出的病灶，必须用**真机全站普查**取证，静态守卫只作补充（R27）；⑫「**只修一处＝没修完**」——病灶是「**一类形态**」不是「一处 bug」：修完必须立刻数「同一形态全站还有几处」（按语义枚举），>1 处即建台账＋真机覆盖，并让守卫把「新增同类未纳入覆盖」变成红灯（R28）。

#### 复核保留（2026-09-03 定，防行为失同步）
- ① TYPE_META/STATUS_META：支部侧（「待党委批复」+desc）与党委侧（「待批复」）文案不同，属**双视角差异**，暂不合并。
- ② makeup `MANDATORY_ACTIVITY_TYPES`（刚性考勤子集+主题党日）与 gallery 色名：业务语义 ≠ 三会子类清单，不并入 subtypes。

#### 复核约定

- 每完成一个行动线条目，回四更新状态并留 T- 日志。
- 评分按季度或重大架构变更后复核一次，防「评估僵尸化」。
- 用户否决某条优先级时，只调顺序不改表结构；新方向裁决追加为后续编号行（P5+/后续跟踪行）。

---

## 四、改造行动线（现行唯一行动清单）

> **语义**：本表收敛原四行动优先级 + 3.3 去重队列（P0a~P2c）+ 超参/组合行动（P3a~P3e）+ 色值与 deploy 改进（8.7-①~④）+ 模板化落地（P4a~P4d）执行状态，为后续开发顺序的唯一依据。**状态记号：✅=已完成 / 立项=在立项 / 后续=后续批次或残项跟踪**；每批完成回本表更新并留 T- 日志，沿革指执行日志（见附）。

| 编号 | 行动内容 | 状态 |
|------|---------|------|
| P0·统一扎口 | 以 forms.js 为样板，为徽章/状态、数据视图等高频组件域逐一建库出口、全站收口（验收：每建一库跑 module-load + E2E；仓库无该域直连残留） | ✅ forms/badges/reporting 三库完成（组件平铺层同域多文件已收敛，纪律见三·3.1）；残项=徽章/选择器等出口仍散、服务层个别 UI 依赖（见二·2.1，随行动线持续收口） |
| P0·数据域接线 | tab/能力声明依赖的 service + mock 整体可替换（验收：新增 demo 分支或后端接入时 UI 零改动，data-adapter 双实现全量走通） | ✅ 收官：person 域/formatter/PEOPLE/机构/登录四批收口完成——UI 层 mock 直连清零，种子仅存 services/core 数据层（契约见三·3.2） |
| P0a | R1 微工具建库：escHtml/fmtDt 收敛到唯一工具出口，10 处本地 `esc` 改 import | ✅ 已完成（2026-09-03；grep 本地 `function esc` 归零，module-load 全绿） |
| P0b | R2/R3 元数据单一源：MEETING_TYPES、write-tab 4 id、calendar blockId 改为引用 constants/manifests（验收：全仓该类型/4 id 无第二份字面量，写路径零行为变化） | ✅ 收口（2026-09-03）；TYPE_META/STATUS_META 与 MANDATORY_ACTIVITY_TYPES/gallery 色名 2 项复核保留（见三·3.3 复核保留） |
| P0c | R5 防失同步测试落地：断言 FLOW_LINKS 键集 == function-catalog flow id 键集（落实 mermaid-sources.js:30 注释承诺） | ✅ 已完成（2026-09-03；flow-catalog-sync 双向断言测试入 server/test 且绿） |
| P1·开源合规包 | LICENSE、示例账号外置 env、部署/贡献说明（验收：新机器按 README 可独立跑通并自建数据） | ✅ 部分→闭环：根 LICENSE + CONTRIBUTING.md + server/.env.example 已在仓（2026-09-04），数据真人化整体可替换（2026-09-06，P4a 换壳指南补验收）；残项=无 English 版、release 发布工作流（见二·2.3） |
| P1a | R7/R8 跨层单源：config 净化与表决枚举改 server 单向权威 or 前端生成 → 注释互链 + 键集测试 | ✅ 已完成（2026-09-03；config-clean.js 共享纯模块 + OPTION_ENUMS 双向断言，28 测试中相关全绿） |
| P1b | R12 运行安全（口径修正后唯一保留项）：server 登录加密码校验（可开关，缺省演示态兼容）——多人/计算中心部署时防凭 personId 冒名 | ✅ 已完成（2026-09-03；缺省 '123456' 可换 `LOGIN_PASSWORD` + `DISABLE_PASSWORD_CHECK=1` 逃逸门，auth-password 5 态测试绿；生产须显式换密） |
| P2·L3 拖拽 | L3 block manifest + 拖拽编排（根 README 总目标）（验收：块声明 inputs/事件/校验契约定稿并经用户确认后编码） | ✅ 契约 v1.1 定稿；L3 落地（manifests 双块 + 校验器 + config.workflowBlocks 配置区 + 主题党日 manifest 驱动 S4；独立表单渲染桥 2026-09-09 代码减负撤回，渲染走 forms.js + manifest 元数据）；拖拽编排全站推广=L1→L5 未达（转 P4d 后续） |
| P2a | R6 结构性统一：tab id 收敛——`core/tab-nav.js` 纯决策 + tab-bar 守卫（原随 L4 一并做的拆分提前单做） | ✅ 已完成（2026-09-03；隐藏 tab 回退首个可见 tab、杜绝静默白屏，tab-nav 4 态绿） |
| P2b | R4 结构性统一：写活动场景选择清单收敛单一源（constants `SCENARIO_WRITE_IDS/SCENARIO_LABELS` 派生） | ✅ 已完成（2026-09-03；scene-write-sync 3 断言绿，写路径无第二份字面量） |
| P2c | R9 结构性统一：授权语义角色集单一源（constants 四常量；至此去重队列 8/8 收口，P0b 2 项复核保留为有意决定） | ✅ 已完成（2026-09-03；五处本地手写角色集清零，roles-sync 4 断言绿） |
| P3a | 阈值参数化：票决门槛（quorum/veto）→ policy-defaults 常量默认 + evaluateWorkforceVotes(opts) 覆盖（2026-09-06 支书裁语义收紧：应到 >2/3 且无反对、弃权允许——附录⑩ S2 R2-3） | ✅ 已做（2026-09-05 初版 + 2026-09-06 语义裁；默认行为不变，用例绿） |
| P3b | 纪检会议考勤类型单源：`MEETING_ATTENDANCE_TYPES` 自 policy-defaults 派生导出，disc tab 引用（消双写） | ✅ 已做（2026-09-05；全仓该类型清单无第二份字面量） |
| P3c | 建立 `core/policy-defaults.js`：业务阈值/名单（上传位例外、超期天数、应到 roster 等）集中默认 + 逐项注释「支部默认/制度固定」 | ✅ 已完成（2026-09-05 spec open-source-deploy-improve；`policy-config.test.mjs::T1–T5` 5/5 绿——该 5 条原为 `policy-defaults-sync.test.mjs`，2026-09-15 批次 47-F 第二组并入同域文件，消费点改引用） |
| P3d | 模块组合声明契约：capabilities/模块元数据补 depends/combinesWith/exclusive（草案）+ config 校验扩展 | ✅ v0 已完成（2026-09-05；module-compose 纯校验 + 失败态测试 6/6 绿 + capability-registry 7/7 绿 + 契约文档节）；服务端 config 校验留 v1 |
| P3e | 色板令牌收敛：styles.css 令牌与 COLOR_SYSTEM 对齐，固定/可调标注在文件头注释与四层表（8.6 落地：党建红/党徽金固定令牌化、`--accent-deep` 历史存量收敛为雾紫三源一致） | ✅ 已完成（2026-09-05；全仓无游离业务色字面量，品牌/角色色一律走令牌） |
| 8.7-① | 支书台分工面板保态（最小操作成本）：发起表单区 `#wf-form-zone` 独立于议题列表容器，列表重建不触碰表单 DOM | ✅ 已完成（2026-09-05） |
| 8.7-② | 支书台分工面板多议题票决判定并行：串行 for…of → `Promise.allSettled`（单失败 console.warn 不阻断）——缩短支书等待 | ✅ 已完成（2026-09-05） |
| 8.7-③ | 纪检会议考勤录入「收起/展开」保态：容器 CSS `hidden` 切换（不销毁 PersonPicker、不重建 innerHTML），仅提交成功后才重置会话（外部 re-render 重建为既有行为，已注释说明） | ✅ 已完成（2026-09-05） |
| 8.7-④ | deploy 文档口径风化修正：server/README 与根 README 测试计数改动态口径、命令段与 package.json scripts 对齐、env 说明补 LOGIN_PASSWORD/DISABLE_PASSWORD_CHECK | ✅ 已完成（2026-09-05；计数遗留经减负评议处理——CLAUDE.md:131 动态口径、SNAPSHOT 去固定计数，见 REVIEW_QUEUE 附录⑥） |
| P4a | 模板型落地最小包：根 README 增「给新组织：30 分钟换壳指南」（clone 跑 → 换 mock 数据 → 角色/术语/配色/policy 默认 → 支部名与分支配置 → npm test 验证）＋ 浏览器演示数据一键重置（URL `?reset=1` 清除本域演示存储键回种子初始态） | ✅ 已完成（2026-09-06；落地=根 README「复用与二次开发」节 + 「快速开始」节；mock-adapter.js `MockAdapter.loadDB()` 读取即检测 `?reset=1`，清除 `workflowos_*`/`gsm1921-*`/`sop_org_os_*` 前缀键与历史遗留键后整页导航回种子初始态；无 API token 时才执行，不清 sessionStorage，不破坏 API 模式） |
| P4b | 换组织向导页（立项④，2026-09-06 立）：引导式完成 people/accounts/branches/policy 替换并生成组织配置包（验收：向导产出可一键应用的换壳配置） | ✅ 主体落地（T-2026-09-044~047：支书 R1–R4 裁定 + 阶段一/二/三目标1，wizard.html 上线——详见 REVIEW_QUEUE 附录⑧）；阶段三候选（数据层覆盖预览/党委默认模板抽象）登记后续 |
| P4c | 演示数据与空组织模板分离（立项⑤，2026-09-06 立）：仓库随附「模板 + 示例组织」双形态、seed 完整性自动校验、一键重置分层服务化 | ✅ 落地（T-2026-09-047/048：空组织模板 + 建新支部正式能力 + ?reset 分层 demo/preview 与服务端对齐，详见 REVIEW_QUEUE 附录⑧）；域分区缺口（业务域非 branch 分区）已注释诚实边界 |
| P4d | 组合能力补强（后续批次）：policy-defaults 接入 config 驱动（超参可调不改码）、requiredRoles 门禁消费、拖拽编排由主题党日推广全站（L1→L5） | ✅ 域参数 config 驱动已落地（2026-09-09 批4：POLICY_OVERRIDABLE 白名单 + 设置「域参数/制度参数」UI 覆盖 + 审计 why/回滚）；全量 config 引擎 / requiredRoles 门禁消费 / 拖拽推广全站 = 后续（残项见二·2.4/2.5 与一速览失分行） |
| 后续·残项跟踪 | 组件出口继续收口（徽章/选择器等、服务层个别 UI 依赖）、manifest 防失同步测试补全、无 English 版、语义化 release 发布工作流 | 后续 |

---

## 五、经验索引与沿革（便于 check；2026-09-15 批次 44 建）

> **本节唯一职责**：让人（支书）与 AI 在**一屏内**看清「哪一轮做了什么 → 收敛到哪里 → 由谁守 → 还欠什么」。**本节不复述细节**——判据在守卫文件、规则在 `DATA_CONSISTENCY_CHECKLIST §0.2`、原始记录在执行日志（单一源 + 指针纪律）。

### 5.1 沿革表（批次 25–47-C，2026-09-14 起）

| 批次 | 主题（病灶类） | 收敛去向（单一源 / 引擎 / 纪律） | 守卫 | 状态 |
|------|----------------|-----------------------------------|------|------|
| 25–35 | 三项「能力/口径散落在调用点」 | 版本号推导 `version-next.mjs`；分页收进统一检索引擎 `list-filter.js`；人×项目矩阵 `relation-matrix.js` | `version-stamp` S1–S3/D1–D6、`filter-row` S1–S8（时点）、`relation-matrix` S1–S4（时点） | 已闭环 |
| 36 | 文档与经验第一次沉淀 | `CLAUDE.md` R 表 + 范本/判例入 05 层 | — | 已提交 `94620ff0` |
| 37 | **未接引擎的手写渲染**（表格 4 + 卡片列表 9+） | 21 文件接入引擎；引擎补「无关键词无分面则不渲染检索条」；分组结构保形 | `filter-row` S11（翻页标记只许 `pager.js` 产出）、S12（手写表格收敛台账） | 已闭环 |
| 38 | 翻页标记**各写一版** | 下沉**叶子件** `components/pager.js`；6 处手写翻页并轨；矩阵补**人维分页**与 `cellClass` 挂载点 | `filter-row` S10、`relation-matrix` S1/S5 | 已闭环 |
| 39 | **宽表（转置）**只在考勤考察 | 支部分工（人×工作项）、专班报名（人×专班）、表态矩阵并入 → 矩阵实现收敛为 1 处、白名单清空 | `relation-matrix` S4（白名单清空后按语义判） | 已闭环 |
| 40 | 守卫与文档的**同步债** | 守卫收紧 + 文档全量同步（README×2 / help / COMPONENT_SPEC / 范本 / 判例 / R 表 / SNAPSHOT / 日志） | 同上（收紧项即守卫） | 已闭环 |
| 41 | 矩阵推广**末域**（思想汇报台账） | 人 × 期次宽表；读侧归一收成唯一出口 `listAllThoughtReports()` | `relation-matrix` S6 + 真机② | 已闭环 |
| 42 | **说明文件里的数字/名称** 34 处漂移；tab 分组两套轴；公邮名不副实；issue 无支部归属 | `doc-consistency.test.mjs` S1–S8 把数字/名称变常驻断言；分组单一轴；公邮**成套废止**（减法）；issue 加 `branchId` 全链路按支部过滤 | `doc-consistency` S1–S8、`issue-branch` S1–S4/D1–D3 | 已闭环 |
| 43 | 分页/宽表只有**结构层**守卫，锁不住**体验** | `page-sweep.test.mjs` 真机全站普查（七台 × 65 tab：分页/宽表/检索/零脚本错误）+ S1「非空转」钉住覆盖基线；六域转置全覆盖 | `page-sweep` S0/P1–P5/S1（9 项） | 已闭环 |
| 44 | **同一病灶只修一处**（支书实报的「报必填、框不在位」类全站 92 处，只修了 1 处）；规则/守卫/状态分散 5 处；**交付可见性**（346 项未提交致「检查发现没变」）；工具脚本改写数据字符串 | 台账 `form-loop-registry.mjs` + 真机闭环 `form-loop-sweep.test.mjs`（S0–S4 + 10 条流程）；`DATA_CONSISTENCY_CHECKLIST §0.2 规则→守卫→状态 总索引` + `doc-consistency` **S9**（索引引用必须真实存在）；`CLAUDE.md` **R-66/R-67**；本 §五 | `form-loop-sweep` S0–S4 + 10 条真机；`doc-consistency` S9 | 已闭环（`Q-23-33` 补戳脚本判据收紧留待下一小批） |
| 45 | 文档全量同步 + 全链版本戳 bump（交付笔，未推送） | 说明文件口径与 `?v=` 全链一致 | `version-stamp` S1–S3（时点） | 已提交 `320c8dbc` / `51293883` |
| 46① | **工具脚本把「数据」当「代码」改写**（`Q-23-33`：补戳正则命中 113 条台账 `file` 路径数据） | 判据收紧为**「缓存键语境」**并下沉单一源 `version-next.mjs::isCacheKeyLine / stampTestFileContent / cacheKeyStamps`；**补戳与收尾自检同判据**；清存量 18 处去戳 + 11 处注释泛化 | `version-stamp` **S4/S5/S6 + D7–D9**（S6＝以活动戳干跑零改写） | 已闭环（含本批提交） |
| 47-A | **守卫存在却没人看得见**（批 43 的 `page-sweep` 至今未进 README 守卫清单） | `doc-consistency` **S10**（§0.2 引用的守卫必须登记进 README 测试清单）+ **S11**（README 断言号必须真实存在）；README 守卫清单全名化并补全 21 个 | `doc-consistency` S10/S11 | 已闭环（`Q-23-37`） |
| 47-B | **真机 ≠ 真环境**（支书实报「人作首列的表格字体小」）：① 单档 13px 只声明在 `<table>` 上被继承，CDN Tailwind 的 `.text-xs`(12px) 同特异性且注入在后 → 生产实际 12px；② 批 43 的普查**把 Tailwind 静音了**，故该越界在量测环境里不存在、一直报 0 违规 | 单档改为**直接声明**到 `th/td`（`styles.css` 特批第 6 次，特异性高于单类工具类）；被静音资源**逐条登记理由** | `filter-row` **D3/S13**；`page-sweep` **P6–P9 + S2 环境自检 + S3**；`CLAUDE.md` **R-68**；`范本第十五` + `§0.1 第十五问` | 已闭环（`Q-23-34` / `Q-23-38`） |
| 47-C | 普查**两个盲区**：只看默认视图（不点二级动作）、只认 3 类载体（卡片/行块完全不覆盖） | 每 tab 追加**二级视图审次**（矩阵展开 + `[data-view]` 视图切换）；`P2` 改按「行维是不是人」分流；新增 **`P11` 自建列表必须分页** | `page-sweep` P11 + `SWEEP.views` 非空转 + 待修台账防僵尸 | 已闭环；`Q-23-40`（2 处接分页）待下一批 |
| 47-H | **自建列表不接引擎即不分页**（支书实报「还有些表格没分页」）+ **注释伪造支书授权**（Q-23-41） | 档案归档 22 块 / 组长台卡点 13 块**接统一检索引擎**（并**取消**上一批「派生清单不接引擎」的例外；「了解进展」改容器委托）；新增 **`CLAUDE.md R-70`** + `doc-consistency` **S12**（授权声明须同行带日期；判据收窄到「支书作为批准者的断言」） | `page-sweep.test.mjs::P11`（台账清空）· `doc-consistency.test.mjs::S12` | 已闭环（`Q-23-40` / `Q-23-41 ① / Q-23-42 / Q-23-43`） |
| 47-I | **要求用户做「人工收集」而数据本可由服务端算出**（支书：「我从来没说过要有一个收集过程…照理说后台服务器已经自动把相关信息都计算汇总好了」） | 抽**纯聚合单一源** `docs/src/services/member-progress.js`（服务端同源 import）＋读接口 `GET /api/v1/leader/member-progress`（`requireAuth`、`today` 参数化）＋前端双态入口 `loadMemberProgress`；组长台四项判定**移出** tab 文件；`today` 提醒**删掉人工归集要求**、改**呈报实况** | `member-progress.test.mjs::S1–S4`（S1＝**结构单一源**：接口源码不得出现自写判定） | 已闭环（`Q-23-41 ②`；`§0.2 R30`） |
| 47-J | **「必填会报错」≠「填对能成」**（`Q-23-44`：`machine:true` 只覆盖**校验分支**，成功路径全无覆盖）＋**测试文件按前缀凑组**（47-F 第三组：「先量形态与编号，再合」） | **① 成功路径第二阶段**：`form-loop-sweep` 增设 `SUCCESS_FLOWS`（填对 → 触发 → ①成功提示 ②**当场**真生效 ③**整页重载后仍成立**＝落库证据），台账口径改为「**两处都命中才算整体已被真机验证**」；`S5` 新增（清单不得缩水 + 每条字段齐备）。**② 47-D 续 ⑨⑩**：组长台批量改状态空值分支（成功路径面归 Q-23-44，**同一条 open 链两处受益**）· 组长台活动管理·发起活动（**一条流程覆盖三处校验点**）。**③ 47-F 第三组先量后合**：`roster.test` 吸纳 `roster-vote-link`（2→1）· 新建 `wizard-config.test` 吸纳 copy+package 写口组（2→1）；`roster-ui-logic` 与 `wizard-report` 因**不同域 / 不同形态（纯函数无桩）单列**。**④ 13 条无日期授权声明逐条回查补证**，`S12` 迁移台账**清空**＝此后零容忍。**⑤ §3.4 补 R28-附/R29/R30 三行**并同步过时计数 | `form-loop-sweep.test.mjs::S5` + **2 条真机成功路径** · `doc-consistency.test.mjs::S12`（台账清空）· `roster.test.mjs` 20/20 · `wizard-config.test.mjs` 15/15 | 已闭环（`Q-23-42 / Q-23-43 / Q-23-44`） |
| 47-K | **台账里的 `reason` 大多不是「实测结论」而是「AI 的假设」**——且其中相当一部分**理由本身就说出了解法**（「需先有考勤记录」「前置交互复杂」「默认全选」「默认预填当日」「文件选择器设不了值」）；另有**「工具够不到」被记成「自动化不了」**（独立页站点因 schema 只走工作台 tab 而长期挂 `machine:false`） | ① 为「够不到」补**五项能力**（每项解锁一类，不是为单条开的口子）：`path`（**独立页** + `?tab=` 深链）· `noticeSel`（**面板内状态区**：`showStatus` 不写 toast，读错载体＝误判静默失败）· `setChecked`（**默认全选/预勾**）· `expect[].submit`（**一个载体多个提交口**）· `ready`（就位标记）；② 五小批 **+24 处**真机覆盖、**+11 条流程**（`machine:true` 37 → **61**；`machine:false` 55 → **31**），全批首跑或二跑即绿；③ 余 31 条按「**为什么还不动**」**分三类**（独立页+前置链 / 需前置业务数据 / **服务层防御性重复守卫**），并明令「该类不得再挂『需先…』」 | `form-loop-sweep.test.mjs` **39/39**（6 守卫 + 31 条真机流程 + 2 条真机成功路径）· `roster` 20/20 · `wizard-config` 15/15 | 已闭环 |
| 47-L | **「不可达」与「没去做」在台账里长得一模一样**（于是每一批都会把已实测过的条目**重新摸底一遍**）；且 `reason` 第三次被真机证伪（「一键汇报浮态由各台 extra 入口打开」→ 其实是**顶栏常驻按钮**；「inspector 内联新建活动」→ 实为**编辑浮窗且三字段全预填**；「表态需进入表态态」→ **无额外门槛**） | ① 新覆盖 **6 处**（顶栏一键汇报 · 巡查信息编辑三处 · 巡查表态 · 活动详情页表决）；② **首次把「已实测不可达」写进 reason**：对 2 条（专班产出说明 · 决议落实三处）做**源码条件 + 种子全量**双向取证后据实回退 `machine:false`，reason 改写为「实测事实 + **纳入条件**」；③ 台账守卫口径随之明确：**真不可达＝写实测证据与纳入条件；没去做＝照类推进** | `form-loop-sweep.test.mjs` **43/43**（6 守卫 + 35 条真机流程 + 2 条真机成功路径） | 已闭环 |
| 47-M | **真机普查抓出一个真 bug（`services/issues.js` 11 处 `showToast` 从未 import → 运行时 `ReferenceError`）：校验失败处「点提交毫无反应」、成功处「事办了却一声不吭」**；根因是这批站点长期被含糊 reason 挂在白名单里 ⇒ **白名单不只是「没测」，它会持续掩盖白名单里的病灶**。另证伪 5 条 reason（文件选择器不可设值 / 需先选定关联活动 / 多步向导 ×2 / 两步点击）＝「理由即解法」**第三次复发** | ① **补第三类判据**：真机跑每条流程后凡有 `pageerror`（`ReferenceError`/`TypeError`/`is not defined`/`Cannot read`）**一律红灯**——前两类判据都从「提示文本」出发，这类病灶**根本不产出文本**，只会被报成「静默失败」而**成因查不出来**；② 修复该 bug（补导入 + 注明为何会有这个洞）；③ 新覆盖 **9 处 / 6 条流程**（我的处置待处置详情两处 · 档案归档两处 · 组长台子记录 · 议题详情独立页 · 向导新建支部两处 · 议程目标阶段）；④ `SUCCESS_FLOWS` 补 **2 条**（其中一条独立页 + 整页重载），并为独立页打通成功路径执行链（`loginAs` 漏传 `path`、重载后仍去切 tab 两处）；⑤ 余 16 条 reason **逐条改写为「实测计数 + 为什么 + 纳入条件」**，并把「**服务层重复守卫**」正式立为独立一类（不修，只标）；⑥ 新形态入库：「**空值把整条数据挡住**」（`collectAgendaRows` 的 `if (!item) return null` 让「已选人未选目标阶段」这一支根本走不到校验）——与「默认预填藏必填分支」同族反向 | `form-loop-sweep.test.mjs` **51/51**（6 守卫 + 41 条真机流程 + 4 条真机成功路径） | 已闭环 |

| 47-N | **成功路径覆盖「占比反而更低」**（47-M 把 `machine:true` 从 67 推到 76，而成功路径只从 2 到 4 ⇒ 覆盖面被自己摊薄）；且阶段一覆盖的**高频写口**（通知发布 / 新建支部）**成功面此前完全无证据** | 按「**先高频 + 校验面已覆盖**」两条判据补 `SUCCESS_FLOWS` **4 → 6**：支书台通知发布（带整页重载）· 党委台新建支部（跨「表单 → 服务端 createBranch → 列表刷新」的完整落库链，带整页重载）。**新记一条断言写法的坑**：`CARRIER_PROBE` 只返回**首个**匹配元素的文本 ⇒ 「列表里有没有新条目」类断言**不能拿多实例选择器当载体**（`[data-branch-card]` 首个恒是既有支部），必须取覆盖全部卡片的外层容器 | `form-loop-sweep.test.mjs` **56/56**（6 守卫 + 41 条真机流程 + 9 条真机成功路径） | 已闭环 |
| 47-O | **「成功路径」的第二小步暴露了一个隐蔽的隔离陷阱**：既然「上报党委」成功路径已能把「待批复事项」造出来，看似可顺势把「党委台驳回意见」转 `machine:true`——**不能**：每条流程用 `browser.newPage()` 起**全新上下文**（localStorage 不跨测试共享），而适配器当前是 **mock 态** ⇒ **上一条测试的写到不了下一条的页面**。「跨测试/跨账号前置」只能走种子，不能靠上一条流程的副作用 | `SUCCESS_FLOWS` **6 → 9**（组织委员台发起专班＝**两步写的成功提示即链路完整性证据** · 独立页向导新建**空支部**＝跨服务端 `createBranch` 的完整落库链 · 支书台上报党委）。两处新手法入库：① **取消勾选靠 `setChecked` 而非 `click`**（`_doCreate` 读 DOM `.checked` 而 `setChecked` 故意不派发 change ⇒ 既不改重渲染、又改到真正被读的值），且 **fill 顺序必须是「先取消勾选、后填名称」**；② `setChecked` 首次用于成功路径（此前只在阶段一清值） | `form-loop-sweep.test.mjs` **56/56** | 已闭环 |
| 47-P | **一个真实的「功能不可达」被记成了一条看起来无害的白名单理由**：党委台「驳回」的旧 reason 写「实测入口计数 0，因为种子无此条目」——**实测是对的，但停在「现象」就下了结论**；真问题是该前置**在两形态下都恒不存在**（`mock/seed.js` 无上报种子、`server/seed.js` 也未播种 `review_requests`）⇒ **支书永远点不到「驳回」**。这是 47-M「白名单会持续掩盖病灶」的**第二次应验**。★ 解锁后新写的流程**首跑即红**，又抓出**第二个真 bug**（见右） | ① **改种子解锁**：新增 `SEED_REVIEW_REQUESTS` 并**两形态同源**播种（mock 走 `mock-adapter._seedInitialData()`、api 走 `server/seed.js`，都读 `mock/seed.js` 一份）——**只种 mock 就是假绿**（守卫绿而支书在 api 演示里仍点不到）；② **真机抓出「单参 `showToast`」**：`party-committee/review-tab.js` 三处 `showToast('文案')` 而接口是 `showToast(type, message)` ⇒ 文案被当成 type、`message` 为 undefined ⇒ 用户只看到一条**只有图标没有文字**的蓝点气泡（驳回失败「为什么不让驳回」一字不说、批准/驳回成功也**看不到结论**）；③ **补守卫盲区**：`ux-guard ⑥` 原**只匹配两参形态**（专抓「参数写反」）⇒ 单参从它眼皮下走过；已补**参数个数**维度。**元教训**：静态守卫要**按接口的完整约定逐维覆盖**，不能只把「上次踩过的那个坑」写成唯一判据；且此类病灶**不是异常、不是缺导入**，**只有「把提示文案当断言对象」的真机判据抓得住** | `form-loop-sweep.test.mjs` **57/57**（42 条真机流程）· `ux-guard.test.mjs` **7/7** · **种子敏感面 109/109**（改种子未打破既有断言） | 已闭环 |
| 47-Q | **「改种子解锁」撞上一个真口径冲突**：余 11 条里最大一簇（内部汇报 4 处）的公共前置是 `kind:'report'` 的汇报，而该域的**内容单一源 `docs/data/issues.json` 同时是公开匿名反馈页的数据源** ⇒ 直种＝**内部汇报出现在公开面**。而这一点在「允许改种子」的授权里并没有被覆盖——**授权范围与实际影响面不等** | 按支书裁定（2026-09-16「加过滤 + 种进同源」）三步：① **公开面加过滤**（公开页列表与统计**同一个 `kind:'feedback'`**——两处若不同 kind，条数与徽标会各算一套）；② 种进同源（两形态共用一份文件）；③ **服务端按 kind 分流脱敏**（真匿名是「公开反馈」的口径，内部汇报按设计带名；一律脱敏会让 api 形态看不见汇报＝**假绿**）。**取证不靠「应该没问题」**：临时探针实测公开页 4 行、`状态：全部 (4)` 且无汇报标题，四条新流程全绿（＝汇报确实进库），**探针跑完即删**。另两处纪实：**两条种子行的日期是刻意早于公开反馈的**（否则会动到既有 `secretary-feedback-*` 流程所点的第一行）；**`doc-consistency S12` 当场拦下我**——新注释写了无日期的「按支书裁定」，守卫从基线 0 涨到 1 即红（它正是为「防注释伪造支书批」而立） | `form-loop-sweep.test.mjs` **61/61**（46 条真机流程）· `page-sweep` 11/11 · `doc-consistency` 12/12 · 种子敏感面定向全绿 | 已闭环 |
| 47-R | **补种子时踩到「种子把一个分支挡住了」的第二形态**：按台账纳入条件把演示账号排为某活动的复盘责任人，**首跑仍失败**（10s 超时、无复盘表单）——原因是随手挑的 `act-3` **已有复盘记录**，行卡因此落进「已复盘」列、渲染的是**只读详情**。**归属只决定「出不出现」，已有记录决定「出在哪一列、渲染哪套 UI」**。另一处：思想汇报「修改并重新提交」的 textarea **出厂预填「上一次的内容」**（不是占位符、不是默认值）⇒ 空提交**直接成功**，「必填分支」根本走不到（「默认预填藏必填分支」**第 14 例**）。★ 还发现**台账自己的一处缺口** | ① 改种子：`act-9`（`mock/review.js` 中**无复盘记录**的活动）增 p5 为 `deep`；`mock/thought-reports.js` 新增 `tr-5`（p5 · `needs_revision` + `reviewHistory`，与 `reviewThoughtReport` 的写入同构）；② 流程 `open[]` 先清空预填；③ **补登 `thought-report-entry.js:287`**（打回须填初阅意见）——**这条本就不在 92 条台账里**，而 S0 只防规模缩水、S2 只防 `machine:true` 漏覆盖，**都不防「漏登记」** ⇒ 一处可达的校验点可以长期**没人登记也无人跑**（`VALIDATION_SITES` 92 → **93**）；④ 新增纪律 **R-73**：**台账完备性有三缺一**（规模不缩水 / 已登记项全覆盖 / **全站是否都已登记**），前两缺有守卫、第三缺至今没有——**本批只能靠人工发现**，如实登记 | `form-loop-sweep.test.mjs` **64/64**（49 条真机流程）· 活动/复盘/待办/进展敏感面 **61/61** | 已闭环 |

| 47-S | **真机成功路径**这次抓到一个**用户可见的「假成功」**：组织委员台「新增成员」提示「名册与应到名单即时生效」，而名册人数**不变、刷新后依旧不变**。取证（一次性探针）：服务端 `users` 表**确有新行**（写是成功的）——红的是**读**：`person.js::_baseMemberRecords()` 的 api 分支**写死 `return [...PEOPLE]`**（静态种子），而写侧 `_syncMockDBUsers()` 同步的是 `mockDB.users`，**api 读侧根本不读它**。**影响面＝名册的新增/编辑/移出三处全废**（同一条读链），而名册是「人」维度的单一源。另修两处**守卫自身**的判据错位 + 一处体验 | ① 按支书裁定**补齐 api 读链**（api 分支读服务端已同步缓存；**不留「空则回退 PEOPLE」的兜底**——静态种子冒充真相正是病灶成因）；② 成功路径 **9 → 13**（党委台下发通知 · 组长台写入活动 · 成员台思想汇报 · 组织台名册新增成员，四条均含整页重载·落库判据）；③ 新增 `knownGap` 白名单机制（形制同阶段一 `machine:false`：**必带 reason + 计入基线 + 只减不增**），修复后**同批撤销、基线 1 → 0**；④ 新纪律 **R-74**（等待窗口不足会被误判成「静默失败」——`writeActivityWithSOP` 串行建 16 个 SOP 任务 ⇒ 约 10s 才弹提示，守卫只等 4s；落 `toastTimeoutMs`）与 **R-75**（判据要落在**事实**上而非**手段**上；**写错的口径比少写一句更贵**——台账原写「普查全程跑在 mock 态」实为 **api 形态**，此错口径正是掩盖 `Q-23-45` 的原因）；⑤ 体验改进：组长台「写入活动」加**提交中反馈**（禁用按钮 + 写入中文案，`finally` 复原）；⑥ 更正 `REVIEW_QUEUE` 与 registry 两处错误口径（结论不变、理由换掉） | `form-loop-sweep.test.mjs` **68/68**（6 守卫 + 49 条真机流程 + 13 条真机成功路径）· `page-sweep` 11/11 · `ux-guard` 7/7 | 已闭环（`Q-23-45`） |

| 47-T | **「默认选中的那一项，恰好是不能写的那一项」**——补「组长台·考勤上传」成功路径，真机首跑报 `考勤上传：新增 0 条；重复跳过 2 条（已确认记录不可覆盖）`：`selectFirstOption` 挑的下拉**第一项**恰是**本人已上传过**的活动（选项文本自带「 · 已上传」），于是写口**空转**、流程白跑。这与「默认预填藏必填分支」（14 例）**同族反向**：那 14 例是「默认填好了 ⇒ 走不到必填分支」，本条是「**默认选中项让写口变成空转 ⇒ 成功路径没有证据**」——**表单的出厂状态决定了你能走到哪一支，而「第一项」往往不是「可写的那一项」**。另发现一处写口**结构上没有成功提示**（`page-refs-doc-write` 的成功分支只有 `closeModal()`）⇒ 成功路径第 ① 条判据在它上面**无法诚实满足** | ① 补成功路径 **13 → 15**：组长台考勤上传 · 宣传台周报报送（**一域两动作的另一半**：阶段一覆盖「新增周次」，本阶段覆盖**真正的报送写口**）；② **新增能力** `selectOption: { selector, notText?, text? }`——按**产品自己渲染的标记**挑选项（避开「已上传」+ 限定「党小组会」），不另造判据；另新增 **`countUp: N` 增量断言**（判「改变了多少」而非「此刻等于几」——原断言绝对值「共 54 人」定向绿 / 全量红，改正后全量再跑又查出「所选活动类型决定增量」）；③ 记下「**载体陷阱**」：正文渲染在**折叠区**时，正解是把载体上移到 **tab 根容器**（`CARRIER_PROBE` 只校验载体自身可见、读其 `textContent`——隐藏子节点文本同样计入），**而非放宽可见性判据**；④ **守卫一致性修正**：成功路径读「成功提示」原只读全局 toast ⇒ 改为与阶段一同一口径 `flow.noticeSel || '#toast-container'`（**同一件事写两遍＝第二套口径，必错一边**）；⑤ **未据发现擅改产品**：制度写入无成功提示 → 立 `Q-23-46` 待裁（补提示 / 只登记 / 挂白名单），**不放宽判据、也不为凑条数把它登记进去**；⑥ ⚠ **全量 684 中 1 项红**（`async-vote::AV5` 等待窗口满载下不足，单跑绿；`timeout: 10000` 全站 63 处**已数、待裁**，未擅自批量放宽） | `form-loop-sweep.test.mjs` **70/70**（6 守卫 + 49 条真机流程 + 15 条真机成功路径） | 已闭环（`Q-23-46` 待裁） |

| 47-U | **新判据自己先撞上一条「判据互相冒充」的坑**：为「支书台·通知编辑（改写并保存）」写成功路径，**最自然的标记**「（成功路径普查）通知标题已更新」与既有流程 `secretary-notification-publish` 的断言「（成功路径普查）通知标题」**互为子串** ⇒ **编辑那条一旦真写入，发布那条的断言就被顶替成立**（哪怕发布根本没成功）——守卫**表面全绿、实则两条判据只剩一条**。这类耦合**不会自己暴露**：方向是把**红变成绿**，比假红更难发现（与「白名单掩盖病灶」同族：都是**让守卫看起来在守**） | ① 成功路径 **15 → 16**：支书台通知编辑保存（**「通知」域的另一半**——阶段一覆盖「编辑浮窗两处必填（预填原值先清空才可达）」，本阶段覆盖**真正的保存写口** `NoticeStore.update` → 列表重渲染）；② **新增常驻静态断言**（`S5` 内）：「**成功路径断言标记不得互相包含**」（只查 `text` 型；`countUp` / `selectValuesAll` 不含字面语料，天然免疫）；③ **上线前先在旧代码上证明它会红**（临时把 `prop-weekly-submit-report` 的标记改成「通知标题」⇒ **三条命中、S5 红**，随即还原）；④ 纪律 **R-77** + 范本第二十四 + §0.1 第二十三问；⑤ 本批**只改测试文件、未动生产代码**（无需补戳） | `form-loop-sweep.test.mjs` **S5 绿**（16/16 基线）· 新流程真机 **11.1s 绿** | 已闭环 |

| 47-V | **「假红」的另一面：守卫自己没把被测对象摆好**——为「组织台·专班管理·添加进度」写成功路径（与「代录贡献 / 提交复盘」**共用同一条 open 链**，边际成本极低），若照 47-T 那样直接 `reload` 后断言，只会得到「载体不在位」——而**面板是真的写进去了**：**详情面板是展开态、不落库**，整页重载后必然收起 ⇒ 那是**假红**（看着像产品病，其实是守卫没把被测对象重新摆好；与 47-S「深链页跳过等 tab 渲染」**同一类错**，只是换了个面）。**它把「假红」与「假绿」这两类守卫自身错误凑齐了**：47-T/U 是**把红变成绿**（空转被当成成功 / 两条判据互相冒充），本条是**把绿变成红**（真写进去了却报失败）；**共同点都是「判据描述的不是被测对象的真实状态」** | ① 成功路径 **16 → 17**；② 新增能力 **`SUCCESS_FLOWS[].reopen[]`**（`reload` 之后、断言之前**重跑的前置步骤**，形态与 `open[]` 同，共用 `runStep`）；**它不削弱 ③**：重载已把 DOM 整体替换，重开后仍能看到那段文本，恰证明它**来自落库数据**而非残留节点；③ 纪律并入 **R-75 ④**（跨整页重载的断言须先把「页面形态」恢复到位）+ `范本第二十二` 同族另一半 + `§0.1 第二十一问 ③`；④ 本批**只改测试文件、未动生产代码**（无需补戳） | `form-loop-sweep.test.mjs` 定向 **2/2 全绿**（S5 打印 `SUCCESS_FLOWS=17（基线 17）`；新流程真机 11.7s） | 已闭环 |

| 47-W | **「不可达」的处置口径换了，且没走那条最省事但会造假的捷径**：`resolution-followup-manager.js` 三处必填点（待落实事项 / 责任人 / 落实时限）长期 `machine:false`——reason **属实**（该区只在**存在 `result:'passed'` 议程项**时挂载，而原种子**没有任何活动带 `result`** ⇒ 真机上恒不渲染）。**最省事的做法**是给 act-31 挂 `result:'passed'`（一行转绿），**但那会让判据变成假绿**：act-31 是线上异步表决样例（`voteConfig.quorumCheck:true`），其 `result` 必经硬校验（出席 ≥6 且赞成 >6），而它真机现状是「已表态 0/12」⇒「**0 票却已通过**」**在产品里永远不可能出现** | ① **`machine:false` 9 → 6**（`machine:true` 84 → 87），`MACHINE_FLOWS` **49 → 50**（新流程 `secretary-inspector-resolution`，一条覆盖三处——与既有两条**共用同一条 open 链**，「同面板多块」第三例）；② **种子走自洽路径**：新增 `act-35`（**线下支委会**，无 `voteConfig` ⇒ 记录决议不经表决硬校验）⇒「某议程项已通过」是**真机可达成**状态，并带与 `recordAgendaResult` 同形的 `recordedBy/recordedAt`；**日期放 8 月**以免扰动既有两条点「日历第一条」的流程；③ **顺手把靠偶然事实成立的入口显式化**（`secretary-inspector-vote` / `-activity-edit` 的入口由 `.cal-activity-item` 改为 `[data-act-id="act-31"]`）；④ 纪律 **R-78** + 范本第二十五 + §0.1 第二十四问；⑤ 顺带更正 `CHECKLIST §2` 过期口径（ACTIVITIES 29 条 → **33 条**，按 M1 实测）；⑥ **改了生产文件**（`docs/src/mock/activities.js`）⇒ 按纪律**补戳 `20260916i` 并全量复跑** | 真机新流程 **7.2s 绿** · `mock-integrity` M1/M2 绿（`ACTIVITIES: 33`）· `agenda-flow` / `agenda-closure` / `today-summary` / `roster` / `permission-gate` / `page-sweep` 全部复测绿（**种子改动的爆炸半径逐个点过**） | 已闭环 |

| 47-X | **一条线再解锁一处，另一条线把一处「以为缺种子」的条目**证伪**了**：① 「专班查看·产出说明」原 reason 属实（填报区只在 `active && isMember` 渲染，而两只 active 专班的成员全非演示账号）⇒ **只加成员、不动状态、容量内**（tf-001 补 p5，2/5→3/5）即解锁，**这是产品自己走得通的状态**；② 台账原写「④区『我提交/参与的反馈』的纳入条件＝种子把议题的 `reporterId`/`participants` 指向演示账号」——**方向错了**：种子**本就**带认人字段（issue-001 `submittedBy:p5`、issue-002/003 的 `participants` 含 p11/p1/p13），真正的原因是**服务端按「真匿名」口径把它们抹成 `匿名`/`[]`**（`GET /api/v1/issues` 实测：4 条公开反馈全脱敏、2 条 `kind:'report'` 留真名），而 `getMyIssues` **恰恰排除 report 类** ⇒ **api 形态下该区结构上恒为空**。**⇒ 这不是种子问题，是「真匿名」与「认人」两种口径在同一份数据上相冲**，且**用户可见**（API 部署下成员永远看不到自己的反馈、无法追加说明），而 `help.html`/`README-members.md` 把它写成了功能 | ① **`machine:false` 6 → 5**（`machine:true` 87 → 88），`MACHINE_FLOWS` **50 → 51**（新流程 `visitor-insight-taskforce-contribution`）；② **口径更正**（R-75 同款处置：不只删不换——把**实测两条证据**（种子 / 接口）与新结论一并写进该条 reason，并说明原结论**不成立**）；③ **未擅自改产品口径**，立 **`Q-23-48`**（三条路待裁：接受缺口改文案 / 存**不可反查的提交人标识** / 恢复真名——后者与公开页隐私承诺冲突，不建议）；④ 种子改的是 `docs/src/mock/taskforces.js`（生产文件）⇒ 按纪律**补戳 `20260916j` 并全量复跑** | 真机新流程 **6.1s 绿** · S0–S5 绿（`machine:true=88 machine:false=5 MACHINE_FLOWS=51`）· `mock-integrity` M1/M2 · `today-summary` / `taskforce-lifecycle` / `workforce-gate` / `page-sweep` 全绿 · 按纪律**补戳 `20260916j` 并全量复跑**：**第一次全量 688/687+1红**（红项＝**我自己新写的这条 reason 缺日期**，被本仓 `doc-consistency::S12` 抓住 ⇒ 补「（2026-09-16 登记）」后该守卫 27/27 绿）→ **第二次全量 688/688 全绿** | 已闭环（`Q-23-48` 待裁） |

| 47-Y | **清 `machine:false` 两条，两条都「先把障碍取全证再动手」——其中一条还证伪了「设计如此」的说法**：① 专班「退回补料」原 reason 只说「需待核产出」，真机实测**多出第二重障碍**：原因取自**浏览器原生 `window.prompt`**，不装应答器时 Playwright 自动 dismiss ⇒ 返回 `null` ⇒ 源码 `if (reason === null) return;` **直接 return，连空值分支都到不了**；② 成员台「去补课」原 reason 说「该域**按设计**无静态种子，由纪检操作派生」——**「设计如此」是错觉**：`data-adapter.js` 那句「空属合理」把「入口在 api 形态下恒不渲染」解释成了正常现象，而真正的第二重障碍是**日期耦合**（该页**只列当月活动**，7 月那条现成的缺勤根本不在当月）。**两条都要「造可达前置」+「补一处 schema/注释口径」** | ① **`machine:false` 5 → 3**（`machine:true` 88 → 90），`MACHINE_FLOWS` **51 → 53**；② **新能力 `dialogAnswer`**（`runStep` 新增；在触发它的那一步**之前**注册一次性应答器，`page.once('dialog', …)` 与 `block-config-ui-e2e` 同源）——`answer:''` 打空值分支、`answer:'某原因'` 可打成功分支（留作后继复用）；③ **两形态同源种子**：`mock/attendance.js` 补 9 月缺勤 **`att900`**（p5·act-31·**已确认**）＋ `mock/seed.js::SEED_MAKEUP_TASKS` 补**其派生结果**（与 `autoGenerateMakeupTask()` 落库形状逐项一致），注入走 `server/seed.js::replaceCollection('makeup_tasks')` ＋ `mock-adapter._seedInitialData`（**只种 mock 就是假绿**）；④ **更正一处过期注释口径**（`data-adapter.js`「makeupTasks 无静态种子…空属合理」）；⑤ **刷新一处过期锚点**（`mock-integrity` M2 规则 4 的「今天」锚点 2026-09-05 → **2026-09-17**，并写明 08-05→09-05→09-17 沿革）——**处置是刷锚点、不是改数据迁就它**，也**不可改成 `new Date()`**（会让判据随运行日漂移）；⑥ 入口**显式化**（专班卡由「第一张」改 `[data-tf-id="tf-001"]`，与 47-W 同一课）；⑦ **踩到并修掉一处「种子 id 空间」坑**：新缺勤初版 id 写成 `att-sep-1`（**不匹配**种子识别口径 `/^att\d+$/`）⇒ `?reset=init` 的剔除漏掉它 ⇒ **空态不空**，被 `reset-tier-init::C2` 当场抓住；改 **`att900`**（隔离段，避开生成器区间）⇒ **纪律并入 R-78 ⑤**（造种子不只是造出内容，还要落在既有的 id 空间里）；⑧ 按纪律补戳（`20260917a`；修 C2 后 `20260917b`）并全量复跑 | 两条新流程真机 **6.0s / 5.7s 绿** · S0–S5 绿（`machine:true=90 machine:false=3 MACHINE_FLOWS=53`）· `mock-integrity` M1/M2 绿（`ATTENDANCE_RECORDS: 154`）· `today-summary` / `member-progress` / `b3-1-makeup-writeback` / `meeting-attendance-rules` / `attendance-batch` / `async-vote` / `page-sweep` 全部复测绿；补戳 `20260917a` 后全量 **690/689+1红**（红项 `reset-tier-init::C2`，即上述 id 空间坑）→ 改 id 后补戳 `20260917b` 复跑（结论见执行日志） | 已闭环 |

| 47-Z | **台账里最后一条可动手的 `machine:false` 清零，而清它的过程本身更正了一条「长期没人核过的 reason」**：支书台「待办·退回」的「请填写退回原因」原挂 `machine:false`，reason 写「退回浮态只在**有 pending 成员变更确认**时挂载……纳入条件：**组织委员**在**某台**发起一次成员变更 → 该待办即出现」。**前半句是实测事实**（入口计数确为 0），**后半句却是一条从没核过的跨人假设**——该队列**纯客户端**：`mockDB.pendingMemberConfirmations` ＋ 自管 localStorage 键 `gsm1921-member-confirmations`（`member-confirmation.js` **没有任何服务端表**，**mock / api 两态同源**）⇒ **请求只存在于产生它的那个浏览器会话里**，组织委员在另一台发起、支书这边**根本看不到**。真正的前置是「**同一会话内先产生过一条请求**」——而支书本人也在该写权内（`person-entry.js::EDIT_ROLES = 支书/副支书/组织委员`） | ① **`machine:false` 3 → 2**（`machine:true` 90 → 91），`MACHINE_FLOWS` **53 → 54**（`FLOWS_BASELINE` 53 → 54）；② **可达链＝真机动作链，不造任何数据**：`person.html?id=p7` →「编辑档案」→ 改「发展阶段」→ 保存 ⇒ 队列出现 pending ⇒ 回支书台「待办」→ 成员发展域 → 组行进详情 ⇒ 退回按钮在位、点它即弹必填浮态——**比造种子少一个「种子是否自洽」的风险**（「造可达前置」的**第二种形态**）；③ **补两步类级能力**：`runStep` 新增 **`{ goto }`**（跨页跳转，同 `flow.path` 口径）与 **`{ openTab }`**（跳回工作台后切 tab）——**跨页前置链**，与 47-Y 的 `dialogAnswer`、47-V 的 `reopen[]` 同规（形态逼出来的能力）；④ reason **逐句更正**并写明「原结论哪一半成立、哪一半不成立」；⑤ 纪律并入 **R-78**（末句「**登记的理由本身也要核**」的最强实例）+ **范本第二十六** + `§0.1 第二十五问`；⑥ **本批只改测试文件、未动生产代码**（无需补戳） | 新流程真机首跑即绿 **12.8s** · S0–S5 绿（`VALIDATION_SITES=93` / `machine:true=91` / `machine:false=2` / `MACHINE_FLOWS=54`）· **收尾全量 691 项 / 690 通过 / 1 红**——红项 `inspection-loop-e2e::组织台·专班考察上传`（等面板挂载超时 33.5s），**单文件独立跑绿 2/2（4.2s / 6.2s）** ⇒ **满载下等待窗口不足**（`Q-23-47` 同族、与本批改动零交集），已补记为该条**第二次实例**、**未擅自放宽窗口** | 已闭环（余 2 条 `machine:false` **均有裁定归属**：`Q-23-48` 待裁的产品缺口 · 服务层重复守卫「标不修」） |



| 48 | **三项待裁一次落地 + 第一次「按裁定做减法」**：① **Q-23-47**「只按需放宽这两处」——`async-vote::AV5` `10000 → 30000`、`inspection-loop-e2e::组织台·专班考察上传` `8000 → 40000`，**同行写明满载实测耗时**，其余 61 处 `timeout: 10000` 与同文件其余 `timeout: 8000` **一律不动**（**放宽的是窗口、不是判据**；选项②「统一常量」未采纳）；② **Q-23-46**「补一条成功提示」——`modules/references.js` 「制度写入」成功分支补 `showToast`，**并按 R-67 把同文件「上传新版」分支一并补**（原**同样只有 `closeModal()`** ⇒ 同形态 **2 处一次收口**）；**按裁定本批不新增 `SUCCESS_FLOWS` 流程**（缺口已修、流程已解除阻塞，留待下一批）；③ **Q-23-48**「接受缺口 + 改文案」——**不改服务端隐私口径、不造种子**（造了只在 mock 显形＝假绿），只把口径写清（`issues.js` 说明行 + 口径注释 · `help.html` 两处 · `README-members.md`），台账该条**保留 `machine:false`**、reason 由「待裁」改为「**已裁定为产品接受的缺口**」；④ **47-F 第四组（减法）**：`server-base.test.mjs` ← 吸纳 `db`+`seed`+`resources`+`uploads`+`snapshot` **五件并一**（**文件 5 → 1**、用例 **11 条等量转移**；**三件逐字重复的样板收敛为一份** `before/after`） | **两处合并特有的工程细节（值得留下）**：① **顺序有意义**——`snapshot` 那条会往 activities 追加 `act-new`，故**置末**，不污染前面几条对列表的断言；② **顺手消除一处变量遮蔽**——原 `snapshot.test.js` 用例内 `const after = …` **遮蔽了 node:test 的 `after`**（单文件无害、合并后是陷阱）⇒ 改名、判据不变；③ **引用链同步 2 处**（`package.json::test:fast` 五处→一处 · `routes/resources.js:414` 注释）；④ **同批被自己的守卫抓到一次**：新写的第二处注释「支书裁定 Q-23-46」**该行漏了日期** ⇒ `doc-consistency::S12` 判红（**基线 0 条＝零容忍**）⇒ 补日期转绿 | 新文件 **11/11** · `test:fast` 全绿 · `doc-consistency` **12/12** · `link-integrity` **5/5** · `version-stamp` **15/15** · 改了生产文件（`references.js` / `issues.js`）⇒ 按纪律**补戳 `20260917c`**（陈旧戳自检 0 处残留）并全量复跑 | 已闭环（三项待裁**全部闭环**） |



### 5.2 三组统计（一屏看进展）

- **模块化分演进**：75 → **78**（批 25–35，541→566）→ **80**（批 37–39，566→569）→ **81**（批 41，569→571）→ **82**（批 42，571→586）→ **83**（批 46①+47A/B/C，586→624）→ **84**（批 47-H/I，624→**631**）→ **85**（批 47-J，631→**642**）→ **86**（批 47-K，642→**653**）→ **87**（批 47-M/N/O/P/Q/R，653→**678**）→ **88**（批 47-S，678→**682**）→ **89**（批 47-T，682→**684**）→ **90**（批 47-U，684→**685**）→ **91**（批 47-V，685→**686**）→ **92**（批 47-W，686→**687**）→ **93**（批 47-X，687→**688**）→ **94**（批 47-Y，688→**690**）→ **95**（批 47-Z，690→**691**）→ **96**（批 48，691→**691**；**用例等量转移、文件 89 → 85**）；开源化 74 → **75**（批 42，未动）。**47-B 为何只 +1**：口径下沉单一源与「索引 ↔ 守卫」双向校验是实质加分，但 47-B 实测暴露一处**隐式耦合**（单档口径原受 CDN Tailwind 加载顺序支配）——「单一源」当时并不真的单源，两相抵后记净 +1。**47-H/I 为何 +1**：47-I 把**聚合口径**从「组长台 tab 文件内联」抽为**服务端同源纯函数**（两端一套口径 + 服务端承担汇总职责）＋新增两条常驻守卫（S12 授权声明、S1–S4 单一源/双态同源）是模块化实质进步；但**同时新增了 1 个测试文件**（与 47-F「合并减量」方向相反），两相抵记净 +1。**47-J 为何 +1**：① 新增 `SUCCESS_FLOWS`＝**一条此前完全不存在的验证能力**（把「必填会报错」补全为「填对能成 + 落库」），且三条判据**分离**（成功提示 / 当场真生效 / 重载后仍成立）不再互相冒充；② 47-F 第三组**净减 2 个测试文件**（与「一个域一个可寻址入口」同向）；③ `S12` 迁移台账**清空**＝此后零容忍。**反向项（故不多给）**：`form-loop-sweep` 单文件已 **28 项 / 全量耗时 ~150s**，而成功路径**只覆盖 2 条**——37 个 `machine:true` 校验点里绝大多数仍**没有**成功路径证据，覆盖尚薄。**47-K 为何 +1**：① 为「够不到」补的**五项能力**都是**类级**的（独立页 / `?tab=` 深链 / 面板内状态区 / 勾选态 / 多提交口），每项解锁**一整类**此前只能长期挂 `machine:false` 的站点，而不是为单条流程开的口子；② 台账真机覆盖 **37 → 61**（`machine:false` 55 → **31**）且**零新增测试文件**（全部落在既有 `form-loop-sweep` 内）——「覆盖面上去、入口数不变」正是 R-67 想要的方向；③ 余 31 条**按「为什么还不动」分了三类**并写进台账，把「不知道能不能做」变成「知道差什么」；④ 顺带实证了制度写入的**成功路径**（前置链要求它真落库并刷出列表行）。**反向项（故不多给）**：`form-loop-sweep` 已 **31 条流程 / 39 项 / 单文件全量耗时 ~220s**，而**成功路径仍只覆盖 2 条**——61 个 `machine:true` 校验点里绝大多数**没有**成功路径证据；且**余下 31 条里有 8 条属「需前置业务数据」**（要造多角色/多状态种子），下一批的边际成本会明显上升。**47-M 为何 +1**：① 补上**第三类判据**「未捕获脚本错误」——前两类判据都从**提示文本**出发，而这类病灶**根本不产出文本**，只会被报成「静默失败」而**成因查不出来**；补齐后**当场抓出一个真 bug**（`services/issues.js` 11 处 `ReferenceError`），并顺势把「**白名单不只是没测，它会持续掩盖白名单里的病灶**」写进评估；② 独立页在**成功路径**阶段也打通了执行链（此前只有阶段一支持 `path`），并把两处结构性缺陷（漏传 `path`、重载后仍去切 tab）修掉；③ 台账 **67 → 76**（`machine:false` 25 → **16**）且**仍是零新增测试文件**，同时把「服务层重复守卫」立为独立分类（不修，只标）——分类口径本身是资产。**反向项（故不多给）**：`form-loop-sweep` 已 **41 条真机流程 + 4 条成功路径 = 51 项 / 单文件耗时 ~300s**（单文件体量已值得警惕）；`SUCCESS_FLOWS` 虽从 2 条增至 **4 条**，但 `machine:true` 同步从 67 涨到 **76** ⇒ 成功路径占比**反而更低**（覆盖面更薄）；余 16 条几乎全是**种子/多角色前置**，须先造种子才能再推进。**47-N / 47-O 为何不单独加分**：两批合起来把成功路径 **4 → 9**（+5 条高频写口：通知发布 · 党委新建支部 · 发起专班 · 向导新建空支部 · 上报党委），其中三条是**跨「表单 → 服务端 → 列表 → 重载仍成立」的完整落库链**；并把「占比被自己摊薄」（9 / 76）、「`CARRIER_PROBE` 只取首个匹配元素 ⇒ 新条目类断言须用外层容器」、「取消勾选要靠 `setChecked` 且 fill 顺序须先取消后填名」、「**测试隔离**：`browser.newPage()` 起全新上下文 + mock 态 ⇒ 跨测试/跨账号前置只能走种子、不能靠上一条流程的副作用」四条**写成可复用的判据**。**但仍并入 47-M 的 +1**，理由：① 属**同一条能力的深化**（46/47-J 已开的能力，非新判据）；② **覆盖缺口仍在扩大**（9 / 76），且两批**未动 `machine:false` 余 16 条**；③ 按「不唯分、不重复上调」的口径不给分。**47-Q 为何不单独加分**：它把最大一簇（4 处）一次解锁，并**在动种子时先解决了一声口径冲突**（公开面加过滤 + 服务端按 kind 分流脱敏 + 探针取证），**「授权范围 ≠ 实际影响面」这条教训也写进了评估**。**但仍并入 47-M 的 +1**，理由同 47-N/O——属**「改种子解锁」这条线的继续**（47-P 已立方法），且本批**未新增判据**（新学到的两条是「实现纪律」而非「验证能力」）。**47-S 为何 +1（本批与 47-N/O/Q/R 不同，不并入 47-M）**：① **产出物是产品修复，不只是覆盖**——真机成功路径抓出并修掉一处**用户可见的「假成功」**（api 形态名册读链返回静态种子 ⇒ 名册的新增/编辑/移出三处操作「成功却看不见」），且该修复**把「服务器权威读」这条口径真正落到了读侧**（此前只有写侧是权威）；② **新增一件治理工具**——`knownGap` 白名单机制（与阶段一 `machine:false` 同形制：**必带 reason + 计入基线 + 只减不增**，并在 S5 里断言「补上缺口必须同批下调基线」），使「暂时证不了」与「没测」在成功路径这一侧也**可分、可审、不会静默消失**；③ **新增两条判据级纪律**（R-74 等待窗口 / R-75 判据落在事实上），并各配一个**范本 + 自问句**——按本文件一贯口径，「判据能力」的增项才计分；④ 顺带修掉两处**守卫自身**的判据错位（`flow.path` 按手段分流 ⇒ 深链页假红；`open[]` 按条数判 ⇒ 逼人写空转步骤）。**反向项（故只 +1、不给更多）**：① 成功路径 13 / `machine:true` 84 ⇒ **占比仍薄**（约 15%），本批只推 4 条；② `machine:false` **余 9 条一条未动**；③ `form-loop-sweep` 单文件已达 **68 项 / 约 10 分钟**，**文件体量警示仍未解决**；④ api 读链只补齐了 `_baseMemberRecords` 一处，**其余域是否还有「写侧权威、读侧静态」的同类**本批**未做全站排查**（属「同类规模未数完」，如实标注为遗留）。**47-T 为何 +1（与 47-S 同线，但新形态与新能力各一，故不并入）**：① 成功路径 **13 → 15**，两条都验到「整页重载后落库」——组长台·考勤上传（`#att-list-host .lf-count` = 「共 54 人」）与宣传台·周报报送（`#prop-tab-content` 正文仍在）；② **抓出一处新形态**：「**默认挑中的那一项，恰好是不能写的那一项**」——`selectFirstOption` 取下拉第一项，而排在最前的那条活动**已上传** ⇒ 提示是「考勤上传：**新增 0 条**；重复跳过 2 条」，**拿到了提示却空转**（它是「默认预填藏必填分支」那一族的**反向**：被预置的不是表单而是**被测动作的输入域**）；③ **同批还换来一件可复用的判据能力 + 一次自我更正**：该流程原断言**绝对值**「共 54 人」，**定向跑绿、全量跑红**（实测 55）⇒ 改为**增量断言 `countUp`**（基线在动作前读），全量再跑又现出真病根——所选活动是**主题党日**（候选＝支部在册成员）⇒ 取的 2 人里有 1 人不属本组（该列表只统计本组人员）⇒ 可见只 **+1**；再补 `selectOption.text:'党小组会'`（候选＝**本组应到名单** ⇒ 稳定 +2）。**「判据要判『这次动作改变了什么』，不要判『世界此刻正好等于什么』」**——绝对值既随种子漂移、也随**同批其它流程**漂移。两件合并立 **R-76** + 范本第二十三 + §0.1 第二十二问；④ 同批**如实立起一处待裁项**（`Q-23-46`：制度发布写入**结构上没有成功提示**）——**既不据此擅改产品，也不为凑条数把它登记进 `SUCCESS_FLOWS`**。**反向项（故只 +1、不给更多）**：① 成功路径 **15 / 84** 个 `machine:true` ⇒ 占比仍薄；② `machine:false` **余 9 条一条未动**；③ `form-loop-sweep` 单文件已 **70 项 / 约 10 分钟**，**文件体量警示仍未解决**；④ 本批**只改测试文件、未动生产代码**（故不必补戳），产出偏「判据能力」而非「产品修复」；⑤ ⚠ **全量 684 中 1 项红**（`async-vote.test.mjs::AV5`，`waitForSelector('#async-vote-section', { timeout: 10000 })` 超时；**单文件独立跑绿 25.1s ⇒ 判为满载下等待窗口不足**，与 **R-74 同族**；`timeout: 10000` 全站 **63 处 / 18 文件**——**已如实登记为「同类规模已数、处置待裁」**，未擅自批量放宽）。**47-Z 为何 +1（与 47-T 同线：新能力 + 一处自我更正）**：① **台账里最后一条可动手的 `machine:false` 清零**（3 → 2，且余 2 条**均有裁定归属**：一条是待裁的产品缺口 `Q-23-48`、一条是**服务层防御性重复守卫**「标不修」）——「同类病灶」这条线**第一次走到底**；② **补两步类级能力** `{ goto }` / `{ openTab }`＝**跨页前置链**（有些校验点的前置**不在同一张页面上**；此前 schema 只能「一张页面走到黑」），与 `dialogAnswer` / `reopen[]` 同规——**形态逼出来的能力**；③ **更正一条长期误导的 reason**：原文写的「纳入条件：**组织委员**在**某台**发起一次成员变更 → 该待办即出现」是**没核过的跨人假设**——该队列**纯客户端**（`mockDB.pendingMemberConfirmations` + 自管 localStorage 键，**mock/api 两态都没有服务端表**）⇒ **组织委员在别的会话发起，支书根本看不见**；真前置是「**同一会话内先产生过一条请求**」，而支书本人也有该写权（`person-entry.js::EDIT_ROLES`）⇒ **不造任何数据、让产品自己走出来**（比造种子少一个「种子是否自洽」的风险）。**反向项（故只 +1、不给更多）**：① 成功路径 **17 / 91** 个 `machine:true` ⇒ 占比仍薄（约 19%），本批**未推成功面**；② `form-loop-sweep` 单文件已 **77 项 / 约 12 分钟**，**文件体量警示仍未解决**（本轮只加了 1 项流程、却加了两步新能力，边际成本上升）；③ 本批**只改测试文件、未动生产代码**（故不必补戳），产出偏「判据能力 + 口径更正」而非「产品修复」；④ 跨页链**只在一条流程上用了一次**（**尚不足以证明它已成通用形态**——如实标注）。**48 为何 +1（本批不含新覆盖，方向是「减法 + 收口」）**：① **三项待裁一次全部闭环**（Q-23-46 / 47 / 48）——台账里 `machine:false` 余 2 条**从此都有终态处置**（一条「已裁定为产品接受的缺口」、一条「标不修」），**「同类病灶」这条线彻底走完**；② **两处用户可见修复**：制度写入（含上传新版）**成功不再静默**（原先只有 `closeModal()`，用户只能自己去列表里找；按 R-67 两处一次收口）· ④区**口径写清楚**（原先 help/README 把它写成功能，而 api 形态结构上恒空）；③ **47-F 第四组＝第一次「净减」**：`server-base.test.mjs` 五件并一（**测试文件 89 → 85**），且**用例等量转移 11 条**、**三份逐字重复的样板收敛为一份**——这是与「加能力」相反方向的实质推进（与 R-67「一个域一个可寻址入口」同向）；④ 两处等待窗口**按需**放宽并写明实测耗时（**放宽的是窗口、不是判据**），使满载下的**两处假红**不再复现。**反向项（故只 +1、不给更多）**：① **本批按裁定「不加新覆盖」**——成功路径仍 **17 / 91**（≈19%），覆盖面未动；② **只合了一组**（净减 4 个文件，对 85 个测试文件是小头），`server/test` 体量与 `form-loop-sweep` 单文件（**77 项 / 约 12 分钟**）**两条警示都仍在**；③ 新增产出的「判据能力」为 0（本批不引入新判据；`doc-consistency::S12` 那次判红是**既有判据在正常工作**，不是新能力）；④ 三项 Q 里有两项（Q-23-46 / Q-23-48）的**根因处置仍是「文案/提示」层**——隐私口径与不可反查标识这个真问题**被明确搁置**（Q-23-48 选项②保留为候选）。**未给更高分的已知短板**：`server/test` **105 个文件 / 1.8 万行**未整合（支书已点出，见 `REVIEW_QUEUE` 批次 47-F；本批仅减 2 个）；综合（五维均值 87/75/75/84/80 = 80.2）按「未过半分不进位」记 **≈80**；⚠ 与 §一 评分表记的 **≈79** 并存——**综合分是否上调属支书裁定**，本文件不擅自改，只如实登记五维当前值。
- **收敛台账规模**：§3.4 表内 **R13–R30 共 19 行**（含 **R28-附 / R29 / R30** 三行，批 47-J 补齐）；`§0.2` 索引与 `§3.4` **本批起口径同步**（`Q-23-43` 闭环），过时计数一并改准。`CLAUDE.md` 新纪律 **R-68**（真机量测须与生产同环境；档位须直接声明）、**R-69**（不得以「等提交/等裁定」结束回合，该问就问）、**R-70**（授权声明必须可核验，功能不得随批次「一起进」）、**R-71**（异常面须下沉到动作级）、**R-72**（守卫覆盖面按接口约定逐维数）、**R-73**（台账完备性三缺一：漏登记时守卫全绿）、**R-74**（等待窗口须覆盖真实耗时——「慢」≠「没成」）、**R-75**（判据落在事实上而非手段上；写错的口径比少写一句更贵；**跨整页重载的断言须先把「页面形态」恢复到位——`reopen[]`**；**写死的「今天」类锚点会过期，过期时表现为「误报」——处置是刷锚点不是改数据，且不可改用 `new Date()`**）、**R-76**（默认挑中的那一项可能是「不能写的那一项」；「有成功提示」不等于「动作生效」；判据判「**改变了什么**」而非「世界此刻等于什么」——绝对值须改**增量 `countUp`**）、**R-77**（判据之间不得互相冒充：写入的标记不得是另一条判据断言语料的子串——**把红变成绿比假红更难发现**）、**R-78**（「不可达」只有两条出口：造**真机可达**的种子或**如实登记**不可达；**造的种子必须自洽**——不得绕过产品自身校验造出「永远不可能出现」的状态，且位置须不扰动既有判据）；可迁移要点 **①–⑫**；自查问句 **二十五问**（批 47-M 增第十六问「静默的成因」、批 47-P 增第十七问「守卫守的是一维还是整个接口」、批 47-Q 增第十八问「这份内容同时还给谁看」、批 47-R 增第十九问「我补的种子会不会把要测的那一支挡住」、批 47-S 增第二十问「我等的那个信号，窗口够不够」与第二十一问「这条判据分流的依据是事实还是手段」、批 47-T 增第二十二问「我默认挑中的那一项，是能写的那一项吗」、批 47-U 增第二十三问「我这条判据的语料，会不会被别人顶替成立」、批 47-W 增第二十四问「我造的这个种子，产品自己跑得出来吗」、批 47-Z 增第二十五问「我写下的这条前置 / 纳入条件，是同一会话、同一张页面就能走到的吗」）；范本 **第六–第二十六**（第十六＝辅助小字样本学习，**学习中未定稿**；第十七＝病灶不产出文本时判据自己也看不见它；第十八＝守卫只守了接口的一维；第十九＝单一源承载两种可见性；第二十＝种子把要测的那一支挡住了；第二十一＝等待窗口不足把「慢」误判成「没成」；第二十二＝判据落在手段上 / 错口径把病灶盖章；第二十三＝默认挑中的那一项恰好是不能写的那一项；第二十四＝两条判据的语料互相包含 ⇒ 会互相冒充成立；第二十五＝判据结构性不可达时的处置——造可达且自洽的前置；第二十六＝登记里的「前置 / 纳入条件」本身可能是一条**跨人假设**——**数据存在哪里决定了谁能看见它**，且「造可达前置」优先选**让产品自己走出来**而非造种子）；判例集 `TEST_AND_VERIFICATION.md`（含「单一源搬迁后引用改签遗漏」「工具脚本副作用」等）。
- **测试规模演进（全量实测口径）**：541 → 566 → 569 → 571 → 586（批 42）→ 595（批 43）→ 批 44 新增定向 16 项（`form-loop` 15 + `doc-consistency` S9）→ **624**（批 46①+47A/B/C 实测：46① 新增 6 项、47-A 2 项、47-B 4 项；47-C 只改普查内部断言、未增删用例）→ **631**（批 47-H/I 实测：47-H `doc-consistency` **S12** 1 项、47-I `member-progress` **4 项**）→ **632**（批 47-D ③ `secretary-notice-edit`；**47-F 试点组合并 5 文件 → 1 文件为「等量转移」**）→ **642**（批 47-J 实测：`form-loop-sweep` **23 → 28**（+`S5` 守卫、+2 条 47-D 真机流程 ⑨⑩、+2 条**真机成功路径**）；`roster` 组与 `wizard` 写口组合并均为**等量转移**（用例数不变，**文件数 6 → 4**）。⚠ 642 较上一记录 635 多 7 项，其中 **5 项**如上可逐项归因，**另 2 项未能逐项追平**——如实标注，不硬凑解释）→ **653**（批 47-K 实测：`form-loop-sweep` **28 → 39**（+11：31 条真机流程 − 原 20 条 = +11；`S5` 与 2 条成功路径已在 642 内计过）；⚠ 本批第一次全量我**误判为「卡死」**并中途停掉——实为 **node 管道 stdout 缓冲**（`Tee-Object` 落盘滞后，日志冻在 548 行而进程仍在跑）；后带 `--test-timeout=180000` 重跑，**14 分钟跑完 653/653**。**教训：以「日志不再增长」判定卡死之前，必须先排除管道缓冲**（否则会误杀一次有效的全量验证））→ **657**（批 47-L 实测：`form-loop-sweep` **39 → 43**（+4：顶栏一键汇报 · 巡查信息编辑 · 巡查表态 · 活动详情页表态）；**本批另有一条「覆盖数未涨却必要」的工作**——把 2 条经真机取证判定「当前种子下不可达」的条目据实回退并写明**纳入条件**，使台账**第一次分清「不可达」与「没去做」**（此前二者长得一样，下一批会重复摸底））→ **665**（批 47-M 实测：`form-loop-sweep` **43 → 51**（+8：**+6 条真机流程**（我的处置待处置详情 · 档案归档上传 · 组长台子记录 · 议题详情独立页 · 向导新建支部 · 议程目标阶段）与 **+2 条真机成功路径**）；**本批另有一条「用例数未涨却最要紧」的判据增量**——每条流程跑完追加「未捕获脚本错误」断言，正是它把 `issues.js` 那 11 处 `ReferenceError` 逼了出来）→ **670**（批 47-N/O 实测：`form-loop-sweep` **51 → 56**，即 `SUCCESS_FLOWS` **4 → 9**（支书台通知发布 · 党委台新建支部 · 组织委员台发起专班 · 向导新建空支部 · 支书台上报党委），五条均首跑即绿含整页重载）→ **671**（批 47-P 实测：`form-loop-sweep` **56 → 57**（+1 条真机流程 `pc-review-reject-note`）；`ux-guard` 用例数不变（**改的是既有 ⑥ 的判据覆盖面**——补「参数个数」维度，非新增用例）；**本条流程首跑即红，抓出第二个真 bug**（单参 `showToast` ⇒ 空提示）并推动 3 处修复）→ **675**（批 47-Q 实测：`form-loop-sweep` **57 → 61**（+4 条真机流程，一条种子改动解锁四处）；本批**动了生产代码**（公开页加 `kind:'feedback'` 过滤 · 服务端按 kind 分流脱敏 · 种子补两条内部汇报）⇒ 按纪律补戳 `20260916f` 并全量复跑）→ **678**（批 47-R 实测：`form-loop-sweep` **61 → 64**（+3 条真机流程：成员台我的复盘 · 思想汇报修改重交 · 思想汇报初阅打回）；**其中一条是补登的台账遗漏**（`thought-report-entry.js:287`，`VALIDATION_SITES` 92 → **93**）；本批改了 `mock/activities.js` 与 `mock/thought-reports.js` ⇒ 补戳 `20260916g` 并全量复跑）→ **682**（批 47-S 实测：`form-loop-sweep` **64 → 68**，即 `SUCCESS_FLOWS` **9 → 13**（党委台下发通知 · 组长台写入活动 · 成员台思想汇报 · 组织台名册新增成员），四条均含整页重载；**本批是「成功路径」第三次抓出真病灶、也是第一次抓出「用户可见的假成功」**（api 形态名册读链返回静态种子）并当场修复；同时新增 `knownGap` 白名单机制与 **R-74 / R-75** 两条纪律；本批**改了生产文件**（`services/person.js` 读链 · `leader/write-tab.js` 提交中反馈）⇒ 按纪律补戳 **`20260916h`** 并全量复跑）→ **684**（批 47-T 实测：`form-loop-sweep` **68 → 70**，即 `SUCCESS_FLOWS` **13 → 15**（组长台考勤上传 · 宣传台周报报送），两条均含整页重载；**本批第一次抓到「有提示却空转」**——`selectFirstOption` 挑到的第一项恰是**已上传**的活动 ⇒ 提示「新增 0 条；重复跳过 2 条」，为此新增 `selectOption` 选步 + **`countUp` 增量断言**（原断言**绝对值**「共 54 人」：定向绿 / 全量红 ⇒ 又查出「所选活动类型决定增量」⇒ 加 `text:'党小组会'`）与 **R-76** 纪律；本批**只改测试文件、未动生产代码**，**无需补戳**。⚠ **全量 684 中 1 项红**：`async-vote.test.mjs::AV5`（`waitForSelector('#async-vote-section', { timeout: 10000 })` 超时；**单文件独立跑绿 25.1s ⇒ 满载下等待窗口不足**，与 **R-74 同族**；`timeout: 10000` 全站 **63 处 / 18 文件**，**规模已数、处置待裁**，未擅自批量放宽）→ **685**（批 47-U 实测：`form-loop-sweep` **70 → 71**（+1 条真机成功路径 `secretary-notice-edit-save`）——**本批最要紧的不是那一条流程，而是 S5 上新增的常驻断言**「成功路径断言标记不得互相包含」（**上线前已在旧代码上证明它会红**）；本批**只改测试文件、未动生产代码**，**无需补戳**）→ **686**（批 47-V 实测：`form-loop-sweep` **71 → 72**（+1 条真机成功路径 `org-taskforce-progress-add`）+ 新增能力 **`reopen[]`**（整页重载后把详情面板重新展开——**重载收起面板不落库，不重开就只会得到「载体不在位」的假红**）；本批**只改测试文件、未动生产代码**，**无需补戳**）→ **687**（批 47-W 实测：`form-loop-sweep` **72 → 73**（+1 条真机流程 `secretary-inspector-resolution`——**一条流程覆盖三处校验点**，即「决议落实」三处由 `machine:false` 转正，`MACHINE_FLOWS` **49 → 50**）；**本批改了生产文件**（`docs/src/mock/activities.js` 新增 `act-35`）⇒ 按纪律补戳 **`20260916i`** 并全量复跑）→ **688**（批 47-X 实测：`form-loop-sweep` **73 → 74**（+1 条真机流程 `visitor-insight-taskforce-contribution`，`MACHINE_FLOWS` **50 → 51**）；**本批改了生产文件**（`docs/src/mock/taskforces.js`：tf-001 补演示账号成员 p5）⇒ 按纪律补戳 **`20260916j`** 并全量复跑。⚠ **补戳后的第一次全量 688 项 / 687 通过 / 1 红**：红项就是**我自己新写的那条台账 reason**——里面出现「待支书裁定」而**该行没带日期**，被本仓自己的 `doc-consistency::S12`（「授权声明必须同行带可核验日期」）当场抓住。**这不是噪音，是守卫在正常工作**：它拦住的正是「新写一句关于支书的断言却不给日期」这一动作，而我恰恰在写「待裁项」时做了它。**已按口径补 `（2026-09-16 登记）` 并复跑该守卫 27/27 绿**；第二次全量复跑见执行日志）→ **690**（批 47-Y 实测：`form-loop-sweep` **74 → 76**（+2 条真机流程：`org-taskforce-reject-reason`（专班退回补料·原生 `prompt` 应答）· `visitor-attendance-makeup-proof`（成员台补课说明）），`MACHINE_FLOWS` **51 → 53**、`machine:false` **5 → 3**；**本批新增能力 `dialogAnswer`** 与**两形态同源种子**（`SEED_MAKEUP_TASKS` ＋ 9 月缺勤 `att-sep-1`），并**刷新一处过期锚点**（`mock-integrity` M2 规则 4：2026-09-05 → 2026-09-17）；**本批改了生产文件**（`mock/attendance.js`·`mock/seed.js`·`core/mock-adapter.js`·`core/data-adapter.js`·`server/seed.js`）⇒ 按纪律补戳 **`20260917a`** 并全量复跑：**第一次全量 690 项 / 689 通过 / 1 红**——红项 `reset-tier-init::C2`（「第 2 轮 loadDB 后 attendances 为空」实测 `1 !== 0`），根因**不是业务**：新缺勤 id 初版写成 `att-sep-1`，**不匹配种子识别口径 `/^att\d+$/`**（`init-reset.js::INIT_SEED_ID_PATTERNS`）⇒ `?reset=init` 的剔除漏掉它 ⇒ 空态不空。**改 id 为 `att900`（隔离段）后补戳 `20260917b` 再复跑**；教训并入 **R-78 ⑤**（造种子不只是造出内容，还要落在既有的种子 id 空间里））→ **691**（批 47-Z 实测：`form-loop-sweep` **76 → 77**（+1 条真机流程 `secretary-todo-reject-reason`——台账里**最后一条可动手的 `machine:false`** 由「**真机动作链**」而非种子解锁，`MACHINE_FLOWS` **53 → 54**、`machine:false` **3 → 2**）；**本批新增两步类级能力 `{ goto }` / `{ openTab }`**（跨页前置链）；**本批只改测试文件、未动生产代码**，故**无需补戳**。**收尾全量 691 项 / 690 通过 / 1 红**——红项 `inspection-loop-e2e::组织台·专班考察上传`（等面板挂载超时），**单文件独立跑绿** ⇒ 满载下等待窗口不足（`Q-23-47` 第二次实例，与本批零交集，**未擅自放宽窗口**））→ **691**（批 48 实测：**用例数不变**——本批是**等量转移**（`server-base.test.mjs` 吸纳五件 `db/seed/resources/uploads/snapshot`，**测试文件 89 → 85**），`SUCCESS_FLOWS` 仍 **17**（依支书裁定④「**先做减法/收口，不加新覆盖**」）；**本批改了生产文件**（`modules/references.js` 两处成功分支补提示 · `services/issues.js` ④区口径说明）⇒ 按纪律补戳 **`20260917c`** 并全量复跑，**691 项 / 691 通过 / 0 红 / 0 跳过**，耗时 **1,169,046ms ≈ 19.5 分钟**——**与批 47-Z 的同一条命令（691 / 690 通过 / 1 红）正面对照，恰是本批按 `Q-23-47` 放宽的那两处等待窗口把假红转绿**，即该 Q 的诊断（「满载下等待窗口不足」而非真病）被这次全量证实）；按新协议**不再每批全量复跑**（定向为主、交付前才全量，见 §5.4 与 `README.md` 开发纪律）。

### 5.3 一页指针（找什么去哪里，不在本文件复述）

| 想查什么 | 去哪里（唯一权威） |
|----------|--------------------|
| **规则 → 由哪个守卫守 → 当前状态** | `content/05_ai_coding/DATA_CONSISTENCY_CHECKLIST.md` **§0.2 总索引**（21+ 行；S9 守卫其引用真实性） |
| 病灶类的**可执行判据**（改前自问） | 同文件 **§0.1 自查问句十四问** + **范本第六–第十四** |
| **判例**（什么条件下会失效、怎么被抓住） | `content/05_ai_coding/TEST_AND_VERIFICATION.md` |
| 单一源**组件**登记（新增件须登记） | `README.md` 开发路径 · 单一源组件登记处 |
| 本轮/历轮的**原始动作记录** | `.ctx/logs/2026-09-EXECUTION_LOG.md`（批次条目） |
| 评审遗留与特批记录 | `.ctx/REVIEW_QUEUE.md`（Q 台账 + 附录） |
| 术语与口径唯一化 | `content/03_doc_system/USAGE_POLICY.md`（母本表见 `SSOT_INDEX.md`） |
| 当前版本基线 | `.ctx/SNAPSHOT.md`（版本里程碑 + 生成段） |

### 5.4 每批任务的入口顺序（三步，谁改谁负责）

1. **改前**：读 `DATA_CONSISTENCY_CHECKLIST §0.2` 扫与本次改动相关的规则，再读 §0.1 十四问自问（答不实**先问支书**）；
2. **改中**：新能力/口径一律收进单一源（引擎/叶子件/常量表），**反向枚举**确认没有绕过单一源的自写实现；**同类病灶先数规模**（>1 处即建台账 + 真机覆盖）；
3. **改后**：更新 §0.2 总索引 + 本节沿革表 + 相应守卫（新增断言即更新 `README` 守卫清单语区）；**交付报告第一屏给提交命令与未提交规模**（R-66）；**定向测试为主，交付前才跑全量**。

---

## 附：沿革与口径注

- **评估沿革（一句）**：v1≈75（2026-09-03 首评宽松）→ v2≈69（同日批判性复评·冗余审计）→ v3≈67（2026-09-05 开源化问询：新增超参数可调性与模块组合双维度、口径最严）→ ≈74（P3a~P3e 回补）→ **v4≈76（2026-09-06 支书再评定调：综合分=二开组合能力、重改进不唯分、交付形态=模板型；P4a 新增可换壳证据）** → **v5≈78（2026-09-09 收尾批·AI 建议分支书拍定：超参数 78→84 = 域参数 POLICY_OVERRIDABLE 白名单 + 设置「域参数/制度参数」UI 覆盖落地；组合能力 78→80 = 设置中心页签个人化 + 支部默认编排 + 配置审计回滚；均值 75/75/74/84/80 ≈78，重改进不唯分注保留）**。
- **关键日期与支书再评定调要点**：2026-09-03 支书问询「100 分评估怎么输出」（首评 + 确立统一扎口做法）；2026-09-03 口径修正（.ctx 日志与 references 历史材料为内部资产保留上传，R12 出仓子项全部撤销、仅留运行安全）；2026-09-03 批判性复评第二轮（去重审计 R1~R12 → P0a~P2c 队列）；2026-09-05 第三轮开源化问询（「超参数保留调整空间 + 模块怎么排列组合」→ 超参数/组合双维 + P3a~P3e + 8.7 修复/部署改进）；2026-09-06 支书第四轮再评定调三句（见一）+ P4a 换壳落地 + P4b/P4c 立项④/⑤；2026-09-08 支书批准「全面引用同步 + 文档改名重构」两项（并批 REVIEW_QUEUE 全局评估总表建表）；2026-09-09 本文件更名「工程化评估与改造行动线」+ 四区重构 + 全仓引用同步（本 commit）。
- **文件沿革（旧路径段，历史叙述保留旧名）**：原名 `content/04_web_design/evolution/MODULARIZATION_ASSESSMENT.md`（2026-09-03 建）→ 2026-09-08 git mv 迁 `.ctx/MODULARIZATION_ASSESSMENT.md`（commit 698306e，评估职能归审计底座）→ 2026-09-09 git mv 更名 `.ctx/ENGINEERING_ASSESSMENT.md` 并重构为「工程化评估与改造行动线」。旧文件名/旧路径仅存于执行日志与本文沿革叙述（含日志内对原「§二/§五/§七/§八」等旧结构的转述）。
- **执行日志索引**：各轮与行动明细见 `.ctx/logs/2026-09-EXECUTION_LOG.md`——T-2026-09-027（统一扎口试点 + 评估归档）、T-2026-09-033/034/035/036（去重审计与评估收敛/色值收敛/开源改进 spec/面板保态）、T-2026-09-042（v4 再评 + P4a 换壳指南 + 演示一键重置）、T-2026-09-044~048（P4b/P4c 换组织向导与模板分离立项执行）；旧结构各节明细指此，不再在本文叠加轮次记录。
